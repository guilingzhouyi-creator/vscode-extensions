# 施工细则：演进 02 EventBus 前端 HUD 事件接线与状态同步架构 —— 阶段4：接线端到端与状态同步验收测试矩阵

> [!NOTE]
> **【施工目标】**：完成阶段1~3 全部数据契约、桥接器业务与配置驱动的全量单元测试、事件端到端集成回归与工程门禁验收闭环；验收「EventBusCore → 主页 HUD」链路从零接线骨架到事件驱动的完整接通。
> **【授权依据】**：**已获项目负责人/用户明确授权 (Explicit Authorization)**——授权进入 `02_长期演进区` 立项本工程。
> **【需求溯源】**：docs/audit/AUDIT_REPORT_前端可视化.md（前端零接线审计基线）+ 阶段1~3 细则验收矩阵。
> **施工开始日期**: 2026-09-05（中午时段）

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：[阶段1 数据契约](阶段1_事件订阅契约与HUD快照数据模型设计.md) 三、节与 [阶段2 业务实现](阶段2_事件桥接器与状态同步算法实现.md) 三、节 + [阶段3 工程化](阶段3_通道路由配置驱动与降级工程化.md) 三、节（各阶段 DoD 矩阵全量转测试断言）
* **核心不变量约束断言**：`全链路验收必须同时覆盖订阅可达性、快照聚合、终端流、视图路由联动、Mock 降级与解绑泄漏六维；测试通过率 100% 且 audit-all / audit-docs 门禁零新增违规`。
* **验收闭环最高指示**：测试用例必须覆盖正常路径、异常分支、极端边界与配置缺省；断言以 `EventBusCore` 信道事件实际到达 + HUD 公共入口实际调用为准，禁止以「仅解析通过」的局部 MVP 断言敷衍。

---

## 一、 自动化测试套件与验收代码 (Test Suite & Verification)

```gdscript
# 模块路径: res://tests/unit/test_hud_event_bridge_pipeline.gd
class_name TestHudEventBridgePipeline extends RefCounted

func run_all_tests() -> Dictionary:
    var results: Array = []
    results.append(_test_s1_snapshot_dto_roundtrip())
    results.append(_test_s1_contract_channel_whitelist())
    results.append(_test_s2_subscribe_idempotent_and_attach())
    results.append(_test_s2_snapshot_aggregate_inject_hud())
    results.append(_test_s2_missing_fields_fallback_last_snapshot())
    results.append(_test_s2_detach_no_leak())
    results.append(_test_s2_unknown_channel_fallback_terminal())
    results.append(_test_s3_config_driven_route_reload())
    results.append(_test_s3_mock_mode_no_drive())
    results.append(_test_s3_bidirectional_channel_consistency())
    results.append(_test_s4_end_to_end_world_entry_routes_hud())
    results.append(_test_s4_prologue_step_reaches_terminal())
    return { "suite": "HudEventBridgePipeline", "passed": not results.has(false), "results": results }

func _test_s2_subscribe_idempotent_and_attach() -> bool:
    # 桩 HUD：桥接器 attach_hud 幂等；重复 attach 仅首次建立订阅
    return true   # 实施时替换为真实断言（订阅计数 == 4 信号且重复调用零增长）

func _test_s4_end_to_end_world_entry_routes_hud() -> bool:
    # 端到端：广播 world_gateway.world_entered → 断言 HUD 收到路由委托调用
    return true   # 实施时替换为真实断言（EventBusCore 信道事件可达 + HUD 公共方法被调）
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1: 数据结构与桥接器核心单测** - 覆盖 DTO 对等、订阅幂等、快照聚合、回退语义全路径
- [ ] **Step 4.2: 极端边界与异常容错测试** - 空 payload / 未知通道 / 缺字段 / 重复 attach / detach 泄漏
- [ ] **Step 4.3: 事件端到端集成回归** - 模拟后端真实通道广播（`character_creation.completed` / `prologue.step_advanced` / `world_gateway.world_entered`）验证信号可达 HUD
- [ ] **Step 4.4: 静态质量门禁与全宗验收闭环** - `audit-all` 全绿、`audit-docs --baseline` 0 新增、`test_runner.gd` 全量 100% PASS

---

## 三、 全量工程验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-EV02-S4-01` | 全量单测套件通过 | `test_runner.gd` 执行 | `TestHudEventBridgePipeline` 全部断言 100% 通过 |
| `TC-EV02-S4-02` | 端到端订阅可达 | 广播序章/网关真实通道 | `domain_event` 到达桥接器并经 HUD 公共入口生效（信号链可追踪） |
| `TC-EV02-S4-03` | 极端异常注入容错 | Null / 超限数值 / 未知通道 | 优雅回退默认值或降级战报行，杜绝崩溃与脏写 |
| `TC-EV02-S4-04` | Mock 降级零回归 | `mock_mode.enabled=true` 跑骨架演示 | 骨架 Mock 数据展示与接线前完全一致（零回归） |
| `TC-EV02-S4-05` | 静态审查与门禁校验 | `audit-all` + `audit-docs --baseline` | 配置/代码风格/硬编码/文档审查 0 error、文档基线 0 新增 |
| `TC-EV02-S4-06` | 解绑生命周期闭环 | HUD 视图销毁流程 | 无悬挂订阅、无泄漏告警，重进 HUD 可再次正常接线 |
