# 施工细则：演进 03 高承压对象池与数据导向无头处理器架构 —— 阶段2：泛化引用计数对象池与无头批处理管线算法实现

> [!NOTE]
> **【施工目标】**：完成通用引用计数对象池（ObjectPool）核心算法、安全归还拦截机制、SoA 连续内存就地更新算法以及无头流式批处理调度器的工程化落地，消除高频瞬态分配与内存抖动。
> **【授权依据】**：**已获项目负责人/用户明确授权 (Explicit Authorization)**——授权进入 `02_长期演进区` 立项本演进工程。
> **【需求溯源】**：阶段1 数据契约规范 + 后端战斗与移动高承压审查结论。
> **施工开始日期**: 2026-09-06

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：[阶段1 数据契约](阶段1_对象池泛化契约与SoA连续内存缓冲区数据模型设计.md) 一、节（接口规范与容量模型）与 [tertiary_timeline_engine.gd](../../../../backend/domains/physics_thermodynamics/tertiary_timeline_engine.gd)（第 82 行 `TimelineEventPointDTO.new()` 瞬态热点）。
* **核心不变量约束断言**：
  - **Inv-POOL2-1（状态纯净）**：借出的对象必须经过 `reset_state()` 彻底初始化，其字段与全新实例化对象严格等价；
  - **Inv-POOL2-2（防双重归还）**：严禁未借出对象或已归还对象被重复放入池中；通过独占标志或哈希校验拦截非法归还并产生诊断预警；
  - **Inv-POOL2-3（批处理单帧零堆分配）**：无头批处理流水线（`HeadlessBatchPipeline`）在执行状态迭代更新时，单次迭代内的堆内存分配增量严格为 0；
  - **Inv-POOL2-4（无头纯净性）**：所有算法严禁依赖 Godot 场景树节点或渲染主循环，纯粹依靠时钟推进函数无头驱动。
* **防漂移最高指示**：对象池内部使用 LIFO（后进先出栈）调度算法以最大化 CPU L1/L2 缓存命中；批处理循环内严禁进行字符串拼接或反射调用。

---

## 一、 核心算法与流水线实现设计 (Algorithm & Pipeline Architecture)

```gdscript
# 模块路径: res://backend/infrastructure/object_pool.gd
# 职责: 泛化引用计数对象池核心实现

class_name ObjectPool extends RefCounted:

	var _config: ObjectPoolConfigDTO = null
	var _pool_name: String = ""
	var _factory_func: Callable = Callable()
	var _idle_stack: Array = []
	var _acquired_set: Dictionary = {} # 实例 hash/id -> bool，用于防双重释放
	var _stats: ObjectPoolStatsDTO = null

	func _init(factory: Callable, config: ObjectPoolConfigDTO) -> void:
		_factory_func = factory
		_config = config if config != null else ObjectPoolConfigDTO.new()
		_pool_name = _config.pool_name
		_stats = ObjectPoolStatsDTO.new()
		_stats.pool_name = _pool_name
		_preallocate()

	func _preallocate() -> void:
		var count := _config.preallocate_size
		for i in range(count):
			var obj: RefCounted = _factory_func.call()
			if obj != null:
				_idle_stack.append(obj)
				_stats.total_allocated += 1
		_stats.idle_in_pool = _idle_stack.size()

	## 借出对象 (O(1) LIFO 优先局部性)
	func acquire() -> RefCounted:
		_stats.total_acquires += 1
		var obj: RefCounted = null
		if not _idle_stack.is_empty():
			obj = _idle_stack.pop_back()
		elif _config.allow_overflow_alloc:
			obj = _factory_func.call()
			_stats.total_allocated += 1
			_stats.total_overflows += 1
		else:
			push_error("ObjectPool[%s] 达到硬性上限且禁止溢出分配" % _pool_name)
			return null

		if obj != null:
			if obj.has_method("reset_state"):
				obj.reset_state()
			var obj_id := obj.get_instance_id()
			_acquired_set[obj_id] = true
			_stats.active_in_use += 1
			_stats.idle_in_pool = _idle_stack.size()
			_stats.peak_active = maxi(_stats.peak_active, _stats.active_in_use)
		return obj

	## 归还对象 (带双重释放与容量钳制守卫)
	func release(obj: RefCounted) -> bool:
		if obj == null:
			return false
		var obj_id := obj.get_instance_id()
		if not _acquired_set.has(obj_id):
			push_warning("ObjectPool[%s] 拦截到非法归还：对象未被借出或已重复归还" % _pool_name)
			return false
		_acquired_set.erase(obj_id)
		_stats.total_releases += 1
		_stats.active_in_use = maxi(0, _stats.active_in_use - 1)

		# 容量超限时丢弃，由 Godot 引用计数自动回收
		if _idle_stack.size() < _config.max_capacity:
			if obj.has_method("reset_state"):
				obj.reset_state()
			_idle_stack.append(obj)
			_stats.idle_in_pool = _idle_stack.size()
			return true
		else:
			# 超过池最大上限，自然脱钩
			_stats.total_allocated = maxi(0, _stats.total_allocated - 1)
			_stats.idle_in_pool = _idle_stack.size()
			return true

	func get_stats() -> ObjectPoolStatsDTO:
		return _stats
```

```gdscript
# 模块路径: res://backend/infrastructure/headless_batch_pipeline.gd
# 职责: 基于 SoA 密集连续内存的流式批处理无头管线

class_name HeadlessBatchPipeline extends RefCounted:

	## 批处理一趟推演：就地计算位置变动与阻尼 (纯密集数组线性步进，零堆分配)
	static func step_kinematics(buffer: SoAEntityBuffer, delta_time: float, damping: float) -> int:
		var count := buffer.active_count
		if count <= 0 or delta_time <= 0.0:
			return 0

		# 局部引用加速寻址，避免属性重复查找
		var px := buffer.pos_x
		var py := buffer.pos_y
		var vx := buffer.vel_x
		var vy := buffer.vel_y
		var fl := buffer.flags

		for i in range(count):
			# 忽略处于冻结或销毁标志的实体 (位运算)
			if (fl[i] & 0x01) != 0:
				continue
			px[i] = px[i] + vx[i] * delta_time
			py[i] = py[i] + vy[i] * delta_time
			vx[i] = vx[i] * damping
			vy[i] = vy[i] * damping

		return count
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 2.1: 通用对象池类与双向防错实现** - 完成 `ObjectPool.gd`，实现基于 LIFO 的栈结构存取、预分配与防二次释放集合。
- [ ] **Step 2.2: 容量溢出钳制与释放防腐** - 实现池满丢弃机制，保证引用计数平稳清零，无内存泄漏与野指针。
- [ ] **Step 2.3: SoA 密集内存流式管线实现** - 编写 `HeadlessBatchPipeline.gd` 线性推演算法，杜绝循环内分配。
- [ ] **Step 2.4: 战斗与空间系统示范接入** - 为 `tertiary_timeline_engine` 的事件调度 DTO 提供对象池化适配接口，实现 100% 平滑切换。

---

## 三、 阶段交付物与验证断言 (DoD & Verification)

| 交付文件 | 核心职责 | 静态/单测断言指标 |
| :--- | :--- | :--- |
| `backend/infrastructure/object_pool.gd` | 泛化引用计数对象池完整实现 | 10,000 次 acquire/release 零泄漏断言；防二次归还拦截断言 |
| `backend/infrastructure/headless_batch_pipeline.gd` | 无头连续内存批处理管线 | 单趟 step 10,000 实体推演耗时 < 5ms，堆分配增量恒为 0 |
| `backend/domains/physics_thermodynamics/timeline_event_pool_adapter.gd` | 战斗时间轴事件池化适配器 | 业务代码行为与原生 `TimelineEventPointDTO` 100% 对齐 |
