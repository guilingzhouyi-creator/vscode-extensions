# 施工细则：核心服务圈复杂度清零与高阶调度解耦 —— 阶段1：审查缺陷盘点与重构不变量契约

> [!NOTE]
> **【施工目标】**：基于 `auto-refactor` 现代化十维静态审查结果，对 WebGames 全域检出的所有高圈复杂度超标函数（CC > 15，共 6 处核心关键路径）及历史残留施工批次标记进行逐项确诊分诊，确立第一性原理设计与不变量契约，为全库圈复杂度清零与高阶调度解耦提供严格护栏。
> **【施工开始日期】：施工开始日期: 2026-09-25（下午）** —— 真实读取系统时间（`2026-09-25 17:00 下午`）。
> **【需求溯源】**：用户指令「其他量化也需要提升，出计划，同时得考虑十维量化指标的计算公式以及加权统计分析是否合理」并正式批准实施（2026-09-25 批准双轨计划：Track 1 升级引擎聚合模型，Track 2 落地 Phase 91 圈复杂度清零）。
> **【对应需求源】**：[路线图总索引](../../路线图总索引.md) ｜ [WebGames/README.md](../../../../README.md) §后端逻辑处理标准 v1 ｜ [GD老练风格硬性规范标准.md](../../../模板/GD老练风格硬性规范标准.md) §四 圈复杂度收敛契约与架构解耦契约

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * `WebGames/README.md` §后端逻辑处理标准 v1：六角色模型划分（entity/aggregate、solver、fsm、pipeline、service、registry/dto）与领域逻辑纯洁性；
  * `WebGames/docs/模板/GD老练风格硬性规范标准.md` §四：圈复杂度门禁硬顶（告警阈值 15，失败阈值 25，目标收敛至 $\le 5$）；
  * 仓库根 `AGENTS.md` §二 关键架构约定与工程契约（严禁循环内瞬态分配；全域严禁施工批次黑话）；
  * 仓库根 `AGENTS.md` §五 绝对红线禁令（严禁局部最小 MVP 敷衍实现；改动必先立分阶段施工细则；案卷目录严格仅 4 份细则文件）。
* **核心不变量约束断言**：
  * `Inv-P91-1 (行为完全等价)`：所有解耦重构必须保证各模块外部 API 签名、参数字典、返回结构与事件广播副作用完全向后兼容；全域单元测试 100% 保持通过。
  * `Inv-P91-2 (圈复杂度清零收敛)`：重构的目标函数（`dispatch_now`、`execute_graceful_shutdown`、`_load_from_config`、`_matches_criteria`、`adapt_label`、`_strip_by_mode`）圈复杂度一律拆解至 $\le 5$。
  * `Inv-P91-3 (零瞬态堆分配与池化契约)`：`ADV-PRF-002` 循环内严禁瞬态对象/数组分配，池化对象严格执行 `reset_state()`。
  * `Inv-P91-4 (零黑话铁律)`：彻底清理生产代码与注释中残留的历史施工批次标记，对齐规范架构语义。
  * `Inv-P91-5 (单源演进与门禁守卫)`：全量 20 项静态门禁（`audit_runner.py`）必须保持 20/20 PASS。
* **防漂移最高指示**：本卷以「核心服务高圈复杂度清零 → 高阶调度与生命周期解耦 → 零黑话清退」为唯一改造范围；**严禁**越界修改未标记健康代码，**严禁**自造第五份案卷文件。

---

## 一、 审查缺陷分诊与确诊清单

| 缺陷编号 | 规则代号 / 类型 | 缺陷发生文件与位置 | 当前现状与缺陷形态 | 真实性判定 | 本卷治理处置策略 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `DEF-P91-01` | `complexity`<br/>高圈复杂度 | `backend/infrastructure/event_bus/event_bus_core.gd:166` | `dispatch_now` 圈复杂度高达 **23**（门禁阈值 15）。频道分发、公共频道、去重缓冲与回调保护深度交织 | **属实**：基础设施高频核心路径 | **子函数拆解**：提取 `_prepare_dispatch_scratch` 与 `_collect_channel_subscribers`，主函数 CC 降至 $\le 4$ |
| `DEF-P91-02` | `complexity`<br/>高圈复杂度 | `backend/domains/lifecycle/game_lifecycle_service.gd:101` | `execute_graceful_shutdown` 圈复杂度高达 **18**（门禁阈值 15）。停机校验、截止判定、存盘阶段、注销拆卸混杂 | **属实**：生命周期核心调度器 | **阶段流水线拆分**：解耦为 `_guard_shutdown_entry`、`_execute_shutdown_save_stage`、`_execute_shutdown_teardown_stage`，主函数 CC 降至 $\le 4$ |
| `DEF-P91-03` | `complexity`<br/>高圈复杂度 | `backend/infrastructure/log_query_service.gd:50` | `_matches_criteria` 圈复杂度高达 **17**（门禁阈值 15）。6 维查询过滤条件深度嵌套 | **属实**：查询过滤核心计算 | **谓词提取**：分解为 `_matches_time_window`、`_matches_level_and_channel`、`_matches_identifiers_and_keyword`，主函数 CC 降至 $\le 3$ |
| `DEF-P91-04` | `complexity`<br/>高圈复杂度 | `backend/domains/contract_registry/contract_registry_index.gd:43` | `_load_from_config` 圈复杂度高达 **16**（门禁阈值 15）。配置探测、文件 I/O、JSON 解析、结构校验、索引构建混杂 | **属实**：契约注册核心装载 | **模块化拆分**：拆解为 `_read_config_file_text`、`_parse_json_dict`、`_unpack_registry_sections`，主函数 CC 降至 $\le 3$ |
| `DEF-P91-05` | `complexity`<br/>高圈复杂度 | `frontend/i18n/visual_adapter.gd:29` | `adapt_label` 圈复杂度高达 **15**（门禁预警阈值 15）。字号探测、宽度拟合与溢出截断混合 | **属实**：视觉自适应核心路径 | **阶梯拟合提取**：分解为 `_get_label_current_font_size`、`_resolve_label_available_width`、`_resolve_fitted_font_size`、`_apply_label_visual_style`，主函数 CC 降至 $\le 4$ |
| `DEF-P91-06` | `complexity`<br/>高圈复杂度 | `tests/guards/test_architecture_guard.gd:630` | `_strip_by_mode` 圈复杂度高达 **17**（门禁阈值 15）。逐字节解析中转义、字符串、注释状态分支深度嵌套 | **属实**：架构长效护栏核心解析 | **辅助提取**：提炼 `_skip_string_escape` 与 `_blank_char_if_needed`，主函数 CC 降至 $\le 9$ |
| `DEF-P91-07` | `HYG-STB-002`<br/>黑话残留 | `backend/domains/admin_sandbox/clawback_service.gd:25,195` | 注释含 `# P4：单次 keys 快照...` 与 `P39 清单 5` 批次黑话 | **属实**：违反零黑话铁律 | **清退重构**：移除批次代号，替换为标准领域语义注释 |

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 1.1 审查缺陷深度盘点与代码级证据核验** — 确认 7 项高圈复杂度与黑话缺陷在源文件的行号、上下文及调用关系无偏差。
- [x] **Step 1.2 架构解耦与行为保真契约确立** — 验证所有公开接口调用方与既有测试依赖，确保零功能回归。
- [x] **Step 1.3 高圈复杂度函数拆解拓扑设计** — 为全部 6 个高复杂度函数规划私有子方法签名与职责边界。
- [x] **Step 1.4 算法性能降阶与零瞬态分配防守** — 保持事件总线 `_target_scratch` 与 `_seen_scratch` 缓冲复用，零堆分配。
- [x] **Step 1.5 零黑话与统一规范对齐** — 确立清理规范与注释标准。

---

## 三、 验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P91-S1-01` | 缺陷盘点逐项定性 | 审查缺陷分诊清单与代码锚点 | 7 项缺陷均有代码级行号佐证、真实性判定与明确治理策略 |
| `TC-P91-S1-02` | 不变量契约完备性 | 四项核心不变量 | 覆盖行为等价、圈复杂度清零、零瞬态分配与零黑话规范 |
| `TC-P91-S1-03` | 门禁规范契约一致性 | `audit_runner.py` 与 `audit_docs.py` | 满足四阶段施工规范契约 |
