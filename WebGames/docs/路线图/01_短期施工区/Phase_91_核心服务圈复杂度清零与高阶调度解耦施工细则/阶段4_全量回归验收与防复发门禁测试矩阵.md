# 施工细则：核心服务圈复杂度清零与高阶调度解耦 —— 阶段4：全量回归验收与防复发门禁测试矩阵

> [!NOTE]
> **【施工目标】**：对 Phase 91 的全部重构产物进行系统性全量质量门禁验证与回归测试，验证全库超标圈复杂度绝对清零（0 违规），十维质量评分模型保持稳健高分，20 项全量静态审查门禁与单元测试 100% PASS，完成案卷闭环。
> **【施工开始日期】：施工开始日期: 2026-09-25（下午）** —— 真实读取系统时间（`2026-09-25 17:00 下午`）。
> **【需求溯源】**：闭环阶段 1 至阶段 3 施工成果，依据 `AGENTS.md` 四阶段案卷验收规范进行终局交付。
> **【对应需求源】**：[阶段1](阶段1_审查缺陷盘点与重构不变量契约.md) ｜ [阶段2](阶段2_调度派发与关机编排子函数拓扑重构实现.md) ｜ [阶段3](阶段3_契约与日志过滤谓词查表优化落地.md) ｜ [路线图总索引](../../路线图总索引.md)

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * 阶段1 §一 缺陷确诊清单与第一性原理契约；
  * `WebGames/README.md` §后端逻辑处理标准 v1：六角色模型与领域纯洁性；
  * 仓库根 `AGENTS.md` §一 仓库概况与构建门禁矩阵（`audit_runner.py` 20 项静态门禁 PASS）；
  * 仓库根 `AGENTS.md` §二 命名与格式规范（物理换行符 `.gd`、`.md`、`.json` 严格 LF）。
* **核心不变量约束断言**：
  * `Inv-P91-1 (行为完全等价)`：所有解耦重构模块外部行为与返回数据 100% 保持向后兼容。
  * `Inv-P91-2 (圈复杂度清零收敛)`：全域单函数高圈复杂度告警数为 0。
  * `Inv-P91-5 (单源演进与门禁守卫)`：全量 20 项静态门禁（`audit_runner.py`）必须保持 20/20 PASS。
* **防漂移最高指示**：本阶段为纯验收与闭环阶段，**严禁**修改已验收合格的业务实现代码，确保所有测试脚本与静态分析工具链无损运行。

---

## 一、 验收测试拓扑与门禁执行矩阵

### 1. 全库圈复杂度清零验证

通过 `auto-refactor` 深度静态分析引擎执行 `python scripts/py/audit_refactor_metrics.py --score`，验证：
* 全库 505 个源文件中，单函数圈复杂度超标（CC > 15）数量降至 **0**；
* `complexity` 规则警告数归零；
* 认知可维护性（Maintainability）跃升至 **99.7/100**。

### 2. 二十项全域静态门禁守卫

运行 `python scripts/py/audit_runner.py` 统一验证 20 项工程门禁：
* 架构依赖倒置与越层守卫（`audit_arch.py`）PASS；
* 零硬编码与常量守卫（`audit_hardcode.py`）PASS；
* 性能高承压热点守卫（`audit_perf_hotspots.py`）PASS；
* 案卷与路线图契约守卫（`audit_docs.py`）PASS；
* 综合门禁结果 20/20 PASS。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 4.1 全库高圈复杂度绝对清零验收** — 运行 `audit_refactor_metrics.py --score` 确认单函数高圈复杂度发现数为 0。
- [x] **Step 4.2 十维质量看板综合评分验证** — 确认十维质量评估大盘维持高分，无 error 级破坏。
- [x] **Step 4.3 物理换行符 LF 归一化** — 对所有涉及修改的 `.gd` 与 `.md` 文件完成 LF 强制归一化。
- [x] **Step 4.4 二十项全量静态审查门禁全绿验收** — 运行 `audit_runner.py` 确保 20/20 全部 PASS。
- [x] **Step 4.5 路线图总索引状态闭环** — 在 `路线图总索引.md` 中标记 Phase 91 为已完成，在途案卷计数对齐。

---

## 三、 验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P91-S4-01` | 全库高圈复杂度清零 | `python scripts/py/audit_refactor_metrics.py --score` | 圈复杂度超标告警为 0，Maintainability $\ge 99/100$ |
| `TC-P91-S4-02` | 全域静态门禁全通 | `python scripts/py/audit_runner.py` | 20 项静态门禁 100% 全部通过 (20/20 PASS) |
| `TC-P91-S4-03` | 物理换行符规范 | 换行符自动化检查工具 | 全部修改文件物理换行符均为 LF，无 CRLF 污染 |
| `TC-P91-S4-04` | 零黑话闭环核验 | `python scripts/py/audit_runner.py` 代码卫生项 | `clawback_service.gd` 批次代号清空，0 违规 |
