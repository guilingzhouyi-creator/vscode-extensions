# 施工细则：静态审查缺陷多维重构与架构治理 —— 阶段4：全量回归测试与质量指标验收矩阵

> [!NOTE]
> **【施工目标】**：对阶段 1~3 实施的架构解耦、复杂度拆解、性能优化与卫生治理执行全量回归验收。断言基线由 770 项扩容至 772 项，验证全域 20 项静态门禁 100% 保持通过，重新运行 `auto-refactor` 验证架构一致性得分恢复至 100 分、目标函数圈复杂度清零、无新增缺陷，实现案卷闭环。
> **【施工开始日期】：施工开始日期: 2026-09-14（晚上）** —— 真实读取系统时间（`2026-09-14 20:25 晚上`）。
> **【对应需求源】**：[阶段1](阶段1_审查缺陷盘点与重构不变量契约.md) ｜ [阶段2](阶段2_架构分层解耦与高圈复杂度拆解实现.md) ｜ [阶段3](阶段3_性能循环热点优化与代码卫生治理.md) ｜ [路线图总索引](../../路线图总索引.md)
> **【验收环境】**：Godot `4.7.2.stable.official.ed1daf0bf`；Python `3.13.14`；Node.js `v22.x`；平台 win32。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * `WebGames/README.md` §持续集成与架构门禁；
  * `WebGames/tests/README.md`（测试用例规范与统一断言底座）；
  * 仓库根 `AGENTS.md` §二 关键架构约定与工程契约（改后 audit-all 20 项门禁全绿且 audit-docs 0 新增）。
* **核心不变量约束断言**：
  * `Inv-P89-1`：全域既有 770 项业务断言 100% 通过，绝不因重构出现退化。
  * `Inv-P89-8 (断言单调递增)`：全域断言数由 770 扩充至 **772**（新增 2 项架构隔离防护断言）。
  * `Inv-P89-9 (门禁全绿留痕)`：20 项静态门禁（`audit_runner.py --group audit`）全部 PASS。
  * `Inv-P89-10 (十维质量雷达质跃)`：`architectureConsistency` 指标由 85 跃升至 **100**，圈复杂度告警函数显著下降。
* **防漂移最高指示**：验收以「自动化测试全绿 + 门禁全绿 + 多维指标实质提升 + 文档零新增违规」为唯一闭环判据。

---

## 一、 实施细则方案

### 1.1 新增架构隔离护栏测试（`TC-ARCH-08A/B`）
在 `tests/unit/frontend/test_frontend_boundary_guard.gd` 或既有架构护栏套件中增设防复发断言：
1. `TC-ARCH-08A`：断言 `account_slot_binding_solver.gd` 源码中绝不包含 `res://backend/infrastructure/` 预加载路径；
2. `TC-ARCH-08B`：断言 `account_slot_binding_solver.gd` 内部无任何 `GameLoopStateStack` 符号引用。

### 1.2 全域门禁与十维审查指标质跃复测
1. 运行 `bash scripts/sh/test-run.sh` 确认断言数达到 772 / 772 PASS；
2. 运行 `python scripts/py/audit_runner.py --group audit` 确认 20 项静态门禁 100% PASS；
3. 运行 `python scripts/py/audit_refactor_metrics.py --score` 确认架构一致性得分为 100/100，圈复杂度超标项降低。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1 新增架构护栏契约断言（`TC-ARCH-08A/B`）** — 固化 `account_slot_binding_solver.gd` 无基础设施层引用断言。
- [ ] **Step 4.2 全域单元测试回归** — 执行 `test-run.sh` 确保 772/772 断言全部通过。
- [ ] **Step 4.3 静态语法极速门禁检验** — 执行 `check-gdscript.sh` 确保无编译或类型解析错误。
- [ ] **Step 4.4 运行 20 项全量静态门禁** — 验证并行调度流水线 20/20 全部 PASS。
- [ ] **Step 4.5 运行 auto-refactor 十维健康度复测** — 确认架构得分升至 100，生成最终审计报告。
- [ ] **Step 4.6 路线图看板状态回写** — 在通过所有验收后，闭环登记至路线图总索引。

---

## 三、 验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P89-S4-01` | 架构防护断言落地 | `res://tests/arch_runner.gd` | 包含 `TC-ARCH-08`，断言无基础设施层依赖 |
| `TC-P89-S4-02` | 全量单元测试回归 | `bash scripts/sh/test-run.sh` | 772 Passed / 0 Failed，既有断言无破坏 |
| `TC-P89-S4-03` | 静态语法与解析 | `bash scripts/sh/check-gdscript.sh` | 全量 `.gd` 语法解析 0 错误 |
| `TC-P89-S4-04` | 20 项静态门禁大盘 | `python scripts/py/audit_runner.py --group audit` | 20 / 20 PASS，0 Error / 0 Warn |
| `TC-P89-S4-05` | 十维架构一致性提升 | `python scripts/py/audit_refactor_metrics.py --score` | 架构一致性 100/100，`ARCH-DIR-001` 归零 |
| `TC-P89-S4-06` | 文档四域规范与基线 | `python scripts/py/audit_docs.py --baseline ...` | 0 Error / 0 Warn / 0 新增违规 |
