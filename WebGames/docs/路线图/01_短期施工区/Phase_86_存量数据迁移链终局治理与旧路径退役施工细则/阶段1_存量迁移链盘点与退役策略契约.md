# 施工细则：存量数据迁移链终局治理与旧路径退役 —— 阶段1：兼容残留盘点与退役策略契约

> [!NOTE]
> **【施工目标】**：承接 Phase 83 白名单退役前置条件，完成三类治理：① M-1 item_id→item_uid 单标识终局迁移链（判定器先行，完成后注销 W-01/W-03）；② M-2 口令加盐重铸（login-time 重铸链，注销 W-02）；③ 旧路径直接退役（F-1 restore 旧引用分支 / F-2 from_dto 双形态 / R-03 timeline 必填化）。
> **【施工开始日期】：施工开始日期: 2026-09-12** —— 真实读取系统时间。
> **【用户决策登记】**（2026-09-12 批准）：① GOAP `goals/legacy`（NPC 临终著绝学）为**产品功能**——`npc_legacy_fsm.gd` 退出退役桶，本卷禁触；② 存量数据迁移（UID 迁移 + 口令加盐重铸）纳入主线。
> **【编号变更记录】**：本卷原以 Phase 85 立项（阶段1 于 2026-09-12 获批），因编号被并行案卷「全域脚本库规范化重构」占用，按序号连续门禁重登记为 **Phase 86**，内容契约不变。
> 状态：✅ 已完成（阶段1 契约全文以本文件为真源）
> **【对应需求源】**：Phase 83 阶段1 §1.8 白名单 W-01/W-02/W-03 退役前置条件；后端兼容层深度扫描报告（2026-09-12，157 条关键词命中 + 8 项结构性扫描）；[路线图总索引](../../路线图总索引.md)。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * `Phase 83 阶段1` §1.8（兼容层白名单 W-01~W-07 契约）；
  * `AGENTS.md` §二（关键架构约定与工程契约、两轮施工机制）；
  * `docs/后端代码注释规范.md` 与 `docs/模板/GD老练风格硬性规范标准.md`。
* **核心不变量约束断言**：
  * `Inv-P86-1-1 (单标识终局)`：迁移完成后 `item_uid` 为实例物品唯一权威标识；五点兼容路径退役以判定器证据为唯一依据；
  * `Inv-P86-1-2 (存量迁移可判定)`：R-01 判定器 = `GameSaveAssembler.audit_legacy_uid`（只读递归扫描 LGC_ 前缀计数）；R-02 判定器 = 全账号档 `salt_version==1` 占比 100%；严禁隐式判定；
  * `Inv-P86-1-3 (白名单注销纪律)`：W-02 随 R-02 证据注销；W-01/W-03 随五点退役批次注销；W-04~W-07 维持（W-05/W-06 建议移出兼容白名单改挂功能清单）；
  * `Inv-P86-1-4 (语义零漂移)`：restore 深载荷重建、战斗轮次事件时序、鉴权结果逐字段一致；盐重铸 login-time 链不强制口令重置；
  * `Inv-P86-1-5 (测试覆盖不降)`：用例总数不减；timeline 必填化后测试 null 夹具改持真实空转引擎；
  * `Inv-P86-1-6 (防漂移红线)`：`npc_legacy_fsm.gd` 及 `domains.npc` legacy 段、`goap/goals/legacy` 禁触；`fallback_*` 缺省族/单例惯例/`meta_legacy_points` 非兼容残留严禁误删；判定器缺位时严禁提前删迁移期读路径。
* **防漂移最高指示**：严格坚守两轮施工机制；严禁触碰产品功能 `npc_legacy_fsm.gd`；所有判定器必须具备确定性数据证据。

---

## 一、 存量迁移链盘点与退役策略分桶

### 1.1 盘点表（C-01~C-05，文件:行 级证据）

| # | 位置 | 证据与裁定 |
| :--- | :--- | :--- |
| **C-01** | `item_uid_generator.gd:28-31,56-58,81-82` / `item_entity.gd:103,111-113,187-189` / `item_instance_factory.gd:5,13,22,27-28` / `clawback_service.gd:147-191` / `matter_disposal_solver.gd:37` | item_id↔item_uid 双标识链五点 → R-01（本卷交付判定器，五点退役列后续批次） |
| **C-02** | `auth_service.gd:26-27,36-38`；`config/domains/account.json auth/salt` **已非空**（KALAR_REALM_2026_SALT） | 空盐历史档 login 必失败 → R-02 login-time 重铸链 |
| **C-03** | `wearable_inventory.gd:239-241` else 分支；9 个 restore 调用方快照均产自唯一深载荷端 `snapshot()` | → F-1 直接删除 |
| **C-04** | `combat_round_coordinator.gd:37,95-96,102,217,237,258-261` 可空注入；测试 null 夹具 ×2 | → R-03 必填化（空转引擎替代 null） |
| **C-05** | `domain_view_mapping_snapshot.gd:57-70` int/String 双形态；生产端恒产 String；**终核修正：文件非死码**（生产链真实消费） | → F-2 去 int 分支保留 String 单形态 |

### 1.2 分桶裁定（唯一互斥）

- **R 迁移型**：R-01=C-01 / R-02=C-02 / R-03=C-04；
- **F 直接删除**：F-1=C-03 / F-2=C-05；
- **退出桶**：`npc_legacy_fsm` 全链（产品功能，NPC 临终著绝学）；
- **白名单注销**：W-02（R-02 证据）、W-01/W-03（五点退役批次）；
- **维持白名单**：W-04/W-07。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] `Step 1.1`：完成存量兼容点 C-01~C-05 穿透扫描与文件行号级证据固定。
- [x] `Step 1.2`：确立 R/F/退出/注销 四桶裁定矩阵与不变量契约。
- [x] `Step 1.3`：明确 GOAP npc_legacy_fsm 为产品业务功能并移出退役桶。
- [x] `Step 1.4`：设计 UID 单轨判定器与口令加盐重铸链技术方案。

---

## 三、 阶段交付物与验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `DOD-P86-S1-01` | 盘点证据完备性 | C-01~C-05 源码行号审查 | 100% 具备确切文件与行号证据，可精准定位 |
| `DOD-P86-S1-02` | 分桶唯一互斥性 | 四桶裁定表比对 | 业务功能与兼容残留彻底解耦，无混淆边界 |
| `DOD-P86-S1-03` | 判定器机检能力 | R-01/R-02 判定指标契约 | 具备确定性数据输出与可统计判定标准 |
| `DOD-P86-S1-04` | 语义零漂移预审 | 兼容退役前后接口签名比对 | 不强制重置口令，空转引擎等价保留旧语义 |
