# 施工细则：存量数据迁移链终局治理与旧路径退役 —— 阶段4：全量回归验收与工程闭环

> [!NOTE]
> **【施工目标】**：完成 Phase 86 全域测试用例新增与改造回归验证，注销白名单 W-02，固化 12 个改动文件的工程交付物，完成全仓自动化门禁自证与工程闭环。
> **【施工开始日期】：施工开始日期: 2026-09-12** —— 真实读取系统时间。
> **【状态】**：✅ 已完成（验收数据实测留痕见文末）。
> **【对应需求源】**：阶段 1~3 施工成果；`Phase 83` 白名单注销契约；[路线图总索引](../../路线图总索引.md)。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * `Phase 86 阶段1~3`（全阶段实施记录与不变量约定）；
  * `AGENTS.md` §二（关键架构约定与工程契约、两轮施工机制）；
  * `docs/后端代码注释规范.md` 与 `docs/模板/GD老练风格硬性规范标准.md`。
* **核心不变量约束断言**：
  * `Inv-P86-4-1 (测试净增与零黑话)`：用例总数不减，净增 5 条核心断言；测试方法与注释严禁出现任何 `p85`/`phase85` 等临时批次黑话标记；
  * `Inv-P86-4-2 (白名单审慎注销)`：W-02（口令盐重铸）凭三态实测数据正式注销；W-01/W-03 严格待五点物理退役时注销；
  * `Inv-P86-4-3 (全门禁全绿零告警)`：20 项全域门禁全绿，文档门禁 0 error / 0 warn / 0 新增违规，测试套件 109 套 766 断言 100% PASS；
  * `Inv-P86-4-4 (字节级LF一致性)`：改动文件行尾严格归一为 LF，无 `\r` 混用。
* **防漂移最高指示**：禁止残留临时代码或黑话；白名单注销与保留必须具备可溯源的物理依据。

---

## 一、 新增/改造用例（5 条，用例总数不减，净增 5 条断言）

| ID | 用例 | 落点 | 结果 |
| :--- | :--- | :--- | :--- |
| `TC-CT-13` | F-1 深载荷唯一入口 | `tests/unit/domains/test_inventory.gd` `test_restore_deep_payload_only` | ✅ PASS |
| `TC-ACC-09` | F-2 单形态契约 | `tests/integration/pipelines/test_contract_registry_pipeline.gd` `test_s4_13_coverage_status_string_single_shape` | ✅ PASS |
| `TC-INV-11` | R-02 盐重铸三态 | `tests/unit/domains/test_account.gd` `test_salt_rehash_migration` | ✅ PASS |
| `TC-SM-09` | R-03 空转引擎等价 | `tests/integration/pipelines/test_combat_dual_random_and_timeline_pipeline.gd` `_test_idle_engine_null_equivalence` | ✅ PASS |
| `TC-CT-09` | R-01 判定器 | `tests/unit/infrastructure/test_save_migration_engine.gd` `test_legacy_uid_audit_detector` | ✅ PASS |

配套变更：`TC-LG-19` 运行时告警白名单登记 2 处新增审计站点（`wearable_inventory` 与 `domain_view_mapping_snapshot`）。

---

## 二、 全域门禁终跑实测留痕

```text
test-run.sh        109 套件、766 / 766 断言全部通过（100.0% PASS）
check-gdscript.sh  通过（scope=all 单进程极速批处理语法检查）
audit-all.sh       20 / 20 PASS（0 Error / 0 Warn）
audit-docs.sh      error 0 / warn 0 / 新增违规 0（基线棘轮看守）
audit_copywriting  违规 0 / 提示 0
```

---

## 三、 工程交付物与闭环留痕

1. **白名单登记演进**：
   - **W-02 正式注销**：基于 R-02 三态判定证据（空盐档 login-time 重铸为 `salt_version==1`，后续按全局配置盐唯一校验）；
   - **W-01/W-03 暂缓注销**：因 R-01 五点兼容路径严格维持未删，随后续批次判定器连续为 0 且物理删除时注销。
2. **改动范围清单（12 文件，已完成零黑话审计）**：
   - 后端 6 份：`wearable_inventory.gd`、`domain_view_mapping_snapshot.gd`、`combat_round_coordinator.gd`、`auth_service.gd`、`account_profile.gd`、`save_assembler.gd`；
   - 测试 6 份：`test_inventory.gd`、`test_account.gd`、`test_contract_registry_pipeline.gd`、`test_save_migration_engine.gd`、`test_combat_dual_random_and_timeline_pipeline.gd`、`test_log_error_base_pipeline.gd`；
   - 边界纪律：`npc_legacy_fsm.gd`、五点退役路径、`fallback_*` 缺省族零触碰。
3. **换行符规范**：改动文件行尾严格归一为 LF。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] `Step 4.1`：交付 5 条核心断言（F-1、F-2、R-02、R-03、R-01），完成零黑话合规清洗。
- [x] `Step 4.2`：在 TC-LG-19 登记 2 处运行时审计站点（wearable_inventory 与 domain_view_mapping_snapshot）。
- [x] `Step 4.3`：注销白名单 W-02，保持 W-01/W-03 暂缓状态并形成批次备忘。
- [x] `Step 4.4`：全量运行 test_runner.gd（766/766）、audit_docs.py 与 audit-all 门禁，确保全绿零违规闭环。

---

## 三、 阶段交付物与验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `DOD-P86-S4-01` | 测试套件全量通过 | `godot --headless -s res://tests/test_runner.gd` | 109 套测试套件、766 / 766 断言全部通过，净增 5 条 |
| `DOD-P86-S4-02` | 测试与注释零黑话 | 全域检索 `p85`/`phase85` 等临时批次标记 | 源码及测试方法名中违规黑话计数恒为 0 |
| `DOD-P86-S4-03` | 白名单状态精准演进 | 审查白名单注销清单 | W-02 正式注销留痕，W-01/W-03 审慎维持待判定器批次 |
| `DOD-P86-S4-04` | 架构与全量门禁看守 | 执行 `audit_docs.py` 与全域门禁流水线 | 文档门禁 0 error / 0 warn，全域门禁全绿 |
