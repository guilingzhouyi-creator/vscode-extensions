# 施工细则：存量数据迁移链终局治理与旧路径退役 —— 阶段2：零风险旧路径直接删除

> [!NOTE]
> **【施工目标】**：对经阶段 1 盘点确认具备零风险特征的存量旧路径执行物理删除（F-1：`wearable_inventory.gd` 旧引用快照 else 分支删除；F-2：`domain_view_mapping_snapshot.gd` 去 int 双形态、保留 String 单形态）。
> **【施工开始日期】：施工开始日期: 2026-09-12** —— 真实读取系统时间。
> **【状态】**：✅ 已完成（实施与验收数据见文末）。
> **【对应需求源】**：阶段 1 分桶裁定（F-1/F-2 直接删除项）；`Phase 83` 阶段 1 白名单契约；[路线图总索引](../../路线图总索引.md)。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * `Phase 86 阶段1` §1.2 分桶裁定（F-1、F-2 判定与不变量约束）；
  * `AGENTS.md` §二（关键架构约定与工程契约、前端可视化边界）；
  * `docs/后端代码注释规范.md` 与 `docs/模板/GD老练风格硬性规范标准.md`。
* **核心不变量约束断言**：
  * `Inv-P86-2-1 (深载荷单入口)`：`wearable_inventory.gd` 的 `restore()` 唯一入口仅接受 `snapshot()` 产出的深载荷，无 `storage_payloads` 严格记录 `RestorePayloadMissing` 警告并返回空容器，严禁静默吞错；
  * `Inv-P86-2-2 (String单形态)`：`domain_view_mapping_snapshot.gd` 的 `from_dto()` 仅接受 String 格式，非 String 入参严格触发 `DtoShapeViolation` 警告并 MISSING 归位，彻底退役 int 双形态分支；
  * `Inv-P86-2-3 (生产端零漂移)`：`snapshot()` 生产端与正常 String DTO 消费链路零破坏，契约断言全量绿；
  * `Inv-P86-2-4 (防漂移红线)`：严禁触碰 `npc_legacy_fsm.gd`；严禁提前删除 R-01 涉及的五点 UID 兼容路径。
* **防漂移最高指示**：专注 F-1 与 F-2 纯删除与单形态收敛，严禁扩大改动面；保留完整防御性警告审计留痕。

---

## 一、 零风险旧路径删除实施方案

### 1.1 F-1：wearable_inventory 旧引用快照 else 分支删除
- **落点文件**：`backend/domains/inventory/wearable_inventory.gd` `restore()`
- **实施措施**：删除「兼容旧引用快照路径」分支（实体引用直取），收敛为「无 `storage_payloads` → 空容器 + `push_warning("RestorePayloadMissing: …")`（显式留痕，不静默）」；题头注释改写为「唯一入口仅接受 snapshot() 深载荷」；`snapshot()` 深载荷生产端零改动。
- **验收结果**：`grep "旧引用"` = 0；测试断言（`test_inventory.gd` 中 `test_restore_deep_payload_only`）全绿通过。

### 1.2 F-2：domain_view_mapping from_dto 去 int 双形态
- **落点文件**：`backend/domains/contract_registry/domain_view_mapping_snapshot.gd` `from_dto()`
- **终核修正**：文件非死码（生产链真实消费），仅退役 int 分支——非 String 入参 → `push_warning("DtoShapeViolation: …")` + MISSING 归位（不静默转换）；String match 归一化保留。
- **验收结果**：`from_dto` 段 int 分支 = 0；测试断言（`test_contract_registry_pipeline.gd` 中 `test_s4_13_coverage_status_string_single_shape`）全绿通过。

### 1.3 附注：P84 影响评估（全阶段通用）
1. **题头归属分离**：P86 只改语义注释，不动题头风格（题头统一归 P84）；
2. **结构变更合规**：P86 的分支删除属合法结构变更，与 P84「仅注释」不重叠；
3. **行号解耦**：行号不硬锚，以语义锚点（注释原文/签名串）重定位。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] `Step 2.1`：执行 F-1，删除 `wearable_inventory.gd` restore() 中旧引用快照 else 分支，接入 `RestorePayloadMissing` 显式审计告警。
- [x] `Step 2.2`：执行 F-2，重构 `domain_view_mapping_snapshot.gd` from_dto()，退役 int 双形态并收敛为 String 单形态，接入 `DtoShapeViolation` 告警。
- [x] `Step 2.3`：编写配套断言用例，确保 `test_inventory.gd` 与 `test_contract_registry_pipeline.gd` 覆盖深载荷与 String 单形态契约。
- [x] `Step 2.4`：运行全量单元测试与回归门禁，验证 766 / 766 断言全绿且无死引用残留。

---

## 三、 阶段交付物与验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `DOD-P86-S2-01` | F-1 旧引用分支清零 | `wearable_inventory.gd` 源码检索 | `grep "旧引用"` 匹配计数恒为 0，深载荷为唯一合法输入 |
| `DOD-P86-S2-02` | F-1 缺失载荷防御 | 注入缺失 storage_payloads 的空载荷 | 触发 RestorePayloadMissing 告警并安全返回空容器 |
| `DOD-P86-S2-03` | F-2 int 分支退役 | `domain_view_mapping_snapshot.gd` 检索 | from_dto 中 int 转换分支恒为 0，String 为唯一处理流 |
| `DOD-P86-S2-04` | F-2 非法入参防御 | 注入非法类型（int/null）DTO 数据 | 触发 DtoShapeViolation 告警并归位为 MISSING |
