# 施工细则：叙事编排降圈治理与契约性能热点消解 —— 阶段1：审查缺陷盘点与重构不变量契约

> [!NOTE]
> **【施工目标】**：基于 `auto-refactor` 现代化十维静态审查结果，对 WebGames 全域检出的叙事编排高圈复杂度超标函数（Top 2: CC 24 / 23）、契约管线嵌套循环性能热点、循环内瞬态堆分配及历史施工黑话残留进行可判定的逐项分诊盘点，确立第一性原理设计与不变量契约，为第二至四阶段系统性重构提供严格护栏。
> **【施工开始日期】：施工开始日期: 2026-09-25（下午）** —— 真实读取系统时间（`.\scripts\ps1\timestamp.ps1 -full` 输出 `2026-09-25 16:31 下午`）。
> **【需求溯源】**：用户指令「出WebGames的计划吧，先处理WebGames 的」并正式批准实施（2026-09-25 批准：按规范立项 Phase 90 并编制四阶段细则，系统性治理叙事编排高圈复杂度、契约算法循环热点、瞬态分配与黑话标记清理）。
> **【对应需求源】**：[路线图总索引](../../路线图总索引.md) ｜ [WebGames/README.md](../../../../README.md) §后端逻辑处理标准 v1 ｜ [GD老练风格硬性规范标准.md](../../../模板/GD老练风格硬性规范标准.md) §四 圈复杂度收敛契约与架构解耦契约

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * `WebGames/README.md` §后端逻辑处理标准 v1：六角色模型划分（entity/aggregate、solver、fsm、pipeline、service、registry/dto）与领域逻辑纯洁性；
  * `WebGames/docs/模板/GD老练风格硬性规范标准.md` §四：圈复杂度门禁硬顶（告警阈值 15，失败阈值 25，目标收敛至 $\le 6$）；
  * `WebGames/docs/模板/GD老练风格硬性规范标准.md` §五：性能热点循环治理（`ADV-PRF-002` 循环内零瞬态堆分配，嵌套循环降阶至 $O(N)$）；
  * 仓库根 `AGENTS.md` §二 关键架构约定与工程契约（严禁循环内 `.new()` / `.duplicate(true)`；全域严禁施工批次黑话）；
  * 仓库根 `AGENTS.md` §五 绝对红线禁令（严禁局部最小 MVP 敷衍实现；改动必先立分阶段施工细则；案卷目录严格仅 4 份细则文件）。
* **核心不变量约束断言**：
  * `Inv-P90-1 (行为完全等价)`：所有优化重构必须保证各模块外部 API 签名、参数字典、返回结构与事件广播副作用完全向后兼容；全域既有 772 项测试断言 100% 保持全绿。
  * `Inv-P90-2 (圈复杂度有界收敛)`：重构的目标函数（`execute_node_action`、`validate_graph`）圈复杂度一律拆解至 $\le 6$。
  * `Inv-P90-3 (循环算法降阶与零瞬态分配)`：循环内严禁 `.duplicate(true)`；契约域视图反向查找双重嵌套循环必须优化至 $O(D + E)$ 预索引哈希查表。
  * `Inv-P90-4 (零黑话铁律)`：彻底清理生产代码与注释中残留的 `P39`、`Phase 08`、`Phase 03` 等历史施工批次黑话，对齐规范架构语义。
  * `Inv-P90-5 (防腐守卫完备)`：`account_profile.gd` 之 `is_slot_occupied` 补充非空入参前置校验，消除无意义薄包装异味。
  * `Inv-P90-6 (单源演进与门禁守卫)`：全量 20 项静态门禁（`audit_runner.py`）必须保持 20/20 PASS。
* **防漂移最高指示**：本卷以「高复杂度降阶 → 循环热点消除 → 零瞬态分配 → 零黑话清退」为唯一改造范围；**严禁**越界修改未标记健康代码，**严禁**自造第五份案卷文件。

---

## 一、 审查缺陷分诊与确诊清单

| 缺陷编号 | 规则代号 / 类型 | 缺陷发生文件与位置 | 当前现状与缺陷形态 | 真实性判定 | 本卷治理处置策略 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `DEF-P90-01` | `HYG-STB-002` / `GOV-SAN-001` / 黑话残留 | `backend/domains/account/entitlement_service.gd:71` | 注释含 `# P39 清单 6：仅接受环境密钥...` 批次黑话 | **属实**：违反零黑话铁律 | **清退重构**：移除 `P39` 批次代号，替换为标准安全元数据注释说明 |
| `DEF-P90-02` | `GOV-SAN-001` / 黑话残留 | `backend/domains/account/save_slot_dto.gd:28` | 注释含 `（Phase 08 隔离域：...）` 批次黑话 | **属实**：违反零黑话铁律 | **清退重构**：移除 `Phase 08`，替换为业务语义说明 |
| `DEF-P90-03` | `HYG-STB-002` / 黑话残留 | `backend/domains/narrative_orchestration/causality_dag_validator.gd:7` | 文件头含 `# 设计依据: 业务域第一性原理 / Phase 03 施工细则规范` | **属实**：违反零黑话铁律 | **清退重构**：移除 `Phase 03`，改写为标准架构指引 |
| `DEF-P90-04` | `complexity` / 高圈复杂度 | `backend/domains/narrative_orchestration/narrative_dag_execution_engine.gd:62` | `execute_node_action` 圈复杂度高达 **24**（门禁告警阈值 15）。函数交织汇聚校验、活跃移除、状态合并、终态判定、边权重仲裁与事件派发 | **属实**：全库最高复杂度，维护风险极高 | **子函数拆解**：解构为 4 个私有原子函数，主函数收敛至 $\le 6$ |
| `DEF-P90-05` | `complexity` / 高圈复杂度 | `backend/domains/narrative_orchestration/causality_dag_validator.gd:14` | `validate_graph` 圈复杂度高达 **23**（门禁告警阈值 15）。单函数串联拓扑图元校验、入度邻接构建、悬挂边校验、Kahn 算法与可达闭包 | **属实**：全库第二高复杂度 | **阶段流水线拆分**：分解为 3 个纯校验辅助方法，主函数收敛至 $\le 5$ |
| `DEF-P90-06` | `PRF-ALG-001` / 嵌套循环性能隐患 | `backend/domains/contract_registry/contract_completeness_guard.gd:228-234` | `for d in domains_seen:` 内部嵌套 `for e in all_entries:`，在条目扩增时存在 $O(D \times E)$ 计算开销 | **属实**：双重循环性能热点 | **预索引降阶**：预置 `domains_with_view` 哈希字典，两层嵌套降阶为两次线性遍历 $O(D + E)$ |
| `DEF-P90-07` | `PRF-ALG-001` / 嵌套循环深度 | `backend/domains/contract_registry/contract_parser.gd:135` | `for e in all_events:` 内部嵌套过滤器遍历 `for k in e.event_filter.keys():` | **属实**：认知深度与循环嵌套 | **守卫抽取**：提取 `_matches_event_filter` 私有守卫，消除主循环嵌套深度 |
| `DEF-P90-08` | `PRF-MEM-001` / 循环内深拷贝堆开销 | `backend/domains/contract_registry/contract_registry_entry.gd:67,71` | `to_dto()` 循环体内重复执行 `fm.duplicate(true)` 与 `ps.duplicate(true)`，违反 `AGENTS.md` 绝对红线 | **属实**：高频堆分配 | **浅拷贝替换**：改用 `duplicate(false)` 浅拷贝，消除不必要的深拷贝递归分配 |
| `DEF-P90-09` | `HYG-WRAP-001` / 空包装穿透 | `backend/domains/account/account_profile.gd:89` | `is_slot_occupied` 直接穿透至 `save_slot_summaries.has(slot_id)`，无前置防御 | **属实**：薄包装异味 | **防腐守卫**：补充 `if slot_id.is_empty(): return false` 防空校验，提升稳健性 |

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

* [ ] **Step 1.1 审查缺陷深度盘点与代码级证据核验** — 确认 9 项缺陷在源文件的行号、上下文及调用关系无偏差。
* [ ] **Step 1.2 架构解耦与行为保真契约确立** — 验证所有公开接口调用方与既有测试依赖。
* [ ] **Step 1.3 高圈复杂度函数拆解拓扑设计** — 为 2 个高复杂度函数规划私有子方法签名与职责边界。
* [ ] **Step 1.4 算法性能降阶与数据结构选型** — 确立 `contract_completeness_guard.gd` 哈希字典预索引。
* [ ] **Step 1.5 零黑话与统一规范对齐** — 确立清理规范与常量命名一致性。


---

## 三、 验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P90-S1-01` | 缺陷盘点逐项定性 | 审查缺陷分诊清单与代码锚点 | 9 项缺陷均有代码级行号佐证、真实性判定与明确治理策略 |
| `TC-P90-S1-02` | 不变量契约完备性 | 6 项核心不变量（`Inv-P90-1~6`） | 涵盖行为保真、圈复杂度、性能降阶、零黑话与门禁看守 |
