# 施工细则：门禁体系可信度恢复与工具链缺陷全量治理 —— 阶段3：配置驱动与文档单一真源改造

> [!NOTE]
> **【施工目标】**：按 `Inv-P87-7` 消除文档中的失实易变数字与失效引用，使「质量现状」「配置表计数」「测试套件计数」等声明与可复现命令口径一致；对无法在本卷内闭环的机器化生成需求，给出显式暂缓登记与后续承载位置，杜绝无主遗留。
> **【施工开始日期】：施工开始日期: 2026-09-13（早上）** —— 真实读取系统时间。
> **【对应需求源】**：[阶段1 数据契约与检出契约](阶段1_门禁可信度缺陷数据契约与检出契约.md) ｜ [阶段2 工具链与运行时缺陷修复实现](阶段2_工具链与运行时缺陷修复实现.md) ｜ [路线图总索引](../../路线图总索引.md)
> **【实施边界】**：只改文档与项目元数据中的**事实性数字与失效引用**；不新增门禁文件、不修改任何基线文件、不改动 `config/**/*.json` 业务配置内容。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * `WebGames/docs/README.md` §二 硬性规范（文档布局、相对链接、命名登记）与 §四 基线棘轮（存量只许消化、不许增长）；
  * `WebGames/config/README.md`（配置驱动分层契约：`config/domains/<name>.json` → 表名 `domains.<name>`）；
  * `WebGames/README.md` §质量现状（本次校正的目标区块）；
  * 仓库根 `AGENTS.md` §四 命名格式与 Agent 行为边界（禁止留存未决标记与占位符）。
* **核心不变量约束断言**：
  * `Inv-P87-7 (文档数字可复现)`：质量现状类数字必须能由命令重现，且与 `audit_config.py` 等门禁口径一致；无法复现的数字不得留存。
  * `Inv-P87-8 (示例路径可用)`：权威文档中的示例路径必须指向真实存在的文件；禁止指向历史路径。
  * `Inv-P87-9 (易变数字去耦合)`：项目元数据（如 `project.godot` description）不得嵌入随版本演进的计数（阶段号、卷数），以免每次演进即漂移。
* **防漂移最高指示**：本轮全部文档改动必须使 `audit-docs.sh --baseline` 保持「新增违规 = 0」；严禁通过 `--update-baseline` 消音。

---

## 一、 失实声明校正与单一真源口径

### 1.1 校正对照（声明 → 真值 → 出处）

| 声明（出处） | 原声明值 | 校正值 | 复现口径 |
| :--- | :--- | :--- | :--- |
| `README.md` 快速开始注释 | 90+ 个测试套件 | **109 个测试套件** | `grep -c "preload(" tests/test_registry.gd` |
| `README.md` §质量现状 backend `.gd` / 行数 | 303 / 约 22,994 行 | **302 / 约 26,688 行** | `find backend -name '*.gd'` 计数与 `wc -l` |
| `README.md` §质量现状 全域 `.gd` / 行数 | 443 / 约 49,697 行 | **513 / 约 62,461 行** | `find . -name '*.gd' -not -path './.godot/*'` |
| `README.md` §质量现状 测试项 | 714（90+ 套件） | **768（109 套件）** | `tests/reports/test_latest.json` 的 `total` |
| `README.md` §质量现状 配置表 | 125 | **123（含 65 张必需表）** | `python3 scripts/py/audit_config.py` |
| `README.md` §质量现状 配置调用点 | 842 | **862** | `grep -rhoE "GameConfig\.(get_[a-z]+\|msg)\(" --include=*.gd backend frontend \| wc -l` |
| `scripts/README.md` `check-*` 行 | 全量 443 个 `.gd` | **全量 513 个 `.gd`** | 同全域 `.gd` 口径 |
| `tests/README.md` 全套件行 | 110+ 套件 | **109 套件** | 同测试套件口径 |
| `config/README.md` 文案层计数 | 50 张 | **49 张** | `ls config/narratives/*.json \| wc -l` |
| `config/README.md` domains 登记示例 | `res://tests/unit/test_inventory.gd` | **`res://tests/unit/domains/test_inventory.gd`** | `ls` 实证（旧路径不存在） |
| `project.godot` `config/description` | 「52阶段第一性原理全域确定性母机与解耦宿主」 | **「第一性原理全域确定性母机与解耦宿主」**（移除易变阶段号） | `Inv-P87-9` |

### 1.2 已核验为「无需改动」的声明

| 声明 | 核验结论 |
| :--- | :--- |
| `README.md`「`GameConfig.get_value` 违禁调用 0」 | **成立**：`backend/` 与 `frontend/` 内实测 0 处；唯一命中位于 `tests/guards/test_configuration_guard.gd:118`，属护栏自身探针，非业务违规 |
| `docs/README.md`「基线已清零（errors: 0, warnings: 0）」 | **成立**：本轮门禁解锁后实测 `audit_docs.py` error 0 / warn 0（753 份文档） |
| `README.md`「领域数 47」 | **成立**：`audit_arch.py` TC-ARCH-01 / TC-ARCH-05 隔离复测 7 项全 PASS |

### 1.3 暂缓登记（本卷不实施，显式留痕）

| 暂缓项 | 原因 | 建议承载位置 |
| :--- | :--- | :--- |
| 质量现状表「机器化生成」（由 `test_latest.json` + `find` 统计自动填充 README 稳定标记块） | 需新增生成脚本并接入 `audit-all` 链，涉及工具注册与失败语义设计；本卷以人工校正消除当前漂移，治本方案独立成卷更清晰 | 后续门禁专项卷 |
| 将质量现状数字纳入 `TC-ARCH-05` 单源断言扩展 | `TC-ARCH-05` 现由架构护栏承载且已守领域计数；扩展需同步 `.gd` 计数与套件计数口径，属护栏演进，须独立评审避免护栏膨胀 | 架构护栏演进批次 |
| `docs/audit/` 4 份历史审计报告加取代标记 | 属归档治理，涉及 `NAMING_RULES` 与归档流程 | 归档治理批次 |
| `SYSTEM_HEARTBEAT_TICK` 心跳发送接线 | 本卷无「心跳驱动者」场景（单机无头母机无外部探活需求）；需先设计驱动时序与调用方，属可观测性架构演进 | 可观测性专项卷 |
| `reload_config()` 的 `changed` 事件载荷消费方 | 当前无监听者消费 `config.security_reloaded.changed`，属潜伏契约；接线需与演进 02 前端事件体系协同 | 演进 02 协同批次 |

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 3.1 逐条取证声明真值（`find` 与 `wc` 与 `grep` 与门禁输出）** — 通过标准：每条声明均有可复现命令与真实输出
- [x] **Step 3.2 校正 README 快速开始与质量现状区块** — 通过标准：109 套件与 302 与 513 与 768 与 123 与 862 全部落表
- [x] **Step 3.3 校正 scripts 与 tests 与 config 三处 README 计数与示例路径** — 通过标准：示例路径 `ls` 可命中；计数与命令输出一致
- [x] **Step 3.4 移除 `project.godot` description 中的易变阶段号** — 通过标准：description 不再含可演进计数
- [x] **Step 3.5 暂缓项显式登记（原因与承载位置）** — 通过标准：暂缓项数量与承载位置明确，无无主遗留

---

## 三、 验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P87-S3-01` | 测试套件计数一致 | `grep -c "preload(" tests/test_registry.gd` 对比 README/tests-README | 三处均为 109 |
| `TC-P87-S3-02` | `.gd` 计数一致 | `find` 口径对比 README 与 scripts/README | 均为 513（全域）/ 302（backend） |
| `TC-P87-S3-03` | 测试项计数一致 | `test_latest.json` 的 `total` 对比 README | 均为 768 |
| `TC-P87-S3-04` | 配置表计数与门禁一致 | `audit_config.py` 输出对比 README | 均为 123 张 / 65 张必需表 |
| `TC-P87-S3-05` | 示例路径可用 | `ls tests/unit/domains/test_inventory.gd` | 文件存在；旧路径不存在 |
| `TC-P87-S3-06` | 元数据去易变计数 | `grep "52阶段" project.godot` | 命中数 0 |
| `TC-P87-S3-07` | 文档门禁零新增 | `bash scripts/sh/audit-docs.sh --baseline benchmarks/reports/docs_baseline.json` | 退出码 0 且新增违规 0 |
| `TC-P87-S3-08` | 暂缓项无悬空 | 阶段3 §1.3 表格 | 每项均有原因与建议承载位置 |
