# 施工细则：门禁体系可信度恢复与工具链缺陷全量治理 —— 阶段4：全量回归验收与防复发测试矩阵

> [!NOTE]
> **【施工目标】**：以可复现的真实执行结果对 `Inv-P87-1 ~ Inv-P87-9` 逐条验收，交付「修复前 → 修复后」对照证据；同时完成架构类残余缺陷的**显式暂缓登记**，确保本卷闭环后无悬空行动项。
> **【施工开始日期】：施工开始日期: 2026-09-13（早上）** —— 真实读取系统时间。
> **【对应需求源】**：[阶段1](阶段1_门禁可信度缺陷数据契约与检出契约.md) ｜ [阶段2](阶段2_工具链与运行时缺陷修复实现.md) ｜ [阶段3](阶段3_配置驱动与文档单一真源改造.md) ｜ [路线图总索引](../../路线图总索引.md)
> **【验收环境】**：Godot `4.7.2.stable.official.ed1daf0bf`；Python `3.13.14`；平台 win32 + Git Bash。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * `WebGames/README.md` §持续集成与架构门禁（「连续两次都红才是真问题」的伪红排除判据）；
  * `WebGames/docs/README.md` §四 基线棘轮（增量门禁语义）；
  * `WebGames/tests/README.md`（断言矩阵与注册表契约）；
  * 仓库根 `AGENTS.md` §三 测试工程化拓扑与零黑话铁律。
* **核心不变量约束断言**：
  * `Inv-P87-1 ~ Inv-P87-9` 全部必须由本阶段的可复现命令给出客观证据，禁止以「已修改」代替「已验证」。
  * `Inv-P87-10 (负向可证伪)`：新增的每一条防复发断言，必须在**临时回退对应修复后转为失败**——否则该断言无效（防假绿）。
  * `Inv-P87-11 (暂缓无悬空)`：无法在本卷闭环的项必须以「暂缓登记」显式留痕，含原因与建议承载位置。
* **防漂移最高指示**：验收以「修复后全绿 + 负向可证伪 + 文档零新增违规」三项同时成立为唯一放行条件，缺一不可。

---

## 一、 修复前 → 修复后 对照证据

| 验收项 | 修复前（本轮审查实测） | 修复后（本卷实测） |
| :--- | :--- | :--- |
| 全域静态门禁 `audit_runner.py` | `TOTAL=20 / PASS=15 / FAIL=5`，退出码 1 | **`20 / 20 PASS`，退出码 0，0 Error / 0 Warn** |
| `audit_config.py --strict` | 崩溃（`NameError: name 'Any' is not defined`，`:57`） | **退出码 0**（配置表 123 张 / 必需表 65 张） |
| `audit_docs.py` | 崩溃（`:182` 同因） | **退出码 0**（error 0 / warn 0，753 份文档） |
| `audit_event_probability.py` | 崩溃（`:45`） | 退出码 0 |
| `audit_config_unused.py` | 崩溃（`:76`，嵌套函数注解） | 退出码 0 |
| `audit_event_channels.py` | 崩溃（`:74`，嵌套函数注解） | 退出码 0 |
| `archive_volume.py` | 崩溃（经 `archive/inspect.py:123`） | **退出码 0** |
| `bash run_tests.sh`（DoD 入口） | 退出码 1（`Invalid project path specified: "/c/..."`） | **退出码 0；768 / 768 断言通过；`tests/reports/test_*.json` 首次生成** |
| `bash scripts/sh/audit-all.sh`（一键门禁） | 退出码 2（`can't open file 'C:\c\Users\...'`） | **退出码 0；20 / 20 PASS** |
| 文档计数声明 | README 443 / 49,697 / 714 / 90+ / 125 等全面失实 | 已按可复现口径校正为 513 / 62,461 / 768 / 109 / 123 |
| `game_config.reload_config()` | 原子交换/失败回滚/变更集三契约实质失效 | 深拷贝隔离后四契约成立（由 `TC-SV-35` / `TC-SV-36` 看守） |
| `ErrorReporter` 脱敏 | 旁路未脱敏，凭证可明文落盘 | 构造期经 `RedactionRule.apply` 后方可广播 |

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 4.1 正向验收：全域门禁与全域测试与两条一键入口** — 通过标准：20/20 PASS、768/768 通过、两入口退出码 0
- [x] **Step 4.2 负向验收：临时回退 `game_config` 修复，确认 `TC-SV-35` / `TC-SV-36` 转红** — 通过标准：回退后为 766/768（108/109 套），恢复后回到 768/768
- [x] **Step 4.3 规则自证：`PY-ANNOTATION-IMPORT` 正反例矩阵** — 通过标准：6/6 符合预期（含嵌套函数与惰性注解与局部导入场景）
- [x] **Step 4.4 文档门禁基线棘轮核验** — 通过标准：新增违规 = 0，禁止使用 `--update-baseline`
- [x] **Step 4.5 架构类残余缺陷显式暂缓登记** — 通过标准：每项含缺陷编号与原因与建议承载位置

---

## 三、 验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P87-S4-01` | 全域门禁 100% 通过 | `bash scripts/sh/audit-all.sh` | 20 / 20 PASS；退出码 0 |
| `TC-P87-S4-02` | 全域测试 100% 通过 | `bash run_tests.sh` | 768 / 768；套件 109 / 109；退出码 0 |
| `TC-P87-S4-03` | 测试报告产物生成 | `ls tests/reports/test_latest.json` | 文件存在且 `failed` 为 0 |
| `TC-P87-S4-04` | 负向可证伪 | 临时回退 `game_config.gd:163` 后跑测试 | 766 / 768（恰好 `TC-SV-35`/`TC-SV-36` 失败） |
| `TC-P87-S4-05` | 防复发规则自证 | `_check_py_annotation_bindings` 正反例矩阵 | 6 / 6 符合预期 |
| `TC-P87-S4-06` | 规则零误报 | `python3 scripts/py/audit_script_comment.py` | 74 份脚本 0 违规，退出码 0 |
| `TC-P87-S4-07` | 文档棘轮零新增 | `bash scripts/sh/audit-docs.sh --baseline benchmarks/reports/docs_baseline.json` | 退出码 0，新增违规 0 |
| `TC-P87-S4-08` | 架构护栏不退化 | `bash scripts/sh/audit-arch.sh` | 退出码 0，TC-ARCH-01~07 全 PASS |
| `TC-P87-S4-09` | 文档计数一致性 | README / scripts-README / tests-README / config-README 计数比对 | 全部与复现口径一致 |
| `TC-P87-S4-10` | 暂缓项无悬空 | 本阶段 §四 暂缓登记表 | 每项含编号、原因、承载位置 |

---

## 四、 架构类残余缺陷暂缓登记

| 编号 | 缺陷 | 严重度 | 暂缓原因 | 建议承载位置 |
| :--- | :--- | :--- | :--- | :--- |
| `DEF-P87-A1` | `solver` 违反「禁 mutator」红线 11 处（如 `elite_mutation/generic_affix_solver.gd:33`、`magic_system/seal_solver.gd:76`、`notification_red_dot/red_dot_tree_solver.gd:14`） | 🟡 中 | 属角色职责归位重构：涉及签名变更与全部调用点，须按领域分批立卷并配套按域回归，单卷内无法安全闭环 | 后端角色归位专项卷（分批） |
| `DEF-P87-A2` | `fsm` 跨聚合编排 32 处（如 `commission_quest/commission_fsm.gd`、`equipment_loadout/equipment_fsm.gd`） | 🟡 中 | 需抽至 pipeline 编排，属跨域职责重划，须先立架构决策记录（ADR）后再实施 | 架构 ADR + 后端角色归位专项卷 |
| `DEF-P87-A3` | 前端上帝对象（`guild_social_view.gd` 1015 行、`character_progression_view.gd` 949 行） | 🟡 中 | 拆分涉及视图节点结构与快照契约联动，与演进 02（前端事件接线）强耦合，应先接线后拆分 | 演进 02 协同批次 |
| `DEF-P87-A4` | 循环内瞬态堆分配 4 处（`error_code_registry.gd:44`、`game_mode_routing_solver.gd:29`、`redaction_rule.gd:65`、`log_query_service.gd:100`） | 🟡 中 | 需先建基准数据证明热点真实存在（避免无实测优化的臆测改动）；与演进 03（对象池）能力协同更优 | 演进 03 协同批次（附 benchmark 证据） |
| `DEF-P87-A5` | 存档自愈缺口（`restore_backup` 无生产调用、`.tmp` 无锁、孤儿临时文件无清理、单代 `.bak`） | 🟠 高 | 涉及持久化协议契约变更与失败/降级语义设计，须按四阶段重新立卷并配套损坏档注入测试 | 持久化韧性专项卷 |
| `DEF-P87-A6` | `ObjectDB` 泄漏告警（68 个实例 / 1 RID / 9 resources in use） | 🟢 低 | 需逐套件定位未释放单例，属测试基建质量项，不影响业务正确性 | 测试基建治理批次 |

> 说明：上述项均为**架构级职责归位与韧性演进**，非本轮事故（门禁体系失效）的成因；按「先恢复可验证性、后推进架构演进」的优先级原则显式暂缓，避免在门禁刚恢复时引入高风险重构。
