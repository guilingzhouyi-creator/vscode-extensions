# 施工细则：架构角色归位与存档韧性残余治理 —— 阶段2：角色归位与存档韧性实现

> [!NOTE]
> **【施工目标】**：按 `Inv-P88-2/3` 完成 `A1` 的**角色重分类与混合角色拆分**（零调用点风险）；按 `Inv-P88-4/5` 完成 `A5` 的**存档自愈闭环**（配置驱动、回滚深度有界、可证伪）。
> **【施工开始日期】：施工开始日期: 2026-09-13（早上）** —— 真实读取系统时间（`2026-09-13 09:47 早上`）。
> **【对应需求源】**：[阶段1 残余缺陷判据收敛与分诊契约](阶段1_残余缺陷判据收敛与分诊契约.md) ｜ [路线图总索引](../../路线图总索引.md)
> **【实施边界】**：`A2`（fsm 跨聚合）与 `A3`（前端上帝对象）本卷不实施（转 ADR 与演进 02）；`A4` 以证据结案不做臆测改码；`A6` 仅登记。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * 阶段1 §1.2 `A1` 真实违规清单与 §一 分诊表；
  * `WebGames/README.md` §后端逻辑处理标准 v1（fsm「允许实例状态 / 状态推进」；entity/aggregate「状态容器，不做业务判定」）；
  * `WebGames/README.md` §存档完整性（原子替换契约与 `data_json` 单字段校验）；
  * `WebGames/config/README.md`（配置驱动：数值/文案/开关一律来自 `config/`）。
* **核心不变量约束断言**：
  * `Inv-P88-2`：归位后，原违规文件中不再存在「`-> void` 且改写外部入参状态」的函数。
  * `Inv-P88-3`：重命名后引用面零残留；全域断言数**不得减少**（770 项基线）。
  * `Inv-P88-4`：`recovery/auto_restore_enabled` 与 `recovery/orphan_tmp_cleanup` 由配置声明，代码仅兜底。
  * `Inv-P88-5`：自动恢复递归深度上限 1；恢复/重读失败一律回退原始失败结果（保留 `error_code`）。
  * `Inv-P88-7 (失败语义不美化)`：自动恢复仅在**损坏类**错误码（`CHECKSUM_MISMATCH` / `JSON_PARSE_FAIL` / `INVALID_ENVELOPE`）上触发；版本方向闸门与文件缺失不参与。
* **防漂移最高指示**：`A1` 只做「声明与归属」的更正与拆分，**不得**顺手改动业务算法；`A5` 只做「接线与回收」，**不得**改动既有校验与原子写语义。

---

## 一、 实施内容

### 1.1 `A1` 角色归位（以 `git mv` 保历史）

| 对象 | 变更前 | 变更后 | 说明 |
| :--- | :--- | :--- | :--- |
| 文件 | `backend/domains/magic_system/seal_solver.gd` | `seal_fsm.gd`（含 `.uid` 同步 `git mv`） | 动词已是 `apply_*`，角色应为 fsm |
| 类名 | `class_name SealSolver` | `class_name SealFsm` | 引用面：`tests/unit/domains/test_magic_system.gd`（已同步） |
| 题头 | 架构定位 `Headless Discrete Solver / Numerical Calculator` | `Domain FSM / State Advancer` | 职责说明增补六角色归位依据 |
| 文件 | `backend/domains/notification_red_dot/red_dot_tree_solver.gd` | `red_dot_tree_fsm.gd`（含 `.uid`） | 原地改写注入 registry，属状态推进 |
| 类名 | `class_name RedDotTreeSolver` | `class_name RedDotTreeFsm` | 引用面：`tests/unit/domains/test_notification_red_dot.gd` + `tests/integration/pipelines/test_log_error_base_pipeline.gd` 的 push 分级白名单路径（均已同步） |
| 方法 | `GenericAffixSolver.apply_elite_affixes(monster, affix_ids) -> void` | `EliteMonsterAggregate.apply_affixes(affix_ids) -> void` | 混合角色拆分：改状态能力回归实体自身（entity 承载自身状态），求解器仅保留纯函数 `roll_random_affixes` / `get_affix` |

**归位后角色一致性**：`generic_affix_solver.gd` 收敛为纯求解器（无任何入参状态改写）；`seal_fsm.gd` 与 `red_dot_tree_fsm.gd` 的题头架构定位与其实质行为（对注入状态执行推进）一致。

### 1.2 `A5` 存档自愈闭环

| 能力 | 实现位置 | 配置键 | 契约 |
| :--- | :--- | :--- | :--- |
| 孤儿临时文件清理 | `SaveManager.ensure_save_directory()` → `_cleanup_orphan_tmp_files()` | `recovery/orphan_tmp_cleanup`（默认 true） | 扫描存档目录内 `*{tmp_suffix}` 并回收，计数 > 0 时告警留痕 |
| 正档损坏自动恢复 | `SaveManager.load_game()` 的损坏类失败路径 → `_fail_or_recover()` | `recovery/auto_restore_enabled`（默认 true） | 仅损坏类错误码触发；`.bak` 不存在或恢复/重读失败则回退原始失败；成功后结果携带 `auto_restored=true` 与 `recovered_from=<原始错误码>` |
| 递归深度上限 | `static var _recovering: bool` 守卫 | — | 深度硬上限 1，防 `.bak` 亦损坏时无界递归 |
| 文案 | `config/infrastructure/persistence.json` `messages.auto_restored` / `messages.orphan_tmp_cleaned` | — | 零硬编码，全部经 `_msg()` 出口 |

### 1.3 契约测试（延续既有同域文件，不新建测试文件）

| 用例 | 承载文件 | 断言 |
| :--- | :--- | :--- |
| `TC-SV-37` | `tests/unit/infrastructure/test_save_consistency_recovery.gd` | 人为遗留孤儿 `.tmp` → 启动清理后不存在 |
| `TC-SV-38` | 同上 | 正档被破坏、`.bak` 合法 → `load_game` 成功且 `auto_restored=true`，恢复内容为上一代正档（`round == 1`） |
| `TC-SV-35` / `TC-SV-36` | `tests/unit/infrastructure/test_editor_hot_reload.gd` | Phase 87 热重载契约（本轮因全市编号占用实测而从 28/29 顺延为 35/36） |

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 2.1 `A1` 文件与类名重命名（含 `.uid`）** — 通过标准：`git mv` 保留历史，引用面实测穷尽并同步
- [x] **Step 2.2 `A1` 题头架构定位与职责说明更正** — 通过标准：声明与实际行为一致，附归位依据
- [x] **Step 2.3 `A1` 混合角色拆分（`EliteMonsterAggregate.apply_affixes`）** — 通过标准：求解器文件不再含改写外部入参的函数；调用点同步
- [x] **Step 2.4 `A5` 配置声明（开关 + 文案）** — 通过标准：`persistence.json` 新增 `recovery` 段与两条文案，无硬编码
- [x] **Step 2.5 `A5` 孤儿 `.tmp` 清理接线** — 通过标准：清理入口在启动/写档路径可达，计数留痕
- [x] **Step 2.6 `A5` 损坏类失败自动恢复接线** — 通过标准：仅损坏类错误码触发，深度上限 1，失败回退原始结果
- [x] **Step 2.7 `TC-SV-37/38` 契约测试用例落地** — 通过标准：正向通过 + 负向可证伪（关闭开关后转红）

---

## 三、 验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P88-S2-01` | 归位后无残留外部入参改写 | `seal_fsm.gd` / `red_dot_tree_fsm.gd` / `generic_affix_solver.gd` | 求解器文件零「`-> void` 改外部入参」；fsm 文件声明与行为一致 |
| `TC-P88-S2-02` | 引用面无残留 | 全域 grep `SealSolver` / `RedDotTreeSolver` / `apply_elite_affixes`，范围限定 `backend/` `frontend/` `tests/` `config/` | **代码引用 0 处**；旧名仅允许作为变更溯源注释与本卷案卷文档保留 |
| `TC-P88-S2-03` | 类缓存与语法可用 | `godot --headless --import` + `check-gdscript.sh` | 均退出码 0 |
| `TC-P88-S2-04` | 存档自愈正向 | `TC-SV-38` | `success=true` 且 `auto_restored=true` 且 `round=1` |
| `TC-P88-S2-05` | 孤儿清理正向 | `TC-SV-37` | 遗留 `.tmp` 被回收 |
| `TC-P88-S2-06` | 负向可证伪 | 关闭 `recovery` 两开关后跑全域测试 | 恰为 768 / 770（`TC-SV-37` 与 `TC-SV-38` 转红） |
| `TC-P88-S2-07` | 失败语义不美化 | 无 `.bak` 场景 | 返回原始 `error_code`（不返回 `auto_restored`） |
| `TC-P88-S2-08` | 断言数不减少 | `bash run_tests.sh` | 770 / 770 通过 |
