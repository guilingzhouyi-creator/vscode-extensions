# 施工细则：架构角色归位与存档韧性残余治理 —— 阶段4：全量回归验收与残余余量登记

> [!NOTE]
> **【施工目标】**：以可复现执行结果验收 `Inv-P88-1 ~ Inv-P88-9`；对**未在本卷实施**的项（`A2` / `A3` / `A4` 结案 / `A6` / `cdkey` 角色归位）给出显式的去向登记，确保本卷闭环后无悬空行动项。
> **【施工开始日期】：施工开始日期: 2026-09-13（早上）** —— 真实读取系统时间（`2026-09-13 09:47 早上`）。
> **【对应需求源】**：[阶段1](阶段1_残余缺陷判据收敛与分诊契约.md) ｜ [阶段2](阶段2_角色归位与存档韧性实现.md) ｜ [阶段3](阶段3_配置驱动与编号纪律协同.md) ｜ [路线图总索引](../../路线图总索引.md)
> **【验收环境】**：Godot `4.7.2.stable.official.ed1daf0bf`；Python `3.13.14`；平台 win32 + Git Bash。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * `WebGames/README.md` §持续集成与架构门禁（「连续两次都红才是真问题」的伪红排除判据）；
  * `WebGames/docs/README.md` §四 基线棘轮；
  * `WebGames/tests/README.md`（测试注册表与断言矩阵契约）；
  * 仓库根 `AGENTS.md` §三（测试拓扑与零黑话铁律）。
* **核心不变量约束断言**：
  * `Inv-P88-3`：全域断言数不得减少（本轮基线由 768 增至 770）。
  * `Inv-P88-10 (负向可证伪)`：本轮新增的每条能力性断言，必须证明其在对应实现被禁用/缺失时**转红**。
  * `Inv-P88-11 (余量无悬空)`：未实施项必须以「去向 + 原因 + 承载位置」三要素登记。
* **防漂移最高指示**：验收以「门禁全绿 + 测试全绿 + 负向可证伪 + 文档零新增违规」四项同时成立为唯一放行条件；**禁止**用「项数完成度」替代质量证据。

---

## 一、 修复前后对照证据

| 验收项 | 本卷前 | 本卷后 |
| :--- | :--- | :--- |
| 全域断言数 | 768 / 768 | **770 / 770**（新增 `TC-SV-37` / `TC-SV-38`） |
| `A1` 角色归位 | `seal_solver.gd` / `red_dot_tree_solver.gd` 声明为纯求解器但原地改写注入状态；`generic_affix_solver.gd` 混合角色 | 归位为 `seal_fsm.gd` / `red_dot_tree_fsm.gd`；改状态能力回归 `EliteMonsterAggregate.apply_affixes`；求解器文件零外部入参改写 |
| `A5` 存档自愈 | `restore_backup` 仅测试调用；孤儿 `.tmp` 无回收流程 | `load_game` 损坏路径自动恢复（深度上限 1）；`ensure_save_directory` 回收孤儿 `.tmp`；两者配置驱动 |
| `A4` 循环内堆分配 | 报告口径 4 处 | **以证据结案**：4 处均非热路径循环（2 处配置加载期、2 处不在循环内），1 处为分页深拷贝隔离 |
| 测试编号唯一性 | `TC-SV-28/29` 跨文件重号 | 顺延为 `TC-SV-35/36`，全域唯一；4 处文档引用同步 |
| 门禁状态 | Phase 87 已恢复 20/20 | **维持 20/20**（本卷改动未引入门禁回归） |

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 4.1 正向验收：门禁 + 测试 + 语法 + 架构护栏** — 通过标准：20/20、770/770、语法通过、TC-ARCH-01~07 全 PASS
- [x] **Step 4.2 负向验收：关闭 `recovery` 两开关** — 通过标准：恰为 768/770（`TC-SV-37`、`TC-SV-38` 转红），恢复后回到 770/770
- [x] **Step 4.3 引用面零残留核验** — 通过标准：旧类名与旧文件路径在活跃代码/文档中命中 0
- [x] **Step 4.4 文档门禁棘轮核验** — 通过标准：新增违规 = 0，未使用 `--update-baseline`
- [x] **Step 4.5 未实施项去向登记** — 通过标准：每项含去向、原因、承载位置三要素

---

## 三、 验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P88-S4-01` | 全域门禁 100% | `bash scripts/sh/audit-all.sh` | 20 / 20 PASS，退出码 0 |
| `TC-P88-S4-02` | 全域测试 100% | `bash run_tests.sh` | 770 / 770，套件 109 / 109，退出码 0 |
| `TC-P88-S4-03` | 语法门禁 | `bash scripts/sh/check-gdscript.sh` | 退出码 0 |
| `TC-P88-S4-04` | 架构护栏不退化 | `bash scripts/sh/audit-arch.sh` | 退出码 0，TC-ARCH-01~07 全 PASS |
| `TC-P88-S4-05` | 负向可证伪 | 关闭 `recovery` 两开关后跑测试 | 768 / 770，且失败项恰为 `TC-SV-37` / `TC-SV-38` |
| `TC-P88-S4-06` | 旧名零残留 | 全域 grep 于 `backend/` `frontend/` `tests/` `config/` | **代码引用 0 处**；实测仅 `generic_affix_solver.gd` 第 6 与 31 行两处变更溯源注释提及旧名 |
| `TC-P88-S4-07` | 文档棘轮零新增 | `audit-docs.sh --baseline` | 退出码 0，新增违规 0 |
| `TC-P88-S4-08` | 余量登记完备 | 本阶段 §四 | 每项含去向、原因、承载位置 |

---

## 四、 未实施项去向登记（本卷余量）

| 编号 | 项 | 本卷去向 | 原因 | 承载位置 |
| :--- | :--- | :--- | :--- | :--- |
| `DEF-P87-A2` | `fsm` 跨聚合编排（32 处，如 `commission_quest/commission_fsm.gd` 双持 Commission + Organization 并双改；`equipment_loadout/equipment_fsm.gd` 镜像同步） | **转 ADR 设计件** | 正确修法为抽至 `pipeline` 编排，属跨域职责重划；无 ADR 直接改会造成编排权归属歧义，且 32 处需按领域分批回归 | 架构 ADR + 后端角色归位专项卷（分批） |
| `DEF-P87-A3` | 前端上帝对象（`guild_social_view.gd` 1015 行、`character_progression_view.gd` 949 行） | **转演进 02 协同批次** | 拆分需视图节点结构与快照契约联动；演进 02（前端 HUD 事件接线）将重写这些视图的数据注入路径，先拆分会导致二次返工 | `02_长期演进区/演进_02_EventBus前端HUD事件接线与状态同步架构` |
| `DEF-P87-A4` | 循环内瞬态堆分配 4 处 | **以证据结案（不做改码）** | 逐点复核均为配置加载期或非循环位置；唯一循环内者为分页查询的刻意深拷贝隔离。按 `ADV-PRF-002` 的热路径适用范围不成立；臆测改动将无收益却引入回归风险 | 本卷阶段1 §一 结案记录 |
| `DEF-P87-A5`（残留） | `cdkey_redemption_solver._record_failure` 的可变级联（经公开 `purge_expired_rate_limits`） | **列入角色归位专项批次** | 归位需连带重构公开方法签名与全部调用方，属半程重构；本卷已用「最小面」策略只做零级联的两个文件与一处拆分 | 后端角色归位专项卷（分批） |
| `DEF-P87-A6` | `ObjectDB` 泄漏 68 实例 / 1 RID / 9 resources | **仅登记** | 属测试基建质量项，不影响业务正确性；定位需逐套件 `--verbose` 归因，成本与收益在本卷不成比例 | 测试基建治理批次 |
| `DEF-P87-A7`（新） | `test_latest.json` 的 `ok` 字段语义反直觉（`ok: 0` 表示通过） | **仅登记** | 改字段语义会破坏既有消费方；需先确认消费面再统一 | 测试基建治理批次 |

> 说明：本卷对「真实且可安全闭环」的项（`A1` 三个文件归位 + `A5` 存档自愈）完成了实施与可证伪验收；对「口径失真」项（`A4`）以证据结案；对「需先设计或存在前置依赖」项（`A2` / `A3` / `A5` 残留 / `A6` / `A7`）给出明确去向，避免在门禁刚恢复时引入高风险重构。
