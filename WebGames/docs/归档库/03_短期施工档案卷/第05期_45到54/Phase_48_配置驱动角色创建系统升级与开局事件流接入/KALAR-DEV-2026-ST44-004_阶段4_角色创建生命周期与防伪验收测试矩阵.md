---
档号: KALAR-DEV-2026-ST44-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST44 (Phase_48_配置驱动角色创建系统升级与开局事件流接入)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_48_配置驱动角色创建系统升级与开局事件流接入 —— 阶段4：角色创建生命周期与防伪验收测试矩阵
形成日期: 2026-09-03
归档日期: 2026-09-05（上午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 角色创建生命周期; domains.json
---

# 施工细则：Phase 48 配置驱动角色创建系统升级与开局事件流接入 —— 阶段4：角色创建生命周期与防伪验收测试矩阵

> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST44-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST44-001_阶段1_配置驱动角色定义与开局事件契约设计.md) ｜ [阶段2](KALAR-DEV-2026-ST44-002_阶段2_角色创建状态机与后端属性生成算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST44-003_阶段3_灰度扩展校验与事件总线发布工程化.md) ｜ **阶段4 (当前)**

> 施工开始日期: 2026-09-03 下午
> 责任人: 卡拉尔世界引擎架构组
> 状态: ✅ 已施工完毕（两轮施工全生命周期闭环，全量单测与 17 项门禁 100% PASS）

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * [阶段1_配置驱动角色定义与开局事件契约设计.md](KALAR-DEV-2026-ST44-001_阶段1_配置驱动角色定义与开局事件契约设计.md)（契约模型）
  * [阶段2_角色创建状态机与后端属性生成算法实现.md](KALAR-DEV-2026-ST44-002_阶段2_角色创建状态机与后端属性生成算法实现.md)（状态机与生成算法）
  * [阶段3_灰度扩展校验与事件总线发布工程化.md](KALAR-DEV-2026-ST44-003_阶段3_灰度扩展校验与事件总线发布工程化.md)（配置与服务）
  * [tests/test_registry.gd](../../../../../tests/test_registry.gd)（测试注册表）
* **核心不变量约束断言**：
  1. **断言 100% 验收通过率不变量**：所有新设计的创角与事件流用例必须实现确定性断言，杜绝脆弱性测试；
  2. **前端属性注入零信任不变量**：测试必须构造携带伪造基础属性的入参，验证后端严密丢弃并仅采用配置生成逻辑；
  3. **防重复创建与档位冲突覆盖**：对已被占用的档位再次发起创角，必须严格返回 `SLOT_ALREADY_OCCUPIED` 报错并阻断；
  4. **全域 17 项工程门禁全绿**：实施后必须通过 `scripts/ps1/audit-all.ps1` 全部审查。

---

## 一、 单元测试套件设计 (`tests/unit/test_character_creation_and_opening_pipeline.gd`)

```gdscript
# 文件路径: res://tests/unit/test_character_creation_and_opening_pipeline.gd
class_name TestCharacterCreationAndOpeningPipeline
extends RefCounted

static func run_all_tests() -> Dictionary:
	var results: Array[Dictionary] = []
	results.append(_test_basic_character_name_and_display_name_unification())
	results.append(_test_race_extensibility_and_non_hardcoded_validation())
	results.append(_test_backend_authority_stat_generation_and_anti_spoofing())
	results.append(_test_first_creation_opening_event_stream_dispatch())
	results.append(_test_race_expansion_canary_gating())
	results.append(_test_full_lifecycle_gateway_to_opening_narrative())

	var all_passed := true
	var passed_cnt := 0
	for r in results:
		if r.get("passed", false):
			passed_cnt += 1
		else:
			all_passed = false

	return {
		"domain": "Character Creation & Opening Event Stream Pipeline (Phase 48)",
		"all_passed": all_passed,
		"passed_count": passed_cnt,
		"total_count": results.size(),
		"results": results
	}
```

---

## 二、 6 大核心用例验收断言矩阵

| 用例 ID | 目标测试点 | 核心断言内容与执行逻辑 | 预期判定基准 |
| :--- | :--- | :--- | :---: |
| **TC-CC-01** | 基础字段创角与名字单一源头 | 输入标准角色名 "晨曦骑士"，性别 "MALE"，种族 "HUMAN"，断言创建成功，且 `profile.character_name == "晨曦骑士"` 且为唯一展示名 | `passed == true` |
| **TC-CC-02** | 种族数据结构可扩展性判定 | 查询 `races_catalog`，断言包含 HUMAN 且具备扩展 ELF 词条；验证非写死逻辑（根据配置结构驱动） | `passed == true` |
| **TC-CC-03** | 后端属性生成与前端伪造拦截 | 模拟前端请求尝试传入包含 `{ "STR": 999, "CON": 999 }` 的伪造属性，断言后端丢弃并根据种族基线严格派生出标准六维 `[3, 3, 3, 3, 3, 3]` | `passed == true` |
| **TC-CC-04** | 首次开档开局事件总线广播 | 对首开档位创建角色，断言触发 `opening_narrative.triggered` 事件，且 `event.is_first_time_creation == true`，包含合法 `account_id`, `world_id`, `slot_id`, `character_id` | `passed == true` |
| **TC-CC-05** | 种族扩展灰度开关限制 | 尝试使用处于未全量状态的 `ELF` 种族创建：非白名单账号断言返回 `RACE_CANARY_RESTRICTED`，白名单账号断言准许创建 | `passed == true` |
| **TC-CC-06** | 网关到开局全链路闭环流转 | 串联测试：`登录 → enter_gateway → select_mode(SINGLE_PLAYER) → 确认单机单档单世界 → 创角 → 收到开局事件流`，全链路状态无断裂 | `passed == true` |

---

## 三、 本阶段交付清单

1. **测试套件实现**：
   - `tests/unit/test_character_creation_and_opening_pipeline.gd`（专属套件，`TestCharacterCreationAndOpeningPipeline`）
2. **测试登记与全量门禁**：
   - 在既有已注册域套件 `tests/unit/test_character_creation.gd`（`TestCharacterCreationDomain`，registry 已登记）的 `run_all_tests` 中串接并入其 results（与 P45 `TestCombatTertiaryTimeline`/P46 `TestItemAttributeAffixSystem` 同构）；**不新增 registry 条目**——`character_creation` 域在 domains.json manifest 中单域单符号，新增条目会破坏 TC-ARCH-03「注册表条目数 == 清单域数 + 横切」计数自洽；
   - 验证通过率 100%（6 / 6 断言 PASS）；
   - 执行 `audit-all.ps1` 确保 17 项全域门禁通过。
