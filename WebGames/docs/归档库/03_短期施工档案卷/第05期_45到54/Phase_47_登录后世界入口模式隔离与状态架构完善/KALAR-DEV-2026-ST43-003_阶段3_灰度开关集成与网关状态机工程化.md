---
档号: KALAR-DEV-2026-ST43-003
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST43 (Phase_47_登录后世界入口模式隔离与状态架构完善)
件号: 003
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_47_登录后世界入口模式隔离与状态架构完善 —— 阶段3：灰度开关集成与网关状态机工程化
形成日期: 2026-09-03
归档日期: 2026-09-05（上午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: WorldGatewayFSM; feature_toggle_canary.json
---

# 施工细则：Phase 47 登录后世界入口模式隔离与状态架构完善 —— 阶段3：灰度开关集成与网关状态机工程化

> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST43-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST43-001_阶段1_世界栏网关与五层状态模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST43-002_阶段2_单联机模式隔离与档位世界路由实现.md) ｜ **阶段3 (当前)** ｜ [阶段4](KALAR-DEV-2026-ST43-004_阶段4_模式隔离与状态流转验收测试矩阵.md)

> 施工开始日期: 2026-09-03 下午
> 责任人: 卡拉尔世界引擎架构组
> 状态: ✅ 已施工完毕（两轮施工全生命周期闭环，全量单测与 17 项门禁 100% PASS）

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * [阶段1_世界栏网关与五层状态模型设计.md](KALAR-DEV-2026-ST43-001_阶段1_世界栏网关与五层状态模型设计.md)（网关状态模型）
  * [阶段2_单联机模式隔离与档位世界路由实现.md](KALAR-DEV-2026-ST43-002_阶段2_单联机模式隔离与档位世界路由实现.md)（模式路由与隔离守卫）
  * [backend/domains/feature_toggle_canary/canary_rollout_solver.gd](../../../../../backend/domains/feature_toggle_canary/canary_rollout_solver.gd)（灰度引擎）
  * [config/domains/feature_toggle_canary.json](../../../../../config/domains/feature_toggle_canary.json)（既有灰度特性表）
* **核心不变量约束断言**：
  1. **零硬编码不变量**：世界预设参数、默认世界 ID、灰度特性标识一律由 `config/domains/world_gateway.json` 驱动，禁绝任何魔数或硬编码字符串；
  2. **网关状态机单向收敛不变量**：`WorldGatewayFSM` 状态转移严格遵循合法图拓扑，禁止逆向越级跳转（如未选模式直接进入世界）；
  3. **EventBus 只读暴露原则**：网关只通过 `EventBus.domain_event("world_gateway.*", payload)` 分发状态，客户端无权反向修改网关内部上下文；
  4. **全表配置注册契约**：新增配置表 `config/domains/world_gateway.json` 必须先落地文件，并在 `GameConfig._required_tables` 中声明注册。

---

## 一、 配置表设计与结构契约 (`config/domains/world_gateway.json`)

```json
{
  "gateway_policy": {
    "auto_enter_gateway_on_login": true,
    "allow_direct_world_bypass": false,
    "session_timeout_seconds": 3600
  },
  "single_player": {
    "default_world_id": "WORLD_DEFAULT_SP_01",
    "default_slot_id": "SLOT_SP_01",
    "max_slots_baseline": 1,
    "max_worlds_baseline": 1
  },
  "canary_features": {
    "FEATURE_MULTI_SLOT": {
      "strategy": "PERCENTAGE_CANARY",
      "rollout_percentage": 0,
      "whitelist": ["DEV_TESTER_01", "QA_AUDITOR_02"]
    },
    "FEATURE_MULTI_WORLD": {
      "strategy": "GLOBAL_DISABLED",
      "rollout_percentage": 0,
      "whitelist": []
    },
    "FEATURE_MULTIPLAYER_GATEWAY": {
      "strategy": "WHITELIST_ONLY",
      "rollout_percentage": 0,
      "whitelist": ["MP_TESTER_01"]
    }
  },
  "worlds_catalog": {
    "WORLD_DEFAULT_SP_01": {
      "world_name": "卡拉尔大陆·起源界",
      "mode": 1,
      "seed_value": 20260901,
      "world_version": "1.0.0",
      "is_active": true,
      "max_concurrent_players": 1,
      "canary_feature_tag": ""
    },
    "WORLD_CANARY_MP_01": {
      "world_name": "卡拉尔公网·测试界域",
      "mode": 2,
      "seed_value": 88888888,
      "world_version": "0.1.0-alpha",
      "is_active": false,
      "max_concurrent_players": 16,
      "canary_feature_tag": "FEATURE_MULTIPLAYER_GATEWAY"
    }
  }
}
```

---

## 二、 网关状态机工程化实现 (`WorldGatewayFSM`)

```gdscript
# 模块路径: res://backend/domains/world_gateway/world_gateway_fsm.gd
class_name WorldGatewayFSM
extends RefCounted

var context: WorldGatewayContextDTO = null

func _init(account_id: String) -> void:
	context = WorldGatewayContextDTO.new()
	context.account_id = account_id
	context.current_phase = WorldGatewayModel.GatewayPhase.AUTHENTICATED

## 1. 登录后进入世界栏
func enter_gateway() -> Dictionary:
	if context.current_phase != WorldGatewayModel.GatewayPhase.AUTHENTICATED:
		return _fail("INVALID_TRANSITION", "当前状态不可重复进入网关: %d" % context.current_phase)

	context.current_phase = WorldGatewayModel.GatewayPhase.GATEWAY_ENTERED
	_notify_event("world_gateway.entered", {
		"account_id": context.account_id,
		"available_modes": [
			WorldGatewayModel.GameMode.SINGLE_PLAYER,
			WorldGatewayModel.GameMode.MULTIPLAYER
		]
	})
	return { "success": true, "phase": context.current_phase }

## 2. 选择游戏模式
func select_mode(mode: WorldGatewayModel.GameMode, canary_flags: Dictionary) -> Dictionary:
	if context.current_phase != WorldGatewayModel.GatewayPhase.GATEWAY_ENTERED:
		return _fail("INVALID_TRANSITION", "未处于世界栏主界面")

	var all_worlds := _load_worlds_catalog()
	var account_slots: Array[SaveSlotStateDTO] = [] # 由账号服务查询注入
	var route_res := GameModeRoutingSolver.resolve_mode_entry(
		context.account_id, mode, all_worlds, account_slots, canary_flags
	)

	if not route_res.success:
		return _fail(route_res.error_code, route_res.message)

	context.selected_mode = mode
	context.selected_world_id = route_res.resolved_world.world_id
	context.current_phase = WorldGatewayModel.GatewayPhase.MODE_SELECTED

	var primary_slot: SaveSlotStateDTO = route_res.available_slots[0]
	context.selected_slot_id = primary_slot.slot_id
	context.is_first_time_creation = route_res.requires_character_creation

	# 若为新开档，直接进入待创角阶段
	if route_res.requires_character_creation:
		context.current_phase = WorldGatewayModel.GatewayPhase.CHARACTER_PENDING

	_notify_event("world_gateway.mode_selected", {
		"account_id": context.account_id,
		"mode": mode,
		"world_id": context.selected_world_id,
		"slot_id": context.selected_slot_id,
		"requires_creation": context.is_first_time_creation,
		"phase": context.current_phase
	})

	return { "success": true, "context": context.to_dto() }

## 3. 关联就绪角色（创角完成或选角完成调用）
func attach_ready_character(character_id: String) -> Dictionary:
	if context.current_phase != WorldGatewayModel.GatewayPhase.CHARACTER_PENDING:
		return _fail("INVALID_TRANSITION", "当前状态非待就绪角色态")

	context.active_character_id = character_id
	context.current_phase = WorldGatewayModel.GatewayPhase.WORLD_ENTRY_PERMITTED

	_notify_event("world_gateway.ready_for_entry", {
		"account_id": context.account_id,
		"character_id": character_id,
		"world_id": context.selected_world_id,
		"slot_id": context.selected_slot_id
	})
	return { "success": true, "phase": context.current_phase }

func _notify_event(channel: String, payload: Dictionary) -> void:
	EventBus.get_instance().emit_signal("domain_event", channel, payload)

func _fail(code: String, msg: String) -> Dictionary:
	return { "success": false, "error_code": code, "message": msg }

func _load_worlds_catalog() -> Dictionary:
	var raw_catalog: Dictionary = GameConfig.get_dict("domains.world_gateway", "worlds_catalog", {})
	var catalog: Dictionary = {}
	for wid in raw_catalog.keys():
		catalog[wid] = WorldInstanceDTO.from_dto(raw_catalog[wid])
	return catalog
```

---

## 三、 本阶段交付清单

1. **配置文件**：
   - `config/domains/world_gateway.json`（网关策略、单机世界预设、灰度矩阵）
   - 在 `backend/infrastructure/game_config.gd` 的 `_required_tables` 中声明 `"domains.world_gateway"`
2. **状态机实现**：
   - `backend/domains/world_gateway/world_gateway_fsm.gd`（网关状态机与事件流发布）
