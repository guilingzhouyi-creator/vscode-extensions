---
档号: KALAR-DEV-2026-ST43-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST43 (Phase_47_登录后世界入口模式隔离与状态架构完善)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_47_登录后世界入口模式隔离与状态架构完善 —— 阶段4：模式隔离与状态流转验收测试矩阵
形成日期: 2026-09-03
归档日期: 2026-09-05（上午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: TestWorldGatewayAndModeIsolation; 模式隔离
---

# 施工细则：Phase 47 登录后世界入口模式隔离与状态架构完善 —— 阶段4：模式隔离与状态流转验收测试矩阵

> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST43-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST43-001_阶段1_世界栏网关与五层状态模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST43-002_阶段2_单联机模式隔离与档位世界路由实现.md) ｜ [阶段3](KALAR-DEV-2026-ST43-003_阶段3_灰度开关集成与网关状态机工程化.md) ｜ **阶段4 (当前)**

> 施工开始日期: 2026-09-03 下午
> 责任人: 卡拉尔世界引擎架构组
> 状态: ✅ 已施工完毕（两轮施工全生命周期闭环，全量单测与 17 项门禁 100% PASS）

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * [阶段1_世界栏网关与五层状态模型设计.md](KALAR-DEV-2026-ST43-001_阶段1_世界栏网关与五层状态模型设计.md)（数据契约）
  * [阶段2_单联机模式隔离与档位世界路由实现.md](KALAR-DEV-2026-ST43-002_阶段2_单联机模式隔离与档位世界路由实现.md)（隔离守卫与路由）
  * [阶段3_灰度开关集成与网关状态机工程化.md](KALAR-DEV-2026-ST43-003_阶段3_灰度开关集成与网关状态机工程化.md)（配置与状态机）
  * [tests/test_registry.gd](../../../../../tests/test_registry.gd)（测试聚合注册表）
* **核心不变量约束断言**：
  1. **全套件 100% 验收通过率不变量**：新增测试套件必须包含在统一批处理中，断言通过率 100%，杜绝任何偶发性失败；
  2. **跨模式零脏写不变量**：断言模拟单机状态向联机写入或联机向单机写入必须 100% 被 `StateMutationIsolationGuard` 拦截报错；
  3. **灰度生效确定性不变量**：白名单账号必定通过灰度校验，非白名单且百分比为 0% 的账号必定被拦截；
  4. **全域 17 项门禁无退化不变量**：实施后必须通过 `scripts/ps1/audit-all.ps1` 全部 17 项质量门禁。

---

## 一、 单元测试套件设计 (`tests/unit/test_world_gateway.gd`)

```gdscript
# 文件路径: res://tests/unit/test_world_gateway.gd
class_name TestWorldGatewayAndModeIsolation
extends RefCounted

static func run_all_tests() -> Dictionary:
	var results: Array[Dictionary] = []
	results.append(_test_gateway_unified_entrance())
	results.append(_test_mode_state_isolation_guard())
	results.append(_test_single_player_single_slot_world_routing())
	results.append(_test_five_tier_state_orthogonality())
	results.append(_test_canary_rollout_integration())
	results.append(_test_fsm_invalid_transition_blocking())

	var all_passed := true
	var passed_cnt := 0
	for r in results:
		if r.get("passed", false):
			passed_cnt += 1
		else:
			all_passed = false

	return {
		"domain": "World Gateway & Mode Isolation (Phase 47)",
		"all_passed": all_passed,
		"passed_count": passed_cnt,
		"total_count": results.size(),
		"results": results
	}
```

---

## 二、 6 大核心用例验收断言矩阵

| 用例 ID | 目标测试点 | 核心断言内容与执行逻辑 | 预期判定基准 |
| :--- | :--- | :--- | :---: |
| **TC-GW-01** | 网关统一入口与直入世界拦截 | 模拟用户登录后，尝试跳过网关直接执行 `enter_world` 操作，断言被 FSM 状态机阻断；执行 `enter_gateway` 后状态跃迁至 `GATEWAY_ENTERED` 并触发事件 | `passed == true` |
| **TC-GW-02** | 单联机模式隔离守卫与写拦截 | 构造单机模式上下文，调用 `StateMutationIsolationGuard.validate_mutation_permission` 尝试写入联机世界 ID 或目标模式为 `MULTIPLAYER` 的操作，断言返回 `GUARD_ERR_CROSS_MODE_MUTATION` | `passed == true` |
| **TC-GW-03** | 单机单档单世界标准路由解析 | 调用 `GameModeRoutingSolver.resolve_mode_entry`，传入空档位清单，断言自动创建 `SLOT_SP_01` 且 `is_first_creation == true`，世界解析为 `WORLD_DEFAULT_SP_01` | `passed == true` |
| **TC-GW-04** | 五层状态正交性不变量校验 | 断言 Account、SaveSlot、World、Character 与 MultiplayerSession 字段相互解耦，不存在跨层隐式污染 | `passed == true` |
| **TC-GW-05** | 灰度控制系统联动生效 | 构造 `FEATURE_MULTIPLAYER_GATEWAY` 灰度配置（仅白名单 `MP_TESTER_01` 可用）；非白名单账号选择联机模式断言被拒（`GATEWAY_ERR_MULTIPLAYER_DISABLED`），白名单账号断言准许 | `passed == true` |
| **TC-GW-06** | 状态机非法跳转与全流程闭环 | 验证 `AUTHENTICATED → GATEWAY_ENTERED → MODE_SELECTED → CHARACTER_PENDING → WORLD_ENTRY_PERMITTED` 状态转移路径；非就绪态直接调用 `attach_ready_character` 断言被拒 | `passed == true` |

---

## 三、 本阶段交付清单

1. **测试套件实现**：
   - `tests/unit/test_world_gateway.gd`
2. **测试登记与全量门禁**：
   - 在 `tests/test_registry.gd` 中注册并在 `tests/unit/test_account.gd` 中串接；
   - 验证通过率 100%（6 / 6 断言 PASS）；
   - 执行 `audit-all.ps1` 确保 17 项全域门禁通过。
