---
档号: KALAR-DEV-2026-ST45-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST45 (Phase_49_文字版开局序章事件流与剧情推进状态机)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_49_文字版开局序章事件流与剧情推进状态机 —— 阶段4：序章事件流与预留桩点验收测试矩阵
形成日期: 2026-09-03
归档日期: 2026-09-05（上午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 序章事件流; prologue_i18n.json
---

# 施工细则：阶段4_序章事件流与预留桩点验收测试矩阵

> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST45-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST45-001_阶段1_文字版序章事件流契约与四段式状态模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST45-002_阶段2_序章状态机与文本因果推挽求解器实现.md) ｜ [阶段3](KALAR-DEV-2026-ST45-003_阶段3_配置驱动文案表与全域事件总线广播工程化.md) ｜ **阶段4 (当前)**

> 施工开始日期: 2026-09-03 下午
> 责任人: 卡拉尔世界引擎架构组
> 状态: ✅ 已完成第2轮闭环验收（100% PASS）

---

## 一、 验收目标与测试集架构 (对齐 P10.10)

本阶段为文字版角色序章构建无头测试套件 `TestPrologueCoreAndPlaceholderPipeline`（`tests/unit/test_prologue_core_and_placeholder_pipeline.gd`），以全自动化单测断言锁死占位符替换、新手装备发放、前后端 i18n 隔离与事件流一致性。

套件直接并入既有 `Domain 15: character_creation`，守住全库 47 个业务领域宏观护栏（`TC-ARCH-01 ~ 05`）。

---

## 二、 6 大核心用例验收矩阵 (DoD Checklist)

| 用例编号 | 用例名称与验证意图 | 触发前置条件与动作 | 核心断言与预期输出 | 对应 P10 标准条目 |
| :--- | :--- | :--- | :--- | :--- |
| **TC-PRO-01** | 角色专属序章上下文初始化 | 传入不同角色名（`艾隆` / `莉娜`）与不同种族（`HUMAN` / `ELF`） | `ctx.assigned_start_location_id` 与装备套件依据配置动态映射，非死值硬编码 | P10.1 / P10.9 |
| **TC-PRO-02** | 占位符全量动态解析与安全注入 | 包含 `{PLAYER_NAME}`、`{RACE_ID}`、`{START_LOCATION}`、`{INITIAL_EQUIPMENT}` 模板 | 文本中所有占位符 100% 替换为实际值，且零 `{...}` 漏替换标记残留 | P10.2 |
| **TC-PRO-03** | 缺失与非法占位符安全降级防御 | 传入包含未知占位符 `{INVALID_TOKEN_XYZ}` 的损坏模板 | 求解器平稳运行不抛异常，返回 `missing_placeholders` 列表，文案注入安全降级标记 | P10.2 / P10.10 |
| **TC-PRO-04** | 新手装备发放与防重发幂等保护 | 连续调用 `StarterLoadoutDispatcher.dispatch_starter_kit` 两次 | 首次返回 `KIT_DISPATCHED`，二次返回 `ALREADY_GRANTED`，金币与道具绝不重复发放 | P10.5 / P10.10 |
| **TC-PRO-05** | 后端 i18n 与前端 UI i18n 隔离性断言 | 检查事件总线广播载荷与文案 Key 命名空间 | 广播载荷包含 `narrative_key` 与 `i18n_params`；`narrative.*` 不与 `frontend.ui.*` Key 重叠 | P10.4 / P10.10 |
| **TC-PRO-06** | 结构化事件流输出与状态解耦验证 | 监听 `EventBus.domain_event`，触发序章初始化 | 捕获 `prologue.step_advanced`，载荷包含结构化突变标记数组（`CharacterCreated`, `EquipmentGranted` 等） | P10.7 / P10.10 |

---

## 三、 测试断言框架规范 (无头脚本示例)

```gdscript
class_name TestPrologueCoreAndPlaceholderPipeline
extends RefCounted

static func run_all_tests() -> Dictionary:
	var results: Array[Dictionary] = []
	results.append(_test_character_context_isolation())
	results.append(_test_placeholder_full_resolution())
	results.append(_test_missing_placeholder_fallback())
	results.append(_test_starter_kit_idempotency())
	results.append(_test_backend_i18n_isolation())
	results.append(_test_event_state_decoupling())

	var passed_count := 0
	for r in results:
		if bool(r.get("passed", false)):
			passed_count += 1

	return {
		"suite_name": "TestPrologueCoreAndPlaceholderPipeline",
		"total": results.size(),
		"passed": passed_count,
		"results": results
	}
```

---

## 四、 17 道全域工程门禁约束

实施阶段完成后，必须经由 `pwsh -File scripts/ps1/audit-all.ps1` 进行全域自动化门禁审查，达到以下硬性标准：

1. **GDScript 语法治理** (`check-gdscript.ps1`)：100% 编译通过，0 语法警告；
2. **确定性与无随机规范** (`audit_arch.py: TC-ARCH-04`)：零裸 `randi()/randf()` 调用，时间戳全量使用系统时间；
3. **零硬编码与配置存在性** (`TC-ARCH-06`)：代码读取的键在 `prologue_i18n.json` 与 `starter_loadout.json` 中真实存在；
4. **文档四域与基线棘轮** (`audit_docs.py`)：0 Error / 0 Warn，文档合规率 100%；
5. **单元测试验收** (`test-run.ps1`)：全量测试套件 100% PASS，断言总数正向增长且 0 失败。
