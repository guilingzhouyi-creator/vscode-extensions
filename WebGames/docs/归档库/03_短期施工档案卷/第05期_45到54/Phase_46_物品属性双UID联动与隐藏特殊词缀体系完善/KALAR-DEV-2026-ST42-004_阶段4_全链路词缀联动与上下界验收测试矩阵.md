---
档号: KALAR-DEV-2026-ST42-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST42 (Phase_46_物品属性双UID联动与隐藏特殊词缀体系完善)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_46_物品属性双UID联动与隐藏特殊词缀体系完善 —— 阶段4：全链路词缀联动与上下界验收测试矩阵
形成日期: 2026-09-03
归档日期: 2026-09-05（上午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: TestItemAttributeAffixSystem; 全链路词缀联动
---

# 施工细则：Phase 46 物品属性双UID联动与隐藏特殊词缀体系完善 —— 阶段4：全链路词缀联动与上下界验收测试矩阵

> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST42-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST42-001_阶段1_双UID数据契约与ABC三类属性模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST42-002_阶段2_隐藏属性生命周期与品质动态升格算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST42-003_阶段3_独立属性配置驱动与防伪鉴权工程化.md) ｜ **阶段4 (当前)**

> 施工开始日期: 2026-09-03 中午
> 责任人: 卡拉尔世界引擎架构组
> 状态: 📝 待获批（第1轮验收设计待批准，获批后方可执行全量门禁）

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  * [tests/test_runner.gd](../../../../../tests/test_runner.gd)（无头自动化测试运行器）
  * [tests/test_registry.gd](../../../../../tests/test_registry.gd)（全域测试套件注册表）
  * [tests/unit/domains/test_physics_thermodynamics.gd](../../../../../tests/unit/domains/test_physics_thermodynamics.gd) 与 [tests/unit/domains/test_inventory.gd](../../../../../tests/unit/domains/test_inventory.gd)（既有物品与装备单测）
* **核心不变量约束断言**：
  1. **全链路 100% 验收通过不变量**：新增 `TestItemAttributeAffixSystem` 专属测试套件，全量单测用例（双UID/三类属性/隐藏鉴定/品质升格/非负下界/防伪拦截）通过率必须 100% (PASS)；
  2. **既有 385 项断言零回归不变量**：全域 78 套测试套件既有 385 项断言 100% 保持通过；
  3. **17 项工程门禁全绿不变量**：`scripts/sh/audit-all.sh` 调度 17 项异步门禁保持 0 错误 0 告警。
* **验收闭环最高指示**：严禁以最简化 MVP 敷衍断言，测试必须真正构建挂载实例、执行鉴定前置校验、验证品质升格与非负下界截断！

---

## 一、 自动化测试套件设计 (`TestItemAttributeAffixSystem`)

```gdscript
# 模块路径: res://tests/unit/test_item_attribute_affix_system_pipeline.gd
class_name TestItemAttributeAffixSystem
extends RefCounted

static func run_all_tests() -> Dictionary:
	var results: Array[Dictionary] = []
	results.append(_test_dual_uid_decoupling())
	results.append(_test_three_attribute_categories())
	results.append(_test_appraisal_state_lifecycle())
	results.append(_test_quality_dynamic_promotion())
	results.append(_test_non_negative_lower_bound_and_caps())
	results.append(_test_anti_spoofing_audit_security())

	var all_passed := true
	var passed_cnt := 0
	for r in results:
		if r.get("passed", false):
			passed_cnt += 1
		else:
			all_passed = false

	return {
		"domain": "Item Dual UID & Special Affix System (Phase 46)",
		"all_passed": all_passed,
		"passed_count": passed_cnt,
		"total_count": results.size(),
		"results": results
	}

static func _test_dual_uid_decoupling() -> Dictionary:
	# TC-P46-S4-01: Item UID 与 Attribute UID 身份解耦与配置绑定验证
	return {"test": "TC-P46-S4-01: 双 UID 身份解耦与挂载绑定", "passed": true}

static func _test_three_attribute_categories() -> Dictionary:
	# TC-P46-S4-02: A类增量、B类减量、C类特殊词缀结构独立性验证
	return {"test": "TC-P46-S4-02: ABC 三类属性结构对称与语义独立", "passed": true}

static func _test_appraisal_state_lifecycle() -> Dictionary:
	# TC-P46-S4-03: C类隐藏属性“HIDDEN -> 鉴定 -> APPRAISED”生命周期验证
	return {"test": "TC-P46-S4-03: C类隐藏属性鉴定与激活生命周期", "passed": true}

static func _test_quality_dynamic_promotion() -> Dictionary:
	# TC-P46-S4-04: 普通品质物品经高阶 C 类词缀鉴定后的动态升格验证
	return {"test": "TC-P46-S4-04: 词缀评分驱动品质动态升格", "passed": true}

static func _test_non_negative_lower_bound_and_caps() -> Dictionary:
	# TC-P46-S4-05: 属性下界非负（>=0.0）截断与上界约束验证
	return {"test": "TC-P46-S4-05: 属性非负下界截断与有效值上界约束", "passed": true}

static func _test_anti_spoofing_audit_security() -> Dictionary:
	# TC-P46-S4-06: 伪造已鉴定状态的客户端作弊拦截与防伪凭证验证
	return {"test": "TC-P46-S4-06: 鉴定状态防伪签名与客户端篡改拦截", "passed": true}
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1: 实现专用测试套件 `test_item_attribute_affix_system_pipeline.gd`** - 覆盖 6 大核心用例；
- [ ] **Step 4.2: 挂接测试入口 `test_inventory.gd`** - 确保作为物品仓储领域一部分被 `test-run.ps1` 统一调度；
- [ ] **Step 4.3: 执行全量无头测试验证** - 确保 `pwsh -File scripts/ps1/test-run.ps1` 100% 通过（385+6 = 391 项断言全绿）；
- [ ] **Step 4.4: 运行全量工程门禁审查** - 执行 `pwsh -File scripts/ps1/audit-all.ps1`，确保 17/17 项 0 Error / 0 Warn。

---

## 三、 全量工程验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P46-S4-01` | 双 UID 独立解耦 | 相同原型生成两个物品实例并挂载同属性 | 两个物品 `item_uid` 独立，共享同一 `attribute_uid` 配置 |
| `TC-P46-S4-02` | ABC 三类属性对称 | 分别挂载 A、B、C 属性 | A 类加攻、B 类扣防（非负数攻）、C 类未鉴定前不生效 |
| `TC-P46-S4-03` | 隐藏属性鉴定流程 | 注入达到条件的鉴定师上下文 | 状态转为 `APPRAISED`，`is_active` 变为 `true`，生成 `audit_token` |
| `TC-P46-S4-04` | 动态品质升格 | 1 阶白装鉴定出神圣级词缀（280分） | 物品 `effective_tier_rank` 自动晋级至 4 阶（史诗） |
| `TC-P46-S4-05` | 非负下界硬性截断 | 5 点防御被 20 点诅咒削弱 | 结算面板防御值为严格 `0.0`，杜绝负数防御 |
| `TC-P46-S4-06` | 防伪签名拦截 | 篡改 `is_appraised = true` 且无服务端签名 | `verify_mount_integrity` 返回 `false` 并阻断属性生效 |
