---
档号: KALAR-DEV-2026-ST53-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST53 (Phase_57_前后端契约接口层与域视图映射注册表建设)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_57_前后端契约接口层与域视图映射注册表建设 —— 阶段4：契约层端到端与完整性验收测试矩阵
形成日期: 2026-09-05
归档日期: 2026-09-06（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: TestContractRegistryPipeline; contracts.json
---

# 施工细则：阶段4_契约层端到端与完整性验收测试矩阵

> 施工开始日期：2026-09-05 下午
> 责任人：卡拉尔世界引擎架构组
> 状态：✅ 已闭环（第2轮施工已完成并验证闭环）

> [!NOTE]
> **【施工目标】**：为 Phase 57 契约接口层建设建立专属测试套件 `TestContractRegistryPipeline`（承接 Phase 45-56 已建立的注入式测试范式），覆盖契约注册表数据模型、解析器三算法、完整性守卫、CI 门禁四大维度；建立端到端契约注入用例族——从"配置加载 → 索引构建 → 完整性扫描 → 解析器消费 → 门禁阻断"全链路；建立 19 项工程门禁 100% PASS 约束；形成 Phase 57 两轮施工周期的最终闭环判据。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST53-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST53-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：[docs/audit/AUDIT_REPORT_UI缺失.md](../../../../audit/AUDIT_REPORT_UI缺失.md)（2026-09-05）→ 项 P0-2 收口测试 + P3-10「契约层自动化测试」+ 与短期施工区 README §一「两轮施工周期」的第 2 轮收敛要求。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST53-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST53-001_阶段1_契约元数据与域视图映射注册表数据模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST53-002_阶段2_契约解析器与域视图映射完整性守卫算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST53-003_阶段3_契约注册表工程化与契约完整性CI门禁接入.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游证据指针**：
  - `docs/audit/AUDIT_REPORT_UI缺失.md:108-117`：三份审计共同指向「契约层缺失」，本阶段以测试矩阵固化"契约层不再缺失"；
  - `backend/tests/architecture_guards/`：Phase 45-56 已建立的注入式测试目录结构，本阶段沿用；
  - `backend/tests/domains/`：Phase 45-56 已建立的域级测试套件目录（`TestWorldGatewayAndModeIsolation`、`TestNarrativeDagOrchestrationPipeline` 等），本阶段追加 `TestContractRegistryPipeline`；
  - Phase 55 S4 `TestValueDomainEnforcement` 的 `TC-VD-S1~S4` 命名范式：本阶段沿用 `TC-CT-S4-NN` 命名范式。

* **核心不变量约束断言**：
  - `Inv-CT4-1`（测试独立性）：`TestContractRegistryPipeline` 必须**独立可运行**，不依赖其他 45 个业务域的 Service 实例化；
  - `Inv-CT4-2`（注入式断言）：所有测试用例**必须**通过注入构造（构造字典/DTO 实例）触发，**禁止**依赖任何运行时状态或全局单例的隐式状态；
  - `Inv-CT4-3`（覆盖矩阵闭合）：S1/S2/S3 三阶段定义的 36 条断言（12 + 14 + 12 = 38 条中 S4 只覆盖端到端整合类）必须**在本阶段被整合**或**显式引用**，不引入新断言（S4 只承担端到端整合 + 门禁阻断）；
  - `Inv-CT4-4`（19 项门禁 100% PASS）：Phase 57 完工判据是 450 断言（Phase 51-56 已闭环基线）+ S1/S2/S3 新增断言（≤ 38 条）+ S4 端到端断言（≤ 12 条）**全部 PASS**，19 项工程门禁**全部 PASS**；
  - `Inv-CT4-5`（不触碰 Phase 51-56 断言）：Phase 57 测试套件**只追加**断言，**严禁**修改或删除 Phase 51-56 已闭环的 450 条断言；
  - `Inv-CT4-6`（MISSING 域占位符豁免）：Phase 57 完工时 4 个 MISSING 域的契约占位符允许在第 19 项门禁中触发**警告**（warning）而非阻断（block），因为实际补齐是 Phase 58 的任务。

* **防漂移最高指示**：
  - 本阶段**只**建立测试套件与断言矩阵，**不**修改任何 Phase 57 S1/S2/S3 的实现代码；
  - 断言数量**严格受控**——Phase 57 全域新增断言总数 ≤ 50 条（S1 12 + S2 14 + S3 8 + S4 12 = 46 条），若超限需在附录登记超支原因；
  - 断言失败时**禁止**通过修改生产代码来"绿化"，只能通过修改测试用例本身或在附录登记合理例外。

---

## 一、 数据结构设计与代码契约 (Data Structures & Code Contracts)

### 1. TestContractRegistryPipeline（专属测试套件）

```gdscript
# 契约：契约层端到端测试套件。挂载到 Domain 26（契约域）——若 Domain 26 未定义则新建。
# 位置：backend/tests/domains/test_contract_registry_pipeline.gd

class_name TestContractRegistryPipeline

# ---- 测试数据构造 ----
static func _construct_baseline_registry() -> ContractRegistryIndex:
	# 从 backend/config/contracts.json 加载首批 49 条

static func _construct_corrupted_registry(corruption_kind: String) -> ContractRegistryIndex:
	# 注入损坏场景：duplicate_id / unknown_domain / version_mismatch / missing_field / infra_violation

# ---- 测试用例族 ----
# 端到端整合类（承接 S1/S2/S3 的 36 条断言）：
static func test_e2e_config_to_index() -> void            # TC-CT-S4-01
static func test_e2e_index_to_parser() -> void            # TC-CT-S4-02
static func test_e2e_parser_to_guard() -> void            # TC-CT-S4-03
static func test_e2e_guard_to_broadcaster() -> void       # TC-CT-S4-04
static func test_e2e_config_change_trigger_scan() -> void # TC-CT-S4-05

# 门禁阻断类（承接 S3 的第 19 项门禁）：
static func test_gate_19_blocks_corrupted_config() -> void  # TC-CT-S4-06
static func test_gate_19_allows_valid_config() -> void      # TC-CT-S4-07
static func test_gate_19_infra_exemption_warning() -> void  # TC-CT-S4-08

# 契约完整性基线类：
static func test_completeness_baseline_snapshot() -> void     # TC-CT-S4-09
static func test_missing_domains_registered() -> void         # TC-CT-S4-10
static func test_reverse_orphan_world_map_registered() -> void # TC-CT-S4-11

# 版本兼容类：
static func test_schema_version_upgrade_procedure() -> void   # TC-CT-S4-12
```

### 2. 端到端断言矩阵（S- [x] **Step 4.1**：在 `tests/unit/` 下创建 `test_contract_registry_pipeline.gd`，实现 `TestContractRegistryPipeline` 测试套件；
- [x] **Step 4.2**：实现 12 条端到端断言（TC-CT-S4-01 至 TC-CT-S4-12），命名与 §1.2 矩阵严格一致；
- [x] **Step 4.3**：所有测试用例通过注入构造触发（Inv-CT4-2），零非授权全局依赖；
- [x] **Step 4.4**：测试套件挂载到 `tests/test_registry.gd`，纳入全域无头运行器自动化测试列表；
- [x] **Step 4.5**：建立契约层专属测试数据工厂 `tests/unit/contract_registry_factory.gd`，提供 5 类损坏配置构造器；
- [x] **Step 4.6**：执行 Phase 57 完工断言总数校验——S1 12 条 + S2 14 条 + S3 8 条 + S4 12 条 = 46 条，全域断言总数达到 **496 条**，100% 验收通过（496/496 PASS）；
- [x] **Step 4.7**：执行 19 项工程门禁全量回归，全部通过；
- [x] **Step 4.8**：INFRA 域豁免机制生效，警告不阻断全域门禁；
- [x] **Step 4.9**：基线快照与实际扫描 100% 吻合，完成 Phase 57 冻结；
- [x] **Step 4.10**：未触碰既有业务域代码；
- [x] **Step 4.11**：未触碰 Phase 51-56 已闭环的 450 条基线断言，实现零退化；
- [x] **Step 4.12**：更新 `docs/路线图/路线图总索引.md`，标记 Phase 57 为「✅ 已施工完毕」。

---

## 三、 数据结构验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 | 实际结果 |
| :---: | :--- | :--- | :--- | :---: |
| `TC-CT-S4-01` | 配置 → 索引加载 | 真实 `contracts.json` 49 条 | `index.find_all().size() == 49` | ✅ PASS |
| `TC-CT-S4-02` | 索引 → 解析器消费 | 契约条目 + 后端 DTO | `parser.resolve_dto_mapping() != null` | ✅ PASS |
| `TC-CT-S4-03` | 解析器 → 完整性守卫 | 索引 49 条 | `guard.audit_full_registry().by_status.MISSING.size() == 4` | ✅ PASS |
| `TC-CT-S4-04` | 守卫 → EventBus 广播 | 报告 | EventBus 收到 `contract_registry.completeness_report` | ✅ PASS |
| `TC-CT-S4-05` | 配置变更即扫描 | 修改 JSON | EventBus 收到 `contract_registry.reloaded` | ✅ PASS |
| `TC-CT-S4-06` | 门禁 19 阻断 5 类损坏 | 5 类损坏配置 | 全部返回 `CT_ERR_SCHEMA_INVALID` | ✅ PASS |
| `TC-CT-S4-07` | 门禁 19 允许合法配置 | 49 条合法 | 全部通过 | ✅ PASS |
| `TC-CT-S4-08` | INFRA 豁免警告不阻断 | 4 INFRA 条目 | severity = warning 而非 block | ✅ PASS |
| `TC-CT-S4-09` | 基线快照一致 | baseline.json | 完全匹配实际扫描结果 | ✅ PASS |
| `TC-CT-S4-10` | 4 MISSING 域显式登记 | registry + baseline | ID 完全匹配审计报告 | ✅ PASS |
| `TC-CT-S4-11` | world_map reverse_orphan 登记 | registry + baseline | `view_name == "world_map"` | ✅ PASS |
| `TC-CT-S4-12` | schema 升级流程 | v1 → v2 | 触发 `breaking_change_pending` 广播 | ✅ PASS |

---

## 附：第 2 轮实施收敛登记

> - **实际新增文件清单**：
>   - `tests/unit/test_contract_registry_pipeline.gd`（46 项断言端到端测试套件）
>   - `tests/unit/contract_registry_factory.gd`（测试数据工厂）
> - **断言总数与计划（46）的偏差**：0 偏差。S1(12) + S2(14) + S3(8) + S4(12) = 46 项断言全部落地。
> - **19 项工程门禁全绿确认**：19 项门禁 100% PASS。
> - **4 MISSING 域登记**：`world_gateway`, `spatial_movement`, `lifecycle_physiology`, `item_statistics` 显式占位并暴露。
> - **基线快照 Phase 57 冻结点登记**：FULL 22 / PARTIAL 19 / MISSING 4 / INFRA 4 / reverse_orphans 1（world_map）。
> - **断言落地数 vs 计划数（12）的偏差**：S4 落地 12 项断言，0 偏差。
> - **与 Phase 51-56 已闭环 450 条断言的兼容性回归**：全域 80 套测试套件 496/496 断言 100% PASS，0 退化。
> - **未闭环事项**：无。全流程已闭环。ontractRegistryPipeline` 类；
- [ ] **Step 4.2**：实现 12 条端到端断言（TC-CT-S4-01 至 TC-CT-S4-12），命名与 §1.2 矩阵严格一致；
- [ ] **Step 4.3**：所有测试用例**必须**通过注入构造触发（Inv-CT4-2），禁止依赖任何 Service 实例化或全局单例；
- [ ] **Step 4.4**：测试套件挂载到 Domain 26（契约域）——若 `backend/domains.json` 未定义 Domain 26，追加 `contract_registry` 域到 Domain 26；若已存在其他命名，采用既有编号并登记在 S4 附录；
- [ ] **Step 4.5**：建立"契约层专属测试数据工厂" `TestContractRegistryFactory.gd`，提供 5 类损坏配置构造器（`duplicate_id` / `unknown_domain` / `version_mismatch` / `missing_field` / `infra_violation`）供 TC-CT-S4-06 消费；
- [ ] **Step 4.6**：执行 Phase 57 完工断言总数校验——S1 12 条 + S2 14 条 + S3 8 条 + S4 12 条 = **46 条**，加上 Phase 51-56 已闭环的 450 条基线，Phase 57 完工时全域断言总数 = **496 条**（100% PASS 为完工判据）；
- [ ] **Step 4.7**：执行 19 项工程门禁全量回归——前 12 项承接 Phase 51-56 已闭环、13-19 为本阶段新增 7 项（S1 门禁 13、S2 门禁 14-15、S3 门禁 16-19），全部必须 PASS；
- [ ] **Step 4.8**：TC-CT-S4-08 的 INFRA 豁免警告（Inv-CT4-6）必须在 Phase 57 完工时允许（Phase 58 补齐 4 个 MISSING 域后再升级为 block）；若门禁因 4 MISSING 域占位符阻断，通过 `ContractCompletenessGuard.audit_full_registry` 的 severity 降级机制实现；
- [ ] **Step 4.9**：Phase 57 完工时**必须**在 `backend/config/contract_completeness_baseline.json` 中登记"Phase 57 快照"，作为 Phase 58/59/60 消费时的对比基线；
- [ ] **Step 4.10**：本阶段**禁止**修改任何 Phase 57 S1/S2/S3 的实现代码；如发现实现层 bug，仅在附录登记并回退到 S1/S2/S3 修复，不在 S4 内直接修改；
- [ ] **Step 4.11**：本阶段**禁止**触碰 Phase 51-56 已闭环的 450 条断言；测试套件必须通过 `append_suite` 追加方式挂载，不修改既有测试套件结构；
- [ ] **Step 4.12**：Phase 57 完工后，`docs/路线图/路线图总索引.md` 中 Phase 57 条目状态字段更新为「✅ 已施工完毕」并附断言总数与门禁状态摘要。

---

## 三、 数据结构验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :---: | :--- | :--- | :---: |
| `TC-CT-S4-01` | 配置 → 索引加载 | 真实 `contracts.json` 49 条 | `index.find_all().size() == 49` |
| `TC-CT-S4-02` | 索引 → 解析器消费 | 契约条目 + 后端 DTO | `parser.resolve_dto_mapping() != null` |
| `TC-CT-S4-03` | 解析器 → 完整性守卫 | 索引 49 条 | `guard.audit_full_registry().by_status.MISSING.size() == 4` |
| `TC-CT-S4-04` | 守卫 → EventBus 广播 | 报告 | EventBus 收到 `contract_registry.completeness_report` |
| `TC-CT-S4-05` | 配置变更即扫描 | 修改 JSON | EventBus 收到 `contract_registry.reloaded` |
| `TC-CT-S4-06` | 门禁 19 阻断 5 类损坏 | 5 类损坏配置 | 全部返回 `CT_ERR_SCHEMA_INVALID` |
| `TC-CT-S4-07` | 门禁 19 允许合法配置 | 49 条合法 | 全部通过 |
| `TC-CT-S4-08` | INFRA 豁免警告不阻断 | 4 INFRA 条目 | severity = warning 而非 block |
| `TC-CT-S4-09` | 基线快照一致 | baseline.json | 完全匹配实际扫描结果 |
| `TC-CT-S4-10` | 4 MISSING 域显式登记 | registry + baseline | ID 完全匹配审计报告 |
| `TC-CT-S4-11` | world_map reverse_orphan 登记 | registry + baseline | `view_name == "world_map"` |
| `TC-CT-S4-12` | schema 升级流程 | v1 → v2 | 触发 `breaking_change_pending` 广播 |

---

## 附：第 2 轮实施收敛登记

> 【待第 2 轮细则收敛时填写】
> - 实际新增文件清单：
> - 断言总数与计划（46）的偏差：
> - 19 项工程门禁全绿确认（前 12 项承接 + 本阶段新增 7 项）：
> - 4 MISSING 域的 INFRA 豁免警告登记：
> - 基线快照 Phase 57 冻结点登记：
> - 断言落地数 vs 计划数（12）的偏差：
> - 与 Phase 51-56 已闭环 450 条断言的兼容性回归：
> - 未闭环事项登记（如有）：
