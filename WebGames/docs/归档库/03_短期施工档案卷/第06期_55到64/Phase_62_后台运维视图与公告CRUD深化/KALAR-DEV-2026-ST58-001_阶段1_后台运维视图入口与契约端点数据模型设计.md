---
档号: KALAR-DEV-2026-ST58-001
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST58 (Phase_62_后台运维视图与公告CRUD深化)
件号: 001
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_62_后台运维视图与公告CRUD深化 —— 阶段1：后台运维视图入口与契约端点数据模型设计
形成日期: 2026-09-05
归档日期: 2026-09-06（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: AdminConsoleView; NarrativeOrchestratorView; 四域 + INFRA 视图入口方案; admin_sandbox 契约端点; cdkey_voucher 契约端点
---

# 施工细则：阶段1_后台运维视图入口与契约端点数据模型设计

> 施工开始日期：2026-09-05 下午
> 责任人：卡拉尔世界引擎架构组
> 状态：📝 待获批（第1轮细则已编制，待批准后进入实现）

> [!NOTE]
> **【施工目标】**：为审计报告识别的 4 个后台运维与叙事编排相关 PARTIAL 域——`admin_sandbox`（5 文件 / 3 静态方法）、`cdkey_voucher`（5 文件 / 1 静态方法）、`narrative_orchestration`（11 文件 / 16 静态方法）、`bulletin_board_maintenance`（3 文件 / 2 静态方法）——设计后台运维视图入口方案与契约端点数据模型。设计原则：GM 与运维操作与用户面视图严格分离，新增 2 个后台视图（`admin_console_view` 承接 GM 沙盒 + CDK 兑换 + 运行时仪表、`narrative_orchestrator_view` 承接剧本 DAG 编排），并扩展 `notification_bulletin` 视图的运维 Tab 承接公告 CRUD；同时把 `world_state` / `feature_toggle_canary` / `telemetry` 三项 INFRA 只读仪表收编到 admin_console_view 的只读仪表 Tab，形成"GM 面板 + CDK 兑换 + 剧本编排 + 公告 CRUD + INFRA 只读仪表"五合一的后台运维枢纽；共登记 12 条契约条目（4 域 × 3 类）+ 3 条 INFRA 只读契约（world_state / feature_toggle_canary / telemetry → admin_console_view.dashboard Tab）。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST58-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST58-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：[docs/audit/AUDIT_REPORT_UI缺失.md](../../../../audit/AUDIT_REPORT_UI缺失.md)（2026-09-05 前后端契约审计）→ 项 P2-6「admin_sandbox / cdkey_voucher 拆出后台管理视图」+ 项 P2-7「narrative_orchestration 剧本查看器」+ 项 P2-8「bulletin_board_maintenance 公告板 CRUD」+ 项 P3-9「INFRA 域只读仪表板」+ Phase 57 契约层基础设施。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST58-ATT_附件_案卷共享契约与上下文.md) ｜ **阶段1 (当前)** ｜ [阶段2](KALAR-DEV-2026-ST58-002_阶段2_运维视图接线与CRUD算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST58-003_阶段3_后台运维视图工程化与既有基础设施接入.md) ｜ [阶段4](KALAR-DEV-2026-ST58-004_阶段4_后台运维契约与视图端到端测试矩阵.md)

---

## 📌 第一性原理溯源指针

* **精准上游证据指针**：
  - `docs/audit/AUDIT_REPORT_UI缺失.md:50`：`narrative_orchestration` 11 文件 / 16 静态方法，DAG 执行引擎当前完全无 UI；
  - `docs/audit/AUDIT_REPORT_UI缺失.md:60-61,64`：`cdkey_voucher` / `admin_sandbox` / `bulletin_board_maintenance` 三域的规模与用户面能力清单；
  - `docs/audit/AUDIT_REPORT_UI缺失.md:132-134`：P2-6/P2-7/P2-8 三条后台运维视图拆分需求；
  - `docs/audit/AUDIT_REPORT_UI缺失.md:137`：P3-9 INFRA 域只读仪表需求；
  - `backend/config/contracts.json`（Phase 57 S1 建立，Phase 59/60 扩展至 77 条）：本阶段为其追加 15 条契约条目（4 域 × 3 类 + INFRA × 3 条只读契约）；
  - `backend/domains/{admin_sandbox,cdkey_voucher,narrative_orchestration,bulletin_board_maintenance}/`：既有后端实现已完备，本阶段**只接线不改后端**。

* **核心不变量约束断言**：
  - `Inv-OP-1`（视图拆分最小增量）：本阶段新增视图**总数 = 2**（`admin_console_view` + `narrative_orchestrator_view`），`notification_bulletin` 扩展 1 个 Tab，禁止将 GM 面板 / CDK 兑换 / 剧本编排散落在 system_save / notification_bulletin / quest_causality 既有视图；
  - `Inv-OP-2`（契约完整性）：4 个后台运维域补齐后，每个域**必须**至少登记 1 条 DTO + 1 条 EVENT + 1 条 COMMAND 契约（承接 Phase 57 S1 Inv-CT-2），加 INFRA 3 条只读契约共 15 条；
  - `Inv-OP-3`（GM 权限闸门）：`admin_console_view` 与 `narrative_orchestrator_view` **必须**在视图层显式声明 `auth_scope: "GM"` 或 `auth_scope: "OPS"`，非 GM 会话进入视图必须被 `BaseViewContractBinder._contract_init` 抛出 `ContractPermissionError`（承接 Phase 59 S2 建立的五态错误工厂）；
  - `Inv-OP-4`（后端零修改）：本阶段**严禁**修改 `backend/domains/{admin_sandbox,cdkey_voucher,narrative_orchestration,bulletin_board_maintenance}/**` 及 `backend/domains/{world_state,feature_toggle_canary,telemetry}/**` 任何后端 `.gd` 文件；接线层全部通过契约端点定义；
  - `Inv-OP-5`（契约版本递增）：4 个域新增契约条目的 `contract_version` 均设为 `1`（首次登记）；`contracts.json` 的 `entry_count_expected` 字段从 77 更新为 77 + 15 = 92 条（Phase 57:49 + Phase 59:12 + Phase 60:16 + Phase 62:15 = 92）；
  - `Inv-OP-6`（INFRA 契约豁免标记）：3 条 INFRA 只读契约**必须**标记 `infra_exempt: true`（承接 Phase 57 S1 `CoverageStatus.INFRA` 枚举），但不进入 MISSING/PARTIAL 分母；
  - `Inv-OP-7`（后台视图 UI 层命名一致）：新增视图命名**必须**遵循既有视图的 `snake_case` 命名范式，且**必须**在 `frontend/views/**/*.tscn` 的枚举中登记；后台视图 Tab 名统一以 `ops_` 前缀区分（如 `ops_dashboard`、`ops_cdkey`、`ops_announcement`）。

* **防漂移最高指示**：
  - 本阶段**只**设计视图入口方案 + 契约端点数据模型，**不**触碰任何 `.gd` 实现代码，也**不**修改任何 `.tscn` 场景树结构；
  - 4 个后台运维域的接线**必须**通过 Phase 57 S2 建立的 `ContractParser` 三算法消费，禁止绕过契约层直接调用后端 Service；
  - 严禁将本阶段扩展为"契约层实现改造"或"后端逻辑重写"——那是 Phase 57 已闭环的任务。

---

## 一、 数据结构设计与代码契约 (Data Structures & Code Contracts)

### 1. 四域 + INFRA 视图入口方案

| 域 | 现有视图 | 入口方式 | 新增/扩展 Tab 名 | 视图规模增量 |
| :---: | :---: | :---: | :---: | :---: |
| `admin_sandbox` | 新建 `admin_console_view` | 新建视图 Tab | `ops_gm_panel` | — |
| `cdkey_voucher` | 新建 `admin_console_view` | 新建视图 Tab | `ops_cdkey` | — |
| `world_state`（INFRA） | 新建 `admin_console_view` | 新建视图 Tab | `ops_dashboard_world` | — |
| `feature_toggle_canary`（INFRA） | 新建 `admin_console_view` | 新建视图 Tab | `ops_dashboard_canary` | — |
| `telemetry`（INFRA） | 新建 `admin_console_view` | 新建视图 Tab | `ops_dashboard_telemetry` | — |
| `narrative_orchestration` | 新建 `narrative_orchestrator_view` | 新建视图 | — | 1 视图 |
| `bulletin_board_maintenance` | `notification_bulletin` | 扩展 Tab | `ops_announcement` | 1 Tab |

### 2. admin_sandbox 契约端点（3 条）

```gdscript
# 契约：admin_sandbox → admin_console_view.ops_gm_panel Tab 契约端点集合。
# 后端源：既有 GM 命令索引 / 沙盒权限审计 / 回滚服务（3 静态方法）。
# 位置：backend/config/contracts.json 追加 3 条 entries。

# CT-AS-AC-01 (DTO)：GM 命令索引快照 → 命令面板列表
{
  "contract_id": "CT-AS-AC-01",
  "contract_version": 1,
  "domain_name": "admin_sandbox",
  "view_name": "admin_console_view",
  "endpoint_kind": "DTO",
  "source_path": "AdminSandboxSolver.command_index_snapshot",
  "target_path": "admin_console_view.ops_gm_panel_tab.command_grid",
  "field_mapping": [
    {"backend_field": "commands", "view_field": "command_grid.items", "transform_kind": "identity"},
    {"backend_field": "current_gm_level", "view_field": "level_indicator.text", "transform_kind": "enum_map", "transform_config": {"map": {"L0":"只读","L1":"查询","L2":"修改","L3":"回滚","L4":"全权"}}},
    {"backend_field": "audit_trail_tail", "view_field": "audit_log_tail.list", "transform_kind": "identity"}
  ],
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}

# CT-AS-AC-02 (EVENT)：GM 命令执行事件 → 视图信号
{
  "contract_id": "CT-AS-AC-02",
  "contract_version": 1,
  "domain_name": "admin_sandbox",
  "view_name": "admin_console_view",
  "endpoint_kind": "EVENT",
  "event_name": "admin_sandbox.command_executed",
  "signal_name": "_on_gm_command_executed",
  "event_filter": {"domain": "admin_sandbox"},
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}

# CT-AS-AC-03 (COMMAND)：GM 命令派发 → AdminSandboxSolver.execute_command
{
  "contract_id": "CT-AS-AC-03",
  "contract_version": 1,
  "domain_name": "admin_sandbox",
  "view_name": "admin_console_view",
  "endpoint_kind": "COMMAND",
  "service_path": "AdminSandboxSolver",
  "method_name": "execute_command",
  "params_schema": [
    {"name": "command_id", "type": "String", "required": true, "rule": "VD_AS_COMMAND_ID_FORMAT"},
    {"name": "payload", "type": "Dictionary", "required": false, "rule": "VD_AS_COMMAND_PAYLOAD_SCHEMA"},
    {"name": "audit_reason", "type": "String", "required": true, "rule": "VD_AS_AUDIT_REASON_MINLEN"}
  ],
  "response_dto": "GmCommandResultDTO",
  "auth_scope": "GM_L2",
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}
```

### 3. cdkey_voucher 契约端点（3 条）

```json
{
  "contract_id": "CT-CV-AC-01",
  "contract_version": 1,
  "domain_name": "cdkey_voucher",
  "view_name": "admin_console_view",
  "endpoint_kind": "DTO",
  "source_path": "CdkeyVoucherSolver.voucher_inventory_snapshot",
  "target_path": "admin_console_view.ops_cdkey_tab.voucher_inventory_panel",
  "field_mapping": [
    {"backend_field": "total_batches", "view_field": "batch_counter.value", "transform_kind": "identity"},
    {"backend_field": "redeemed_ratio", "view_field": "redeem_progress_bar.value", "transform_kind": "identity"},
    {"backend_field": "batches", "view_field": "batch_grid.items", "transform_kind": "identity"},
    {"backend_field": "eligibility_rules", "view_field": "rule_panel.list", "transform_kind": "identity"}
  ]
}

{
  "contract_id": "CT-CV-AC-02",
  "contract_version": 1,
  "domain_name": "cdkey_voucher",
  "view_name": "admin_console_view",
  "endpoint_kind": "EVENT",
  "event_name": "cdkey_voucher.batch_updated",
  "signal_name": "_on_cdkey_batch_updated"
}

{
  "contract_id": "CT-CV-AC-03",
  "contract_version": 1,
  "domain_name": "cdkey_voucher",
  "view_name": "admin_console_view",
  "endpoint_kind": "COMMAND",
  "service_path": "CdkeyVoucherSolver",
  "method_name": "redeem_or_revoke",
  "params_schema": [
    {"name": "voucher_id", "type": "String", "required": true, "rule": "VD_CV_VOUCHER_ID_FORMAT"},
    {"name": "operation", "type": "String", "required": true, "rule": "VD_CV_OPERATION_ENUM"},
    {"name": "audit_reason", "type": "String", "required": true, "rule": "VD_AS_AUDIT_REASON_MINLEN"}
  ],
  "response_dto": "VoucherOperationResultDTO",
  "auth_scope": "GM_L2"
}
```

### 4. narrative_orchestration 契约端点（3 条，含新视图）

```json
{
  "contract_id": "CT-NO-NV-01",
  "contract_version": 1,
  "domain_name": "narrative_orchestration",
  "view_name": "narrative_orchestrator_view",
  "endpoint_kind": "DTO",
  "source_path": "NarrativeOrchestrationSolver.dag_snapshot",
  "target_path": "narrative_orchestrator_view.dag_canvas",
  "field_mapping": [
    {"backend_field": "dag_nodes", "view_field": "dag_canvas.nodes", "transform_kind": "identity"},
    {"backend_field": "dag_edges", "view_field": "dag_canvas.edges", "transform_kind": "identity"},
    {"backend_field": "chronology_pipeline", "view_field": "chronology_panel.items", "transform_kind": "identity"},
    {"backend_field": "probability_solver_results", "view_field": "prob_results_panel.items", "transform_kind": "identity"}
  ]
}

{
  "contract_id": "CT-NO-NV-02",
  "contract_version": 1,
  "domain_name": "narrative_orchestration",
  "view_name": "narrative_orchestrator_view",
  "endpoint_kind": "EVENT",
  "event_name": "narrative_orchestration.dag_recompiled",
  "signal_name": "_on_dag_recompiled"
}

{
  "contract_id": "CT-NO-NV-03",
  "contract_version": 1,
  "domain_name": "narrative_orchestration",
  "view_name": "narrative_orchestrator_view",
  "endpoint_kind": "COMMAND",
  "service_path": "NarrativeOrchestrationSolver",
  "method_name": "recompile_or_run",
  "params_schema": [
    {"name": "dag_id", "type": "String", "required": true, "rule": "VD_NO_DAG_ID_FORMAT"},
    {"name": "operation", "type": "String", "required": true, "rule": "VD_NO_OPERATION_ENUM"},
    {"name": "audit_reason", "type": "String", "required": true, "rule": "VD_AS_AUDIT_REASON_MINLEN"}
  ],
  "response_dto": "NarrativeRecompileResultDTO",
  "auth_scope": "OPS"
}
```

### 5. bulletin_board_maintenance 契约端点（3 条）

```json
{
  "contract_id": "CT-BB-NB-01",
  "contract_version": 1,
  "domain_name": "bulletin_board_maintenance",
  "view_name": "notification_bulletin",
  "endpoint_kind": "DTO",
  "source_path": "BulletinBoardMaintenanceSolver.board_snapshot",
  "target_path": "notification_bulletin_view.ops_announcement_tab.board_editor",
  "field_mapping": [
    {"backend_field": "announcements", "view_field": "announcement_grid.items", "transform_kind": "identity"},
    {"backend_field": "active_messaging", "view_field": "marquee_indicator.text", "transform_kind": "identity"},
    {"backend_field": "schedule_pipeline", "view_field": "schedule_panel.items", "transform_kind": "identity"},
    {"backend_field": "total_views", "view_field": "view_counter.value", "transform_kind": "identity"}
  ]
}

{
  "contract_id": "CT-BB-NB-02",
  "contract_version": 1,
  "domain_name": "bulletin_board_maintenance",
  "view_name": "notification_bulletin",
  "endpoint_kind": "EVENT",
  "event_name": "bulletin_board_maintenance.announcement_changed",
  "signal_name": "_on_announcement_changed"
}

{
  "contract_id": "CT-BB-NB-03",
  "contract_version": 1,
  "domain_name": "bulletin_board_maintenance",
  "view_name": "notification_bulletin",
  "endpoint_kind": "COMMAND",
  "service_path": "BulletinBoardMaintenanceSolver",
  "method_name": "create_update_delete_announcement",
  "params_schema": [
    {"name": "operation", "type": "String", "required": true, "rule": "VD_BB_OPERATION_ENUM"},
    {"name": "announcement", "type": "Dictionary", "required": true, "rule": "VD_BB_ANNOUNCEMENT_SCHEMA"},
    {"name": "audit_reason", "type": "String", "required": true, "rule": "VD_AS_AUDIT_REASON_MINLEN"}
  ],
  "response_dto": "BulletinCRUDResultDTO",
  "auth_scope": "OPS"
}
```

### 6. INFRA 只读仪表契约端点（3 条）

```gdscript
# 契约：3 个 INFRA 域 → admin_console_view.ops_dashboard_* Tab 只读契约集合。
# 后端源：world_state / feature_toggle_canary / telemetry（INFRA 分类，无写操作）。
# 位置：backend/config/contracts.json 追加 3 条 entries（endpoint_kind 仅为 DTO，不含 EVENT/COMMAND）。

# CT-WS-AC-01 (DTO, INFRA 只读)：世界态快照 → world_state 仪表
{
  "contract_id": "CT-WS-AC-01",
  "contract_version": 1,
  "domain_name": "world_state",
  "view_name": "admin_console_view",
  "endpoint_kind": "DTO",
  "source_path": "WorldState.get_snapshot",
  "target_path": "admin_console_view.ops_dashboard_world_tab.metrics_panel",
  "field_mapping": [
    {"backend_field": "tick_count", "view_field": "tick_counter.value", "transform_kind": "identity"},
    {"backend_field": "connected_sessions", "view_field": "session_counter.value", "transform_kind": "identity"},
    {"backend_field": "active_worlds", "view_field": "worlds_list.list", "transform_kind": "identity"}
  ],
  "infra_exempt": true,
  "read_only": true,
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}

# CT-FT-AC-01 (DTO, INFRA 只读)：灰度开关快照 → feature_toggle_canary 仪表
{
  "contract_id": "CT-FT-AC-01",
  "contract_version": 1,
  "domain_name": "feature_toggle_canary",
  "view_name": "admin_console_view",
  "endpoint_kind": "DTO",
  "source_path": "FeatureToggleCanary.snapshot",
  "target_path": "admin_console_view.ops_dashboard_canary_tab.flags_panel",
  "field_mapping": [
    {"backend_field": "flags", "view_field": "flags_list.items", "transform_kind": "identity"},
    {"backend_field": "canary_users", "view_field": "user_list.list", "transform_kind": "identity"}
  ],
  "infra_exempt": true,
  "read_only": true
}

# CT-TL-AC-01 (DTO, INFRA 只读)：遥测快照 → telemetry 仪表
{
  "contract_id": "CT-TL-AC-01",
  "contract_version": 1,
  "domain_name": "telemetry",
  "view_name": "admin_console_view",
  "endpoint_kind": "DTO",
  "source_path": "Telemetry.get_snapshot",
  "target_path": "admin_console_view.ops_dashboard_telemetry_tab.metrics_panel",
  "field_mapping": [
    {"backend_field": "fps_series", "view_field": "fps_chart.series", "transform_kind": "identity"},
    {"backend_field": "error_rate_series", "view_field": "error_chart.series", "transform_kind": "identity"},
    {"backend_field": "latency_p99_series", "view_field": "latency_chart.series", "transform_kind": "identity"}
  ],
  "infra_exempt": true,
  "read_only": true
}
```

### 7. AdminConsoleView / NarrativeOrchestratorView 新视图契约声明

```gdscript
# 契约：两个新视图的 class_name 与路由入口声明。
# 位置：frontend/views/admin_console_view.gd + frontend/views/narrative_orchestrator_view.gd
# （Phase 62 S2 阶段落地，本阶段仅登记 class_name 与 auth_scope 元数据。）

class_name AdminConsoleView

# 视图职责：GM 命令面板、CDK 兑换编排、INFRA 只读仪表（world_state/canary/telemetry）
# 主要数据源：admin_sandbox 域（3 条）+ cdkey_voucher 域（3 条）+ INFRA 3 条只读
# 视图类型：独立视图（GM/OPS 权限闸门）
# 契约继承：BaseViewContractBinder（承接 Phase 59 S2）

# 路由入口（承接 view_router.gd 已建立的 19 视图路由注册，Phase 62 追加为第 20 视图）
static func get_route_name() -> String:
	return "admin_console"

static func get_display_name() -> String:
	return "后台运维台"

static func get_auth_scope() -> String:
	return "GM_L1"  # 至少需 L1 查询权限才能进入视图

class_name NarrativeOrchestratorView

# 视图职责：剧本 DAG 编排、概率 solver 结果可视化、编年器 pipeline 编辑
# 主要数据源：narrative_orchestration 域（3 条契约）
# 视图类型：独立视图（OPS 权限闸门）
# 契约继承：BaseViewContractBinder（承接 Phase 59 S2）

# 路由入口（承接 view_router.gd 已建立的 19 视图路由注册，Phase 62 追加为第 21 视图）
static func get_route_name() -> String:
	return "narrative_orchestrator"

static func get_display_name() -> String:
	return "剧本编排台"

static func get_auth_scope() -> String:
	return "OPS"
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 1.1**：在 `backend/config/contracts.json` 中追加 15 条契约条目（4 域 × 3 类 + INFRA × 3 类只读），contract_id 前缀分别为 `CT-AS-AC-*`、`CT-CV-AC-*`、`CT-NO-NV-*`、`CT-BB-NB-*`、`CT-WS-AC-*`、`CT-FT-AC-*`、`CT-TL-AC-*`；
- [ ] **Step 1.2**：更新 `contracts.json` 顶部 `entry_count_expected` 字段从 `77` 更新为 `92`（Inv-OP-5：49 + 12 + 16 + 15 = 92）；
- [ ] **Step 1.3**：为 4 个后台运维域新增的 12 条契约条目全部标记 `infra_exempt: false`、`deprecated: false`、`created_at: "2026-09-05"`、`last_reviewed_at: "2026-09-05"`；为 3 条 INFRA 契约标记 `infra_exempt: true`、`read_only: true`；
- [ ] **Step 1.4**：新增 10 条 VD_* 值域校验规则（承接 Phase 57/59/60 VD_* 规则族范式）：`VD_AS_COMMAND_ID_FORMAT`、`VD_AS_COMMAND_PAYLOAD_SCHEMA`、`VD_AS_AUDIT_REASON_MINLEN`、`VD_CV_VOUCHER_ID_FORMAT`、`VD_CV_OPERATION_ENUM`、`VD_NO_DAG_ID_FORMAT`、`VD_NO_OPERATION_ENUM`、`VD_BB_OPERATION_ENUM`、`VD_BB_ANNOUNCEMENT_SCHEMA`、`VD_AUTH_SCOPE_ENUM`；
- [ ] **Step 1.5**：更新 `backend/config/contract_completeness_baseline.json` 的 `by_status.PARTIAL` 数组——移除 4 项 `[admin_sandbox, cdkey_voucher, narrative_orchestration, bulletin_board_maintenance]`，`PARTIAL` 计数从 19 降至 15（Phase 59/60 已移除 4 MISSING 域与 4 严重 PARTIAL 域，本阶段再移除 4 后台运维 PARTIAL 域）；
- [ ] **Step 1.6**：更新 baseline 的 `by_status.FULL` 数组——追加 4 项（4 个后台运维 PARTIAL 域补齐后转为 FULL）；
- [ ] **Step 1.7**：更新 baseline 的 `by_status.INFRA` 数组——追加 3 项（`world_state`、`feature_toggle_canary`、`telemetry` 三项 INFRA 域由"完全无契约"转为"有只读契约"）；
- [ ] **Step 1.8**：更新 `frontend/views/view_router.gd` 的 19 视图注册数组——追加 `admin_console_view` 与 `narrative_orchestrator_view` 为第 20/21 个视图（Inv-OP-1 新视图上限 = 2）；
- [ ] **Step 1.9**：为 `notification_bulletin` 的枚举声明追加 `ops_announcement` Tab 名（Inv-OP-7 命名前缀 `ops_`），但**不修改** `.tscn` 场景树结构；
- [ ] **Step 1.10**：本阶段**禁止**：修改任何 `backend/domains/{admin_sandbox,cdkey_voucher,narrative_orchestration,bulletin_board_maintenance,world_state,feature_toggle_canary,telemetry}/**` 文件；本阶段新增文件**不超过 4 个**（2 个新视图 `.gd` 骨架 + 2 个新视图 `.tscn` 骨架）；
- [ ] **Step 1.11**：本阶段新增断言数**控制在 10~14 条**，命名前缀统一 `TC-OP-S1-NN`；
- [ ] **Step 1.12**：两个新视图的 class_name 与 view_router 注册名称**必须**严格一致，禁止大小写漂移；
- [ ] **Step 1.13**：所有新契约的 `field_mapping` 中 `transform_kind` 值**必须**在 Phase 57 S2 已实现的转换规则内（identity/cast/format/enum_map/aggregate/filter），不允许新增值；
- [ ] **Step 1.14**：4 个后台运维域的契约条目**必须**在 `contracts.json` 中按域名字母序排列，便于 CI 门禁扫描与人工审查；
- [ ] **Step 1.15**：2 个新视图**必须**声明 `get_auth_scope()` 静态方法，返回 `GM_L1`（AdminConsoleView）或 `OPS`（NarrativeOrchestratorView）；该值将被 `BaseViewContractBinder._contract_init` 消费并校验（Inv-OP-3）。

---

## 三、 数据结构验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :---: | :--- | :--- | :---: |
| `TC-OP-S1-01` | 15 条新契约条目注册成功 | 加载更新后的 `contracts.json` | `ContractRegistryIndex.find_all().size() == 92` |
| `TC-OP-S1-02` | 4 后台运维域 DTO 契约登记 | `find_by_domain("admin_sandbox")` 等 4 域 | 每域返回条目数 ≥ 1 且 endpoint_kind 含 DTO |
| `TC-OP-S1-03` | 4 后台运维域 EVENT 契约登记 | 同上 | 每域 endpoint_kind 含 EVENT |
| `TC-OP-S1-04` | 4 后台运维域 COMMAND 契约登记 | 同上 | 每域 endpoint_kind 含 COMMAND |
| `TC-OP-S1-05` | 3 INFRA 域只读契约登记 | `find_by_domain("world_state")` 等 3 域 | 每域返回条目数 = 1 且 endpoint_kind == DTO 且 `infra_exempt == true` |
| `TC-OP-S1-06` | 契约完整性收敛（Inv-OP-2） | `guard.audit_domain_coverage("admin_sandbox")` 等 4 域 | 返回 `{coverage_status: FULL, missing_endpoints: []}` |
| `TC-OP-S1-07` | baseline PARTIAL 数组收敛 | 加载更新后的 baseline | `baseline.by_status.PARTIAL.size() == 15`（19 - 4） |
| `TC-OP-S1-08` | baseline FULL 数组扩展 | 加载更新后的 baseline | `baseline.by_status.FULL` 包含 4 个新域 |
| `TC-OP-S1-09` | baseline INFRA 数组扩展 | 加载更新后的 baseline | `baseline.by_status.INFRA` 包含 3 个新增契约域 |
| `TC-OP-S1-10` | VD_* 新增 10 条值域规则 | `audit_config.gd` 中 VD_CT_RULES 数量 | 数量 = 49（Phase 57/59/60 累计）+ 10（Phase 62 S1 新增 10 条）= 59 |
| `TC-OP-S1-11` | entry_count_expected 与实际一致 | 加载 contracts.json | `$schema.version == 1` 且 `entry_count_expected == 92` 且 `entries.size() == 92` |
| `TC-OP-S1-12` | 两个新视图注册 | `view_router.gd` 视图数组 | 数组长度从 19 增至 21，包含 `admin_console_view` + `narrative_orchestrator_view` |
| `TC-OP-S1-13` | 后端零修改（Inv-OP-4） | 扫描 7 个后台运维/INFRA 域 `.gd` 文件 mtime | 7 个域全部 `.gd` 文件的修改时间早于 Phase 62 施工开始时间 |
| `TC-OP-S1-14` | 视图权限闸门声明 | `admin_console_view.gd` + `narrative_orchestrator_view.gd` | 两视图均含 `get_auth_scope()` 静态方法，返回值分别为 `GM_L1` / `OPS` |

---

## 附：第 2 轮实施收敛登记

> 【待第 2 轮细则收敛时填写】
> - 实际新增文件清单：
> - 15 条契约条目最终落库确认：
> - 10 条 VD_* 规则上线结果：
> - baseline 快照 Phase 62 冻结点登记（PARTIAL -4 / FULL +4 / INFRA +3）：
> - 两个新视图注册与 view_router 一致性核验：
> - 断言落地数 vs 计划数（10~14）的偏差：
> - 未闭环事项登记（如有）：
