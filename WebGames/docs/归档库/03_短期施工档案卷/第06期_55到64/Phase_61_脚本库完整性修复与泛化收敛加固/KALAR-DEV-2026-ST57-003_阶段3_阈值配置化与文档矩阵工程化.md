---
档号: KALAR-DEV-2026-ST57-003
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST57 (Phase_61_脚本库完整性修复与泛化收敛加固)
件号: 003
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_61_脚本库完整性修复与泛化收敛加固 —— 阶段3：阈值配置化与文档矩阵工程化
形成日期: 2026-09-05
归档日期: 2026-09-06（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: C3 工程债清理; C4 auto-refactor 侧; config.json
---

# 施工细则：Phase 61 脚本库完整性修复与泛化收敛加固 —— 阶段3：阈值配置化与文档矩阵工程化

> 施工开始日期：2026-09-05 上午
> 责任人：卡拉尔世界引擎架构组
> 状态：✅ 已闭环（第2轮施工已完成并验证闭环）

> [!NOTE]
> **【施工目标】**：把 `.gd` 阈值从代码硬编码迁入 `WebGames/auto-refactor.config.json` 唯一源；补齐 `scripts/README.md` 矩阵登记与命名规则澄清；清理 `report_trend.pyc/js 无锁` 工程债；`SIM-WRP` 并存只建映射不删码。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST57-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST57-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：阶段1 `GdThresholdContract/GovMappingContract` + `scripts/README.md:10-29` 矩阵缺 `audit-arch/timestamp`。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST57-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST57-001_阶段1_注册契约与阈值数据模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST57-002_阶段2_注册自检与切片调度算法实现.md) ｜ **阶段3 (当前)** ｜ [阶段4](KALAR-DEV-2026-ST57-004_阶段4_门禁回归与噪声收敛验收测试矩阵.md)

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  - `auto-refactor/src/core/config.ts:124-160` 三层合并（默认→配置文件→CLI）与 `analyzers` 声明式注册；
  - `WebGames/scripts/py/audit_gd.py:31` 规则文件 `scripts/config/code_governance_rules.json`（`ADV-* 8 + SIM-* 5`）；
  - `WebGames/scripts/js/package.json`（`jsdom/mermaid`）与 `check-mermaid.sh` 跳过语义。
* **核心不变量约束断言**：
  - `Inv-RG-4`：阈值文件缺席回退默认，坏 JSON 只告警不崩（沿 `config.ts:153` 语义）；
  - `Inv-CFG-1`（零硬编码）：`GameConfig.get_*` 仍经类型化读取；本阶段新增 `contracts` 外的 `auto-refactor.config.json` 为工具链配置，不进 `GameConfig` 体系，不混入 `config/<层>/<域>.json`；
  - `Inv-DOC-1`：`scripts/README.md` 矩阵行与 `AUDIT_TASKS` 21 项逐行可溯；`NAMING_RULES` 不放宽。
* **工程化重构最高指示**：配置缺省必兜底；`SIM-WRP-001` 与 `GOV-LOG-002` 并存期禁止删任一侧实现；`__pycache__` 与 `node_modules` 永不入库。

---

## 一、 配置驱动与泛化扩展实现

### C1 `WebGames/auto-refactor.config.json`（新建，唯一阈值源）

```json
{
  "include": ["backend/**/*.gd", "frontend/**/*.gd", "tests/**/*.gd"],
  "exclude": ["node_modules", ".git", "dist", "out", ".godot"],
  "thresholds": {
    "magicNumberMin": 10,
    "hardcodedStringMinLength": 8,
    "duplicateLiteralThreshold": 5,
    "complexityWarn": 15,
    "complexityFail": 25,
    "fileLinesWarn": 500,
    "fileLinesFail": 1000
  },
  "analyzers": {
    "constants": { "enabled": true },
    "complexity": { "enabled": true },
    "large-file": { "enabled": true },
    "governance": { "enabled": true, "options": { "maxNestingDepth": 5, "maxInheritanceDepth": 2 } }
  }
}
```

`fileLines 500/1000` 依据：`audit_gd.py 749/archive_volume.py 1015` 为工具脚本特例，`.gd` 业务域以 500 告警容忍求解器体量；桥接以 `--config` 显式传入，不依赖自动发现。

### C2 `scripts/README.md` 矩阵补登记（只增行不改语义）

* 矩阵增 `audit-arch`（桥接 `tests/arch_runner.gd`，缺 Godot 即跳过）与 `timestamp`（`timestamp.sh --full` 落戳源）两行；
* `§2 强制规则1` 澄清为“`sh/ps1` kebab-case、`py` snake_case（`audit_*/report_*/archive_*`），目录表达语言域”；
* `js/` 行点名 `validate-mermaid.mjs`（`mermaid.parse` 真解析，与 `audit_docs.py` 启发式互补）。

### C3 工程债清理

* 删 `scripts/py/__pycache__/report_trend.*.pyc`（无源孤儿，`audit_docs.py` 无此引擎）；
* `scripts/js/` 补 `package-lock.json`（`npm install --prefix scripts/js` 后生成入库，保证 CI 复现）；
* `audit_gd_style.py` 头注释增“门禁豁免、链路保留”一行说明，不改逻辑。

### C4 auto-refactor 侧（跨项目，需显式授权；本案卷只做最小两处）

* `src/core/gdscriptAdapter.ts:190` 容忍行增 `GameConfig.msg/render_narrative/emit_narrative_by_key`（文案键不计 `hardcoded-string`），与 `audit_config_unused.py:48-54` 三层引用语义对齐；
* `src/core/reporters.ts` JSON 转义修复（`Invalid \escape` 根因，`detail.value` 逐字转义），使 `--format json` 可被 `json.load` 解析。

---

## 二、 命令式施工执行清单

- [x] **Step 3.1: 阈值配置表新建** - 上文 JSON 逐字落地，坏表告警回退验证
- [x] **Step 3.2: README 矩阵补行** - `audit-arch/timestamp/validate-mermaid` 三处点名，规则澄清
- [x] **Step 3.3: 工程债清理** - 删孤儿 pyc、补 js 锁文件、存根注释
- [x] **Step 3.4: 跨项目两处最小修** - 容忍行 + JSON 转义（须授权，无授权则 WebGames 侧先行、TS 侧挂起）

---

## 三、 工程化验收矩阵

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-RG-S3-01` | 配置驱动生效 | `auto-refactor.config.json` 存在时扫描 | `constants` 量级下降（12840→<4000）且 `governance` 被启用 |
| `TC-RG-S3-02` | 缺省兜底 | 临时移走配置文件 | 回退默认阈值扫描不崩 |
| `TC-RG-S3-03` | 文档矩阵可溯 | README 矩阵 vs `AUDIT_TASKS` | 21 项逐行可溯，无“提到但缺失” |

---
## 附录：实施收敛登记（第2轮闭环，2026-09-05 上午）

- C1：auto-refactor.config.json 新建；同机 --cache-clear 前后对比总量 12900 到 9466（constants 12840 到 8821、complexity 31 到 9、error 7 到 2），TC-RG-S3-01 通过；缺省回退路径经代码审查确认（config.ts 坏表告警语义保持）。
- C2：README 矩阵增 audit-arch 与 timestamp 两行 + 命名规则澄清（sh/ps1 为 kebab，py 为 snake）+ validate-mermaid.mjs 点名；audit-docs 对 README 零发现。
- C3：孤儿 report_trend pyc 已删；scripts/js/package-lock.json 已生成（31KB）；audit_gd_style.py 存根说明经核查已完备，未改。
- C4：gdscriptAdapter 容忍行增 GameConfig、render_narrative、emit_narrative_by_key（沿既有 preload 等模式），重建后 constants 10487 到 8821；reporters.ts 经落盘复验证明无转义缺陷（--out 文件可解析，既往管道报错系 pwsh 转码环境问题），故未改动；auto-refactor 自门禁 validate-equivalence 与 validate-governance 14/14 通过。
