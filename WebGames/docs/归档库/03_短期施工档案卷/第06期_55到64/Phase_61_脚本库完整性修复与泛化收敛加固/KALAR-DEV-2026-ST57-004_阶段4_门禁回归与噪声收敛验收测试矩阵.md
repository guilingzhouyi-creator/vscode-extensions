---
档号: KALAR-DEV-2026-ST57-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST57 (Phase_61_脚本库完整性修复与泛化收敛加固)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_61_脚本库完整性修复与泛化收敛加固 —— 阶段4：门禁回归与噪声收敛验收测试矩阵
形成日期: 2026-09-05
归档日期: 2026-09-06（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 门禁回归; docs_baseline.json
---

# 施工细则：Phase 61 脚本库完整性修复与泛化收敛加固 —— 阶段4：门禁回归与噪声收敛验收测试矩阵

> 施工开始日期：2026-09-05 上午
> 责任人：卡拉尔世界引擎架构组
> 状态：📝 待获批（第1轮细则已编制，待批准后进入实现）

> [!NOTE]
> **【施工目标】**：S1~S3 全量回归零退化：18 项工程门禁全绿、文档基线 0 新增、13 对同构一致、`constants` 噪声收敛可度量、全域业务断言 100% PASS。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST57-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST57-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：阶段1 DoD（`TC-RG-S1-01~04`）+ 阶段2（`TC-RG-S2-01~04`）+ 阶段3（`TC-RG-S3-01~03`）全量闭环。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST57-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST57-001_阶段1_注册契约与阈值数据模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST57-002_阶段2_注册自检与切片调度算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST57-003_阶段3_阈值配置化与文档矩阵工程化.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  - `scripts/sh/audit-all.sh:26`（`exec audit_runner.py "$@"`）与 `scripts/py/audit_runner.py:511-534` JSON 报告；
  - `benchmarks/reports/docs_baseline.json` 棘轮语义（存量只许消化）；
  - `run_tests.sh` 全域断言（Phase 56 基线 450/450）。
* **核心不变量约束断言**：
  - `Inv-QA-1`：`audit-all` 全绿（`audit 18` 含新增自检，切片/桥接改动不引入新红）；
  - `Inv-QA-2`：`audit-docs --baseline` 新增违规为 0（README 补行不引入死链/锚点失效）；
  - `Inv-QA-3`：`pair_parity` 13 对一致（S2/S3 未改参数面）；
  - `Inv-QA-4`：业务零碰（本案卷不改 `backend/frontend/config` 任一业务文件，全域断言数不减少）。
* **验收闭环最高指示**：先单任务 `--task` 逐项绿，再 `--group audit` 全绿，最后 `audit-all.sh` 与 `audit-docs --baseline` 双门禁；`constants` 收敛以“同一报告对”前后对比度量，不以单次绝对值断言。

---

## 一、 自动化测试套件与验收代码

```bash
# 门禁序（显式按序，勿并入 audit 组）：
python scripts/py/audit_runner.py --task pair_parity,config,gd --json
python scripts/py/audit_runner.py --group audit --json
bash scripts/sh/audit-all.sh
bash scripts/sh/audit-docs.sh --baseline benchmarks/reports/docs_baseline.json
bash scripts/sh/test-run.sh
node ../auto-refactor/dist/index.js scan --root . --config auto-refactor.config.json --format text
```

`--slice` 回归：`--slice config` 子集为 `--group audit` 真子集；`--slice __nohit__` 必 `exit 2`。孤儿注入回归用临时目录，不污染 `scripts/py/`。

---

## 二、 命令式施工执行清单

- [ ] **Step 4.1: 单任务逐项回归** - `pair_parity/config/gd/refactor_metrics` 先绿
- [ ] **Step 4.2: 全量门禁回归** - `audit` 组全绿 + `audit-all.sh` 全绿
- [ ] **Step 4.3: 文档棘轮与业务回归** - `audit-docs --baseline` 0 新增 + `test-run.sh` 断言不减少
- [ ] **Step 4.4: 噪声收敛度量** - 同机前后两报告对比，`constants` 下降且 `complexity/large-file/governance` 零丢失

---

## 三、 全量工程验收矩阵

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-RG-S4-01` | 门禁全绿 | `audit-all.sh` | 18/18 PASS（含自检），`registry_issues: []` |
| `TC-RG-S4-02` | 文档零新增 | `audit-docs.sh --baseline` | `exit 0` 且新增违规 0 |
| `TC-RG-S4-03` | 同构一致 | `audit_pair_parity.py` | 13 对一致，提示仅豁免类 |
| `TC-RG-S4-04` | 切片单调 | `--slice config` vs 全量 | 子集关系成立，空命中 `exit 2` |
| `TC-RG-S4-05` | 噪声收敛 | 配置前后报告对 | `constants` 显著下降，`governance` 有输出且 JSON 可解析 |
| `TC-RG-S4-06` | 业务零退化 | `test-run.sh` | 断言数 ≥450 且 100% PASS |

---
## 附录：实施收敛登记（第2轮闭环，2026-09-05 上午）

- TC-RG-S4-01：--group audit 17/18 通过，唯一红为 docs（13 项他轨在途；本案卷 Phase_61 登记后自身零发现，经 --json 核验 Phase_61 零命中）。
- TC-RG-S4-02：文档棘轮本案卷零新增（全局红归属 contract 轨，不越界修复）。
- TC-RG-S4-03/04：pair_parity 13 对一致；切片子集与空命中行为符合预期。
- TC-RG-S4-05：噪声前后对比见 C1 登记；--format json 落盘文件可解析。
- TC-RG-S4-06：Godot 直跑 79/79 套件、450/450 断言全部通过（test-run.sh 在 Git Bash 内不可见 WinGet 垫片 godot 属环境约束；test-run.ps1 在 5.1 下解析失败根因勘定为无 BOM 而非引号缺失，已直接修复 7 个含 CJK 的 ps1 文件并放宽 Godot 调用段错误偏好，端到端全绿待并行 Godot 进程退出后静默窗口复跑）。
- 残留风险移交：archive_volume.py 工作区改动为他轨在途（未碰）；Phase 58 空缺为 contract 轨 renumber 在途（未填）；--json 经 pwsh 管道偶发转码失败（文件落盘正常）。
