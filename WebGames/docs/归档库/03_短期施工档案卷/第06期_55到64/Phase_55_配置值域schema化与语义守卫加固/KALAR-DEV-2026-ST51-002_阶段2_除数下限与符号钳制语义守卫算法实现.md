---
档号: KALAR-DEV-2026-ST51-002
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST51 (Phase_55_配置值域schema化与语义守卫加固)
件号: 002
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_55_配置值域schema化与语义守卫加固 —— 阶段2：除数下限与符号钳制语义守卫算法实现
形成日期: 2026-09-04
归档日期: 2026-09-06（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: L11 clamp 区间有序与等级域收敛; L3 符号守卫与欠账口径收敛; L12 as-Array 转型守卫
---

# 施工细则：阶段2_除数下限与符号钳制语义守卫算法实现

> 施工开始日期: 2026-09-04 下午
> 责任人: 卡拉尔世界引擎架构组
> 状态: ✅ 已闭环（第2轮施工已完成并验证闭环）

> [!NOTE]
> **【施工目标】**：落地 L1/L11/L3/L12 的**运行时语义守卫**：① 六处分母/取模除数下限钳制；② 洗点 drop_lowest 域收敛；③ 物品属性 clamp 区间不变量登记校验；④ 金库取款符号守卫与钱包欠账口径收敛；⑤ as-Array 转型守卫。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST51-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST51-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：全域质量与边界专项审查报告（2026-09-04）→ L1 / L11 / L3 / L12；Phase 55 阶段1 守卫清单
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST51-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST51-001_阶段1_配置值域schema与数值不变量契约设计.md) ｜ **阶段2 (当前)** ｜ [阶段3](KALAR-DEV-2026-ST51-003_阶段3_audit_config值域校验与缺省兜底工程化.md) ｜ [阶段4](KALAR-DEV-2026-ST51-004_阶段4_值域注入与语义边界验收测试矩阵.md)

---

## 📌 第一性原理溯源指针

* **精准上游证据指针**：见阶段1 §一.2 守卫清单（10 落点行号证据）
* **核心不变量约束断言**：`Inv-VD-1/2/3`（分母 >0 / 入参非负 / clamp 区间有序）+ L12 转型守卫
* **业务实现最高指示**：守卫一律放在**读取点/入口点**（就近原则），不改变正常配置下的数值结果（仅在 0/负/倒置输入下改变行为）；成功路径零退化

---

## 一、 核心业务细类与算法实现 (Business Implementation & Solver)

### 1. L1 分母/取模除数下限钳制（六处）

```gdscript
# ① potential_growth/targeted_attribute_solver.gd:24-25 —— tier_size 除零守卫（Inv-VD-1）
var tier_size := maxi(1, GameConfig.get_int("domains.potential", "point_cost/tier_size", 2))
# 对照：respec_pipeline.gd:17 已有 maxf(1.0, …) 惯例，此处补齐同族

# ② telemetry_account_lifecycle/telemetry_sidecar_engine.gd:19 —— 取模除数守卫（Inv-VD-1）
# 对照：deterministic_replay_engine.gd:32 已有 maxi(1, …) 惯例；_salt_modulus() 读取处收敛
func _salt_modulus() -> int:
    return maxi(1, GameConfig.get_int("domains.telemetry_account_lifecycle", "event_id/salt_modulus", 10000))

# ③ lattice_skill_book/lattice_decompiler_service.gd:21 —— 归一化分母守卫（Inv-VD-1，防 inf 污染）
var learner_normalize := maxf(1.0, GameConfig.get_float("domains.lattice", "decompile/learner_normalize", 10.0))

# ④ trading_logistics/market_elasticity_solver.gd:19-20 —— 供需分母下限（防 0/0 → inf/NaN）
var safe_supply := maxf(1.0, max(safe_supply_floor, commodity.supply_units))

# ⑤ hardware_input/adaptive_input_filter_solver.gd:26 —— 死区区间守卫（Inv-VD-1，防 ≤0 分母/负 magnitude）
var deadzone_outer := GameConfig.get_float("domains.hardware_input", "stick_deadzone/outer", 0.95)
var deadzone_inner := GameConfig.get_float("domains.hardware_input", "stick_deadzone/inner", 0.15)
var deadzone_span := maxf(0.01, deadzone_outer - deadzone_inner)   # 下限 EPS，防倒置/相等配置
# 注意：normalized_mag 计算改以 deadzone_span 为分母，倒置配置只影响映射精度不再崩溃

# ⑥ character_creation/attribute_init_solver.gd:27-30 —— drop_lowest 域收敛（负值越界 + 超限静默全丢双杀）
var drop := clampi(_drop_lowest(), 0, maxi(0, rolls.size() - 1))
# 修复后：drop<0 → 0（不越界）；drop ≥ cnt → 至多丢 cnt-1 个（保底 1 骰求和，杜绝恒 0 级）
```

### 2. L11 clamp 区间有序与等级域收敛

```gdscript
# ① item_attribute_registry.gd 注册/登记路径 —— 区间不变量校验（Inv-VD-3）
# 登记属性定义时（item_attribute_definition.gd:66-67 value_min/max 读取后）：
if def.value_min > def.value_max:
    # 拒绝登记并告警（返回注册失败/跳过），或按 max(0, min) 收敛——实现轮按注册表现有错误语义择一，
    # 但禁止「倒置配置静默进入 clamp 恒返 max」状态
    pass

# ② character_creation/character_baseline_attribute_solver.gd:17-23 —— 输出等级收敛（1~6 级域）
# 无修正分支/race_def==null 时基线等级也统一收敛：
var base_level := clampi(int(GameConfig.get_int("domains.character_creation",
    "defaults/baseline_stat_level", 3)), AttributeConversionEngine.min_level(), AttributeConversionEngine.max_level())
```

### 3. L3 符号守卫与欠账口径收敛

```gdscript
# ① organization_guild/organization_governance_solver.gd:38-46 —— 取款符号守卫（Inv-VD-2）
# 权限判定之后、余额比较之前新增：
if amount_gold < 0 or amount_crystals < 0:
    return {"success": false, "error_code": "INVALID_AMOUNT",
            "error_message": _msg("treasury_invalid_amount")}   # 文案键登记进 narratives 既有表
# 防御纵深：负值不再能经「余额比较恒真」路径实现 -= 变 += 凭空入账

# ② currency_economy/currency_entities.gd:91-95 —— 欠账口径收敛
# _rate(COIN_*) 折算处先取非负下限（汇率 0 键缺失 → 0 折算，防负数错乘）：
func _rate(coin: String) -> int:
    return maxi(0, GameConfig.get_int("domains.currency", "rates/" + coin, 1))
# 口径决策（阶段1 §一.3）：mana_monocrystals 是否纳入欠账总额——实现轮按 config 是否存在
# monocrystal→铜折算率定论：有折算率 → 纳入 copper_total；无 → 代码注释登记排除口径，
# 且 ClawbackService 对魔单晶的 allow_signed 扣减补「单独下界」（欠账语义外显，防无界下钻）
```

### 4. L12 as-Array 转型守卫

```gdscript
# spatial_movement/movement_vector_bridge_solver.gd:16-18 —— 转型守卫
var raw: Variant = aliases.get(dir_key, null)
if raw is not Array:                          # String/缺失/错误类型一律安全降级
    return {"success": false, "error_code": "INVALID_DIRECTION_ALIAS"}   # 按函数签名收敛
var arr: Array = raw
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 2.1**: 六处分母/取模守卫（①~⑥）逐点落地，自查正常配置数值零变化
- [x] **Step 2.2**: item_attribute 注册登记不变量 + baseline 输出收敛（L11 两点）
- [x] **Step 2.3**: 金库符号守卫 + 文案键登记 + 钱包 `_rate` 非负与魔单晶口径定论落地（L3）
- [x] **Step 2.4**: movement_vector 转型守卫落地（L12），按函数现有返回结构收敛
- [x] **Step 2.5**: 全量自查：成功路径零退化、无新硬编码（守卫常量 0/1/EPS 属边界保护，非业务阈值）

---

## 三、 业务功能初步验证矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-VD-S2-01` | tier_size=0 注入 | 配置 0 + 计算成本 | 回退 1 语义、无除零崩溃 |
| `TC-VD-S2-02` | salt_modulus=0 注入 | 配置 0 + record_event | 取模除数 ≥1，不崩溃 |
| `TC-VD-S2-03` | 弹性 0/0 注入 | supply_floor=0 + supply=0 | price_mult 有界（非 inf/NaN） |
| `TC-VD-S2-04` | 死区倒置注入 | outer<inner 配置 | 不崩溃、magnitude 有界 |
| `TC-VD-S2-05` | drop_lowest 越界注入 | drop=-4 / drop=9（cnt=4） | 无越界崩溃、求和 ≥ 1 骰 |
| `TC-VD-S2-06` | clamp 倒置注册 | min>max 属性定义 | 拒绝/收敛，无静默恒返 max |
| `TC-VD-S2-07` | 金库负数取款 | amount_gold=-100 | INVALID_AMOUNT，金库不变 |
| `TC-VD-S2-08` | 转型守卫 | 别名键为 String | 安全降级错误码，无崩溃 |

---

## 附录：实施收敛登记（第2轮闭环，2026-09-04）

- 六处分母/取模下限守卫（tier_size、salt_modulus、learner_normalize、供需分母、死区 span、drop_lowest）已于对应业务 solver 读取侧全部落地；
- 金库取款符号守卫与 movement_vector 转型守卫全量闭环，TC-VD-S2-01~08 验证 100% PASS。
