---
档号: KALAR-DEV-2026-ST56-001
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST56 (Phase_60_四个严重PARTIAL域UI深化与视图拆分)
件号: 001
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_60_四个严重PARTIAL域UI深化与视图拆分 —— 阶段1：四域深层契约与视图拆分方案数据模型设计
形成日期: 2026-09-05
归档日期: 2026-09-06（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: InventoryView; 四域契约扩展方案; character_creation 深层契约; magic_system 深层契约; contracts.json
---

# 施工细则：阶段1_四域深层契约与视图拆分方案数据模型设计

> 施工开始日期：2026-09-05 下午
> 责任人：卡拉尔世界引擎架构组
> 状态：📝 待获批（第1轮细则已编制，待批准后进入实现）

> [!NOTE]
> **【施工目标】**：为审计报告识别的 4 个严重 PARTIAL 域——`character_creation`（16 文件 / 43 静态方法）、`magic_system`（12 文件 / 48 静态方法）、`physics_thermodynamics`（9 文件 / 22 静态方法）、`inventory`（13 文件 / 28 静态方法）——设计深层契约扩展与视图拆分方案；将"骨架阶段"的视图升级为"契约驱动的完整覆盖"，消除审计报告中"UI 骨架完备但接线层为零"的镜像问题；建立 inventory 与 equipment_loadout 的独立视图拆分方案（当前挤在 character_progression 的 8 Tab 中）。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST56-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST56-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：[docs/audit/AUDIT_REPORT_UI缺失.md](../../../../audit/AUDIT_REPORT_UI缺失.md)（2026-09-05 前后端契约审计）→ 项 P1-3「character_creation FSM 接线」+ P1-4「magic_system / physics_thermodynamics 规则编辑 UI」+ P1-5「inventory / equipment_loadout 拆分独立视图」。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST56-ATT_附件_案卷共享契约与上下文.md) ｜ **阶段1 (当前)** ｜ [阶段2](KALAR-DEV-2026-ST56-002_阶段2_深层契约接线与视图拆分算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST56-003_阶段3_视图拆分工程化与深层契约接入既有基础设施.md) ｜ [阶段4](KALAR-DEV-2026-ST56-004_阶段4_深层契约接线与视图拆分端到端测试矩阵.md)

---

## 📌 第一性原理溯源指针

* **精准上游证据指针**：
  - `docs/audit/AUDIT_REPORT_UI缺失.md:44-48`：4 个严重 PARTIAL 域的后端规模与前端截断部分清单；
  - `docs/audit/AUDIT_REPORT_UI缺失.md:125-129`：P1 修复优先级建议（Phase 60 前完成）；
  - `backend/domains/character_creation/character_creation_fsm.gd`：Phase 48 S2 建立的 10 层 FSM，本阶段为其契约化暴露；
  - `frontend/views/character_progression.gd`：8 Tab 负载过重，本阶段将 inventory/equipment_loadout 拆出；
  - Phase 59 S1 建立的契约注册表扩展范式：本阶段沿用 3 类契约条目模式。

* **核心不变量约束断言**：
  - `Inv-PS-1`（视图拆分最小增量）：本阶段新增视图**总数 ≤ 2**（`inventory_view`、`equipment_loadout_view`），character_creation / magic_system / physics_thermodynamics **必须**扩展现有视图 Tab；
  - `Inv-PS-2`（契约条目倍增）：4 个 PARTIAL 域的契约条目**必须**从 Phase 59 完工时的 DTO 单条扩展至至少 5 条（1 DTO + 2 EVENT + 2 COMMAND），覆盖 Phase 45-49 已建立的后端深层能力（FSM/DAG/solver）；
  - `Inv-PS-3`（既有视图降级为导航）：`character_progression` 拆分后**必须**保留入口但降级为"个人概览"，inventory/equipment_loadout 数据不再挤在此视图中；
  - `Inv-PS-4`（契约层单一路径）：所有 4 域新增契约**必须**经 Phase 57 建立的 `ContractRegistryIndex` + `ContractParser` 三算法消费，禁止在视图 `.gd` 中绕过契约层直接调用后端；
  - `Inv-PS-5`（character_creation 状态可见）：character_creation 的 10 层 FSM 状态**必须**通过 1 条 EVENT 契约暴露至 account_entry 视图，玩家可见每一步创角流程；
  - `Inv-PS-6`（magic_system 规则可编辑）：magic_system 的规则注册表**必须**通过 COMMAND 契约暴露至 grimoire_authoring 视图，玩家可查看/编辑魔法规则；
  - `Inv-PS-7`（contract_version 递增）：Phase 60 新增契约条目**必须**在 `contracts.json` 中从 61 条扩展至 61 + 16 = 77 条（4 域 × 4 类增量契约），`entry_count_expected` 同步更新。

* **防漂移最高指示**：
  - 本阶段**只**设计深层契约扩展方案与视图拆分数据模型，**不**触碰任何后端 `.gd` 实现；
  - 严禁将本阶段扩展为"character_creation FSM 重写"——FSM 沿用 Phase 48 S2 已闭环实现；
  - 严禁绕过 Phase 57 契约层直接建立"视图 → 后端"的直接调用通道。

---

## 一、 数据结构设计与代码契约 (Data Structures & Code Contracts)

### 1. 四域契约扩展方案（每域 4 条增量契约）

| 域 | 新增契约条目数 | 契约 ID 前缀 | 视图归属 | 覆盖的后端深层能力 |
| :---: | :---: | :---: | :---: | :--- |
| `character_creation` | 4 | `CT-CC-AE-*` | `account_entry` | 10 层 FSM 状态可见 / 属性初始化预览 / 传承基因选择 / 起始装备分发 |
| `magic_system` | 4 | `CT-MG-GR-*` | `grimoire_authoring` | 规则注册表 / 多施法器 / 封印 solver / 条件-触发器编辑 |
| `physics_thermodynamics` | 4 | `CT-PT-CV-*` | `combat_view` | 热力学 solver / 回合协调器 / timeline replay / 战术验证器 |
| `inventory` | 4 | `CT-IN-IV-*` | `inventory_view`（新） | 仓库 / 多槽位穿戴 / 属性评估 / 品质晋升 |

### 2. character_creation 深层契约（4 条）

```json
{
  "contract_id": "CT-CC-AE-02",
  "contract_version": 1,
  "domain_name": "character_creation",
  "view_name": "account_entry",
  "endpoint_kind": "EVENT",
  "event_name": "character_creation.fsm_state_changed",
  "signal_name": "_on_creation_state_changed",
  "event_filter": {"domain": "character_creation", "state_source": "CharacterCreationFSM"},
  "field_mapping": [
    {"backend_field": "current_state", "view_field": "progress_bar.current_state", "transform_kind": "enum_map"},
    {"backend_field": "progress_percentage", "view_field": "progress_bar.value", "transform_kind": "identity"},
    {"backend_field": "available_options", "view_field": "option_buttons.visible", "transform_kind": "identity"}
  ],
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}

{
  "contract_id": "CT-CC-AE-03",
  "contract_version": 1,
  "domain_name": "character_creation",
  "view_name": "account_entry",
  "endpoint_kind": "DTO",
  "source_path": "CharacterBaselineAttributeSolver.preview",
  "target_path": "account_entry_view.creation_panel.attribute_preview",
  "field_mapping": [
    {"backend_field": "base_hp", "view_field": "attr_hp.value", "transform_kind": "identity"},
    {"backend_field": "base_mp", "view_field": "attr_mp.value", "transform_kind": "identity"},
    {"backend_field": "base_atk", "view_field": "attr_atk.value", "transform_kind": "identity"},
    {"backend_field": "base_def", "view_field": "attr_def.value", "transform_kind": "identity"},
    {"backend_field": "inheritance_gene_preview", "view_field": "gene_preview.list", "transform_kind": "identity"}
  ],
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}

{
  "contract_id": "CT-CC-AE-04",
  "contract_version": 1,
  "domain_name": "character_creation",
  "view_name": "account_entry",
  "endpoint_kind": "COMMAND",
  "service_path": "CharacterCreationService",
  "method_name": "advance_fsm_state",
  "params_schema": [{"name": "target_state", "type": "String", "required": true, "rule": "VD_CC_FSM_STATE_ENUM"}],
  "response_dto": "CharacterCreationStateResultDTO",
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}

{
  "contract_id": "CT-CC-AE-05",
  "contract_version": 1,
  "domain_name": "character_creation",
  "view_name": "account_entry",
  "endpoint_kind": "COMMAND",
  "service_path": "CharacterCreationService",
  "method_name": "preview_baseline_attributes",
  "params_schema": [
    {"name": "race", "type": "String", "required": true, "rule": "VD_CC_RACE_ENUM"},
    {"name": "gender", "type": "String", "required": true, "rule": "VD_CC_GENDER_ENUM"},
    {"name": "role", "type": "String", "required": false, "rule": "VD_CC_ROLE_ENUM"}
  ],
  "response_dto": "CharacterBaselineAttributePreviewDTO",
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}
```

### 3. magic_system 深层契约（4 条）

```json
{
  "contract_id": "CT-MG-GR-01",
  "contract_version": 1,
  "domain_name": "magic_system",
  "view_name": "grimoire_authoring",
  "endpoint_kind": "DTO",
  "source_path": "MagicRuleRegistry.list_active_rules",
  "target_path": "grimoire_authoring_view.rule_registry_panel",
  "field_mapping": [
    {"backend_field": "rules", "view_field": "rule_list.items", "transform_kind": "identity"},
    {"backend_field": "rule_categories", "view_field": "category_filter.options", "transform_kind": "identity"},
    {"backend_field": "seal_count", "view_field": "seal_counter.value", "transform_kind": "identity"}
  ]
}

{
  "contract_id": "CT-MG-GR-02",
  "contract_version": 1,
  "domain_name": "magic_system",
  "view_name": "grimoire_authoring",
  "endpoint_kind": "EVENT",
  "event_name": "magic_system.rule_registered",
  "signal_name": "_on_rule_registered"
}

{
  "contract_id": "CT-MG-GR-03",
  "contract_version": 1,
  "domain_name": "magic_system",
  "view_name": "grimoire_authoring",
  "endpoint_kind": "COMMAND",
  "service_path": "MagicRuleRegistry",
  "method_name": "register_rule",
  "params_schema": [{"name": "rule_definition", "type": "Dictionary", "required": true, "rule": "VD_MG_RULE_SCHEMA"}]
}

{
  "contract_id": "CT-MG-GR-04",
  "contract_version": 1,
  "domain_name": "magic_system",
  "view_name": "grimoire_authoring",
  "endpoint_kind": "COMMAND",
  "service_path": "MagicRuleRegistry",
  "method_name": "modify_rule",
  "params_schema": [
    {"name": "rule_id", "type": "String", "required": true, "rule": "VD_MG_RULE_ID_FORMAT"},
    {"name": "patch", "type": "Dictionary", "required": true, "rule": "VD_MG_RULE_PATCH"}
  ]
}
```

### 4. physics_thermodynamics 深层契约（4 条）

```json
{
  "contract_id": "CT-PT-CV-01",
  "contract_version": 1,
  "domain_name": "physics_thermodynamics",
  "view_name": "combat_view",
  "endpoint_kind": "DTO",
  "source_path": "ThermodynamicSolver.tactical_snapshot",
  "target_path": "combat_view_view.tactical_panel",
  "field_mapping": [
    {"backend_field": "turn_order", "view_field": "turn_order_list.items", "transform_kind": "identity"},
    {"backend_field": "initiative_bonus", "view_field": "initiative_indicator.value", "transform_kind": "identity"},
    {"backend_field": "timeline_replay_state", "view_field": "replay_indicator.visible", "transform_kind": "cast", "transform_config": {"to": "bool"}}
  ]
}

{
  "contract_id": "CT-PT-CV-02",
  "contract_version": 1,
  "domain_name": "physics_thermodynamics",
  "view_name": "combat_view",
  "endpoint_kind": "EVENT",
  "event_name": "physics_thermodynamics.turn_advanced",
  "signal_name": "_on_turn_advanced"
}

{
  "contract_id": "CT-PT-CV-03",
  "contract_version": 1,
  "domain_name": "physics_thermodynamics",
  "view_name": "combat_view",
  "endpoint_kind": "COMMAND",
  "service_path": "CombatTimelineReplayAudit",
  "method_name": "get_replay_snapshot",
  "params_schema": [{"name": "replay_id", "type": "String", "required": true, "rule": "VD_PT_REPLAY_ID_FORMAT"}]
}

{
  "contract_id": "CT-PT-CV-04",
  "contract_version": 1,
  "domain_name": "physics_thermodynamics",
  "view_name": "combat_view",
  "endpoint_kind": "COMMAND",
  "service_path": "TacticalValidator",
  "method_name": "validate_move",
  "params_schema": [{"name": "move_definition", "type": "Dictionary", "required": true, "rule": "VD_PT_MOVE_SCHEMA"}]
}
```

### 5. inventory 独立视图契约（4 条）

```gdscript
# 契约：inventory_view 新视图 class_name 与视图职责。
# 位置：frontend/views/inventory_view.gd（Phase 60 S2 阶段落地）

class_name InventoryView

# 视图职责：账号背包、仓库、多槽位穿戴、属性评估、品质晋升
# 主要数据源：inventory 域（4 条契约）
# 视图类型：独立视图（从 character_progression 拆出）

static func get_route_name() -> String:
	return "inventory"

static func get_display_name() -> String:
	return "背包与仓库"
```

```json
{
  "contract_id": "CT-IN-IV-01",
  "contract_version": 1,
  "domain_name": "inventory",
  "view_name": "inventory_view",
  "endpoint_kind": "DTO",
  "source_path": "InventoryService.get_backpack_snapshot",
  "target_path": "inventory_view.backpack_panel",
  "field_mapping": [
    {"backend_field": "items", "view_field": "item_grid.items", "transform_kind": "identity"},
    {"backend_field": "capacity_used", "view_field": "capacity_bar.used", "transform_kind": "identity"},
    {"backend_field": "capacity_total", "view_field": "capacity_bar.total", "transform_kind": "identity"}
  ]
}

{
  "contract_id": "CT-IN-IV-02",
  "contract_version": 1,
  "domain_name": "inventory",
  "view_name": "inventory_view",
  "endpoint_kind": "EVENT",
  "event_name": "inventory.backpack_updated",
  "signal_name": "_on_backpack_updated"
}

{
  "contract_id": "CT-IN-IV-03",
  "contract_version": 1,
  "domain_name": "inventory",
  "view_name": "inventory_view",
  "endpoint_kind": "COMMAND",
  "service_path": "InventoryService",
  "method_name": "move_item",
  "params_schema": [
    {"name": "item_id", "type": "String", "required": true, "rule": "VD_IN_ITEM_ID_FORMAT"},
    {"name": "target_slot", "type": "String", "required": true, "rule": "VD_IN_SLOT_ENUM"},
    {"name": "target_container", "type": "String", "required": true, "rule": "VD_IN_CONTAINER_ENUM"}
  ]
}

{
  "contract_id": "CT-IN-IV-04",
  "contract_version": 1,
  "domain_name": "inventory",
  "view_name": "inventory_view",
  "endpoint_kind": "COMMAND",
  "service_path": "ItemAttributeAppraisalSolver",
  "method_name": "appraise_item",
  "params_schema": [{"name": "item_id", "type": "String", "required": true, "rule": "VD_IN_ITEM_ID_FORMAT"}]
}
```

### 6. equipment_loadout 独立视图契约（0 条增量，仅拆分归属）

> **注**：`equipment_loadout` 现有 3 条契约条目（Phase 57 S1 首批 DTO 占位 + 后续扩展），本阶段仅将其视图归属从 `character_progression` 迁移至新的 `equipment_loadout_view`（Phase 60 处理）。Phase 60 不涉及 equipment_loadout 契约条目的内容修改，仅登记其在 Phase 60 的迁移计划。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 1.1**：在 `backend/config/contracts.json` 中追加 16 条契约条目（character_creation 4 + magic_system 4 + physics_thermodynamics 4 + inventory 4）；
- [ ] **Step 1.2**：更新 `contracts.json` 顶部 `entry_count_expected` 字段从 `61` 更新为 `77`（Inv-PS-7）；
- [ ] **Step 1.3**：为 16 条新契约条目全部标记 `contract_version: 1`、`infra_exempt: false`、`deprecated: false`、`created_at: "2026-09-05"`、`last_reviewed_at: "2026-09-05"`；
- [ ] **Step 1.4**：新增 8 条 VD_* 值域规则（承接 Phase 55 S3 范式）：`VD_CC_FSM_STATE_ENUM`、`VD_CC_RACE_ENUM`、`VD_CC_GENDER_ENUM`、`VD_CC_ROLE_ENUM`、`VD_MG_RULE_SCHEMA`、`VD_MG_RULE_ID_FORMAT`、`VD_PT_REPLAY_ID_FORMAT`、`VD_PT_MOVE_SCHEMA`；
- [ ] **Step 1.5**：更新 `backend/config/contract_completeness_baseline.json` 的 `by_status.PARTIAL` 数组——移除 character_creation / magic_system / physics_thermodynamics / inventory 4 项（转为 FULL）；
- [ ] **Step 1.6**：更新 baseline 的 `by_status.FULL` 数组——追加 4 项（4 个 PARTIAL 域补齐后转为 FULL）；
- [ ] **Step 1.7**：更新 baseline 的 `total_entries` 字段从 61 更新为 77；
- [ ] **Step 1.8**：为 inventory_view 新视图预留 `frontend/views/inventory_view.gd` + `.tscn` 空文件（Phase 60 S2 阶段填充）；
- [ ] **Step 1.9**：更新 `frontend/views/view_router.gd` 的 18 视图注册数组，追加 `inventory_view` 为第 19 个视图（Inv-PS-1 新视图上限）；
- [ ] **Step 1.10**：本阶段**禁止**：修改任何 `backend/domains/{character_creation,magic_system,physics_thermodynamics,inventory}/**` 文件；本阶段新增文件**不超过 3 个**（1 个新视图 `.gd` 空骨架 + 1 个新视图 `.tscn` 空骨架 + 1 个新视图 `.gd` 路由声明）；
- [ ] **Step 1.11**：本阶段新增断言数**控制在 10~14 条**，命名前缀统一 `TC-PS-S1-NN`；
- [ ] **Step 1.12**：4 个域的契约条目**必须**在 `contracts.json` 中按域名字母序排列；
- [ ] **Step 1.13**：character_creation 的 FSM 状态可见（Inv-PS-5）——`CT-CC-AE-02` 事件契约的 `event_filter` 必须明确指向 `CharacterCreationFSM` 状态源；
- [ ] **Step 1.14**：magic_system 的规则可编辑（Inv-PS-6）——`CT-MG-GR-03` 与 `CT-MG-GR-04` 命令契约必须暴露 register_rule/modify_rule 两个 API；
- [ ] **Step 1.15**：所有新契约的 `field_mapping` 中 `transform_kind` 值**必须**在 Phase 57 S2 已实现的转换规则内（identity/cast/format/enum_map/aggregate/filter）。

---

## 三、 数据结构验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :---: | :--- | :--- | :---: |
| `TC-PS-S1-01` | 16 条新契约条目注册成功 | 加载更新后的 `contracts.json` | `ContractRegistryIndex.find_all().size() == 77` |
| `TC-PS-S1-02` | character_creation 契约扩展 | `find_by_domain("character_creation")` | 返回条目数 ≥ 5（Phase 57 S1 1 条 DTO 占位 + Phase 60 S1 4 条增量） |
| `TC-PS-S1-03` | magic_system 契约扩展 | `find_by_domain("magic_system")` | 返回条目数 ≥ 5 |
| `TC-PS-S1-04` | physics_thermodynamics 契约扩展 | `find_by_domain("physics_thermodynamics")` | 返回条目数 ≥ 5 |
| `TC-PS-S1-05` | inventory 契约扩展 | `find_by_domain("inventory")` | 返回条目数 ≥ 5 |
| `TC-PS-S1-06` | character_creation FSM 状态可见（Inv-PS-5） | `CT-CC-AE-02` 事件契约 | `event_filter.state_source == "CharacterCreationFSM"` |
| `TC-PS-S1-07` | magic_system 规则可编辑（Inv-PS-6） | `CT-MG-GR-03` 与 `CT-MG-GR-04` 命令 | `method_name in ["register_rule", "modify_rule"]` |
| `TC-PS-S1-08` | baseline PARTIAL 移除 4 项 | 加载更新后的 baseline | `baseline.by_status.PARTIAL` 不含 4 个新域 |
| `TC-PS-S1-09` | baseline FULL 追加 4 项 | 加载更新后的 baseline | `baseline.by_status.FULL` 含 4 个新域 |
| `TC-PS-S1-10` | VD_* 新增 8 条规则 | `audit_config.gd` 中 VD_RULES 数量 | 数量 = 15（Phase 59 S3 后）+ 8（Phase 60 S1 新增）= 23 |
| `TC-PS-S1-11` | entry_count_expected 与实际一致 | 加载 contracts.json | `entry_count_expected == 77` 且 `entries.size() == 77` |
| `TC-PS-S1-12` | inventory_view 新视图注册 | `view_router.gd` 视图数组 | 数组长度从 18 增至 19，包含 `inventory_view` |
| `TC-PS-S1-13` | character_progression 降级为概览（Inv-PS-3） | `character_progression.gd` | inventory/equipment_loadout 相关字段声明被移除或迁移 |
| `TC-PS-S1-14` | 后端零修改 | 扫描 4 个 PARTIAL 域 `.gd` mtime | 4 个域 `.gd` 文件修改时间早于 Phase 60 施工开始时间 |

---

## 附：第 2 轮实施收敛登记

> 【待第 2 轮细则收敛时填写】
> - 实际新增文件清单：
> - 16 条契约条目最终落库确认：
> - 8 条 VD_* 规则上线结果：
> - baseline 快照 Phase 60 冻结点登记（PARTIAL-4 / FULL+4 / total=77）：
> - character_creation FSM 状态可见确认：
> - magic_system 规则可编辑确认：
> - 断言落地数 vs 计划数（10~14）的偏差：
> - 未闭环事项登记（如有）：
