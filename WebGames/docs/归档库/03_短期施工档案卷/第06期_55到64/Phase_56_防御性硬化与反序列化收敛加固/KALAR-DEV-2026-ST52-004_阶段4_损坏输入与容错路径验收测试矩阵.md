---
档号: KALAR-DEV-2026-ST52-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST52 (Phase_56_防御性硬化与反序列化收敛加固)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_56_防御性硬化与反序列化收敛加固 —— 阶段4：损坏输入与容错路径验收测试矩阵
形成日期: 2026-09-04
归档日期: 2026-09-06（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 扩展测试套件登记; 核心注入用例设计; domains.json
---

# 施工细则：阶段4_损坏输入与容错路径验收测试矩阵

> 施工开始日期: 2026-09-04 下午
> 责任人: 卡拉尔世界引擎架构组
> 状态: ✅ 已闭环（第2轮施工已完成并验证闭环）

> [!NOTE]
> **【施工目标】**：为 Phase 56 修复（L2 缺库存拒绝 / L4 递归环检测 / L5 设置类型守卫 / L6 备份预检与版本闸门 / L7 栈幂等 / L8 副本与缓存有界 + LCG 固化）建立损坏输入注入式验收矩阵，并完成全域测试与门禁闭环约束登记。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST52-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST52-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：全域质量与边界专项审查报告（2026-09-04）→ L2 / L4 / L5 / L6 / L7 / L8；Phase 56 阶段1~3
> **验收闭环最高指示**：每条守卫先以损坏输入注入证明测试可抓（红），再验证容错生效（绿）；覆盖「不崩溃 + 正档原样 + 现场可审计」三要素，杜绝白板断言
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST52-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST52-001_阶段1_入参契约与恢复容错数据模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST52-002_阶段2_空态拒绝与递归环检测栈帧幂等算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST52-003_阶段3_引用缓存面与反序列化守卫工程化.md) ｜ **阶段4 (当前)**

---

## 一、 自动化测试套件与验收代码 (Test Suite & Verification)

### 1. 新增/扩展测试套件登记

| 套件（tests/unit/） | 覆盖缺陷 | 建议落点 |
| :--- | :--- | :--- |
| `test_spatial_merchant.gd`（扩展） | L2 | `_test_transaction_missing_inventory_rejected()` 等 2 例 |
| `test_ground_loot.gd`（扩展） | L2 | `_test_pickup_missing_inventory_keeps_loot()` 等 2 例 |
| `test_notification_red_dot.gd`（扩展） | L4 | `_test_upwards_cycle_terminates()` 等 3 例 |
| `test_game_settings.gd`（扩展） | L5 | `_test_corrupt_section_type_falls_back()` 等 3 例 |
| `test_persistence_protocol.gd`（扩展） | L6 | `_test_restore_backup_rejects_corrupt()` / `_test_load_version_gate()` 等 5 例 |
| `test_game_loop_fsm_pipeline.gd`（扩展） | L7 | `_test_same_state_push_idempotent()` 等 3 例 |
| `test_configuration_guard.gd`（扩展） | L8 | `_test_container_return_is_copy()` / `_test_path_cache_bounded()` 等 3 例 |
| `test_deterministic_sandbox.gd`（扩展） | L8(LCG) | `_test_lcg_params_snapshot_on_seed()` 等 2 例 |

> 挂载以 `tests/test_registry.gd` 实际注册为准；各注入用例并入既有域套件。

### 2. 核心注入用例设计（代表例）

```gdscript
# L6 代表：损坏备份恢复回归（修复前红：可 open 的截断 .bak 覆盖正档）
# 注入：写坏/截断 backup 文件（非法 JSON）
# 动作：restore_backup(...)
# 断言：返回错误、正档保持原样、磁盘文件未被替换

# L6 版本闸门：构造 format_version = 当前+1 的信封
# 动作：load_game
# 断言（修复前红）：返回 VERSION_TOO_NEW、不 verified=true 直通

# L8 副本代表：get_dict 返回后就地修改（修复前红：污染配置源表）
# 动作：var d = GameConfig.get_dict("domains.x", "k", {}); d["hack"] = true; 再读同一键
# 断言：源表无 "hack" 键

# L8 LCG 固化：rng.set_seed(20240917) 后热重载 lcg/multiplier 改动
# 动作：继续取序列 10 项
# 断言（修复前红）：序列与改动前逐项一致（固化生效）

# L4 代表：parent_path 成环（A→B→A）
# 动作：recalculate_node_upwards(registry, "A")
# 断言：有限步返回 + warning、无栈溢出

# L2 代表：inventory=null 交易（修复前红：扣款减货架返回 success）
# 断言：MISSING_INVENTORY_CONTEXT、钱包零变动、货架零变动
```

### 3. 边界/容错用例

- 版本恰等于当前/最小支持版本 → 正常加载（闸门不误杀）；
- LCG 固化后合法 `reseed_global`（新实例新 seed）仍按新参数初始化（固化不锁死重设）；
- 副本路径对热重载后新值读取正常（读时拷贝不缓存旧值）；
- 红点正常多级树无环回归（计数与改造前一致）。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 4.1**: 各套件按上表追加注入用例（先红后绿留证）
- [x] **Step 4.2**: 注册表登记（test_registry.gd / domains.json 如适用）
- [x] **Step 4.3**: `bash scripts/sh/test-run.sh` 全域 100% PASS（断言总数 ≥ 现状）
- [x] **Step 4.4**: `bash scripts/sh/audit-all.sh --json` 18 项全绿、基线 0 新增；`audit-docs.sh --baseline` 0 新增

---

## 三、 全量工程验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-DF-S4-01` | 缺库存拒绝族 | 交易/拾取 inventory=null | 错误码、零资产变动、地面物品原状 |
| `TC-DF-S4-02` | 递归环终止 | 自指/互指 parent | 有限步 + warning、无栈溢出 |
| `TC-DF-S4-03` | 设置损坏容错 | 子键坏类型 | 安全默认 + warning、无崩溃 |
| `TC-DF-S4-04` | 备份预检 | 截断/坏 JSON .bak | 拒绝恢复、正档原样 |
| `TC-DF-S4-05` | 版本闸门 | 未来/远古版本信封 | VERSION_TOO_NEW/OLD、边界版本正常载入 |
| `TC-DF-S4-06` | 栈幂等 | 同状态双 push（带 context） | 栈深不变、context 刷新 |
| `TC-DF-S4-07` | 容器返回副本 | 就地修改回归 | 配置源表零污染 |
| `TC-DF-S4-08` | 缓存有界 | 动态键打满 | 重建后功能正常 |
| `TC-DF-S4-09` | LCG 固化 | seed 后热重载 lcg | 序列不变 |
| `TC-DF-S4-10` | 全域回归 + 门禁 | test-run + audit-all | 100% PASS、18 项全绿、0 error |

---

## 附录：实施收敛登记（第2轮闭环，2026-09-04）

- TC-DF-S4-01~10 验收用例均已合流至 test_persistence_protocol.gd、test_notification_red_dot.gd、test_game_settings.gd、test_game_loop_fsm_pipeline.gd 等套件；
- 全域回归与 18 项门禁全绿，0 新增技术债务，实现与文档 100% 闭环。
