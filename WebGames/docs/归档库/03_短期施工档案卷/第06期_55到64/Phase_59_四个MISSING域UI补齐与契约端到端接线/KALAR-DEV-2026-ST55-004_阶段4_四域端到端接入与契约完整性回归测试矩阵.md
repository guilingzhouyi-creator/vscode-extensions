---
档号: KALAR-DEV-2026-ST55-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST55 (Phase_59_四个MISSING域UI补齐与契约端到端接线)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_59_四个MISSING域UI补齐与契约端到端接线 —— 阶段4：四域端到端接入与契约完整性回归测试矩阵
形成日期: 2026-09-05
归档日期: 2026-09-06（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: TestMissingDomainsWiringPipeline; Phase; 端到端断言矩阵; 完工判据汇总表; contract_completeness_baseline.json
---

# 施工细则：阶段4_四域端到端接入与契约完整性回归测试矩阵

> 施工开始日期：2026-09-05 下午
> 责任人：卡拉尔世界引擎架构组
> 状态：📝 待获批（第1轮细则已编制，待批准后进入实现）

> [!NOTE]
> **【施工目标】**：为 Phase 59 建立专属测试套件 `TestMissingDomainsWiringPipeline`，覆盖 4 个 MISSING 域（world_gateway/spatial_movement/lifecycle_physiology/item_statistics）的契约端到端接入、视图字段填充、事件订阅、命令分发、契约错误降级五大维度；建立"契约注册表 → 契约解析器 → 视图接线 → 事件订阅 → 命令分发 → 后端 Service"全链路端到端断言；建立 Phase 59 完工判据——全域断言总数从 Phase 57 的 496 条扩展至 520 条以上，19 项工程门禁全绿，反向孤儿完全消除，契约完整性基线 Phase 59 冻结点闭合。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST55-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST55-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：[docs/audit/AUDIT_REPORT_UI缺失.md](../../../../audit/AUDIT_REPORT_UI缺失.md)（2026-09-05）→ 项 P0-1 收口测试 + 与 Phase 57 契约层基础设施的端到端集成验收。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST55-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST55-001_阶段1_四域视图入口与契约端点数据模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST55-002_阶段2_契约驱动视图接线与事件订阅算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST55-003_阶段3_新视图工程化与4域契约接入既有基础设施.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游证据指针**：
  - `docs/audit/AUDIT_REPORT_UI缺失.md:118-117`：三份审计共同指向"契约层缺失"，本阶段以 4 域端到端测试证明"契约层已闭环"；
  - `backend/tests/domains/`：Phase 45-56 已建立的域级测试套件目录，本阶段追加 `TestMissingDomainsWiringPipeline`；
  - `frontend/tests/`：若尚未建立，本阶段建立 `frontend/tests/views/` 目录（视图层端到端测试）；
  - Phase 57 S4 `TestContractRegistryPipeline` 的 `TC-CT-S4-NN` 命名范式：本阶段沿用 `TC-MI-S4-NN` 命名范式。

* **核心不变量约束断言**：
  - `Inv-MJ4-1`（4 域端到端独立可测）：`TestMissingDomainsWiringPipeline` 必须**独立可运行**，不依赖其他 45 个业务域的运行时状态；
  - `Inv-MJ4-2`（Mock 数据仅测试夹具）：Phase 59 完工后 4 域的 Mock 数据**必须**从视图运行时完全移除，仅保留为契约测试夹具；
  - `Inv-MJ4-3`（契约层错误端到端覆盖）：所有 `ContractParserErrorCode` 枚举值**必须**在端到端测试中被触发验证（5 个错误码全覆盖）；
  - `Inv-MJ4-4`（19 项门禁全绿完工判据）：Phase 59 完工时 19 项工程门禁**必须全部 PASS**，全域断言总数 ≥ 520 条 100% PASS；
  - `Inv-MJ4-5`（Phase 51-56 断言不动）：Phase 59 只**追加**断言，**严禁**修改或删除 Phase 51-56 已闭环的 450 条基线断言；
  - `Inv-MJ4-6`（Phase 57 契约层回归）：Phase 59 完工时必须回归 Phase 57 的 46 条断言（S1 12 + S2 14 + S3 8 + S4 12），全部保持通过。

* **防漂移最高指示**：
  - 本阶段**只**建立测试套件与断言矩阵，**不**修改 Phase 59 S1/S2/S3 的实现代码；
  - 断言数量严格受控——Phase 59 全域新增断言 ≤ 44 条（S1 12 + S2 14 + S3 12 + S4 6 = 44 条，S4 端到端只承担整合验证）；
  - 断言失败时**禁止**通过修改生产代码来"绿化"，只能在附录登记合理例外。

---

## 一、 数据结构设计与代码契约 (Data Structures & Code Contracts)

### 1. TestMissingDomainsWiringPipeline（专属测试套件）

```gdscript
# 契约：4 域端到端测试套件。挂载到 Domain 26（契约域，Phase 57 已建立）与 Domain 46（前端视图域）。
# 位置：backend/tests/domains/test_missing_domains_wiring_pipeline.gd

class_name TestMissingDomainsWiringPipeline

# ---- 测试数据构造 ----
static func _construct_world_gateway_context() -> Dictionary:
	return {
		"available_ports": ["PORT_MAINLAND", "PORT_EAST"],
		"current_mode": "SINGLEPLAYER",
		"canary_flags": {"multiplayer": false, "multiworld": false}
	}

static func _construct_spatial_movement_snapshot() -> Dictionary:
	return {
		"flash_teleport_cd": 3.5,
		"blink_move_cd": 0.0,
		"space_jump_ready": true
	}

static func _construct_lifecycle_physiology_snapshot() -> Dictionary:
	return {
		"current_age": 25,
		"vitality_curve": [100, 95, 90, 85, 80],
		"aging_stage": "YOUTH",
		"physiological_states": ["HUNGER", "HEALTH"]
	}

static func _construct_item_statistics_snapshot() -> Dictionary:
	return {
		"total_items": 128,
		"rarity_distribution": {"common": 80, "uncommon": 30, "rare": 10, "epic": 5, "legendary": 3},
		"collected_catalog": [
			{"item_id": "ITEM_001", "rarity": "common"},
			{"item_id": "ITEM_002", "rarity": "rare"}
		]
	}

# ---- 端到端测试用例族 ----
# 4 域端到端（每域 1 条）：
static func test_e2e_world_gateway_full_flow() -> void         # TC-MI-S4-01
static func test_e2e_spatial_movement_full_flow() -> void      # TC-MI-S4-02
static func test_e2e_lifecycle_physiology_full_flow() -> void  # TC-MI-S4-03
static func test_e2e_item_statistics_full_flow() -> void       # TC-MI-S4-04

# 契约错误端到端覆盖（Inv-MJ4-3）：
static func test_all_contract_error_codes_covered() -> void    # TC-MI-S4-05

# 完工判据：
static func test_phase58_completion_criteria() -> void         # TC-MI-S4-06
```

### 2. 端到端断言矩阵（S4 独享，6 条）

| 断言 ID | 校验目标 | 关键输入 | 断言表达式 |
|---|---|---|---|
| TC-MI-S4-01 | world_gateway 端到端 | context + event + command | DTO 填充 → EVENT 订阅 → COMMAND 分发，三类契约均生效 |
| TC-MI-S4-02 | spatial_movement 端到端 | snapshot + cd_updated event + activate command | 3 个按钮触发 3 次命令，参数分别通过 VD_SM_MOVEMENT_ENUM 校验 |
| TC-MI-S4-03 | lifecycle_physiology 端到端 | snapshot + aging_progressed event + state command | DTO 填充 → EVENT 订阅 → COMMAND 分发 |
| TC-MI-S4-04 | item_statistics 端到端 | snapshot + catalog_updated event + refresh command | collection_view 3 类契约全激活 |
| TC-MI-S4-05 | 5 个 ContractParserErrorCode 全覆盖 | 依次注入 5 类错误 | 每种错误码在 `_handle_contract_error` 中被识别并触发对应 log level |
| TC-MI-S4-06 | Phase 59 完工判据 | 全域扫描 | 断言总数 ≥ 520 条 100% PASS、19 项门禁全绿、reverse_orphans == 0、MISSING == 0 |

### 3. 完工判据汇总表（Phase 59 收口）

| 维度 | Phase 57 完工状态 | Phase 59 完工目标 | 差异 |
|---|---|---|---|
| 全域断言总数 | 496 条 | ≥ 520 条 | +24 条 |
| 工程门禁数 | 19 项 | 19 项（含 19-A~19-E） | 0 |
| 契约条目总数 | 49 条 | 61 条 | +12 条 |
| MISSING 域数量 | 4 | 0 | -4 |
| reverse_orphans 数量 | 1 (world_map) | 0 | -1 |
| 视图总数 | 17 | 18 | +1 |
| VD_* 规则族 | 10 条 | 15 条 | +5 条 |
| Mock 数据驱动视图数 | 17/17 | 13/18（4 域已接线） | -4 域 |

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1**：在 `backend/tests/domains/` 下创建 `test_missing_domains_wiring_pipeline.gd`，实现 §1.1 的 `TestMissingDomainsWiringPipeline` 类；
- [ ] **Step 4.2**：在 `frontend/tests/views/` 下创建目录（若不存在），为视图层端到端测试提供挂载点；
- [ ] **Step 4.3**：实现 6 条端到端断言（TC-MI-S4-01 至 TC-MI-S4-06），命名与 §1.2 矩阵严格一致；
- [ ] **Step 4.4**：所有测试用例**必须**通过注入构造触发（Inv-MJ4-1），禁止依赖任何运行时状态或全局单例的隐式状态；
- [ ] **Step 4.5**：4 个域的端到端断言**必须**依次验证 DTO/EVENT/COMMAND 三类契约，缺一不可（Inv-MJ4-1）；
- [ ] **Step 4.6**：建立"契约错误端到端触发工厂" `TestContractErrorFactory.gd`，提供 5 类错误触发方法（对应 `ContractParserErrorCode` 5 个枚举值）供 TC-MI-S4-05 消费；
- [ ] **Step 4.7**：执行 Phase 59 完工断言总数校验——S1 12 条 + S2 14 条 + S3 12 条 + S4 6 条 = **44 条**，加上 Phase 57 已闭环的 496 条基线，Phase 59 完工时全域断言总数 = **540 条**（≥ 520 达标）；
- [ ] **Step 4.8**：执行 19 项工程门禁全量回归——前 19 项必须全部 PASS，含第 19 项契约完整性门禁的 5 个子项（19-A~19-E）；
- [ ] **Step 4.9**：Phase 59 完工时**必须**在 `backend/config/contract_completeness_baseline.json` 中登记"Phase 59 快照"，作为 Phase 59/60 消费时的对比基线；
- [ ] **Step 4.10**：Phase 57 契约层 46 条断言回归（Inv-MJ4-6）——`TestContractRegistryPipeline` 全部保持通过；
- [ ] **Step 4.11**：Phase 51-56 已闭环的 450 条断言回归（Inv-MJ4-5）——全部保持通过，无回归；
- [ ] **Step 4.12**：反向孤儿完全消除验收（Inv-MK-6 收口）——`guard.audit_full_registry().reverse_orphans.size() == 0`；
- [ ] **Step 4.13**：Mock 数据完全移除验收（Inv-MJ4-2）——4 个新接线视图的 `_ready` 中无 Mock 数据消费；
- [ ] **Step 4.14**：本阶段**禁止**修改任何 Phase 59 S1/S2/S3 的实现代码；如发现实现层 bug，仅在附录登记并回退到 S1/S2/S3 修复；
- [ ] **Step 4.15**：Phase 59 完工后，`docs/路线图/路线图总索引.md` 中 Phase 59 条目状态字段更新为「✅ 已施工完毕」并附断言总数与门禁状态摘要。

---

## 三、 数据结构验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :---: | :--- | :--- | :---: |
| `TC-MI-S4-01` | world_gateway 端到端 | context + event + command | 3 类契约均生效，view 字段填充无 Mock 残留 |
| `TC-MI-S4-02` | spatial_movement 端到端 | snapshot + event + command | 3 个按钮触发 3 次命令，参数校验通过 |
| `TC-MI-S4-03` | lifecycle_physiology 端到端 | snapshot + event + command | 3 类契约均生效 |
| `TC-MI-S4-04` | item_statistics 端到端 | snapshot + event + command | collection_view 3 类契约全激活 |
| `TC-MI-S4-05` | 5 个错误码全覆盖（Inv-MJ4-3） | 依次注入 5 类错误 | 全部被 `_handle_contract_error` 识别并降级 |
| `TC-MI-S4-06` | Phase 59 完工判据 | 全域扫描 | 断言 ≥ 520、19 项门禁全绿、reverse_orphans == 0、MISSING == 0 |

---

## 附：第 2 轮实施收敛登记

> 【待第 2 轮细则收敛时填写】
> - 实际新增文件清单：
> - 断言总数与计划（44）的偏差：
> - Phase 57 契约层 46 条断言回归结果：
> - Phase 51-56 已闭环 450 条断言回归结果：
> - 19 项工程门禁全绿确认：
> - 4 域端到端断言全绿确认：
> - 反向孤儿完全消除确认（reverse_orphans == 0）：
> - Mock 数据完全移除确认：
> - baseline Phase 59 冻结点登记：
> - 断言落地数 vs 计划数（6）的偏差：
> - 未闭环事项登记（如有）：
