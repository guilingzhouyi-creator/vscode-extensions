---
档号: KALAR-DEV-2026-ST55-001
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST55 (Phase_59_四个MISSING域UI补齐与契约端到端接线)
件号: 001
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_59_四个MISSING域UI补齐与契约端到端接线 —— 阶段1：四域视图入口与契约端点数据模型设计
形成日期: 2026-09-05
归档日期: 2026-09-06（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: CollectionView; 四域视图入口方案; world_gateway 契约端点; spatial_movement 契约端点; contracts.json
---

# 施工细则：阶段1_四域视图入口与契约端点数据模型设计

> 施工开始日期：2026-09-05 下午
> 责任人：卡拉尔世界引擎架构组
> 状态：📝 待获批（第1轮细则已编制，待批准后进入实现）

> [!NOTE]
> **【施工目标】**：为审计报告识别的 4 个 MISSING 域——`world_gateway`（7 文件 / 11 静态方法）、`spatial_movement`（4 文件 / 3 静态方法）、`lifecycle_physiology`（3 文件 / 11 静态方法）、`item_statistics`（2 文件 / 8 静态方法）——设计视图入口方案与契约端点数据模型；每个域必须补齐 DTO / EVENT / COMMAND 三类契约条目，使 Phase 57 S1 建立的契约完整性守卫从 MISSING 收敛到 FULL/PARTIAL；设计原则：优先扩展现有视图的 Tab（world_map Tab 承接 world_gateway、main_hud 承接 spatial_movement、character_progression 承接 lifecycle_physiology、新建 collection_view 承接 item_statistics），避免过度新增视图。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST55-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST55-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：[docs/audit/AUDIT_REPORT_UI缺失.md](../../../../audit/AUDIT_REPORT_UI缺失.md)（2026-09-05 前后端契约审计）→ 项 P0-1「补齐 4 个 MISSING 域」+ Phase 57 建立的契约注册表基础设施。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST55-ATT_附件_案卷共享契约与上下文.md) ｜ **阶段1 (当前)** ｜ [阶段2](KALAR-DEV-2026-ST55-002_阶段2_契约驱动视图接线与事件订阅算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST55-003_阶段3_新视图工程化与4域契约接入既有基础设施.md) ｜ [阶段4](KALAR-DEV-2026-ST55-004_阶段4_四域端到端接入与契约完整性回归测试矩阵.md)

---

## 📌 第一性原理溯源指针

* **精准上游证据指针**：
  - `docs/audit/AUDIT_REPORT_UI缺失.md:28-36`：4 个 MISSING 域的规模与用户面能力清单；
  - `docs/audit/AUDIT_REPORT_UI缺失.md:37`：world_map 视图无对应后端域的反向孤儿异常；
  - `docs/audit/AUDIT_REPORT_UI缺失.md:123-124`：P0-1 补齐 4 MISSING 域修复优先级；
  - `backend/config/contracts.json`（Phase 57 S1 建立）：本阶段为其补齐 4 个 MISSING 域的完整三类契约条目；
  - `backend/domains/world_gateway/`：Phase 47 S1-S4 已闭环的后端实现，本阶段**只接线不改后端**。

* **核心不变量约束断言**：
  - `Inv-MI-1`（视图入口最小增量）：本阶段新增视图**总数 ≤ 1**（仅 `collection_view` 新建），其余 3 域**必须**扩展现有视图的 Tab；
  - `Inv-MI-2`（契约完整性）：4 个 MISSING 域补齐后，每个域**必须**至少登记 1 条 DTO 契约 + 1 条 EVENT 契约 + 1 条 COMMAND 契约（承接 Phase 57 S1 Inv-CT-2 契约完整性不变量）；
  - `Inv-MI-3`（反向孤儿收敛）：`world_gateway` 契约的视图目标**必须**是 `world_map`，收敛 Phase 57 S2 已登记的 `world_map` reverse_orphan（即"world_map 视图无对应域"）——补齐后世界栏网关接入 world_map，反向孤儿消除；
  - `Inv-MI-4`（后端零修改）：本阶段**严禁**修改 `backend/domains/{world_gateway,spatial_movement,lifecycle_physiology,item_statistics}/**` 任何后端 `.gd` 文件；接线层全部通过契约端点定义；
  - `Inv-MI-5`（契约版本递增）：4 个域新增契约条目的 `contract_version` 均设为 `1`（首次登记），但**必须**在 `contracts.json` 的 `entry_count_expected` 字段中从 49 更新为 49 + 12 = 61 条（4 域 × 3 类契约）；
  - `Inv-MI-6`（视图 Tab 命名一致）：新增 Tab 命名**必须**遵循既有视图的 `snake_case` 命名范式，且**必须**在 `frontend/views/**/*.tscn` 的枚举中登记。

* **防漂移最高指示**：
  - 本阶段**只**设计视图入口方案 + 契约端点数据模型，**不**触碰任何 `.gd` 实现代码，也**不**修改任何 `.tscn` 场景树结构；
  - 4 个 MISSING 域的接线**必须**通过 Phase 57 S2 建立的 `ContractParser` 三算法消费，禁止绕过契约层直接调用后端 Service；
  - 严禁将本阶段扩展为"契约层实现改造"——那是 Phase 57 已闭环的任务。

---

## 一、 数据结构设计与代码契约 (Data Structures & Code Contracts)

### 1. 四域视图入口方案

| 域 | 现有视图 | 入口方式 | 新增/扩展 Tab 名 | 视图规模增量 |
| :---: | :---: | :---: | :---: | :---: |
| `world_gateway` | `world_map` | 扩展 Tab | `travel_gateway` | 1 Tab |
| `spatial_movement` | `main_hud` | 扩展快捷动作 | `quick_action_spatial` | 1 快捷动作按钮 |
| `lifecycle_physiology` | `character_progression` | 扩展 Tab | `physiology_life` | 1 Tab |
| `item_statistics` | 新建 `collection_view` | 新建视图 | — | 1 视图 |

### 2. world_gateway 契约端点（3 条）

```gdscript
# 契约：world_gateway → world_map.travel_gateway Tab 契约端点集合。
# 后端源：Phase 47 S1-S4 已闭环的 WorldGatewayFSM / GameModeRoutingSolver。
# 位置：backend/config/contracts.json 追加 3 条 entries。

# CT-GW-WM-01 (DTO)：世界栏网关上下文 → 传送门列表
{
  "contract_id": "CT-GW-WM-01",
  "contract_version": 1,
  "domain_name": "world_gateway",
  "view_name": "world_map",
  "endpoint_kind": "DTO",
  "source_path": "WorldGatewayFSM.gateway_context",
  "target_path": "world_map_view.travel_gateway_tab.portal_list",
  "field_mapping": [
    {"backend_field": "available_ports", "view_field": "portal_grid.items", "transform_kind": "identity"},
    {"backend_field": "current_mode", "view_field": "mode_indicator.text", "transform_kind": "enum_map", "transform_config": {"map": {"SINGLEPLAYER":"单机","MULTIPLAYER":"联机"}}},
    {"backend_field": "canary_flags", "view_field": "hidden_features.hidden", "transform_kind": "filter", "transform_config": {"keep": false}}
  ],
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}

# CT-GW-WM-02 (EVENT)：世界栏模式切换事件 → 视图信号
{
  "contract_id": "CT-GW-WM-02",
  "contract_version": 1,
  "domain_name": "world_gateway",
  "view_name": "world_map",
  "endpoint_kind": "EVENT",
  "event_name": "world_gateway.mode_switched",
  "signal_name": "_on_gateway_mode_switched",
  "event_filter": {"domain": "world_gateway"},
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}

# CT-GW-WM-03 (COMMAND)：传送门选择命令 → GameModeRoutingSolver.route
{
  "contract_id": "CT-GW-WM-03",
  "contract_version": 1,
  "domain_name": "world_gateway",
  "view_name": "world_map",
  "endpoint_kind": "COMMAND",
  "service_path": "GameModeRoutingSolver",
  "method_name": "route_to_port",
  "params_schema": [
    {"name": "port_id", "type": "String", "required": true, "rule": "VD_GW_PORT_ID_FORMAT"},
    {"name": "mode", "type": "String", "required": true, "rule": "VD_GW_MODE_ENUM"}
  ],
  "response_dto": "GatewayRouteResultDTO",
  "created_at": "2026-09-05",
  "last_reviewed_at": "2026-09-05"
}
```

### 3. spatial_movement 契约端点（3 条）

```json
{
  "contract_id": "CT-SM-MH-01",
  "contract_version": 1,
  "domain_name": "spatial_movement",
  "view_name": "main_hud",
  "endpoint_kind": "DTO",
  "source_path": "SpatialMovementSolver.cooldown_snapshot",
  "target_path": "main_hud_view.quick_action_spatial",
  "field_mapping": [
    {"backend_field": "flash_teleport_cd", "view_field": "flash_btn.cd_overlay.value", "transform_kind": "identity"},
    {"backend_field": "blink_move_cd", "view_field": "blink_btn.cd_overlay.value", "transform_kind": "identity"},
    {"backend_field": "space_jump_ready", "view_field": "jump_btn.disabled", "transform_kind": "cast", "transform_config": {"to": "bool", "invert": true}}
  ]
}

{
  "contract_id": "CT-SM-MH-02",
  "contract_version": 1,
  "domain_name": "spatial_movement",
  "view_name": "main_hud",
  "endpoint_kind": "EVENT",
  "event_name": "spatial_movement.cd_updated",
  "signal_name": "_on_spatial_cd_updated"
}

{
  "contract_id": "CT-SM-MH-03",
  "contract_version": 1,
  "domain_name": "spatial_movement",
  "view_name": "main_hud",
  "endpoint_kind": "COMMAND",
  "service_path": "SpatialMovementSolver",
  "method_name": "activate_movement",
  "params_schema": [{"name": "movement_kind", "type": "String", "required": true, "rule": "VD_SM_MOVEMENT_ENUM"}]
}
```

### 4. lifecycle_physiology 契约端点（3 条）

```json
{
  "contract_id": "CT-LP-CP-01",
  "contract_version": 1,
  "domain_name": "lifecycle_physiology",
  "view_name": "character_progression",
  "endpoint_kind": "DTO",
  "source_path": "LifecyclePhysiologySolver.physiology_snapshot",
  "target_path": "character_progression_view.physiology_life_tab",
  "field_mapping": [
    {"backend_field": "current_age", "view_field": "age_indicator.value", "transform_kind": "identity"},
    {"backend_field": "vitality_curve", "view_field": "vitality_chart.series", "transform_kind": "identity"},
    {"backend_field": "aging_stage", "view_field": "aging_stage_label.text", "transform_kind": "enum_map", "transform_config": {"map": {"INFANT":"幼体","YOUTH":"青壮","MIDDLE":"中年","ELDER":"暮年"}}},
    {"backend_field": "physiological_states", "view_field": "state_badges.list", "transform_kind": "identity"}
  ]
}

{
  "contract_id": "CT-LP-CP-02",
  "contract_version": 1,
  "domain_name": "lifecycle_physiology",
  "view_name": "character_progression",
  "endpoint_kind": "EVENT",
  "event_name": "lifecycle_physiology.aging_progressed",
  "signal_name": "_on_aging_progressed"
}

{
  "contract_id": "CT-LP-CP-03",
  "contract_version": 1,
  "domain_name": "lifecycle_physiology",
  "view_name": "character_progression",
  "endpoint_kind": "COMMAND",
  "service_path": "LifecyclePhysiologySolver",
  "method_name": "apply_physiological_state",
  "params_schema": [{"name": "state_kind", "type": "String", "required": true, "rule": "VD_LP_STATE_ENUM"}]
}
```

### 5. item_statistics 契约端点（3 条，含新视图）

```json
{
  "contract_id": "CT-IS-CV-01",
  "contract_version": 1,
  "domain_name": "item_statistics",
  "view_name": "collection_view",
  "endpoint_kind": "DTO",
  "source_path": "ItemStatisticsSolver.account_statistics",
  "target_path": "collection_view.stats_panel",
  "field_mapping": [
    {"backend_field": "total_items", "view_field": "total_counter.value", "transform_kind": "identity"},
    {"backend_field": "rarity_distribution", "view_field": "rarity_chart.series", "transform_kind": "identity"},
    {"backend_field": "collected_catalog", "view_field": "catalog_grid.items", "transform_kind": "aggregate", "transform_config": {"group_by": "rarity"}}
  ]
}

{
  "contract_id": "CT-IS-CV-02",
  "contract_version": 1,
  "domain_name": "item_statistics",
  "view_name": "collection_view",
  "endpoint_kind": "EVENT",
  "event_name": "item_statistics.catalog_updated",
  "signal_name": "_on_catalog_updated"
}

{
  "contract_id": "CT-IS-CV-03",
  "contract_version": 1,
  "domain_name": "item_statistics",
  "view_name": "collection_view",
  "endpoint_kind": "COMMAND",
  "service_path": "ItemStatisticsSolver",
  "method_name": "refresh_statistics",
  "params_schema": [],
  "response_dto": "ItemStatisticsSnapshotDTO"
}
```

### 6. CollectionView 新视图契约声明

```gdscript
# 契约：新视图的 class_name 与路由入口声明。
# 位置：frontend/views/collection_view.gd（Phase 59 S2 阶段落地）

class_name CollectionView

# 视图职责：账号物品库统计、收藏图鉴、稀有度分布
# 主要数据源：item_statistics 域（3 条契约）
# 视图类型：独立视图（非 Tab 扩展）

# 路由入口（承接 view_router.gd 已建立的 17 视图路由注册）
static func get_route_name() -> String:
	return "collection"

static func get_display_name() -> String:
	return "物品图鉴"
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 1.1**：在 `backend/config/contracts.json` 中追加 12 条契约条目（4 域 × 3 类），contract_id 前缀分别为 `CT-GW-WM-*`、`CT-SM-MH-*`、`CT-LP-CP-*`、`CT-IS-CV-*`；
- [ ] **Step 1.2**：更新 `contracts.json` 顶部 `entry_count_expected` 字段从 `49` 更新为 `61`（Inv-MI-5）；
- [ ] **Step 1.3**：为 4 个域新增的 12 条契约条目全部标记 `infra_exempt: false`、`deprecated: false`、`created_at: "2026-09-05"`、`last_reviewed_at: "2026-09-05"`；
- [ ] **Step 1.4**：新增 5 条 VD_* 值域校验规则（承接 Phase 57 S3 VD_CT_* 规则族范式）：`VD_GW_PORT_ID_FORMAT`、`VD_GW_MODE_ENUM`、`VD_SM_MOVEMENT_ENUM`、`VD_LP_STATE_ENUM`、`VD_IS_RARITY_ENUM`；
- [ ] **Step 1.5**：更新 `backend/config/contract_completeness_baseline.json` 的 `by_status.MISSING` 数组——从 4 项 `[world_gateway, spatial_movement, lifecycle_physiology, item_statistics]` 收敛为空数组 `[]`；
- [ ] **Step 1.6**：更新 baseline 的 `reverse_orphans` 数组——`world_map` 项从 `view_without_domain` 状态变更为 `covered_by: world_gateway`（Inv-MI-3 反向孤儿收敛）；
- [ ] **Step 1.7**：更新 baseline 的 `by_status.FULL` 数组——追加 4 项（4 个 MISSING 域补齐后转为 FULL）；
- [ ] **Step 1.8**：更新 `frontend/views/view_router.gd` 的 17 视图注册数组——追加 `collection_view` 为第 18 个视图（Inv-MI-1 新视图上限）；
- [ ] **Step 1.9**：为 3 个现有视图（`world_map`、`main_hud`、`character_progression`）的枚举声明追加新 Tab/按钮名（`travel_gateway`、`quick_action_spatial`、`physiology_life`），但**不修改** `.tscn` 场景树结构；
- [ ] **Step 1.10**：本阶段**禁止**：修改任何 `backend/domains/{world_gateway,spatial_movement,lifecycle_physiology,item_statistics}/**` 文件；本阶段新增文件**不超过 3 个**（1 个新视图 `.gd` 骨架 + 1 个新视图 `.tscn` 骨架 + 1 个新视图 `.gd` 路由声明）；
- [ ] **Step 1.11**：本阶段新增断言数**控制在 8~12 条**，命名前缀统一 `TC-MI-S1-NN`；
- [ ] **Step 1.12**：新增视图 `collection_view` 的 class_name 与 view_router 注册名称**必须**严格一致，禁止大小写漂移；
- [ ] **Step 1.13**：所有新契约的 `field_mapping` 中 `transform_kind` 值**必须**在 Phase 57 S2 已实现的转换规则内（identity/cast/format/enum_map/aggregate/filter），不允许新增值；
- [ ] **Step 1.14**：4 个域的契约条目**必须**在 `contracts.json` 中按域名字母序排列，便于 CI 门禁扫描与人工审查。

---

## 三、 数据结构验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :---: | :--- | :--- | :---: |
| `TC-MI-S1-01` | 12 条新契约条目注册成功 | 加载更新后的 `contracts.json` | `ContractRegistryIndex.find_all().size() == 61` |
| `TC-MI-S1-02` | 4 MISSING 域 DTO 契约登记 | `find_by_domain("world_gateway")` 等 4 域 | 每域返回条目数 ≥ 1 且 endpoint_kind 含 DTO |
| `TC-MI-S1-03` | 4 MISSING 域 EVENT 契约登记 | 同上 | 每域 endpoint_kind 含 EVENT |
| `TC-MI-S1-04` | 4 MISSING 域 COMMAND 契约登记 | 同上 | 每域 endpoint_kind 含 COMMAND |
| `TC-MI-S1-05` | 契约完整性收敛（Inv-MI-2） | `guard.audit_domain_coverage("world_gateway")` | 返回 `{coverage_status: FULL, missing_endpoints: []}` |
| `TC-MI-S1-06` | 反向孤儿收敛（Inv-MI-3） | `guard.audit_full_registry().reverse_orphans` | `reverse_orphans.size() == 0`（world_map 已被 world_gateway 覆盖） |
| `TC-MI-S1-07` | baseline MISSING 数组清空 | 加载更新后的 baseline | `baseline.by_status.MISSING == []` |
| `TC-MI-S1-08` | baseline FULL 数组扩展 | 加载更新后的 baseline | `baseline.by_status.FULL` 包含 4 个新域 |
| `TC-MI-S1-09` | VD_* 新增 5 条值域规则 | `audit_config.gd` 中 VD_CT_RULES 数量 | 数量 = 5（Phase 57 S3 的 5 条）+ 5（Phase 59 S1 新增 5 条）= 10 |
| `TC-MI-S1-10` | entry_count_expected 与实际一致 | 加载 contracts.json | `$schema.version == 1` 且 `entry_count_expected == 61` 且 `entries.size() == 61` |
| `TC-MI-S1-11` | collection_view 新视图注册 | `view_router.gd` 视图数组 | 数组长度从 17 增至 18，包含 `collection_view` |
| `TC-MI-S1-12` | 后端零修改（Inv-MI-4） | 扫描 4 个 MISSING 域 `.gd` 文件 mtime | 4 个域全部 `.gd` 文件的修改时间早于 Phase 59 施工开始时间 |

---

## 附：第 2 轮实施收敛登记

> 【待第 2 轮细则收敛时填写】
> - 实际新增文件清单：
> - 12 条契约条目最终落库确认：
> - 5 条 VD_* 规则上线结果：
> - baseline 快照 Phase 59 冻结点登记（MISSING=0 / FULL+4 / reverse_orphans=0）：
> - collection_view 视图注册与 view_router 一致性核验：
> - 断言落地数 vs 计划数（8~12）的偏差：
> - 未闭环事项登记（如有）：
