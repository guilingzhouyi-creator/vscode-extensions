---
档号: KALAR-DEV-2026-ST24-001
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST24 (Phase_28_Python脚本库治理收口)
件号: 001
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_28_Python脚本库治理收口 —— 阶段1：治理契约与施工边界
形成日期: 2026-09-01
归档日期: 2026-09-02（下午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 建议合并; 验证 3 项落地结论; 保留独立
---

# 施工细则：Python 脚本库治理收口 —— 阶段1：治理契约与施工边界（含验证修正）

> **施工开始日期**：2026-09-01

> [!NOTE]
> **【施工目标】**：按 Python 脚本库分级审查报告（2026-09-01）收敛 28 文件/6316 行脚本库——**必须合并 6 项 / 建议合并 4 项 / 验证 3 项落地**；建立 `audit_common.py` 共享底座收敛复制样板；**零行为回归**（18 个挂载 audit 任务重构前后输出一致）。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST24-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST24-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：用户立项（审查报告分级清单）+ 调研补充修正（⑬ report_sweep 活跃降级保留等）。
> **精准上游规范指针**：审查报告（必须合并 6 / 建议 4 / 保留独立 / 验证 3）；audit_runner 调度（18 任务）；scripts/sh 门禁（audit-all/bench-sweep/audit-perf 引用面）。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST24-ATT_附件_案卷共享契约与上下文.md) ｜ **阶段1 (当前)** ｜ [阶段2](KALAR-DEV-2026-ST24-002_阶段2_共享底座与收敛实现.md) ｜ [阶段3](KALAR-DEV-2026-ST24-003_阶段3_依赖简化与门禁适配.md) ｜ [阶段4](KALAR-DEV-2026-ST24-004_阶段4_全量测试与工程验收矩阵.md)

---

## 一、 治理契约（分级清单转施工契约，含调研修正）

### 1.1 必须合并/处置（修正后 6 项）

| # | 项 | 依据（审查+验证） | 处置 |
| :---: | :--- | :--- | :--- |
| 1 | `audit_event_descriptions.py` + `audit_item_descriptions.py` | Phase 24 已由 audit_copywriting 接管，未挂载无引用 | **删除**（功能已唯一化） |
| 2 | `deep_audit_backend.py` | 1 次提交、无 sh/文档引用（一次性深审计残留） | **删除**（git 历史可溯） |
| 3 | `refactor_frontend_archives.py` | 1 次提交、无引用（一次性重构工具） | **删除** |
| 4 | `report_trend.py` | **验证修正**：无 sh 引用（report_sweep 活跃但 trend 废弃） | **删除** |
| 5 | `audit_test_coverage.py` | 未挂载 + 无 sh 调用；**归档文档有链接引用**（Phase_06 档案） | **删除 + 同步文档**（归档链接改指审计门禁总入口或移除） |
| 6 | ROOT 路径解析统一 | 47 处样板、`parents[2]`/`parent.parent.parent`/`parent` 三变体（深度假设漂移） | **统一**为 `audit_common.resolve_repo_root()` |

### 1.2 建议合并（4 项）

| # | 项 | 依据 | 处置 |
| :---: | :--- | :--- | :--- |
| 7 | `audit_gd_style/advanced/simplification` → `audit_gd.py` | GD 质量域同域三拆分（10+2+3 检查点、同变化方向） | 合并为单文件分节（规则细分保留） |
| 8 | 占位符残留/零内联/键格式 三检查项收敛 | 散布 5~6 文件各自重写 | 抽共享助手（`scan_legacy_placeholders`/`contains_cjk`/键契约正则） |
| 9 | `audit_common.py` 共享底座 | ROOT 47 / utf-8 样板 22 / json 读取散布 / Finding 与结论打印 19 文件各自实现 | 建立共享底座（resolve_repo_root/ensure_utf8_stdout/load_json/Finding/结论打印） |
| 10 | 白名单类规则集 | ITEM_PARAM_WHITELIST/JUDGMENT_FIELDS 内联 | **评估后**集中到共享底座常量区（不制造过度配置） |

### 1.3 验证 3 项落地结论（调研修正）

| 验证项 | 结论 | 处置 |
| :--- | :--- | :--- |
| ⑪ `audit_item_keys` | 职责**唯一**（`KALAR:` canonical ID 引用一致性检查，与 name_keys/config 键格式不重叠） | **保留独立** |
| ⑫ `audit_secrets` | **有实际逻辑**（私钥/PGP 密钥正则扫描 + git 命令） | **保留独立** |
| ⑬ report 三件套 | `report_sweep` **活跃**（bench-sweep.sh 调用）→ 降级保留；`audit_perf_hotspots` 被 audit-perf.sh **直接调用** → 保留 | sweep/aggregate/perf 保留；trend 归 1.1-4 |

### 1.4 保留独立（不动的脚本）

`audit_runner`（调度引擎）/ `audit_docs`（19 规则最大）/ `audit_config` / `audit_name_keys` / `audit_magic_dimensions` / `audit_copywriting` / `audit_cdc` / `audit_bounds` / `audit_event_probability` / `audit_config_unused` / `audit_secrets` / `audit_hardcode` / `audit_item_keys` / `audit_perf_hotspots` / `archive_volume` / `report_aggregate` / `report_sweep` / `audit_gd`（三合一后）——职责清晰、独立复用、变化方向不同。

---

## 二、 施工边界契约（不变量）

1. **audit 门禁行为零回归**：18 个挂载任务重构后**输出与重构前一致**（S4 用输出哈希对比验证）；
2. **audit_runner 调度语义保持**：task_id/挂载表/scopes 不因重构改变（除 gd 三合一后 task_id 合并）；
3. **sh 直接调用路径不破坏**：bench-sweep.sh → report_sweep、audit-perf.sh → report_aggregate/audit_perf_hotspots 保持可用；
4. **删除脚本 git 历史可溯**：不新建归档文件（git 历史即归档）；
5. **不得为消除少量重复制造过度配置**（共享底座仅收敛真实复制样板）。

---

## 三、 数据结构验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-PGC-S1-01` | 契约完整 | 契约清单 vs 实际脚本 | 28 文件全部分类（处置/保留），零遗漏 |
| `TC-PGC-S1-02` | 验证修正落地 | ⑪⑫⑬ 结论 | item_keys/secrets/sweep/perf 保留依据成立 |
| `TC-PGC-S1-03` | 边界不变量 | 重构范围声明 | 不碰保留独立脚本的规则语义 |
| `TC-PGC-S1-04` | 删除清单可溯 | 删除文件 git 历史 | 均有提交记录（可恢复） |
| `TC-PGC-S1-05` | sh 路径保护 | bench-sweep/audit-perf 引用 | 调用面不变（sweep/aggregate/perf 保留） |
| `TC-PGC-S1-06` | 共享底座边界 | 收敛范围 | 仅收敛真实复制样板，不制造过度配置 |
