---
档号: KALAR-DEV-2026-ST24-003
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST24 (Phase_28_Python脚本库治理收口)
件号: 003
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_28_Python脚本库治理收口 —— 阶段3：依赖简化与门禁适配
形成日期: 2026-09-01
归档日期: 2026-09-02（下午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 删除清单; audit_test_coverage 处置修正; audit_runner 任务表更新; audit_baseline_before.json
---

# 施工细则：Python 脚本库治理收口 —— 阶段3：依赖简化与门禁适配

> **施工开始日期**：2026-09-01

> [!NOTE]
> **【施工目标】**：死代码/退役脚本处置（删除 5 项 + **audit_test_coverage 保留修正**——历史归档文档链接依赖，删除会致 LINK-DEAD）；sh 引用面核查（bench-sweep/audit-perf 调用路径不破坏）；audit_runner 任务表 18→**16**（gd 三合一 -2）；**重构前后输出一致性哈希对比**（零回归验证方案）。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST24-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST24-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：S1 治理契约（必须合并/处置）+ S2 收敛。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST24-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST24-001_阶段1_治理契约与施工边界.md) ｜ [阶段2](KALAR-DEV-2026-ST24-002_阶段2_共享底座与收敛实现.md) ｜ **阶段3 (当前)** ｜ [阶段4](KALAR-DEV-2026-ST24-004_阶段4_全量测试与工程验收矩阵.md)

---

## 一、 依赖简化执行清单

### 1.1 删除清单（git 历史可溯，不新建归档文件）

| 文件 | 依据 | 动作 |
| :--- | :--- | :--- |
| `audit_event_descriptions.py` | Phase 24 退役，功能已由 audit_copywriting 接管 | `git rm` |
| `audit_item_descriptions.py` | 同上 | `git rm` |
| `deep_audit_backend.py` | 1 次提交、无引用（一次性深审计） | `git rm` |
| `refactor_frontend_archives.py` | 1 次提交、无引用（一次性重构工具） | `git rm` |
| `report_trend.py` | 无 sh 引用（trend 废弃；sweep/aggregate 活跃保留） | `git rm` |

### 1.2 audit_test_coverage 处置修正（S1 契约修订）

- **保留文件**（不删除）：`docs/归档库/03_短期施工档案卷/第01期/Phase_06/...阶段4.md` 等**历史封存档案**链接引用 `audit_test_coverage.py`——删除会致归档文档 LINK-DEAD（audit-docs error）且历史档案不修改；
- 保持**不挂载**现状（audit_runner 任务表不含 coverage——运行面已由 test-run/audit-all 覆盖）；
- 从「必须合并」降级为「**保留独立**」（历史链接依赖），在脚本头注释声明保留依据。

### 1.3 audit_runner 任务表更新（18 → 16）

| 变化 | 前 | 后 |
| :--- | :--- | :--- |
| gd 三合一 | `gd_style` / `gd_advanced` / `gd_simplification`（3 task_id） | `gd`（1 task_id，audit_gd.py 分节聚合） |

- 其余 15 任务（bounds/cdc/config/config_unused/copywriting/docs/event_probability/hardcode/item_keys/magic_dimensions/name_keys/perf/refactor_metrics/secrets + gd）保持；
- **sh 门禁自动跟随**：audit-all.sh 遍历 audit_runner 任务表，无需改 sh。

### 1.4 sh 引用面核查（不破坏）

| sh 脚本 | 调用的 py | 状态 |
| :--- | :--- | :--- |
| `bench-sweep.sh` | `report_sweep.py`（活跃） | **保留不动** |
| `audit-perf.sh` | `report_aggregate.py`（--gate）+ `audit_perf_hotspots.py`（直接调用） | **保留不动** |

- 删除清单中无任何 sh 引用的脚本（已核查）→ sh 门禁零影响。

---

## 二、 重构前后输出一致性对比方案（零回归验证）

| 步骤 | 动作 | 验证断言 |
| :--- | :--- | :--- |
| 1 | 重构前：`audit_runner --json` 全量输出 → 存 `benchmarks/reports/audit_baseline_before.json` 哈希 | 基线记录 |
| 2 | 实施 S2 收敛 + S3 删除/任务表更新 | — |
| 3 | 重构后：`audit_runner --json` 全量输出 → 对比每任务 finding 集合哈希 | **每任务 findings 哈希一致**（零回归） |
| 4 | 删除文件确认：`git status` 删除清单 + 无 sh 断链 | 删除 5 项、引用面完整 |

- 对比粒度：按 task_id 的 findings（rule/file/line/message）集合哈希——**规则语义不变**，仅样板收敛；
- gd 三合一后 findings 归并到 `gd` task_id（语义等价：style+advanced+simplification 全量检查合并）。

---

## 三、 验证矩阵 (DoD)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-PGC-S3-01` | 删除清单完整 | git rm 后 status | 5 文件删除、无残留引用（grep 零命中） |
| `TC-PGC-S3-02` | test_coverage 保留 | 归档文档链接 | LINK-DEAD 零（audit-docs 通过）、文件保留带依据注释 |
| `TC-PGC-S3-03` | 任务表 16 | audit_runner 挂载验证 | 16 个 task_id（gd 合并、其余保持） |
| `TC-PGC-S3-04` | sh 引用面 | bench-sweep/audit-perf 运行 | sweep/aggregate/perf 调用正常（exit=0） |
| `TC-PGC-S3-05` | 输出一致性 | 重构前后 --json 对比 | 每任务 findings 哈希一致（零回归） |
| `TC-PGC-S3-06` | audit-docs | 全库链接 | 审查通过（删除不产生断链） |
