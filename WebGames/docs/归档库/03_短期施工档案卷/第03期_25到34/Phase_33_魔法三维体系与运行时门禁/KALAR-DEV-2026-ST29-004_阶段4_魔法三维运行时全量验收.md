---
档号: KALAR-DEV-2026-ST29-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST29 (Phase_33_魔法三维体系与运行时门禁)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_33_魔法三维体系与运行时门禁 —— 阶段4：魔法三维运行时全量验收
形成日期: 2026-09-01
归档日期: 2026-09-02（下午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 魔法三维运行时全量验收
---

# 施工细则：魔法三维体系与运行时门禁 —— 阶段4：魔法三维运行时全量验收

> **施工开始日期**：2026-09-01 下午

> [!NOTE]
> **施工目标**：证明三维魔法体系在真实 `GameBootstrap.assemble()` 路径、热重载、序列化和消费方中成立；`audit_magic_dimensions.py` 通过不等价于运行时接线通过，必须完成本矩阵。
> **验收范围**：registry 装配、六类配置校验、三维解析、DTO、名称键、基线 resolver、审计和性能。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST29-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST29-001_阶段1_魔法三维数据契约与审查分级.md) ｜ [阶段2](KALAR-DEV-2026-ST29-002_阶段2_魔法注册表接线与解析器实现.md) ｜ [阶段3](KALAR-DEV-2026-ST29-003_阶段3_魔法配置本地化与审计工程化.md) ｜ **阶段4 (当前)**

## 一、验收分级

| 级别 | 判定对象 | 通过条件 |
| :--- | :--- | :--- |
| P0 规范/配置 | 命名、数量、键值、i18n、审计规则 | 静态门禁无新增违规；缺项和错配均拒绝 |
| P0 设计正确性 | registry 接线、band 互斥、ability 语义、DTO 范围 | 真实启动路径 ready；三维字段类型和不变量稳定 |
| P1 风险隔离 | 热重载、旧 DTO、i18n 冲突、非法 rank | 失败不污染上一有效版本；错误可观测且不可静默降级 |
| P1 性能 | 装配、重复解析、批量快照和名称查询 | 运行时不重复读配置；性能不低于改造前基准 |
| P2 可维护性/优化 | 私有字段访问、兼容别名、缓存失效 | 所有边界有公开 API、版本记录和迁移证据 |

## 二、测试矩阵

| 检验项 ID | 场景 | 预期断言 |
| :--- | :--- | :--- |
| `TC-M3-S4-01` | 通过 `GameBootstrap.assemble()` 首次和重复启动 | 返回唯一 registry，ready 与配置版本一致，重复调用幂等 |
| `TC-M3-S4-02` | tiers/aptitudes/forms/professions 数量、键值、阈值和能损错误 | 装配失败并保留诊断，不暴露半成品 |
| `TC-M3-S4-03` | band [1,3]/[4,6]/[7,9]/[10,11] 边界、重叠和空洞 | 合法每阶唯一；任何重叠/空洞/越界均拒绝 |
| `TC-M3-S4-04` | ability 重叠候选、主体标量、越级白名单 | `ability_tier_candidates` 可多项，`ability_tier` 不被数组污染 |
| `TC-M3-S4-05` | rank_to_level(0/12/99)、非法 DTO、真神与形态错配 | 显式失败码；不静默转一阶；不构造非法快照 |
| `TC-M3-S4-06` | 旧版本 DTO、旧 `man_adaptor` 键、i18n 删除和跨域冲突 | 迁移可追踪；旧键按白名单兼容；冲突阻断且无残留 |
| `TC-M3-S4-07` | 基线 resolver、物品/角色/技能消费方真实调用链 | 全部经 registry；强度、mana_gate 和三维字段零回归 |
| `TC-M3-S4-08` | 热重载进行中读取快照、重复解析和批量导出 | 事务版本一致；旧缓存失效；无共享字典并发污染 |
| `TC-M3-S4-09` | 静态审计通过但故意移除 assemble 接线 | 集成测试失败，证明审计与运行时门禁互补 |

## 三、门禁与交付证据

- 执行 `bash scripts/sh/test-run.sh`、`bash scripts/sh/check-gdscript.sh`、`bash scripts/sh/audit-all.sh`，并单独运行 `python3 scripts/py/audit_magic_dimensions.py`。
- 记录真实启动路径、坏配置、重叠区间、DTO 迁移、i18n 冲突和故障注入日志；每项结果包含 `config_version`。
- 执行 `bash scripts/sh/bench-sweep.sh` 和 `bash scripts/sh/audit-perf.sh`，比较装配前后解析耗时、内存和批量快照吞吐；结果漂移必须有解释。
- 路线图四阶段全部通过、文档门禁 `new_violations = 0`、代码门禁和单元/集成测试全绿后，才可申请归档；在此之前状态保持 `📝 待获批`。

本阶段是验收设计，不授权跳过阶段1～3直接修改魔法业务代码。
