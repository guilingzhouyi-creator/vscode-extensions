---
档号: KALAR-DEV-2026-ST15-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST15 (Phase_19_统一名称注册表)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_19_统一名称注册表 —— 阶段4：全量测试与工程验收矩阵
形成日期: 2026-09-01
归档日期: 2026-09-01
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 全量测试
---

# 施工细则：统一名称注册表 —— 阶段4：全量测试与工程验收矩阵

> [!NOTE]
> **【施工目标】**：`test_name_registry.gd` 六项断言契约全绿；确认既有 266 断言零回归（Phase 17 品质 / Phase 18 魔法基线行为不变）；test_registry 登记 + 横切计数同步；静态质量门禁（test-run / check-gdscript / audit-all / audit-docs）全绿，存量迁移批次 1 完成。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST15-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST15-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：立项需求 ②（解析）④（audit）⑤（唯一性）；既有测试基线 266 断言不可回归。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST15-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST15-001_阶段1_全局名称键数据契约与既有体系盘点.md) ｜ [阶段2](KALAR-DEV-2026-ST15-002_阶段2_resolve名称解析与全局冲突检测实现.md) ｜ [阶段3](KALAR-DEV-2026-ST15-003_阶段3_词典配置结构与audit校验落地.md) ｜ **阶段4 (当前)**

---

## 一、 测试设计（test_name_registry.gd 六项断言契约）

```gdscript
# 覆盖 TC-NAM-01~06：既有键盘点 / 键规范唯一 / 英文 fallback 必达 / 未登记告警 /
#                    跨域冲突拦截 / 存量迁移清单
static func _test_existing_keys_inventory() -> Dictionary:
	# 物品 loc_name_key 与魔法 name_key 全量进入全局注册表且可解析
	var catalog := LocalizationRegistryCatalog.new()
	# 物品登记（抽样）+ 魔法登记（全量 11 位阶 + 4 资质 + 4 形态 + 13 实力）
	# 断言：registered_name_keys.size() >= 32 且 resolve 命中英文兜底

static func _test_name_key_format_unique() -> Dictionary:
	# 三段式格式校验 + 重复登记拒绝（TC-NAM-S1-02 / S2-03）

static func _test_en_us_fallback_required() -> Dictionary:
	# en_US 词典覆盖登记键 100%（TC-NAM-S1-03 / S3-02）

static func _test_unregistered_warning() -> Dictionary:
	# 未登记键 resolve → [key] + missing_name_keys +1（TC-NAM-S1-04 / S2-02）

static func _test_cross_domain_collision() -> Dictionary:
	# 同 key 双域登记 → NAME_KEY_COLLISION（TC-NAM-S1-05 / S2-03）

static func _test_migration_batch_one_done() -> Dictionary:
	# 批次 1 九处后端 fallback 全部 name_key 化；missing_name_keys == 0（TC-NAM-S1-06 / S3-05）
```

**测试用例矩阵（六项断言契约）**：

| 测试项 | 覆盖需求条款 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-NAM-01` | 既有键盘点 | 物品/魔法登记结果 | 物品 loc_name_key + 魔法 name_key 全量登记，可经 resolve 解析 |
| `TC-NAM-02` | 键规范唯一 | 三段式键集 | 格式校验通过；重复登记返回失败 |
| `TC-NAM-03` | 英文 fallback 必达 | 登记键 vs en_US 词典 | 覆盖率 100%，零缺失 |
| `TC-NAM-04` | 未登记告警 | 未登记键 resolve | `[key]` 原样 + 计数 +1，不抛 Fatal |
| `TC-NAM-05` | 跨域冲突拦截 | 同 key 双域 | `NAME_KEY_COLLISION` 且原所有者不被覆盖 |
| `TC-NAM-06` | 存量迁移批次 1 | 9 处 fallback 全量检查 | 全部 name_key 化；`missing_name_keys == 0`；剧情展示层中文保留 |

**登记与计数同步**：

- `test_registry.gd`：preload + 清单登记 `TestNameRegistryDomain`；注释计数同步（后端套件 47→48；全域 66→67）；
- `test_architecture_guard.gd`：`CROSS_CUTTING_SUITE_COUNT` 4→5（名称注册表 = 第 5 个跨域全局标准：配置层护栏 / 架构护栏 / Phase 17 品质 / Phase 18 魔法 / Phase 19 名称注册表）；守卫公式按既有语义同步；
- README 计数：测试项 266→272（+6）、套件 66→67。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1: 数据结构与解析器单测** - `test_name_registry.gd` 六项断言，覆盖正常业务流全路径
- [ ] **Step 4.2: 极端边界与异常容错测试** - 跨域冲突/英文缺失/未登记告警/空 catalog 优雅拦截
- [ ] **Step 4.3: 工程化配置与全域回归测试** - 确认既有 266 断言零回归（品质/魔法/物品统计行为不变）
- [ ] **Step 4.4: 静态质量门禁与全宗验收闭环** - test-run / check-gdscript / audit-all（含新名称键校验）/ audit-docs 全绿；套件登记 test_registry

---

## 三、 验收闭环与延后声明

- **本卷完成标准**：六断言全绿 + 266→272 断言零回归 + audit-all（含 `audit_name_keys`）exit=0 + audit-docs 0 新增；
- **延后至后续案卷**：批次 2（config 逻辑 fallback 中文批量迁移）与前端显示层全量切换 `resolve_name_key`（本卷仅提供解析出口与登记机制，前端消费切换按既有前端案卷节奏推进）；
- **不变量声明**：英文为机器可读唯一底座（名称键本体禁中文）；显示文本永不作逻辑判断依据；名称键跨域唯一。
