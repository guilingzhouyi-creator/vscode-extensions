---
档号: KALAR-DEV-2026-ST16-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST16 (Phase_20_剧情事件流系统改造)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_20_剧情事件流系统改造 —— 阶段4：全量测试与工程验收矩阵
形成日期: 2026-09-01
归档日期: 2026-09-01
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 全量测试
---

# 施工细则：剧情事件流系统改造 —— 阶段4：全量测试与工程验收矩阵

> [!NOTE]
> **【施工目标】**：`test_narrative_flow.gd` 六项断言契约全绿；确认既有 272 断言零回归（名称注册表/品质/魔法基线行为不变）；test_registry 登记 + 计数同步；静态质量门禁（test-run / check-gdscript / audit-all / audit-docs）全绿；narratives 文案迁移批次 1 完成（%s 归零）。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST16-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST16-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：立项需求 ②（占位符）③（配置为源）④（热切换）；既有测试基线 272 断言不可回归。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST16-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST16-001_阶段1_事件流数据契约与既有体系盘点.md) ｜ [阶段2](KALAR-DEV-2026-ST16-002_阶段2_事件流解析器与热切换管线实现.md) ｜ [阶段3](KALAR-DEV-2026-ST16-003_阶段3_narratives配置迁移与audit校验落地.md) ｜ **阶段4 (当前)**

---

## 一、 测试设计（test_narrative_flow.gd 六项断言契约）

```gdscript
# 覆盖 TC-NAV-01~06：配置为源 / 占位符填充 / 未填充拦截 / 热切换零打断 /
#                    %s 归零 / 语言热切换
static func _test_config_as_source() -> Dictionary:
	# config/narratives/*.json 经 GameConfig 递归扫描加载（表 narratives.<文件>）
	# 断言：combat 表存在且含 parry_interrupt 模板；事件流键登记名称注册表可查

static func _test_placeholder_fill() -> Dictionary:
	# NarrativeFlowResolver.resolve_flow("combat", "parry_interrupt", 全参)
	# 断言：success:true + 文本全部 {param} 填充、零残留

static func _test_missing_params_intercept() -> Dictionary:
	# 缺参调用 → MISSING_PARAMS + 缺失清单 + missing_params_count 增长

static func _test_hot_switch_session_kept() -> Dictionary:
	# NarrativeFlowSession.migrate_on_reload：已触发会话 → has_triggered/timestamp 保持

static func _test_percent_placeholder_zero() -> Dictionary:
	# 扫描 config/narratives/*.json 模板：%s/%d/%.*f 归零，全部 {param}

static func _test_locale_hot_switch() -> Dictionary:
	# switch_language 语义：resolve_flow 传 locale_override 文本即时切换
```

**测试用例矩阵（六项断言契约）**：

| 测试项 | 覆盖需求条款 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-NAV-01` | ③ 配置为源 | narratives 表 + 事件流键 | 配置加载（零装配）+ `narrative.*` 键名称注册表登记可查 |
| `TC-NAV-02` | ② 占位符填充 | 合法模板 + 全参 | `success:true` + 文本占位符全部填充零残留 |
| `TC-NAV-03` | ② 未填充拦截 | 缺参模板 | `MISSING_PARAMS` + 缺失清单 + 计数增长，不抛 Fatal |
| `TC-NAV-04` | ④ 热切换零打断 | 重载后会话迁移 | 已触发事件流 `has_triggered/timestamp` 保持，文案刷新 |
| `TC-NAV-05` | ② 占位符统一 | 全库 narratives 模板 | `%s`/`%d` 归零，全部 `{param}` 语义化 |
| `TC-NAV-06` | ④ 语言热切换 | locale_override 切换 | 文本随 locale 即时切换（词典/模板一致） |

**登记与计数同步**：

- `test_registry.gd`：preload + 清单登记 `TestNarrativeFlowDomain`；注释计数（后端 48→49；全域 67→68）；
- `test_architecture_guard.gd`：`CROSS_CUTTING_SUITE_COUNT` **保持 5**（剧情事件流属 narrative 域内能力改造，非新增跨域全局标准；守卫公式按 manifest 条目同步）；
- README 计数：测试项 272→278（+6）、套件 67→68。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1: 数据结构与解析器单测** - `test_narrative_flow.gd` 六项断言，覆盖正常业务流全路径
- [ ] **Step 4.2: 极端边界与异常容错测试** - 缺参拦截/事件流未登记/空表/热重载会话迁移优雅处理
- [ ] **Step 4.3: 工程化配置与全域回归测试** - 确认既有 272 断言零回归（名称注册表/品质/魔法行为不变）
- [ ] **Step 4.4: 静态质量门禁与全宗验收闭环** - test-run / check-gdscript / audit-all（含 narrative_flows 校验）/ audit-docs 全绿；套件登记 test_registry

---

## 三、 验收闭环与延后声明

- **本卷完成标准**：六断言全绿 + 272→278 断言零回归 + audit-all（含 `narrative_flows`）exit=0 + audit-docs 0 新增 + narratives `%s` 全量归零；
- **延后至后续案卷**：批次 2 消费方全量切换（combat/character_creation 等更多叙事触发点接入 `resolve_flow`——本卷提供解析出口与文案迁移，消费方按既有节奏推进）；前端叙事展示层接入热切换；
- **不变量声明**：剧情文本 100% 配置为源（零代码内联）；占位符统一 `{param}` 语义化（禁 `%s`）；显示文本永不作逻辑判断依据（事件流 ID/参数名是逻辑键）；热切换零打断（触发状态保持、文案刷新）。
