---
档号: KALAR-DEV-2026-ST20-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST20 (Phase_24_统一文案配置系统)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_24_统一文案配置系统 —— 阶段4：全量测试与工程验收矩阵
形成日期: 2026-09-01
归档日期: 2026-09-01
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 全量测试
---

# 施工细则：统一文案配置系统 —— 阶段4：全量测试与工程验收矩阵

> [!NOTE]
> **【施工目标】**：`test_copywriting.gd` 六项断言契约全绿；确认既有 290 断言零回归（物品描述/事件描述/概率/名称注册表行为不变——统一接入后语义保持）；test_registry 登记 + 计数同步；静态质量门禁（test-run / check-gdscript / audit-all / audit-docs）全绿；统一配置布局与 audit 合并闭环。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST20-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST20-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：立项需求 ①（统一体系）②（共享核心）③（配置布局）④（audit 合并）⑤（域红线保留/零回归）。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST20-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST20-001_阶段1_数据契约与既有体系盘点.md) ｜ [阶段2](KALAR-DEV-2026-ST20-002_阶段2_共享文案解析核心与域接入.md) ｜ [阶段3](KALAR-DEV-2026-ST20-003_阶段3_统一配置布局与audit合并.md) ｜ **阶段4 (当前)**

---

## 一、 测试设计（test_copywriting.gd 六项断言契约）

| 测试项 | 覆盖需求条款 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-COPY-01` | ② 统一键解析 | 合法 copy_key + 全参 | `success:true` + 文本 `{param}` 填充零残留 |
| `TC-COPY-02` | ② 条件段拼接 | 状态匹配 when | 条件 `text` 段拼接（物品语义保持） |
| `TC-COPY-03` | ② 结果分支 | outcome success/failure | 分支模板选择 + base 兜底（事件语义保持） |
| `TC-COPY-04` | ② 未填充拦截 | 缺参模板 | `MISSING_PARAMS` + 计数增长（一处实现） |
| `TC-COPY-05` | ⑤ 域解析器零回归 | 物品/事件描述解析 | 行为零变化（接入共享核心后回归） |
| `TC-COPY-06` | ④ 热重载 + 键登记 | reload + 四域文案键 | 最新文案实时生效；名称注册表登记可查跨域唯一 |

**登记与计数同步**：

- `test_registry.gd`：preload + 清单登记 `TestCopywritingDomain`；注释计数（后端 51→52；全域 70→71）；
- `test_architecture_guard.gd`：`CROSS_CUTTING_SUITE_COUNT` 8→9（统一文案配置 = 第 9 个跨域全局标准：配置层护栏 / 架构护栏 / Phase 17 品质 / Phase 18 魔法 / Phase 19 名称注册表 / Phase 21 物品描述 / Phase 22 事件概率 / Phase 23 事件描述 / Phase 24 统一文案）；
- README 计数：测试项 290→296（+6）、套件 70→71。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1: 数据结构与解析器单测** - `test_copywriting.gd` 六项断言，覆盖正常业务流全路径
- [ ] **Step 4.2: 极端边界与异常容错测试** - 键非法/条目缺失/缺参/条件空组合/未知 outcome 优雅拦截
- [ ] **Step 4.3: 工程化配置与全域回归测试** - 确认既有 290 断言零回归（物品描述/事件描述/概率/名称注册表行为不变）
- [ ] **Step 4.4: 静态质量门禁与全宗验收闭环** - test-run / check-gdscript / audit-all（含 copywriting 统一 audit）/ audit-docs 全绿；套件登记 test_registry

---

## 三、 验收闭环与延后声明

- **本卷完成标准**：六断言全绿 + 290→296 断言零回归 + audit-all（含 `copywriting` 统一 audit，旧 audit_item_descriptions/event_descriptions 退役）exit=0 + audit-docs 0 新增 + `%s` 存量归零 + 域红线保留校验闭环；
- **延后至后续案卷**：概率文案/公告通知的**消费方接入**（本卷提供 `probability.<事件>.result` / `bulletin.<键>` 统一出口与配置，展示消费按既有节奏推进）；narratives 剧情文案存量（12+ 文件）批量迁移入 `copywriting.narrative`（Phase 20 获批后一并推进）；旧表适配层退役（迁移完成后 audit 提示清零）；
- **不变量声明**：统一键三段式 + `{param}` 唯一语法（`%s` 归零）；域红线不因统一而放宽（物品属性白名单 / 事件判定禁入 / 概率只读）；文案 100% 配置为源（零内联）；热重载零缓存；显示文本永不作逻辑判断依据（copy_key/参数名是逻辑键）。
