---
档号: KALAR-DEV-2026-ST13-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST13 (Phase_17_物品品质等级统一度量标准)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_17_物品品质等级统一度量标准 —— 阶段4：全量测试与工程验收矩阵
形成日期: 2026-09-01
归档日期: 2026-09-01
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: TestItemQualityDomain; 全量测试
---

# 施工细则：物品品质等级统一度量标准 —— 阶段4：全量测试与工程验收矩阵

> [!NOTE]
> **【施工目标】**：完成品质等级体系 1~3 层全量单元测试（枚举单调/区间端点/唯一映射/配置兜底/扩展受限）、全域回归与质量门禁验收闭环，确保新基线零行为破坏并成为全局物品设计的统一事实基线。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST13-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST13-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：立项需求四条款（2026-09-01）——①~④ 全条款验收。
> **延后容许**：与 item_statistics 递归树的品质维度归档联动、前端品阶色令牌接线属跨域高阶集成，允许延后至后续案卷闭环（本卷保证契约与基线查询正确性）。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST13-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST13-001_阶段1_品质等级数据契约与枚举定义.md) ｜ [阶段2](KALAR-DEV-2026-ST13-002_阶段2_品质解析与数值基线实现.md) ｜ [阶段3](KALAR-DEV-2026-ST13-003_阶段3_配置驱动与全域基线接线.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：立项需求 ① 六档单调序列 ② 神话区间端点 ③ 全域作用 ④ 唯一映射与基线服从约束
* **核心不变量约束断言**：全量断言 100% 通过；六档单调/区间端点从属/唯一映射/扩展受限四项不变量逐条可证；既有 253 项断言零回归
* **验收闭环最高指示**：测试用例必须覆盖正常路径、异常分支、极端边界与配置缺省，做到断言精准、证据确凿，禁止以最小 MVP 敷衍！

---

## 一、 自动化测试套件与验收代码 (Test Suite & Verification)

```gdscript
# 模块路径: res://tests/unit/test_item_quality.gd
class_name TestItemQualityDomain extends RefCounted

func run_all_tests() -> Dictionary:
	var results: Array[Dictionary] = []
	results.append(_test_tier_monotonic())
	results.append(_test_mythic_interval_semantics())
	results.append(_test_unique_mapping())
	results.append(_test_no_tier_inversion())
	results.append(_test_config_completeness())
	results.append(_test_extension_constraint())
	var passed := 0
	for r in results:
		if r.get("passed", false):
			passed += 1
	return {
		"suite": "ItemQualityTestSuite",
		"total_count": results.size(),
		"passed_count": passed,
		"results": results,
		"all_passed": passed == results.size(),
	}
```

**测试用例矩阵（六项断言契约）**：

| 测试项 | 覆盖需求条款 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-ITEM-QT-01` | ① 六档单调序列 | 六档配置表 | 普通→精品→史诗→传奇→神话→古代等级值严格单调递增（1~6），无跨级倒置 |
| `TC-ITEM-QT-02` | ② 神话级细分 | 神话物品声明准神器/真神器 | 解析成功且区间端点仅存在于神话级内部；真神器强度权重 > 准神器（低端/高端边界语义） |
| `TC-ITEM-QT-03` | ④ 唯一映射 | 同一物品重复/冲突声明 | 恰好一个主品质 + 至多一个区间；冲突声明被校验拒绝 |
| `TC-ITEM-QT-04` | ① 禁跨级倒置 | 配置等级值断裂/倒置（构造性） | 注册表启动校验失败，拒绝加载（`INV-1` 拦截） |
| `TC-ITEM-QT-05` | ③ 全域作用/④ 配置完整 | 缺表/缺键（构造性删除） | 必需表缺失由 `audit_config` 报错；运行时缺失安全兜底普通档基线 |
| `TC-ITEM-QT-06` | ④ 扩展受限 | 未登记品质/非神话带区间的新物品声明 | 返回 `UNREGISTERED_TIER` / `INTERVAL_OUTSIDE_MYTHIC`，未登记物品不产生任何基线消费 |

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1: 编写数据结构与核心逻辑单测** - `test_item_quality.gd` 六项断言，覆盖正常业务流全路径
- [ ] **Step 4.2: 实施极端边界与异常容错测试** - 配置倒置/缺表/跨级区间/重复声明等非法输入优雅拦截
- [ ] **Step 4.3: 执行工程化配置与全域回归测试** - 确认 gacha/锻造/发放等既有链路零退化（既有 253 断言全过）
- [ ] **Step 4.4: 静态质量门禁与全宗验收闭环** - test-run / check-gdscript / audit-all / audit-docs 全绿；测试套件登记 test_registry

---

## 三、 全量工程验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-ITEM-QT-S4-01` | 全量单测套件通过 | `test_runner.gd` 执行 | 本卷六项断言 + 既有 253 项断言 100% 通过 (PASS) |
| `TC-ITEM-QT-S4-02` | 极端异常注入容错 | Null/超限等级/非法区间字符 | 优雅处理或抛受控错误码，杜绝崩溃与脏状态 |
| `TC-ITEM-QT-S4-03` | 静态审查与门禁校验 | `audit-all` / `audit-docs --baseline` 运行 | 配置/风格/硬编码/文档审查 0 error、0 新增违规 |

**归档周期登记**：本卷计入 `01_短期施工区` 第 02 期在途案卷（3/10），验收通过后随周期归档机制编入 `KALAR-DEV-2026-ST{NN}-{PPP}` 标准档号。
