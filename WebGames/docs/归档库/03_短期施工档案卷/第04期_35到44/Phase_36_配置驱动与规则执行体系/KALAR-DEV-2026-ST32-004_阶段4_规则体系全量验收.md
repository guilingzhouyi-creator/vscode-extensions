---
档号: KALAR-DEV-2026-ST32-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST32 (Phase_36_配置驱动与规则执行体系)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_36_配置驱动与规则执行体系 —— 阶段4：规则体系全量验收
形成日期: 2026-09-02
归档日期: 2026-09-03（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 规则体系全量验收
---

# 施工细则：配置驱动与规则执行体系 —— 阶段4：规则体系全量验收

> **施工开始日期**：2026-09-02 中午
> **施工状态**：✅ 已施工完毕（2026-09-02 获批并闭环，代码与配置已落地，总索引看板已同步）

> [!NOTE]
> **施工目标**：以无头测试、失败注入与文档/配置门禁验证规则引擎泛化、作用域解析、组合规则、非法配置拒绝与向后兼容闭环；未通过任一 P0/P1 断言不得宣称验收。
> **验收范围**：规则业务解耦、组合规则能力、配置作用域、兼容校验机制。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST32-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST32-001_阶段1_规则引擎泛化与配置作用域契约.md) ｜ [阶段2](KALAR-DEV-2026-ST32-002_阶段2_规则执行器与作用域解析实现.md) ｜ [阶段3](KALAR-DEV-2026-ST32-003_阶段3_配置校验与兼容策略.md) ｜ **阶段4 (当前)**

## 一、验收分级

| 级别 | 判定对象 | 通过条件 |
| :--- | :--- | :--- |
| P0 规范/数据一致性 | 规则段字段、operator 白名单、作用域标注 | 审计零新增；坏输入零静默回退 |
| P0 设计正确性 | 作用域覆盖、组合聚合、运行时注入 | 判定确定性可解释，状态零部分写入 |
| P1 风险隔离 | 非法配置拒绝、兼容兜底 | 可诊断可恢复，不扩散到业务判定 |
| P2 可维护性 | 引擎与业务解耦、配置校验泛化 | 有配置与文档证据，未引入额外抽象负担 |

## 二、测试矩阵

| 检验项 ID | 场景 | 预期断言 |
| :--- | :--- | :--- |
| `TC-RULE-S4-01` | 全局 + 兑换码级规则合并 | 兑换码级覆盖全局同 key；判定结果确定 |
| `TC-RULE-S4-02` | AND/OR/NOT 组合与嵌套 | 聚合正确，NOT 取反，嵌套受控 |
| `TC-RULE-S4-03` | 运行时指标注入两次 | 无跨次残留，纯函数可重放 |
| `TC-RULE-S4-04` | 非法 operator/缺字段/版本不兼容 | 受控拒绝 + 结构化原因 + 旧快照保留 |
| `TC-RULE-S4-05` | 无规则配置 | 默认允许（显式默认行为），无隐式约束 |

## 三、门禁和交付证据

- 执行 `bash scripts/sh/test-run.sh`、`bash scripts/sh/check-gdscript.sh`、`bash scripts/sh/audit-all.sh`，确认配置、风格、硬编码、热点、覆盖、死配置、密钥及上下界检查无新增问题。
- 执行 `bash scripts/sh/audit-docs.sh --baseline`，确认文档门禁 `new_violations = 0`。
- 记录作用域合并、组合判定、非法配置拒绝与兼容兜底证据；测试输出必须包含配置版本，保证跨机复核。
- 路线图四阶段均标记完成、单元测试全绿且文档门禁零新增后，才可申请归档；本阶段细则本身在获批前保持 `📝 待获批`。

本阶段验收设计已随全量门禁闭环（test-run/check-gdscript/audit-all 已通过，总索引 ✅）。
