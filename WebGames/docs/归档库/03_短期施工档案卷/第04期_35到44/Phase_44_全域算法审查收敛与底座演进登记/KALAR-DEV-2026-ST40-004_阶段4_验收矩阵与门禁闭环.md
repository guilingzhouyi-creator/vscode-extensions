---
档号: KALAR-DEV-2026-ST40-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST40 (Phase_44_全域算法审查收敛与底座演进登记)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_44_全域算法审查收敛与底座演进登记 —— 阶段4：验收矩阵与门禁闭环
形成日期: 2026-09-03
归档日期: 2026-09-03（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: views.json
---

# 施工细则：全域算法审查收敛与底座演进登记 —— 阶段4：验收矩阵与门禁闭环

> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST40-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST40-001_阶段1_性能反模式数据契约与改造边界.md) ｜ [阶段2](KALAR-DEV-2026-ST40-002_阶段2_装配期单例化与发放链路实现设计.md) ｜ [阶段3](KALAR-DEV-2026-ST40-003_阶段3_配置热路径缓存与统计树索引工程化.md) ｜ **阶段4 (当前)**

> **施工开始日期**：2026-09-03
> **施工状态**：🚧 第 1 轮施工中（获批后更新为 ✅）

## 一、验收矩阵（TC-P44-*）

| 编号 | 断言目标 | 落点（测试文件） | 判定 |
| :--- | :--- | :--- | :--- |
| TC-P44-P1-01 | QualityTierRegistry 装配单例复用：连续两次 `GameBootstrap.quality_tier_registry()` 返回**同一实例**；发放热循环内不再出现 `new()+reload_configuration()` 重建（以实例引用一致断言） | `tests/unit/domains/test_item_statistics.gd` 或既有仓库注册相关套件（不新增测试文件，扩展现有） | registry_a == registry_b 且 is_ready() |
| TC-P44-P1-02 | `ItemInstanceFactory.build_instance` 品质快照产出与改造前一致：同 proto/quality_tier 输入 → 同一 registry 实例下 resolve 快照字段等价 | 上述套件 | quality_snapshot 非空且关键字段与基线一致 |
| TC-P44-P2-01 | `GameConfig.get_many(table, paths)` 与逐键 `get_value` 结果逐位等价（含数值宽松转换路径、数组下标路径、缺失键） | `tests/unit/test_config.gd`（若存在）或扩展现有配置套件 | 全键相等 |
| TC-P44-P2-02 | 路径缓存语义：同路径重复读取返回同值；热重载后新表值生效且旧路径解析无残留 | 配置套件 | reload 前后值按新表返回 |
| TC-P44-P3-01 | 统计树 canonical 索引：`find_item_node`/`apply_item_delta` 经索引定位与既有线性扫结果一致（命中节点 + path 等价），serialize/deserialize 往返后索引懒重建可用 | `tests/unit/domains/test_item_statistics.gd`（域 27 统计台账套件） | delta 后 current_quantity 与基线断言一致 |
| TC-P44-P4-01 | FifoBudget.trim_oldest：溢出 k 条时单次裁剪最旧 k 条、容器 ≤ 上限、未溢出零改动 | 扩展现有含裁剪套件或新增断言于既有文件 | 尺寸与最旧保留断言 |
| TC-P44-P5-01 | EquipmentFSM 前置失败零快照：非法槽位/物品不在 storage 等前置失败路径调用后 inventory 状态未变（快照先行改造不破坏失败语义） | `tests/unit/test_equipment_loadout.gd` | 失败码不变 + 状态未变 |
| TC-P44-P6-01 | 三处无界容器有界化：超上限注入后容器 ≤ max_entries 且最旧先出 | 所属域套件（server_grant/npc/chat_command 对应既有测试） | 尺寸 ≤ 上限断言 |
| TC-P44-P7-01 | multi_cast SAME 模式零拷贝共享：逐击结果与改造前一致（total_damage/hits 逐位相同）且 snapshot_mode 冻结语义不破 | `tests/unit/test_magic_system.gd`（既有 TC-CARD-S2-* 扩展） | hits 长度与 damage 等价 |
| TC-P44-P7-02 | magic_rule_registry 查询零拷贝：返回引用与内部配置同实例（只读共享），reload 后新引用生效 | 上述套件 | 引用一致性 + reload 后新值 |
| TC-P44-P7-03 | ground_loot cleanup 单遍过滤：衰变清理后活跃列表正确、drop_id 反查稳定 | `tests/unit/test_ground_loot.gd` | cleanup 后 active 集合与基线一致 |
| TC-P44-E1~E4 | E 类登记边界：本卷对 E1~E4 **零代码改动**（坐标/击杀事件签名/位移桥接/渲染宿主均未触碰） | 验收人核对 git diff 范围 | diff 不含 E 类文件 |

## 二、回归零漂移基线

- 既有断言 **379/379 全绿**（Phase 43 闭环基线）——P1~P7 改造后零回归；
- 既有套件计数不变（**不新增测试文件**，全部扩展现有套件，避免 TestRegistry/TC-ARCH-03 计数震荡——沿用 Phase 43 测试扩展策略）；
- check-gdscript 三 scope（backend/tests/frontend）0 失败。

## 三、门禁清单（固定顺序，不可调换；失败修复后从头重跑）

1. `check-gdscript.sh`（backend 189 / tests 83 / frontend 25 全 0 失败）
2. `test-run`（test_runner.gd：78/78 套件、379/379 + 新增断言全绿）
3. `audit_config.py --strict`（含本卷新增配置键结构校验：server_grant/npc/chat_command 三组 max_entries 键零死键、类型合法）
4. `audit-arch`（TC-ARCH-01~06 全 PASS）
5. `audit-all`（17/17 PASS）
6. `audit-docs.sh --baseline`（0 新增违规——新卷 4 份细则 + README 经 NAMING-FILE/LAYOUT/DATE-STAMP/LINK-DEAD 校验）

## 四、范围外复核（实施后 diff 核验）

- Phase 43 已改文件（auth_service/gm_audit_service/equipment_fsm/disposal_pipeline/combat_pipeline_fsm/gacha/world_boss/ground_loot_event_listener/multi_cast_solver/views.json/ui.json/admin.json/domains.json/monster.json/matter_disposal.json/combat.json 等）——本卷**零编辑**（引用处兼容即可）；
- Phase 45 并行工作树（tertiary_timeline_engine/combat_round_coordinator/action_card_staging_bar/combat_initiative_solver/combat_timeline_replay_audit + test_combat_tertiary_timeline）——不读不改不归并；
- auto-refactor 项目全部改动——不触碰。

## 五、闭环判定

- 门禁全绿 + 总索引/README Phase 44 S1~S4 标记 ✅（第 2 轮闭环执行时）——本细则为第 1 轮交付，获批后实施 P1~P7 并跑全量门禁。
