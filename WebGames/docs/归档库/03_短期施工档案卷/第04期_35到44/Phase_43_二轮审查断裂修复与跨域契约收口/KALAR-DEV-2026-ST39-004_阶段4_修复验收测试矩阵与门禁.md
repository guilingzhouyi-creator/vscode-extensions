---
档号: KALAR-DEV-2026-ST39-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST39 (Phase_43_二轮审查断裂修复与跨域契约收口)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_43_二轮审查断裂修复与跨域契约收口 —— 阶段4：修复验收测试矩阵与门禁
形成日期: 2026-09-03
归档日期: 2026-09-03（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 门禁
---

# 施工细则：二轮审查断裂修复与跨域契约收口 —— 阶段4：修复验收测试矩阵与门禁

> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST39-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST39-001_阶段1_装备同步与处置守恒数据契约.md) ｜ [阶段2](KALAR-DEV-2026-ST39-002_阶段2_击杀掉落链路与装备同步算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST39-003_阶段3_跨域清单对账与配置收口.md) ｜ **阶段4 (当前)**

> **施工开始日期**：2026-09-03（上午）
> **施工状态**：✅ 已施工完毕（2026-09-03 第2轮闭环，6套件扩展断言全绿 + 全量门禁通过，范围外登记已移交）

## 一、测试扩展策略

不新增测试文件（避免 `TestRegistry`/TC-ARCH-03 计数震荡），全部扩展现有套件并同步修正受行为修复影响的既有断言：

| 套件 | 扩展/修正 | 对应断裂 |
| :--- | :--- | :--- |
| `tests/unit/test_equipment_loadout.gd` | 修正 TC-EQUIP-03/04 适配镜像同步语义；新增「穿包扩容」「满包换包守卫回滚」「分歧守卫」断言 | N1 |
| `tests/unit/test_matter_disposal.gd` | 新增多产量守恒断言（实例数==台账数）；`_test_furnace_disposal_and_residue_generation` 适配逐件发放 | N3 |
| `tests/unit/test_ground_loot.gd` | 新增「战斗击杀→监听器→按 id 拾取出队」链路断言 | N2 |
| `tests/unit/test_account.gd` | 新增会话有界化断言（过期优先清理） | N10 |
| `tests/unit/test_admin_sandbox.gd` | 新增审计日志上限断言 | P2-12 |
| `tests/unit/test_inventory.gd` | 新增 `unequip_item_ref` 聚合辅助断言 | N3 配套 |

## 二、验收矩阵（DoD）

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-P43-S4-01 | 穿包扩容 | 空 inventory 经 FSM 穿 BACKPACK(volume=5) | 容量 0→5，storage 空且 equipped_slots["BACKPACK"]==该实例 |
| TC-P43-S4-02 | 满包换包回滚 | BACKPACK 穿戴且储物 5/5 → 换护甲 | `INVENTORY_CAPACITY_EXCEEDED`，恢复后储物体积/穿戴引用与调用前等价 |
| TC-P43-S4-03 | 卸下回容量 | 满 5/5 卸下 BACKPACK | `INVENTORY_CAPACITY_EXCEEDED`（卸包后容量不足以装回自身内容为合法拒斥），非容物品卸下成功 |
| TC-P43-S4-04 | 多件产物守恒 | 大质量物品销毁 | 背包新增 slag 实例数 == `yield_slag_ash_count`，FORGED 记账数一致 |
| TC-P43-S4-05 | 击杀掉落拾取闭环 | `is_monster=true` 受击者 hp→0，`attempt_pickup_from_ground` | success=true；监听器活跃掉落减一；统计 GRANTED +1 |
| TC-P43-S4-06 | 会话有界 | 注入超 `session/max_entries` 会话 | 尺寸 ≤ 上限；过期条目先于有效条目被清理 |
| TC-P43-S4-07 | 审计有界 | 注入超 `audit/max_entries` 审计 | `audit_log.size() ≤ 上限`，最旧先出 |
| TC-P43-S4-08 | 通道零重复落报 | 十连与阶段迁移后检查叙事包 | 叙事事件数与补发前一致（纯结构化通道不产文案） |

## 三、门禁全量验收清单（第 2 轮收口前逐项留痕）

- [x] `godot --headless -s res://tests/test_runner.gd` 全域套件 100% PASS（含 6 个扩展套件新增断言）
- [x] `bash scripts/sh/audit-arch.sh` TC-ARCH-01~06 全 PASS（清单变更后复检）
- [x] `python scripts/py/audit_config.py --strict` 通过（新键/格式/必需表）
- [x] `bash scripts/sh/audit-docs.sh --json` 新增违规 0（四阶段细则落档）
- [x] 二轮审查借用扫描复跑：depends_on 断裂仅剩共享表豁免项
- [x] `git status --porcelain` 仅含本卷申报文件（细则×4 + 台账 + 代码 + 配置 + 测试）

## 四、范围外登记（移交后续案卷/长期演进区）

1. `GameSaveAssembler`/`InventoryTransaction` 生产接线（存档/事务端到端）——需玩法侧存档入口设计，非缺陷修复。
2. solver `-> void` 角色重划（cdkey `_record_failure`、`red_dot_tree_solver`、`generic_affix_solver`、`seal_solver.rollback_seal`、`ascension_solver.apply_ascension`）——纯重构，行为中性，宜独立案卷批量处理。
3. `CopywritingResolver` 家族 `code`→`error_code` 契约归一——涉及多套件断言联动。
4. `profession_promoted` 信号消费方（联机属性同步预留）与 `frontend/i18n` 双词典全量对账——依赖下游消费设计。
