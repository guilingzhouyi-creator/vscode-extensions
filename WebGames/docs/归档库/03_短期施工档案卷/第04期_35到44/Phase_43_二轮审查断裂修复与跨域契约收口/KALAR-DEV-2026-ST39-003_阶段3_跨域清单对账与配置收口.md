---
档号: KALAR-DEV-2026-ST39-003
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST39 (Phase_43_二轮审查断裂修复与跨域契约收口)
件号: 003
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_43_二轮审查断裂修复与跨域契约收口 —— 阶段3：跨域清单对账与配置收口
形成日期: 2026-09-03
归档日期: 2026-09-03（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: depends_on 补齐清单; _meta; field_notes; domains.json
---

# 施工细则：二轮审查断裂修复与跨域契约收口 —— 阶段3：跨域清单对账与配置收口

> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST39-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST39-001_阶段1_装备同步与处置守恒数据契约.md) ｜ [阶段2](KALAR-DEV-2026-ST39-002_阶段2_击杀掉落链路与装备同步算法实现.md) ｜ **阶段3 (当前)** ｜ [阶段4](KALAR-DEV-2026-ST39-004_阶段4_修复验收测试矩阵与门禁.md)

> **施工开始日期**：2026-09-03（上午）
> **施工状态**：✅ 已施工完毕（2026-09-03 获批并闭环，domains.json借用对账与共享表豁免/配置表变更全量落地）

## 一、domains.json 借用对账（P2-1）

### 1.1 depends_on 补齐清单（可归属借用，逐条对应代码字面量）

| 域 | 新增 depends_on | 代码证据 |
| :--- | :--- | :--- |
| account | `["world_navigation"]` | `account_slot_binding_solver.gd:63` 读 `domains.world` |
| organization_guild | `["world_navigation"]` | `organization_aggregate.gd:48` |
| spatial_merchant | `["world_navigation"]` | `merchant_shop_aggregate.gd:21` |
| spatial_movement | `["world_navigation"]` | `spatial_location_entity.gd:12` |
| ground_loot | `["world_navigation"]` | `ground_dropped_item_aggregate.gd:19` |
| matter_disposal | `["world_navigation"]` | `item_disposal_facility_dto.gd:18` |
| world_state | `["world_navigation"]` | `world_instance.gd:22,36`、`world_recombination_stub.gd:17,34,40` |
| quest_causality | `["inventory"]` | quest 侧读 `domains.inventory` 物品键段 |
| admin_sandbox | `["currency_economy"]` | clawback 读 `domains.currency` 债务上限段 |
| mail_system | `["inventory"]` | 邮件附件发放读 `domains.inventory` 容量/默认段 |

### 1.2 共享表豁免契约（`_meta.field_notes` 增补）

新增字段说明：`shared_tables`——`domains.attribute` / `domains.economy` / `domains.event_probability` / `domains.magic_tiers` / `domains.quality_tiers` 为跨域共享数值表（无单一属主域），借用方**无需**声明 depends_on；该豁免与基础设施层 `infrastructure.*` 豁免同构。受影响域（character_creation / trading_logistics / narrative_orchestration / item_namespace_registry）维持 `depends_on` 现状，漂移以此契约收口。

## 二、配置表变更清单（全部先落表后落码，符合顺序铁律）

| 表 | 变更 | 消费方 |
| :--- | :--- | :--- |
| `config/infrastructure/admin.json` | 新增 `session/max_entries: 1000`、`audit/max_entries: 500` | auth_service / gm_audit_service（阶段2） |
| `config/domains/combat.json` | `participant_defaults` 增 `is_monster: false` | CombatParticipant 字段兜底 |
| `config/domains/monster.json` | 新增 `loot/default_drop_canonical_ids` | 战斗击杀发射器 |
| `config/domains/matter_disposal.json` | 移除 `yield_mass` 段（死键退役） | 无（阶段2 移除读取） |
| `config/frontend/views.json` | 回填 fe03/fe05/fe06/fe09/fe12 共 25 键（值=当前代码兜底值，改表即改行为）+ 新增 `rarity_labels` 段 | 对应前端视图 / `placeholder_filler.format_rarity` |
| `config/frontend/ui.json` | 汇总/横幅文案键值口径「业务领域→测试套件」同步 | main_dashboard |
| `config/infrastructure/domains.json` | §1.1 借用声明 + §1.2 field_notes | TC-ARCH-01 / GameSaveAssembler / EventBus.resolve_narrative_table |

## 三、门禁自检顺序（实现完成后执行）

1. `python scripts/py/audit_config.py --strict`（新键命名/格式一致性/必需表）；
2. `bash scripts/sh/audit-arch.sh`（TC-ARCH-01~06：`domains.json` 变更后双向差集、键级漂移复检）；
3. `bash scripts/sh/audit-docs.sh --json`（新增四阶段细则文档质量棘轮）；
4. 全域测试 `godot --headless -s res://tests/test_runner.gd`。

## 四、工程化验收矩阵

| 检验项 ID | 校验目标 | 验证方式 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-P43-S3-01 | 清单借用对账 | 二轮审查同款借用扫描脚本复跑 | 未声明借用仅剩共享表豁免项，0 断裂 |
| TC-P43-S3-02 | 新增键全落地 | TC-ARCH-06 + 配置扫描 | `sync/mode` 式静默回退 0 处 |
| TC-P43-S3-03 | 死键退役 | 配置死键审计 | `yield_mass` 段 0 残留 |
| TC-P43-S3-04 | 文档棘轮零新增 | audit-docs --baseline | 新增违规 0 |
