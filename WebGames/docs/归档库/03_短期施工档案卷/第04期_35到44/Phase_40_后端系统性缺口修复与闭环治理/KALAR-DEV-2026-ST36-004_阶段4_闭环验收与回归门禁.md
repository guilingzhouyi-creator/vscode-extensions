---
档号: KALAR-DEV-2026-ST36-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST36 (Phase_40_后端系统性缺口修复与闭环治理)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_40_后端系统性缺口修复与闭环治理 —— 阶段4：闭环验收与回归门禁
形成日期: 2026-09-02
归档日期: 2026-09-03（中午）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 路径完整性验证; 闭环验收; 回归门禁; elite.json
---

# 施工细则：后端系统性缺口修复与闭环治理 —— 阶段4：闭环验收与回归门禁

> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST36-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST36-001_阶段1_系统性缺口数据契约与修复边界.md) ｜ [阶段2](KALAR-DEV-2026-ST36-002_阶段2_P0P1核心缺口修复实现.md) ｜ [阶段3](KALAR-DEV-2026-ST36-003_阶段3_P2P3治理收口与边界文档化.md) ｜ **阶段4 (当前)**

> **施工开始日期**：2026-09-02
> **施工状态**：✅ 已施工完毕（2026-09-02 获批并闭环，TC-FINAL 全矩阵 + audit-all/check-gdscript 已通过，总索引已同步）

## 一、验收范围

本阶段承接阶段2/3的全部修复，形成系统性验收矩阵与门禁闭环。

## 二、地面拾取链全路径验收

### 路径完整性验证（GAP-02 + GAP-01 联合）

| 检验项 ID | 场景 | 验证步骤 | 预期输出 |
| :--- | :--- | :--- | :--- |
| `TC-FINAL-LOOT-01` | 正常拾取（有效 canonical_id，传入 library） | 创建 `GroundDroppedItemAggregate`→设置 `item_entity.template_id`→调用 `attempt_pickup`→查询 library | 物品进入背包；`library.find_item_node` 返回 `current_quantity=1`；无运行时错误 |
| `TC-FINAL-LOOT-02` | 正常拾取（不传 library） | 同上，不传 `library` 参数 | 物品进入背包；向后兼容；不崩溃 |
| `TC-FINAL-LOOT-03` | 已衰变掉落物 | `drop.is_decayed = true`→调用 `attempt_pickup` | 返回 `success:false, error_code:ITEM_DECAYED`；library 不被调用 |
| `TC-FINAL-LOOT-04` | 超出拾取半径 | 玩家位置距掉落超过 `pickup_radius_meters` | 返回 `success:false, error_code:OUT_OF_PICKUP_RANGE` |
| `TC-FINAL-LOOT-05` | 归属保护期内非击杀者 | `protection_remain_seconds > 0`，传入非 `killer_account_id` | 返回 `success:false, error_code:PROTECTED_BY_KILLER` |
| `TC-FINAL-LOOT-06` | 背包满 | `inventory.add_item` 返回 false | 返回 `success:false, error_code:INVENTORY_FULL`；library 不被调用；`drop.item_entity` 未被置 null |

## 三、精英词缀概率分布验收（GAP-04）

| 检验项 ID | 场景 | 验证步骤 | 预期输出 |
| :--- | :--- | :--- | :--- |
| `TC-FINAL-AFFIX-01` | 确定性验证 | 相同种子 `DeterministicRNG`，调用 `roll_random_affixes(1, 2, rng)` 两次 | 两次返回完全相同的 `Array[String]` |
| `TC-FINAL-AFFIX-02` | 后端算法主权 | 100次 `roll_random_affixes(1, 3, null)` 统计词缀频率 | 词缀出现频率与 `elite.json` `weight` 字段成正比（允许 ±15% 统计误差） |
| `TC-FINAL-AFFIX-03` | Tier 回退 | 传入不存在的 tier（如 `tier=99`） | 回退 `global` 池，返回有效词缀数组（非空） |
| `TC-FINAL-AFFIX-04` | 空池安全 | 临时清空 `affix_pool` 配置 | 返回 `[]`，不崩溃，不报错 |
| `TC-FINAL-AFFIX-05` | 原接口零回归 | 调用 `apply_elite_affixes(monster, ["BLOODSUCKER"])` | 怪物词缀设置行为与修复前完全一致 |
| `TC-FINAL-AFFIX-06` | `pick_weighted` 零除保护 | 所有词缀 `weight=0` | 退化为均匀随机，不产生除零错误，正常返回 |

## 四、委托幂等验收（GAP-03）

| 检验项 ID | 场景 | 验证步骤 | 预期输出 |
| :--- | :--- | :--- | :--- |
| `TC-FINAL-COMM-01` | 接受委托重复提交 | 同一 `transaction_id` 调用 `accept_commission` 两次 | 保证金只扣一次；第二次返回缓存结果 |
| `TC-FINAL-COMM-02` | 结算成功重复提交 | 同一 `transaction_id` 调用 `settle_successful_commission` 两次 | 奖励只发一次；公会金库只增一次 |
| `TC-FINAL-COMM-03` | 违约重复提交 | 同一 `transaction_id` 调用 `handle_commission_breach` 两次 | 没收只一次；公会金库只增一次 |
| `TC-FINAL-COMM-04` | 不传 transaction_id | 现有调用方风格（无 tx_id） | 每次调用均执行，行为与修复前完全一致 |

## 五、掉落事件总线验收（GAP-05）

| 检验项 ID | 场景 | 验证步骤 | 预期输出 |
| :--- | :--- | :--- | :--- |
| `TC-FINAL-DROP-01` | 事件接线正常 | 发出 `monster.killed` 含2个有效 `drop_declarations` | `GroundLootEventListener.get_active_drops().size()` 增加2 |
| `TC-FINAL-DROP-02` | 无效 canonical_id 防御 | `drop_declarations` 含不存在注册表中的 `canonical_id` | 安全跳过；`_active_drops` 不增加；不崩溃 |
| `TC-FINAL-DROP-03` | 衰变清理 | `_active_drops` 含已衰变掉落物→调用 `cleanup_decayed()` | 衰变掉落物从 `_active_drops` 移除 |
| `TC-FINAL-DROP-04` | 与拾取链联动 | 触发 `monster.killed`→等待监听器处理→调用 `attempt_pickup` | 完整的怪物死亡→掉落生成→玩家拾取→背包入账链路贯通 |

## 六、CDKey 职责边界文档化验收（GAP-06）

| 检验项 ID | 场景 | 验证方式 | 预期输出 |
| :--- | :--- | :--- | :--- |
| `TC-FINAL-CDKEY-01` | 开发通道标注 | `grep "[DEV CHANNEL]" reward_dispatch_pipeline.gd` | 文件头含 `[DEV CHANNEL]` 标注和警告注释 |
| `TC-FINAL-CDKEY-02` | 运营通道标注 | `grep "[PRODUCTION CHANNEL]" cdkey_redemption_solver.gd` | 文件头含 `[PRODUCTION CHANNEL]` 标注 |
| `TC-FINAL-CDKEY-03` | 代码无改动 | `git diff --stat HEAD` | 仅注释变更，无逻辑行变更 |

## 七、全量门禁检查

本阶段施工完毕后运行以下门禁：

```
bash scripts/sh/audit-all.sh        # 18 项静态门禁，无新增违规
bash scripts/sh/check-gdscript.sh   # GDScript 语法检查，无新增错误
npm run test:unit                   # 现有单元测试 0 回归
```

| 门禁项 | 检查内容 | 通过条件 |
| :--- | :--- | :--- |
| `audit_gd` | 硬编码/命名/简化规范 | 无新增违规；已有违规数不增加 |
| `audit_config` | 配置驱动一致性 | `elite.json` `affix_pool` 段格式合规；`weight` 字段存在 |
| `check-gdscript` | GDScript 语法 | 0 新增语法错误 |
| 单元测试基线 | 全量单测 | 测试通过数 ≥ 修复前 |
| `audit-docs` | 文档棘轮 | 0 新增文档违规 |

## 八、本卷修复影响文件汇总

| 文件 | 改动类型 | GAP |
| :--- | :--- | :--- |
| `backend/domains/ground_loot/ground_loot_pickup_solver.gd` | 修改（API对齐 + 统计接入 + 签名扩展） | GAP-02 / GAP-01 |
| `backend/domains/elite_mutation/generic_affix_solver.gd` | 修改（新增方法，不删改现有） | GAP-04 |
| `backend/infrastructure/deterministic_rng.gd` | 修改（新增 `pick_weighted` 方法，如未存在） | GAP-04 |
| `config/domains/elite.json` | 修改（追加 `affix_pool` 段） | GAP-04 |
| `backend/domains/commission_quest/commission_fsm.gd` | 修改（追加幂等字典 + 可选参数） | GAP-03 |
| `backend/domains/ground_loot/ground_loot_event_listener.gd` | **新增** | GAP-05 |
| `backend/domains/chat_command/reward_dispatch_pipeline.gd` | 仅注释变更 | GAP-06 |
| `backend/domains/cdkey_voucher/cdkey_redemption_solver.gd` | 仅注释变更 | GAP-06 |
