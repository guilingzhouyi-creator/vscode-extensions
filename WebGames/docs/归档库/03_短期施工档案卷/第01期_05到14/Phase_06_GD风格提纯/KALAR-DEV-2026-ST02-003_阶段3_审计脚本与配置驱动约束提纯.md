---
档号: KALAR-DEV-2026-ST02-003
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST02 (Phase_06_GD风格提纯)
件号: 003
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_06_GD风格提纯 —— 阶段3：审计脚本与配置驱动约束提纯
形成日期: 2026-08-31
归档日期: 2026-09-01
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 审计脚本; 配置驱动约束提纯
---

# 施工细则：Phase 06 GD老练风格提纯 —— 阶段3：审计脚本与配置驱动约束提纯

> [!NOTE]
> **【施工目标】**：提纯审计脚本与配置驱动约束，覆盖 `frontend` 与 `static` 盲区，收敛尾随空白与预留叙事表。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST02-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST02-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：[WebGames 配置分层规范](..\..\..\..\..\config\README.md) 与 [audit_docs 规则注册表](..\..\..\..\..\scripts\py\audit_docs.py)。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST02-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST02-001_阶段1_变量命名与GD类型契约规范.md) ｜ [阶段2](KALAR-DEV-2026-ST02-002_阶段2_硬性写法与工程品味实现.md) ｜ **阶段3 (当前)** ｜ [阶段4](KALAR-DEV-2026-ST02-004_阶段4_全量门禁与品味验收.md)

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：[audit_docs.py --rules](..\..\..\..\..\scripts\py\audit_docs.py) ｜ [config/README.md 文件格式规范](..\..\..\..\..\config\README.md)
* **核心不变量约束断言**：`audit_gd_style 覆盖 backend/frontend；audit_hardcode 覆盖 backend+frontend；audit_config_unused 单机预留 9 张叙事表不计死配置`。
* **防漂移最高指示**：严禁 `static var UPPER_SNAKE` 逃逸、`frontend` 游离门禁、尾随空白堆积。

---

## 一、 配置驱动与工程化改造 (Generalization)

```gdscript
# 模块路径: scripts/py/audit_gd_style.py
roots = [ROOT / "backend", ROOT / "frontend", ROOT / "benchmarks"]  # 新增 frontend
m = re.search(r"^\s*(?:static\s+)?var\s+([A-Za-z_][A-Za-z0-9_]*)\s*[:=]", ln)  # 覆盖 static
```

```gdscript
# 模块路径: scripts/py/audit_config_unused.py
RESERVED_NARRATIVES = {"narratives.event_driven_audio", ...}  # 9 张联机预留不计死配置
for search_dir in (BACKEND_DIR, FRONTEND_DIR):  # 覆盖 frontend 动态叙事
```

| 审计脚本 | 改造前盲区 | 改造后覆盖 |
| :--- | :--- | :--- |
| `audit_gd_style` | `static var` 漏检、`frontend` 0 覆盖 | `static var/func/const` 正则 + `backend/frontend` 默认 |
| `audit_hardcode` | 仅 `backend` | `backend+frontend` 2264 候选 |
| `audit_config_unused` | 仅 `backend`，9 张预留误报死配置 | `backend+frontend` + 预留 9 张 `ℹ` 提示 |

---

## 二、 边界与合法性约束

| 约束字段 | 类型 | 边界范围 | 违规处理 |
| :--- | :--- | :--- | :--- |
| `RESERVED_NARRATIVES` | `Set[String]` | 9 张联机预留 | 单机不计死配置，联机启用需补 `_msg` |
| `trim_trailing_whitespace` | `Bool` | `.editorconfig: true` | 94 文件已清理，0 残留 |

## 三、 验收矩阵 (DoD Matrix)

| 检验项 ID | 测试目标 | 上游真理约束指针 | 输入断言 | 预期输出断言 |
| :--- | :--- | :--- | :--- | :--- |
| `TC-P06-S3-01` | 前端覆盖 | [audit_gd_style.py:84](..\..\..\..\..\scripts\py\audit_gd_style.py) | `audit_gd_style` 默认扫描 | 168 文件（43 域）通过 |
| `TC-P06-S3-02` | 预留收敛 | [audit_config_unused.py:13](..\..\..\..\..\scripts\py\audit_config_unused.py) | `audit_config_unused` | `未引用 0 / 预留 9` |
| `TC-P06-S3-03` | 尾随空白 | [.editorconfig](../../../../../.editorconfig) | `rg "[ \t]+$" backend/` | 0 命中 |
