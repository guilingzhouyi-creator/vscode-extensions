---
档号: KALAR-DEV-2026-ST01-001
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST01 (Phase_05_核心引擎演进)
件号: 001
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_05_核心引擎演进 —— 阶段1：游戏主循环与状态栈契约
形成日期: 2026-08-31
归档日期: 2026-09-01
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: GameLoopStateStack; 游戏主循环; 状态栈契约
---

# 施工细则：Phase 05 核心引擎演进 —— 阶段1：游戏主循环与状态栈契约

> [!NOTE]
> **【施工目标】**：定义卡拉尔世界引擎顶层生命周期主循环状态机、游戏主状态栈（GameLoopStateStack）与场景切入切出 DTO 契约。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST01-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST01-ATT_附件_案卷共享契约与上下文.md)。
> **对应需求源**：[后端架构需求表索引](../../../../后端架构/后端架构需求表索引.md)。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST01-ATT_附件_案卷共享契约与上下文.md) ｜ **阶段1 (当前)** ｜ [阶段2](KALAR-DEV-2026-ST01-002_阶段2_2D瓦片物理与视口跟随实现.md) ｜ [阶段3](KALAR-DEV-2026-ST01-003_阶段3_世界时钟推进与NPC离线自发演化.md) ｜ [阶段4](KALAR-DEV-2026-ST01-004_阶段4_联机权威同步与验收测试.md)

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：[一、 数据结构与领域实体](../../../../后端架构/后端架构需求表索引.md)
* **核心不变量约束断言**：`GameLoopStateStack 必须保证栈顶单活，状态转换幂等且不丢失全局上下文`。
* **防漂移最高指示**：严禁私自硬编码场景状态字符串，全部使用 GameConfig 与枚举强类型。

---

## 一、 数据结构设计与代码契约 (Data Structures & Code Contracts)

```gdscript
# 模块路径: res://backend/infrastructure/game_loop_fsm.gd
class_name GameLoopStateStack extends RefCounted:
    enum EngineState {
        BOOT_INIT,          # 引擎冷启动与配置预热
        LOGIN_AUTH,         # 鉴权登录与会话初始化
        CHARACTER_SELECT,   # 创角与角色槽位选择
        WORLD_EXPLORATION,  # 大地图探索与城镇漫游
        COMBAT_ENCOUNTER,   # 局部战术回合侵彻战斗
        SETTLEMENT_REWARD,  # 战后结算与任务交付
        PAUSED_MODAL        # 模态弹窗与暂停冻结
    }

    var state_stack: Array[EngineState] = []
    var current_state: EngineState = EngineState.BOOT_INIT
    var global_context: Dictionary = {}

    func push_state(new_state: EngineState, context_payload: Dictionary = {}) -> void:
        state_stack.append(new_state)
        current_state = new_state
        global_context.merge(context_payload, true)

    func pop_state() -> EngineState:
        if state_stack.size() > 1:
            state_stack.pop_back()
            current_state = state_stack.back()
        return current_state
```

---

## 二、 边界与合法性约束 (Validation Constraints)

| 约束字段 | 类型 | 边界范围 | 违规处理 |
| :--- | :--- | :--- | :--- |
| `state_stack` | `Array[EngineState]` | 长度 $\in [1, 10]$ | 栈深超限触发警告并阻止深层入栈 |
| `global_context` | `Dictionary` | 键名非空 String | 屏蔽无效上下文注入 |
