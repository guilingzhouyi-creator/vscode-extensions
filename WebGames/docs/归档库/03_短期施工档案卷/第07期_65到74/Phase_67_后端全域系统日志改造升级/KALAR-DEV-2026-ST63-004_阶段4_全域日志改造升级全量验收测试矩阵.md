---
档号: KALAR-DEV-2026-ST63-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST63 (Phase_67_后端全域系统日志改造升级)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_67_后端全域系统日志改造升级 —— 阶段4：全域日志改造升级全量验收测试矩阵
形成日期: 2026-09-06
归档日期: 2026-09-09（晚上）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 配置与迁移门禁用例; log.json
---

# 施工细则：Phase 67 后端全域系统日志改造升级 —— 阶段4：全域日志改造升级全量验收测试矩阵

> 施工开始日期：2026-09-06
> 验收完成日期：2026-09-06
> 责任人：卡拉尔世界引擎架构组
> 状态：✅ 已完成（DoD 100% 达成，85 套测试 574 项全 PASS，18 项门禁全绿）

> [!NOTE]
> **【施工目标】**：对阶段1~3 交付物（`StructuredLogRecord` / `RedactionRule` / `TelemetryAggregate` / `LogQueryDTO` / `LogCollector` / `LogRingBuffer` / `LogQueryService` / `LogRetentionCleaner` / `log.json` 四段配置 / 存量迁移 M1~M9）建立全量无头测试验收矩阵，覆盖：
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST63-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST63-ATT_附件_案卷共享契约与上下文.md)。
> 1. 数据模型契约（结构化记录超集、脱敏三模式、遥测分位、检索契约）；
> 2. 采集/缓冲/遥测/检索/保留期核心算法；
> 3. 脱敏安全红线（Token/密钥零明文）与存量迁移门禁（print 零残留）；
> 4. 与 P66 错误底座协同（错误码走 `ErrorReporter`、日志走 `LogCollector`，边界不重叠）及既有套件零回归。
> **对应需求源**：后端无头模块四维度审查报告 → 维度四日志体系升级；P67 阶段1~3 契约（Inv-LS-1~5）验收闭环。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST63-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST63-001_阶段1_全域结构化日志数据模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST63-002_阶段2_日志采集导出脱敏遥测聚合与检索算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST63-003_阶段3_log.json全域升级配置与存量日志点迁移工程化.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  - `docs/README.md` 三 §3.3 改后验收：重跑 `audit-docs.sh --baseline` → 新增违规 = 0；
  - `AGENTS.md` WebGames 门禁链：`test-run.sh → check-gdscript.sh → audit-all.sh`（含配置值域/文档棘轮）；
  - `tests/unit/test_game_lifecycle_pipeline.gd`：既有管线测试形态参考（单例复位模式）；
  - `tests/test_registry.gd`：新测试套件登记（当前 84 套 → 85 套，P66 新增 1 套后基数）。

* **核心不变量约束断言**：
  - `Inv-LS-1`（结构化完整性不变量）：全域日志经 `LogCollector` 采集，全库业务路径 `print(` 零残留（迁移 M1~M9 完成）；
  - `Inv-LS-2`（脱敏不可逆不变量）：构造含敏感键 context 的记录后，落盘/缓冲/检索零明文（安全红线测试）；
  - `Inv-LS-3`（链路追踪不变量）：业务入口 `begin_trace` 后子日志 trace_id 一致，跨域可串联；
  - `Inv-LS-4`（遥测单向不变量）：`consume` 不改 record，遥测开关不影响日志级别/内容；
  - `Inv-LS-5`（检索只读不变量）：检索路径零写操作断言。

* **防漂移最高指示**：
  - 禁止为通过门禁删除/放宽断言（如删除 print 扫描规则或脱敏断言）；
  - 既有 84 套测试零回归（`test-run.sh` 全绿为硬门槛）；
  - 新套件命名遵循 `test_*.gd` 并登记 `test_registry.gd`，禁止绕过注册表直跑。

---

## 一、 测试套件与用例矩阵 (Test Suite & Case Matrix)

**新套件**：`tests/unit/test_structured_log_pipeline.gd`（class_name `TestStructuredLogPipeline`），注册入 `tests/test_registry.gd` 横切套件组。

### 1.1 数据模型契约用例（阶段1 交付物）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-LS-01 | `StructuredLogRecord` 超集兼容 | 构造全字段记录（含 trace_id/duration/redacted_keys） | 与 P66 `LogRecordDTO` 前六字段一致（消费方零改动）；`to_dto()` 深拷贝 context/redacted_keys |
| TC-LS-02 | `RedactionRule` mask 模式 | context 含 `token=TOKEN_ABC123XYZ`，规则前缀4后缀4 | 掩码值含首4尾4明文、中间 `****`；redacted_keys 含 "token" |
| TC-LS-03 | `RedactionRule` drop 模式 | context 含 `password_hash=...`，规则 drop | 键被整键丢弃（dict 无该键）；redacted_keys 含 "password_hash" |
| TC-LS-04 | `RedactionRule` hash 模式 | context 含 `account_id=ACC_1`，规则 hash | 值为 SHA-256 指纹（64 位 hex），非明文；相同输入指纹一致 |
| TC-LS-05 | 未命中规则保守兜底 | context 含未知潜在敏感键，default_mode=mask | 按 mask 兜底处理，键保留掩码值 |
| TC-LS-06 | `LogQueryDTO` 契约 | Criteria 全字段 + Result 构造 | page_size 钳制 [1,500]；Result 含 total/page/records/truncated |

### 1.2 采集/缓冲/遥测/检索算法用例（阶段2 交付物）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-LS-07 | `LogCollector.begin_trace` 签发 | 业务入口调用 | 返回非空 trace_id；后续 `log()` 记录继承同一 trace_id（Inv-LS-3） |
| TC-LS-08 | 构造期脱敏入流 | `log()` 传含 token 的 context | 记录 context 无明文 token；redacted_keys 已登记（Inv-LS-2） |
| TC-LS-09 | source 字段补全 | `log()` 调用 | source_file 仅 basename（无盘符绝对路径）；source_line 为正整数 |
| TC-LS-10 | `LogRingBuffer` 有界覆盖 | 容量 3，追加 5 条 | buffer 恒 ≤3；最旧 2 条被覆盖；query 返回存活记录 |
| TC-LS-11 | 缓冲容量热重载 | 改 `structured/ring_buffer_capacity` + reload | 容量变更生效；重建保留最近 N 条（不丢冷数据） |
| TC-LS-12 | 遥测按域/分类聚合 | consume 多条 combat 记录 | slot 计数正确；total_duration_ms 累计正确 |
| TC-LS-13 | 遥测 p50/p95 分位 | 注入 10 条已知耗时样本 | p50/p95 与排序样本计算一致 |
| TC-LS-14 | 遥测窗口滑动重置 | 模拟跨 window_seconds | 满窗后槽位清空重计（Inv-LS-4 单向） |
| TC-LS-15 | 遥测不反写 record | consume 前后比对 record | record 级别/内容/context 零变化（Inv-LS-4） |
| TC-LS-16 | 检索六维过滤 | 注入多维度记录集 | trace_id/error_code/keyword/时间窗过滤命中精确 |
| TC-LS-17 | 检索双源合并 + 分页 | 环形缓冲 + 导出文件混合数据 | 合并结果正确；分页切片正确；越界页返回空 |
| TC-LS-18 | 检索截断标记 | 命中超 MAX_RESULTS | truncated=true；records 不超过上限（Inv-LS-5 只读） |
| TC-LS-19 | 脱敏安全红线（全路径） | 构造含 token/密钥的完整链路（采集→落盘→检索） | 导出文件、环形缓冲、检索结果三处零明文（Inv-LS-2 红线） |

### 1.3 配置与迁移门禁用例（阶段3 交付物）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-LS-20 | `log.json` 四段配置装载 | structured/redaction/telemetry/retention 全量配置 | 各段字段映射正确；缺段回退安全（不抛错） |
| TC-LS-21 | `LogRetentionCleaner` 过期清理 | 构造超 max_days 的轮转归档 | 过期归档删除；主文件保留；间隔节流生效（不提前清理） |
| TC-LS-22 | 全库 print 零残留 | 全库业务路径 grep `print(` | 零命中（M1~M9 迁移完成 + 无新增；scripts/py 与测试夹具豁免） |
| TC-LS-23 | 与 P66 错误底座协同 | 错误场景：ErrorReporter + LogCollector 并存 | 错误码记录走 ErrorReporter（error_code 登记）；业务日志走 LogCollector（无错误码）；二者不重复广播 |
| TC-LS-24 | 既有套件零回归 | 全量 `test-run.sh` | 84 套既有 + 新套件全绿（85/85） |

## 二、 门禁与覆盖率验收 (Gates & Coverage Acceptance)

| 门禁 | 命令 | 通过标准 |
| :--- | :--- | :--- |
| 单元测试 | `bash scripts/sh/test-run.sh` | exit 0，85/85 套件全绿（含新增 TC-LS-01~24） |
| GDScript 语法 | `bash scripts/sh/check-gdscript.sh --scope backend` | 0 解析错误 |
| 配置值域 | `bash scripts/py/audit_config.py` | 0 违规（log.json 四段值域校验通过） |
| 文档门禁 | `bash scripts/sh/audit-docs.sh --baseline benchmarks/reports/docs_baseline.json` | exit 0 且新增违规 = 0 |
| 一键门禁 | `bash scripts/sh/audit-all.sh` | 全绿（含基准/热点/覆盖/密钥/收口） |
| 覆盖补强 | `report_aggregate.py --promote` 前核对 | 新代码路径覆盖率不降（采集/脱敏/遥测/检索核心分支 ≥ 目标） |

## 三、 验收结论判定 (Acceptance Criteria)

| 判定 | 条件 | 处置 |
| :--- | :--- | :--- |
| ✅ 通过 | TC-LS-01~24 全 PASS + 门禁全绿 + 84 套零回归 | 案卷标记完成，闭环路线图总索引 |
| ⚠️ 部分通过 | 门禁绿但个别用例 FAIL | 修复后重跑；禁止删用例消音 |
| ❌ 不通过 | 门禁红（print 残留/脱敏明文/套件回归） | 回退至对应阶段修复，复审 |

## 四、 阶段4 交付物与验收断言

| 交付物 | 路径 | 验收断言 |
| :--- | :--- | :--- |
| `test_structured_log_pipeline.gd` | `tests/unit/test_structured_log_pipeline.gd` | 24 用例全 PASS；注册入 test_registry 横切组 |
| `test_registry.gd` 注册 | `tests/test_registry.gd` | 新增 `TestStructuredLogPipeline` 且套件计数 84 → 85 |
| 门禁全绿 | `audit-all.sh` | 配置/文档/脚本/热点/覆盖零新增违规 |
