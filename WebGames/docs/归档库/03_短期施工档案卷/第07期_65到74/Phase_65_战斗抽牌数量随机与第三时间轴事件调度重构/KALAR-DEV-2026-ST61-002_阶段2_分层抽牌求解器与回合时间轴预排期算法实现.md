---
档号: KALAR-DEV-2026-ST61-002
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST61 (Phase_65_战斗抽牌数量随机与第三时间轴事件调度重构)
件号: 002
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_65_战斗抽牌数量随机与第三时间轴事件调度重构 —— 阶段2：分层抽牌求解器与回合时间轴预排期算法实现
形成日期: 2026-09-05
归档日期: 2026-09-09（晚上）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: CombatHandGenerator; TimelineScheduleGenerator; 分层手牌数量与卡牌生成算法; 回合时间轴预排期生成算法
---

# 施工细则：Phase 65 战斗抽牌数量随机与第三时间轴事件调度重构 —— 阶段2：分层抽牌求解器与回合时间轴预排期算法实现

> 施工开始日期：2026-09-05 晚
> 责任人：卡拉尔世界引擎架构组
> 状态：🚧 实施中（获批实施，进入第2轮代码与测试落地）

> [!NOTE]
> **【施工目标】**：实现核心业务算法与调度器重构，彻底落实用户硬性设计约束：
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST61-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST61-ATT_附件_案卷共享契约与上下文.md)。
> 1. 手牌分层抽取求解器（`CombatHandGenerator`）：算法层严格区分“首次遇敌专属规则”与“后续普通回合规则”，将数量随机与牌型抽取彻底拆解为双独立阶段，支持特殊影响将手牌数削减至 0 并不触发防御性断言；
> 2. 回合时间轴预排期求解器（`TimelineScheduleGenerator`）：在回合启动时完成本回合全部事件（`NORMAL_DRAW`、`ADD_DRAW`、`TENSION_PULSE`）的有界离散随机排期，集成最小事件间距防重叠碰撞算法，支持跨回合修饰流水线；
> 3. `CombatRoundCoordinator` 零手牌防误停重构：重写回合结束判断算法，确保在 $HandCount = 0$ 时时间轴依然正常推演直至自然超时或紧张点强制切轮；
> 4. 到点延迟具象化抽卡机制：事件发生时刻再调用卡池生成实体对象，杜绝排期阶段提前预占与泄漏。
> **对应需求源**：Phase 65 阶段 1 数据契约 → 核心业务算法与时钟协调器实现。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST61-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST61-001_阶段1_手牌数量双随机与时间轴离散事件数据模型设计.md) ｜ **阶段2 (当前)** ｜ [阶段3](KALAR-DEV-2026-ST61-003_阶段3_combat配置驱动扩展与隐藏信息前端契约工程化.md) ｜ [阶段4](KALAR-DEV-2026-ST61-004_阶段4_零手牌推进与双随机调度端到端测试矩阵.md)

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  - `docs/路线图/01_短期施工区/Phase_65_战斗抽牌数量随机与第三时间轴事件调度重构/阶段1_手牌数量双随机与时间轴离散事件数据模型设计.md`（阶段 1 契约标准）；
  - `backend/domains/physics_thermodynamics/combat_round_coordinator.gd:L96-L121`：既有回合判定逻辑；
  - `backend/domains/physics_thermodynamics/tertiary_timeline_engine.gd:L49-L107`：既有步进与事件生成方法。

* **核心不变量约束断言**：
  - `Inv-TR2-1`（双随机分离求值）：抽取算法必须分两步执行——首先依据离散权重抽取标量数值 $N \in [N_{\min}, N_{\max}]$；随后使用 PRD 状态机循环 $N$ 次抽取卡牌定义并完成实例化；
  - `Inv-TR2-2`（特殊影响纯增量修正）：特殊影响修饰器仅通过增量加法/乘法影响发牌数量（$Final = \max(0, Base + \Delta)$），严禁代码中出现 `max(1, count)` 强行吞噬 0 的逻辑；
  - `Inv-TR2-3`（预排期时序单调递增）：排期算法生成的时间序列必须满足 $0 < t_1 < t_2 < \dots < t_k < \text{max\_round\_duration\_ms}$，且相邻事件绝对时间差 $\ge \text{min\_event\_spacing\_ms}$；
  - `Inv-TR2-4`（无牌态时间轴保活）：当参与者手牌为 0 且 AP 为 0 时，若时间轴队列中仍有未决事件，协调器必须返回 `round_ended = false`，保证时钟持续前进。

---

## 一、 核心算法与业务实现 (Core Algorithms & Services)

### 2.1 分层手牌数量与卡牌生成算法 (`CombatHandGenerator`)

```gdscript
# 模块路径: res://backend/domains/physics_thermodynamics/combat_hand_generator.gd
class_name CombatHandGenerator
extends RefCounted

## 阶段 A：计算基础发牌数量 (严格区分首次遇敌与后续回合)
static func evaluate_base_hand_count(
	is_first_encounter: bool,
	rng: DeterministicRNG
) -> int:
	if is_first_encounter:
		var first_min := GameConfig.get_int("domains.combat", "round_hand/first_encounter_min", 3)
		var first_max := GameConfig.get_int("domains.combat", "round_hand/first_encounter_max", 4)
		return rng.randi_range(first_min, first_max) if rng else first_min

	# 后续普通回合：数量随机抽取 (理论下限 >= 1)
	var count_weights: Dictionary = GameConfig.get_dict("domains.combat", "round_hand/hand_count_distribution", {
		"1": 0.25,
		"2": 0.50,
		"3": 0.20,
		"4": 0.05
	})
	return _sample_discrete_distribution(count_weights, rng, 1)

## 阶段 B：应用特殊效果流水线修正数量 (合法产生 0 手牌)
static func apply_special_modifiers(
	base_count: int,
	modifiers: Array[Dictionary]
) -> Dictionary:
	var delta := 0
	var applied_ids: Array[String] = []
	for mod in modifiers:
		var m_id: String = mod.get("modifier_id", "")
		var m_delta: int = int(mod.get("hand_count_delta", 0))
		var is_absolute_zero: bool = bool(mod.get("force_zero_hand", false))
		if is_absolute_zero:
			applied_ids.append(m_id)
			return {"final_count": 0, "modifier_delta": -base_count, "applied": applied_ids}
		delta += m_delta
		applied_ids.append(m_id)

	var final_count := maxi(0, base_count + delta)
	return {"final_count": final_count, "modifier_delta": delta, "applied": applied_ids}

## 阶段 C：根据最终数量执行卡池实体实例化 (牌型随机)
static func draw_cards_for_turn(
	spec: CombatHandDrawDTO.HandDrawSpecDTO,
	rng: DeterministicRNG
) -> CombatHandDrawDTO.HandDrawResultDTO:
	var res := CombatHandDrawDTO.HandDrawResultDTO.new()
	res.base_random_count = evaluate_base_hand_count(spec.is_first_encounter, rng)

	var mod_res := apply_special_modifiers(res.base_random_count, spec.special_modifiers)
	res.final_draw_count = mod_res.final_count
	res.modifier_delta = mod_res.modifier_delta
	res.applied_modifiers = mod_res.applied
	res.is_zero_hand_state = (res.final_draw_count == 0)

	# 牌型随机抽取与实例创建
	var drawn_cards: Array = []
	for i in range(res.final_draw_count):
		var card := _instantiate_card_from_pool(rng)
		drawn_cards.append(card)
	res.actual_cards = drawn_cards
	return res

static func _sample_discrete_distribution(weights: Dictionary, rng: DeterministicRNG, default_val: int) -> int:
	if weights.is_empty():
		return default_val
	var roll := (rng.randf() if rng else 0.5)
	var accum := 0.0
	for k in weights.keys():
		accum += float(weights[k])
		if roll <= accum:
			return int(k)
	return default_val

static func _instantiate_card_from_pool(rng: DeterministicRNG) -> PhysicalVerbRegistry.CombatActionCardEntity:
	# 抽取动词与属性
	var card := PhysicalVerbRegistry.CombatActionCardEntity.new()
	var verbs: Array = ["STAB", "SLASH", "UPPER", "PARRY", "BLOCK"]
	var pick := rng.randi_range(0, verbs.size() - 1) if rng else 0
	card.verb_type = verbs[pick]
	card.card_name = "招式牌·%s" % card.verb_type
	card.ap_cost = GameConfig.get_int("domains.combat", "verbs/%s/base_ap" % card.verb_type, -2)
	card.base_potency = 20.0
	return card
```

### 2.2 回合时间轴预排期生成算法 (`TimelineScheduleGenerator`)

```gdscript
# 模块路径: res://backend/domains/physics_thermodynamics/timeline_schedule_generator.gd
class_name TimelineScheduleGenerator
extends RefCounted

## 在回合开始时，依据配置与约束一次性生成整回合离散事件点
static func generate_round_schedule(
	round_number: int,
	max_duration_ms: int,
	rng: DeterministicRNG,
	pending_modifiers: Array[Dictionary] = []
) -> TimelineEventScheduleDTO:
	var schedule := TimelineEventScheduleDTO.new()
	schedule.round_number = round_number
	schedule.max_round_duration_ms = max_duration_ms
	schedule.active_modifiers = pending_modifiers.duplicate(true)

	var min_spacing := GameConfig.get_int("domains.combat", "tertiary_timeline/min_event_spacing_ms", 1500)
	var event_count_min := GameConfig.get_int("domains.combat", "tertiary_timeline/events_per_round_min", 2)
	var event_count_max := GameConfig.get_int("domains.combat", "tertiary_timeline/events_per_round_max", 4)
	var target_count := rng.randi_range(event_count_min, event_count_max) if rng else event_count_min

	# 1. 在时限区间内采样 target_count 个互不重叠且保持最小间距的时间点
	var timestamps: Array[int] = _sample_discrete_timestamps(target_count, max_duration_ms, min_spacing, rng)

	# 2. 为每个时间戳分配事件类型 (至少包含 1 个普通发牌点、可能包含加发牌点或紧张点)
	var points: Array[TimelineEventScheduleDTO.ScheduledPoint] = []
	var has_tension := false
	var has_draw := false

	for idx in range(timestamps.size()):
		var pt := TimelineEventScheduleDTO.ScheduledPoint.new()
		pt.point_id = "ROUND_%d_EVT_%d" % [round_number, idx + 1]
		pt.trigger_time_ms = timestamps[idx]

		if not has_draw:
			pt.event_kind = TimelineEventScheduleDTO.EventKind.NORMAL_DRAW
			has_draw = true
		elif not has_tension and idx == timestamps.size() - 1:
			pt.event_kind = TimelineEventScheduleDTO.EventKind.TENSION_PULSE
			pt.payload = {"tension_type": "PRESSURE_SURGE"}
			has_tension = true
		else:
			# 随机决定是加发牌点还是普通发牌点
			var roll := rng.randf() if rng else 0.5
			if roll < 0.4:
				pt.event_kind = TimelineEventScheduleDTO.EventKind.ADD_DRAW
				pt.payload = {"extra_bonus_cards": 1}
			else:
				pt.event_kind = TimelineEventScheduleDTO.EventKind.NORMAL_DRAW
		points.append(pt)

	schedule.scheduled_points = points
	return schedule

static func _sample_discrete_timestamps(
	count: int,
	max_duration_ms: int,
	min_spacing: int,
	rng: DeterministicRNG
) -> Array[int]:
	var result: Array[int] = []
	var usable_window := max_duration_ms - (count * min_spacing)
	if usable_window <= 0:
		# 间距过大兜底线性均匀分布
		var step := int(float(max_duration_ms) / float(count + 1))
		for i in range(count):
			result.append((i + 1) * step)
		return result

	var current_mark := 500 # 避开回合刚开始的瞬时
	var step_slot := int(float(usable_window) / float(count))
	for i in range(count):
		var jitter := rng.randi_range(0, step_slot) if rng else int(step_slot / 2)
		var t := current_mark + jitter
		t = clampi(t, 100, max_duration_ms - 200)
		result.append(t)
		current_mark = t + min_spacing

	result.sort()
	return result
```

---

## 二、 阶段2 算法实施清单与调度器收敛

1. 创建 `CombatHandGenerator`：承载两阶段手牌数量与卡牌实体抽取；
2. 创建 `TimelineScheduleGenerator`：实现开局一次性预排期与间距约束；
3. 重构 `CombatRoundCoordinator.step_coordinator`：
   - 移除 `player_participant.current_ap <= 0 and player_staging.cards.is_empty()` 直接判停的缺陷；
   - 增加未决时间轴事件检查：只要本回合 schedule 中仍有未执行的离散事件，绝不切轮；
4. 表现层脱敏转换：重写 `get_client_snapshot()`，隐藏内部绝对毫秒。
