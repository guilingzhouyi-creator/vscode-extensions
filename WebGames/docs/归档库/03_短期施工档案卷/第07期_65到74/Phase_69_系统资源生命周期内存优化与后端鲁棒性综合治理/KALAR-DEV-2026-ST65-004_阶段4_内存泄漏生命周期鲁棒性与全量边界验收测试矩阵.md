---
档号: KALAR-DEV-2026-ST65-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST65 (Phase_69_系统资源生命周期内存优化与后端鲁棒性综合治理)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_69_系统资源生命周期内存优化与后端鲁棒性综合治理 —— 阶段4：内存泄漏生命周期鲁棒性与全量边界验收测试矩阵
形成日期: 2026-09-07
归档日期: 2026-09-09（晚上）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 全量测试零泄漏不变量; 全边界安全回退不变量; 组合状态原子一致性不变量
---

# 施工细则：Phase 69 系统资源生命周期内存优化与后端鲁棒性综合治理 —— 阶段4：内存泄漏生命周期鲁棒性与全量边界验收测试矩阵

> 施工开始日期：2026-09-07 下午
> 责任人：卡拉尔世界引擎架构组
> 状态：✅ 已完成（Round 2 实施与全量验证闭环）

> [!NOTE]
> **【施工目标】**：依据阶段1~3资源治理、后端鲁棒性与统一英文日志基础设施，构建全量闭环测试与边界条件审计矩阵：
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST65-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST65-ATT_附件_案卷共享契约与上下文.md)。
> 1. 针对历史实测泄漏（151 ObjectDB、56 CanvasItem RID、20 Resource），构建严格的**内存泄漏与资源释放自动化回归套件**；
> 2. 覆盖**全量基础边界**（0, 1, 极大值, 极小值, 负数, null, 空字符串, 空集合, 超大集合, 重复/缺失/过期数据, 连续重复调用, 释放后二次访问）；
> 3. 覆盖**复杂业务组合边界**（加载+热更新、保存+热更新、保存+退出、场景切换+异步任务、角色切换+背包结算、配置更新+运行时缓存）；
> 4. 构建 4 套独立单元测试套件（共计 36 项细粒度断言：`TC-RM-01` ~ `TC-RM-36`），接入 `tests/test_runner.gd` 与全域门禁；
> 5. 确立 5 项核心验收不变量（Inv-RM-16 ~ Inv-RM-20）。
> **对应需求源**：P69 总体目标（十二、边界问题全量审查、十六、工程审查流程、十七、验收标准、十八、最终工程约束）。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST65-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST65-001_阶段1_系统资源所有权模型与内存生命周期审计设计.md) ｜ [阶段2](KALAR-DEV-2026-ST65-002_阶段2_资源释放内存回收与后端逻辑鲁棒性核心算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST65-003_阶段3_统一英文日志基础设施与配置驱动边界扩展.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  - `backend/infrastructure/resource_governance/resource_lifecycle_manager.gd`：资源生命周期纳管中枢；
  - `backend/infrastructure/resource_governance/scoped_event_subscriber.gd`：作用域事件解绑代理；
  - `backend/infrastructure/resource_governance/bounded_resource_cache.gd`：有界缓存淘汰容器；
  - `backend/infrastructure/robustness/backend_robustness_guard.gd`：后端数据防御守卫；
  - `backend/infrastructure/robustness/idempotency_controller.gd`：幂等性控制器；
  - `backend/infrastructure/logging/unified_logger_service.gd`：统一英文日志服务。
* **核心不变量约束断言**：
  - `Inv-RM-16 (全量测试零泄漏不变量)`：在全域 85+ 测试套件执行完毕后，Godot 引擎进程退出时打印的泄漏告警中，受控资源的 ObjectDB 与 RID 泄漏数严格收敛，测试期间创建的节点 100% 显式安全回收。
  - `Inv-RM-17 (全边界安全回退不变量)`：面对边界值（0、1、负数、极大极小值、null、畸形字典与超大数组），后端所有算子不得抛出未捕获崩溃或 NaN 污染，必须安全截断回退至确定性合法状态。
  - `Inv-RM-18 (组合状态原子一致性不变量)`：在热更新重载、场景重置或存档保存过程中，若遇到并发访问或中途取消，系统数据状态必须保持原子性，不出现部分写入或悬挂半状态。
  - `Inv-RM-19 (英文日志输出纯洁性不变量)`：全量测试执行期间，`UnifiedLoggerService` 分发出的所有开发者可读日志文本必须全部为规范英文，且所有占位符成功插值，严禁业务中文硬编码。
  - `Inv-RM-20 (门禁与测试 100% 通过不变量)`：全域 89 套测试套件通过率恒定 100%，`audit-all.ps1` 门禁 18 项全部 PASS，文档棘轮审查 0 Error / 0 Warn / 0 违规新增。
* **防漂移最高指示**：严禁仅以“程序目前能跑”作为通过标准；所有测试必须严格模拟边界应力环境（高频重复调用、极端脏数据、热切冲突）！

---

## 一、 测试套件规划与职责划分 (Test Suites Architecture)

全阶段共划分 4 套细粒度自动化测试，总计 36 项严密断言：

```text
tests/unit/
├── test_resource_lifecycle_governance.gd  (TC-RM-01 ~ TC-RM-10: 资源管理、节点安全销毁、事件自动解绑)
├── test_bounded_cache_and_idempotency.gd  (TC-RM-11 ~ TC-RM-20: 有界LRU淘汰、TTL过期、幂等防重控制)
├── test_backend_robustness_guard.gd       (TC-RM-21 ~ TC-RM-28: 脏数据清洗、极值截断、异常分类与隔离)
└── test_unified_logger_service.gd         (TC-RM-29 ~ TC-RM-36: 英文日志渲染、分级过滤、单机/联机隔离)
```

---

## 二、 全量验收测试用例矩阵 (DoD Assertion Matrix)

### 套件 1: `test_resource_lifecycle_governance.gd` (资源治理与生命周期释放)

| 编号 | 测试用例名称 | 测试操作与输入 | 预期断言 |
| :--- | :--- | :--- | :--- |
| `TC-RM-01` | 资源管理器单例/实例纳管 | 传入 Control 节点并调用 `track_resource` | 返回非空描述符，初始状态为 `INITIALIZED` |
| `TC-RM-02` | 状态机流转合法性校验 | 依次推进 `INITIALIZED` ➔ `IN_USE` ➔ `IDLE` | 每次流转返回 true，非法跳跃返回 false |
| `TC-RM-03` | 单个节点安全销毁与物理回收 | 对在树中的 Control 节点调用 `dispose_resource` | 节点自动脱离父级并执行销毁，管理器映射表清空 |
| `TC-RM-04` | 批量资源全量释放 | 注册 10 个虚拟资源并调用 `dispose_all` | 返回销毁数量 10，内部映射表长度为 0 |
| `TC-RM-05` | 重复释放幂等性防护 | 对同一已释放的资源 ID 再次调用 `dispose_resource` | 返回 false，不发生崩溃或 Double Free 错误 |
| `TC-RM-06` | 作用域订阅代理基础绑定 | 使用 `ScopedEventSubscriber` 订阅 2 个自定义信号 | 信号连接建立，内部托管令牌数为 2 |
| `TC-RM-07` | 作用域订阅代理重复绑定防御 | 对相同信号与相同 Callable 再次调用 subscribe | 返回 false，内部托管令牌数不重复增加 |
| `TC-RM-08` | 作用域订阅单项手动解绑 | 调用 `unsubscribe_from` 解绑指定回调 | 对应信号连接断开，内部令牌减少 1 |
| `TC-RM-09` | 宿主析构自动全量断开 | 调用 `clear()` 或 `dispose()` | 托管的所有信号连接 100% 断开，令牌数归零 |
| `TC-RM-10` | 存活资源度量指标正确性 | 创建 3 个 UI 节点和 2 个通用资源并统计 | `get_live_metrics()` 精确返回 active_ui_nodes=3, active_other=2 |

---

### 套件 2: `test_bounded_cache_and_idempotency.gd` (有界缓存与幂等性控制)

| 编号 | 测试用例名称 | 测试操作与输入 | 预期断言 |
| :--- | :--- | :--- | :--- |
| `TC-RM-11` | 缓存基础读写与默认回退 | 写入 key 并读取；读取不存在 key | 写入值精确匹配；缺失 key 返回指定 default |
| `TC-RM-12` | LRU 容量上限硬约束淘汰 | 设最大容量为 5，连续写入 8 个不同 key | 容器 size 恒 <= 5，最旧的前 3 个 key 被驱逐 |
| `TC-RM-13` | LRU 热点访问更新命中 | 访问旧 key 后写入新 key | 被访问的旧 key 保持存活，未被访问的旧 key 被驱逐 |
| `TC-RM-14` | TTL 超时自动失效与淘汰 | 设 TTL=0.1 秒，休眠 0.15 秒后读取 | 返回 default，对应条目被安全剔除 |
| `TC-RM-15` | 热重载清理通道生效验证 | 设置 `allow_hot_reload_clear=true` 并触发 | 缓存条目全部清空，size 归零 |
| `TC-RM-16` | 热重载保护通道禁用验证 | 设置 `allow_hot_reload_clear=false` 并触发 | 缓存条目不受热重载影响，全部保留 |
| `TC-RM-17` | 幂等控制器首次执行记录 | 传入全局唯一 Token 并记录成功结果 | `already_executed` 为 false，结果已登记 |
| `TC-RM-18` | 幂等控制器二次重复拦截 | 传入相同 Token 再次调用 | `already_executed` 为 true，透传返回首次结果 |
| `TC-RM-19` | 幂等控制器空 Token 安全防护 | 传入空字符串作为 Token | 返回 `already_executed = false`，不进行记录 |
| `TC-RM-20` | 幂等控制器历史记录容量上限 | 连续记录超过 200 个不同 Token | 历史字典大小不超过上限，自动剔除最旧 Token |

---

### 套件 3: `test_backend_robustness_guard.gd` (后端鲁棒性与异常隔离)

| 编号 | 测试用例名称 | 测试操作与输入 | 预期断言 |
| :--- | :--- | :--- | :--- |
| `TC-RM-21` | 整型极值与负数截断清洗 | 传入 -999，下限 0，上限 100 | 安全截断为 0，不产生越界 |
| `TC-RM-22` | 整型极大值溢出防护 | 传入 9999999，下限 0，上限 1000 | 安全截断为 1000 |
| `TC-RM-23` | null 与非法类型整数清洗 | 传入 null, "bad_str", [] 作为整型参数 | 返回指定的 fallback 默认值 |
| `TC-RM-24` | 字符串空值与前后空格清洗 | 传入 "   test_name   " 与 null | 前后空格被剔除；null 返回空字符串默认值 |
| `TC-RM-25` | 字典深拷贝与防污染隔离 | 传入包含嵌套结构的字典并清洗 | 返回深拷贝副本，修改副本不影响原字典 |
| `TC-RM-26` | 数组深拷贝与防污染隔离 | 传入包含元素的数组并清洗 | 返回深拷贝副本，互不污染 |
| `TC-RM-27` | 超大集合压力截断测试 | 传入 10000 长度的超大数组 | 守卫正常处理，不产生堆栈溢出或内存失控 |
| `TC-RM-28` | 异常分级分类确定性测试 | 模拟配置缺失、业务校验失败与底层错误 | 各异常按枚举明确分类，业务可捕获可隔离 |

---

### 套件 4: `test_unified_logger_service.gd` (统一英文日志与隔离扩展)

| 编号 | 测试用例名称 | 测试操作与输入 | 预期断言 |
| :--- | :--- | :--- | :--- |
| `TC-RM-29` | 统一日志初始化与模板库加载 | 载入包含 5 个英文模板的字典 | 模板库正确装载，内部 catalog 映射无遗漏 |
| `TC-RM-30` | 英文模板参数全量替换 | 传入模板 `{owner}` 和 `{count}` 及相应参数 | 格式化结果为纯正英文且占位符完全匹配替换 |
| `TC-RM-31` | 未定义事件码默认回退 | 传入未在 catalog 注册的事件码 | 自动使用标准回退英文模板，不抛出异常 |
| `TC-RM-32` | 严重级别过滤门槛测试 | 设置最低等级为 `ERROR`，发送 `INFO` | 返回 null，无控制台打印，无 Sink 派发 |
| `TC-RM-33` | 严重级别放行门槛测试 | 设置最低等级为 `ERROR`，发送 `CRITICAL` | 返回有效 LogRecord，严重等级为 CRITICAL |
| `TC-RM-34` | 单机模式本地日志隔离阻断 | 在单机模式下触发网络广播 Sink | 触发隔离守卫，阻断网络传播，保护本地隐私 |
| `TC-RM-35` | 性能遥测度量样本记录 | 启用 profiling 并采样 5 个内存指标 | `get_samples_by_category("MEMORY")` 返回 5 条样本 |
| `TC-RM-36` | 性能遥测度量容量有界约束 | 持续写入超过 500 个样本 | 样本队列长度严格保持为 500，自动淘汰旧样本 |

---

## 三、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 4.1: 编写资源治理与生命周期测试套件 (`test_resource_lifecycle_governance.gd`)** - 覆盖 TC-RM-01 ~ TC-RM-10，严密断言 Node 释放与信号解绑。
- [x] **Step 4.2: 编写有界缓存与幂等性测试套件 (`test_bounded_cache_and_idempotency.gd`)** - 覆盖 TC-RM-11 ~ TC-RM-20，严密断言容量驱逐与防重记录。
- [x] **Step 4.3: 编写后端鲁棒性守卫测试套件 (`test_backend_robustness_guard.gd`)** - 覆盖 TC-RM-21 ~ TC-RM-28，严密断言全边界极端脏数据输入。
- [x] **Step 4.4: 编写统一英文日志测试套件 (`test_unified_logger_service.gd`)** - 覆盖 TC-RM-29 ~ TC-RM-36，严密断言纯正英文渲染与单机联机隔离。
- [x] **Step 4.5: 全域测试总线接入与泄漏复测** - 将 4 套新测试并入 `tests/test_registry.gd`，运行 `pwsh scripts/ps1/test-run.ps1` 验证 93 套套件 100% 通过且泄漏收敛。
- [x] **Step 4.6: 全域门禁一键审查** - 执行 `pwsh scripts/ps1/audit-all.ps1`，确保 18 项全部 PASS，文档棘轮 0 新增违规。

---

## 四、 验收基线与归档判据 (DoD Criteria)

1. **测试全通**: 全域 89 套测试套件全部 PASS，600+ 断言无一失败。
2. **泄漏消除**: 受控资源（Control/Node/订阅）在测试后 100% 释放，无未纳管悬挂句柄。
3. **零硬编码**: 业务代码无自然语言日志文本硬编码，全走事件码配置驱动。
4. **全量边界验证**: 0、1、极大极小值、负数、null、空集合、重复并发均具备明确安全行为。
5. **门禁满分**: `audit-all.ps1` 18 项全部 PASS，文档规范 `audit_docs.py` 0 警告 0 错误。
