---
档号: KALAR-DEV-2026-ST66-003
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST66 (Phase_70_配置库唯一事实提纯与冗余收敛治理)
件号: 003
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_70_配置库唯一事实提纯与冗余收敛治理 —— 阶段3：冗余双写分批收敛迁移与全域接线工程化
形成日期: 2026-09-08
归档日期: 2026-09-09（晚上）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 批次迁移明细表; 接线改造范式; 遗留适配层零残留不变量; items.json
---

# 施工细则：Phase 70 配置库唯一事实提纯与冗余收敛治理 —— 阶段3：冗余双写分批收敛迁移与全域接线工程化

> 施工开始日期：2026-09-08 下午
> 责任人：卡拉尔世界引擎架构组
> 状态：🔄 Round 1 细则编制完成（待批准后开工）

> [!NOTE]
> **【施工目标】**：按阶段 1 裁决表（D1~D10）与阶段 2 判重引擎基线，执行**分批收敛迁移与全域接线工程化**，只收敛"存储与读取通道"，严禁改动任何数值/文案语义（X-4 铁律）：
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST66-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST66-ATT_附件_案卷共享契约与上下文.md)。
> 1. G1 文案迁移遗留退役：descriptions/items.json、copywriting/probability.json 退役，narratives/events.json 转占位骨架，CopywritingResolver 遗留回读分支删除；
> 2. G2 公告双写收敛：属主 copywriting.bulletin，两个 reader 改造接线，narratives.bulletin_board_maintenance 转占位骨架；
> 3. G3 CDK 奖励收敛：属主 domains.chat_command.cdkeys，account.json.cdkeys 重复段退役或改引用（现场证据裁决）；
> 4. G4 演示快照与默认值溯源治理：frontend 快照补 provenance 注解，locale 三默认语义注解互指；
> 5. G5 域表文案直书分批迁移：17 表存量按决策树迁至 narratives.<域>，白名单只减不增；
> 6. G6 错误消息双通道互认归零 + G7 文档计数与层描述同步（config/README.md），同批提交防护栏漂红。
> **对应需求源**：Phase 70 阶段 1 裁决表 + 阶段 2 引擎基线（D1~D10 零偏差复现）。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST66-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST66-001_阶段1_配置事实归属矩阵与唯一属主契约设计.md) ｜ [阶段2](KALAR-DEV-2026-ST66-002_阶段2_配置血缘静态判重引擎与漂移检测算法实现.md) ｜ **阶段3 (当前)** ｜ [阶段4](KALAR-DEV-2026-ST66-004_阶段4_唯一事实收敛零漂移全域验收与回归矩阵.md)

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**（收敛目标与防破坏基座）：
  - 读取方现状（2026-09-08 静态引用实测）：`descriptions.items` 仅 `copywriting_resolver.gd:71`（legacy 回读）与 `tests/unit/test_item_description.gd` 消费；`narratives.events` 经 `copywriting_resolver.gd:73` legacy 回读；`narratives.bulletin_board_maintenance` 由 `bulletin_push_pipeline.gd:31` 与 `bulletin_board_aggregate.gd:35` 消费；`domains.account.cdkeys` 消费方需 R2 现场 grep 复核（静态三层引用未命中则无消费）；
  - `config/README.md` 分层树为文档棘轮校验对象（计数必须与实况一致，禁止先行改测试基线）；
  - `domains.json` 的 `narrative` 字段映射要求每域有对应 narratives 表文件存在——故退役表一律**转占位骨架而非删文件**（除无映射的 `narratives.events` 与无消费的 `copywriting.probability.json`，现场证据允许时整文件删除）；
  - 占位骨架格式先例：8 张 98B `_comment` 占位表（event_driven_audio/game_settings/hardware_input/identity_disguise/localization_i18n/narrative_orchestration/notification_red_dot/telemetry_account_lifecycle）；
  - `audit_config_unused.py` `RESERVED_NARRATIVES`：占位表不判死配置的豁免机制（新增占位表须同步登记）。
* **核心不变量约束断言**：
  - `Inv-SS-10 (遗留适配层零残留不变量)`：阶段 3 结束后 `copywriting_resolver.gd` 的 `_legacy_fallback` 分支必须删除，`descriptions/` 目录与 `copywriting.probability.json` 零残留；audit_copywriting"适配层激活中"提示级项转红线（激活即 ERROR）。
  - `Inv-SS-11 (读取通道一致不变量)`：每个文案事实的读取方键位与其属主表一致；静态三层引用扫描（audit_config_unused 同款识别）0 个指向已退役/已转占位表（占位白名单除外）。
  - `Inv-SS-12 (占位骨架正式态不变量)`：占位骨架仅允许 `_comment` 单键（注明退役来源与裁决号），禁止携带任何 `%s`/`{param}`/正文文案残留。
  - `Inv-SS-13 (文档计数同批一致不变量)`：config/README.md 层表计数、层目录树与实况 120→N 表一致，同一 commit 提交（TC-ARCH-05 文档同步校验与 audit-docs baseline 0 新增）。
* **防漂移最高指示**：每批收敛前先跑判重引擎留存"批前基线"，收敛后立即复跑"批后基线"，逐批清零；禁止同批跨组混合提交（diff 不可审即回退）；禁止改动任何数值与文案语义（仅漂移裁决项按属主裁定真值并记录 diff 证据）。

---

## 一、 分批收敛迁移与工程化接线 (Convergence & Wiring)

### 1. 批次迁移明细表

| 批次 | 收敛内容 | 迁移动作（含具体路径） | 完成判据 |
| :--- | :--- | :--- | :--- |
| G1a | 物品描述双写 | 以 `copywriting/item.json` 为属主核对全量条目；`tests/unit/test_item_description.gd` 断言改指 copywriting 通道；删除 `config/descriptions/items.json` 及空目录；删除 `copywriting_resolver.gd:68-74` `_legacy_fallback` 中 item 分支 | R2 对账 0 漂移；无消费者残留 |
| G1b | 事件叙事双写 | 属主 `copywriting/narrative.json`（EVT_BEAST_TIDE / EVT_MARKET_CRASH 全分支）；`narratives/events.json` 转 `_comment` 占位骨架（注明 D2 裁决）；`_legacy_fallback` narrative 分支删除 | R2 一致/占位；R6 无违规 |
| G1c | 概率文案漂移副本 | 现场核对 EVT_BEAST_TIDE 概率文案消费方（若存在则改读属主 `copywriting.narrative` 同条目）；删除 `config/copywriting/probability.json`（无消费方时） | 引用 0 残留；文件删除 |
| G2 | 公告双写收敛 | 属主 `copywriting/bulletin.json`（三则文案与 `{server}`/`{minutes}` 占位符为唯一语义）；`bulletin_push_pipeline.gd:31` 与 `bulletin_board_aggregate.gd:35` 改经 `CopywritingResolver.resolve("bulletin.<条目>", params)`；`narratives/bulletin_board_maintenance.json` 转占位（注明 D4 裁决）；同步 `RESERVED_NARRATIVES` 白名单 | 公告三则单源；reader 键位一致 |
| G3 | CDK 奖励双写 | R2 现场复核 `domains.account.cdkeys` 消费方：无消费 → 删除重复段并加 `_meta` 说明指向 chat_command；有消费 → 改读 `domains.chat_command.cdkeys.<code>` 并删除重复段；两表 cdkey 条目以 chat_command（含 item_id）为属主 | R3 对账命中 0 孪生对 |
| G4 | 快照与默认值溯源 | `frontend/ui.json` `_meta.provenance` 注解（`player/wallet` 等快照指向属主键路径；纯演示数值标注 `demo_fixture` + 理由）；locale 三默认按 X-1 各加 `_meta.semantic_note`（localization=运行时当前语言、game_settings=玩家持久偏好、fe15=骨架默认指向属主） | R5 对账 0 漂移；注解字段 schema 校验通过 |
| G5 | 域表直书迁移 | 按决策树分批（每批 3~5 表）：combat（verbs name/desc）、character_creation（origins name/desc）、elite（affixes name/desc）、sovereignty（title_names）、world_gateway（world_name）、monster（species/part_name）…共 17 表；迁入 `narratives.<域>` 单层扁平键（`verb_stab_name` 范式），读取方同步 `_msg`/get_string 改键；迁移表同步删除域表内直书键 | 每批 R6 白名单收缩；迁移后单测全绿 |
| G6 | 错误消息互认归零 | 8 个孤儿键现场裁决：语义属错误消息 → `errors_catalog.json` 补条目（message_key 指向 narratives.errors）；纯展示语义 → manifest 备注豁免；最终 R4 双向差集 0 | R4 0 孤儿 0 悬空 |
| G7 | 文档计数同步 | `config/README.md`：目录树补齐 infrastructure 4 张（errors_catalog/contracts/contract_completeness_baseline/lifecycle）、i18n/copywriting/ssot_manifest 整层说明、退役与占位表清单、文案三通道定界小节；层计数更新为实测值（domains 51→迁移后终值、narratives 49→终值） | audit-docs 0 违规；README 计数 == GameConfig 实扫 |

### 2. 接线改造范式（reader 收敛样例，非唯一写法）

```gdscript
# 改造前（直读已退役/双写表）
var msg: String = GameConfig.get_string("narratives.bulletin_board_maintenance", "remaining_minutes", "")
# 改造后（公告域属主经 CopywritingResolver 统一解析）
var result := CopywritingResolver.resolve("bulletin.remaining_minutes", {"server": server_id, "minutes": minutes})
```

```json
// frontend/ui.json 快照溯源注解范式（G4）
"_meta": {
  "provenance": [
    { "path": "player/wallet/gold", "kind": "snapshot_of", "owner": "domains.starter_loadout", "owner_path": "starter_kits/KIT_HUMAN_DEFAULT/initial_gold" },
    { "path": "dummy_enemy/name", "kind": "demo_fixture", "note": "演示战斗桩数据，无领域属主（X-2）" }
  ]
}
```

### 3. 退役/占位转换检查单（每批强制）

1. `GameConfig.get_dict/get_string("descriptions.*"…)` 与 `narratives.events` 引用 0 残留（含 tests/）；
2. 占位骨架文件格式 == `{"_comment": "…（D2/D4 裁决退役，属主见 ssot_manifest.json）"}`；
3. 白名单三处同步：`audit_config_unused.py` RESERVED_NARRATIVES、判重引擎 exemptions、`code_governance_rules.json.exemptions`；
4. 批后引擎复跑：对应检测器清零且无新增；
5. 同批 diff 仅含本批收敛文件（config + reader + tests + 文档），数值/文案语义零改动。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 3.1: G1 文案遗留退役** - 按 G1a/G1b/G1c 顺序执行并逐项跑 R2/R4/R6 清零。
- [ ] **Step 3.2: G2/G3 双写收敛接线** - bulletin reader 改造与 cdkeys 裁决执行，白名单同步。
- [ ] **Step 3.3: G4/G5 快照溯源与域表直书分批迁移** - provenance 注解落地；17 表按决策树批次清零（每批单测回归）。
- [ ] **Step 3.4: G6/G7 互认归零与文档同步** - errors_catalog 补登记；config/README.md 计数与目录树实况对齐；全量回归与 audit-docs baseline 0 新增。

---

## 三、 工程化验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-SS-S3-01` | G1 退役零残留 | 全库静态三层引用扫描 + 文件系统检查 | `descriptions/` 目录与 `copywriting/probability.json` 不存在；`_legacy_fallback` 分支删除；R2 对应对清零 |
| `TC-SS-S3-02` | G2 公告单源接线 | bulletin_push_pipeline 触发公告 | 文案取自 copywriting.bulletin 且 `{server}`/`{minutes}` 填充正确；narratives.bulletin_board_maintenance 为 `_comment` 占位 |
| `TC-SS-S3-03` | G3 CDK 单源收敛 | manifest R3 对账 + account/chat_command diff | `account.json` 无 cdkeys 重复段（或改引用）；R3 孪生对 0 |
| `TC-SS-S3-04` | G4 快照溯源可检 | provenance 注解 schema 校验 + R5 复跑 | 注解字段完备；快照与属主值 0 漂移 |
| `TC-SS-S3-05` | G5 域表文案清零 | R6 复跑 + 白名单对比 | 17 表 CJK 直书收敛至 0（或白名单仅剩 X-2 豁免且只减不增）；迁移后各域单测 100% PASS |
| `TC-SS-S3-06` | G6/G7 互认与文档一致 | R4 双向差集 + config/README.md 实扫比对 + audit-docs | R4 孤儿 0 悬空 0；README 层计数/目录树与实况一致；audit-docs baseline 0 新增 |
