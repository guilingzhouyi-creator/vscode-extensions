---
档号: KALAR-DEV-2026-ST66-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST66 (Phase_70_配置库唯一事实提纯与冗余收敛治理)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_70_配置库唯一事实提纯与冗余收敛治理 —— 阶段4：唯一事实收敛零漂移全域验收与回归矩阵
形成日期: 2026-09-08
归档日期: 2026-09-09（晚上）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: TestConfigSsotGovernance; 既有套件适配清单; 验收证据链; 边界与退化验证; ui.json
---

# 施工细则：Phase 70 配置库唯一事实提纯与冗余收敛治理 —— 阶段4：唯一事实收敛零漂移全域验收与回归矩阵

> 施工开始日期：2026-09-08 下午
> 责任人：卡拉尔世界引擎架构组
> 状态：🔄 Round 1 细则编制完成（待批准后开工）

> [!NOTE]
> **【施工目标】**：完成阶段 1~3 交付物的全量验收闭环，确保"提纯唯一事实"与"回归零漂移"双向成立：
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST66-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST66-ATT_附件_案卷共享契约与上下文.md)。
> 1. 新增 GDScript 无头护栏测试套件（`test_config_ssot_governance.gd`）承载 manifest 契约、占位骨架规范、快照溯源注解、通道白名单只减不增四类运行时断言；
> 2. 扩展既有套件（`test_configuration_guard.gd` / `test_item_description.gd` 等）适配收敛后键位，并回归全量存量断言；
> 3. 判重引擎（阶段 2）纳入 audit-all 门禁链并跑批前/批后基线对账，收敛语义零改动抽查（除 manifest 明列漂移裁决项外全库 JSON 值 diff 为空）；
> 4. 文档棘轮（config/README.md 计数=实扫、audit-docs baseline 0 新增）与路线图总索引 Phase 70 闭环登记（第 07 期 6/10）。
> **对应需求源**：Phase 70 阶段 1~3；全域施工制度（两轮施工周期：Round 2 全域测试验收后更新路线图总索引闭环）。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST66-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST66-001_阶段1_配置事实归属矩阵与唯一属主契约设计.md) ｜ [阶段2](KALAR-DEV-2026-ST66-002_阶段2_配置血缘静态判重引擎与漂移检测算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST66-003_阶段3_冗余双写分批收敛迁移与全域接线工程化.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  - 全域测试基线（路线图总索引 2026-09-08 实况）：93 套测试套件 / 644 项断言 100% PASS，18 项门禁全绿——本卷验收不得低于该基线；
  - `tests/test_registry.gd` 分组注册（TC-ARCH-03 校验 `get_all_test_classes()` 收录，新增套件必须登记）；
  - `tests/unit/test_architecture_guard.gd`（TC-ARCH-01~05：目录/表齐备/测试注册/确定性/文档计数同步）——README 计数更新必须同批提交，否则护栏立即漂红；
  - `scripts/py/audit_docs.py`（--baseline 文档棘轮）、`scripts/sh/audit-all.sh`（或 ps1 等价）、`audit_runner.py --format sarif` 工业级导出；
  - 既有先例：Phase 66 统一日志底座（第 84 套）、Phase 69 资源治理（第 93 套）皆为基础设施级套件注册范式。
* **核心不变量约束断言**：
  - `Inv-SS-14 (全域回归零退化不变量)`：收敛后全域单测 100% PASS（套件数 ≥ 93 + 新增护栏套件），0 断言回归；既有断言若因键位迁移更新，必须逐条注释"迁移前键位 → 迁移后键位"。
  - `Inv-SS-15 (语义零改动不变量)`：除 manifest `action` 为漂移裁决（属主裁定真值）与文档注解条目外，收敛前后全库 JSON 值差集为空——git diff 白名单式人工复核 + 引擎复跑双证据。
  - `Inv-SS-16 (门禁基线双向一致不变量)`：判重引擎首跑基线 == 阶段 1 裁决清单（D1~D10）；批后复跑逐批清零且 0 新增；`audit-docs --baseline` 0 新增。
  - `Inv-SS-17 (占位与豁免零泄漏不变量)`：占位骨架/豁免白名单条目均有 manifest 或 rules exemptions 登记，无登记即违规（R6 白名单外 ERROR 0）。
* **防漂移最高指示**：验收以「门禁全绿 + 断言全过 + 值差集为空 + 基线归零」四证据闭环；禁止为通过验收而临时放宽任何 ERROR 检测或改动已发布基线；路线图总索引状态更新仅允许在 Round 2 全域验收通过后进行。

---

## 一、 自动化测试套件与验收代码 (Test Suite & Verification)

### 1. 新增护栏套件：`tests/unit/test_config_ssot_governance.gd`

```gdscript
# 模块路径: res://tests/unit/test_config_ssot_governance.gd
class_name TestConfigSsotGovernance extends RefCounted

## 断言分组（与 manifest 契约一一对应）：
## M1 manifest 结构：schema_version=1，10 条 record 六要素齐备，表名全部可解析
## M2 属主表存在性：record.owner_table 与 duplicate_tables 逐一命中 config/ 实文件
## M3 占位骨架规范：narratives/*.json 仅 _comment 单键者必在占位白名单，反之白名单表必为占位态
## M4 快照溯源注解：frontend/ui.json + views.json _meta.provenance schema 校验（kind ∈ snapshot_of|demo_fixture）
## M5 通道白名单只减不增：域表 CJK 直书豁免清单与 rules.json exemptions 双向差集为 0
func run_all_tests() -> Dictionary:
    # 返回 {"suite": "TestConfigSsotGovernance", "passed": bool, "cases": N}
    # 断言通过 = 上述 M1~M5 全绿 + 引擎 --json 输出 0 违规（子进程调用门禁模式）
    ...
```

### 2. 既有套件适配清单（键位迁移同步更新，逐条注释新旧键位）

| 套件 | 适配内容 | 更新方式 |
| :--- | :--- | :--- |
| `test_item_description.gd` | descriptions.items 通道退役 | 断言改指 `copywriting.item` 属主解析结果（G1a） |
| `test_event_probability.gd` 及相关事件文案用例 | probability/narratives.events 通道变化 | 断言改指 `copywriting.narrative` 同条目（G1b/G1c，依现场裁决） |
| `test_configuration_guard.gd` | 键位变更抽样回归 | 追加 manifest/占位/注解相关键位抽查行，删除指向退役表的行 |
| `test_fe_*`（fe01/fe10/fe15 等） | 快照注解与默认值语义不变量 | 仅当 G4 注解不改变读取键位时零改动；若键位变则同步（不允许） |

### 3. 验收证据链（四证据闭环，缺一不可）

1. **门禁全绿**：`scripts/ps1/audit-all.ps1`（或 sh 等价）含新并入的 audit-config-ssot 门禁，18→19 项（随实况）0 error；
2. **断言全过**：全域单测 100% PASS（第 94 套起，断言数随实况增长），新增护栏套件独立运行日志留档；
3. **值差集为空**：收敛前/后全库 JSON 规范化 diff 输出为空（manifest 漂移裁决项单独 diff 佐证属主裁定真值）；
4. **基线归零**：判重引擎批后复跑 0 违规、0 豁免漂移；`audit_docs.py --baseline` 0 新增；README 计数与实扫一致。

### 4. 边界与退化验证

- 快照漂移注入：人为改 `starter_loadout` 初始资金 → R5 告警且 `--strict` 下 audit-all 变红（证明门禁对"源值演进未同步快照"敏感）；
- 占位泄漏注入：向占位骨架注入正文 → R6/护栏套件 M3 变红；
- 退役通道复活注入：重建 `descriptions/items.json` → 引擎 R1/R2 与引用扫描变红；
- 豁免扩张注入：域表新增未登记 CJK 直书 → R6 ERROR（白名单只减不增成立）；
- 回归基底：Phase 69 移交基线（93 套/644 断言）之上只增不减。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1: 护栏套件与既有套件适配** - 新增 `test_config_ssot_governance.gd`（M1~M5），注册进 `tests/test_registry.gd`；既有套件键位适配逐条注释新旧键位。
- [ ] **Step 4.2: 全域回归与四证据闭环** - 全量单测 100% PASS；值差集为空；判重引擎与 audit-docs baseline 归零；audit-all 全绿。
- [ ] **Step 4.3: 注入式反例验证** - 按"四类退化注入"各跑一次并确认门禁变红后还原，日志留档。
- [ ] **Step 4.4: 路线图闭环登记** - 全域验收通过后更新 `docs/路线图/路线图总索引.md`：Phase 70 看板（S1~S4 明细链接 + 完成状态），第 07 期在途 6/10；按仓库提交规约输出收敛 diff（docs/feat 分型提交，禁止跨批混合）。

---

## 三、 全量工程验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-SS-S4-01` | 全域单测 100% PASS | `tests/test_runner.gd` 全量执行 | 套件数 ≥ 93+1，断言 100% 通过，0 回归 |
| `TC-SS-S4-02` | 护栏套件 M1~M5 全绿 | `test_config_ssot_governance.gd` | manifest/占位/注解/白名单四维断言 0 失败 |
| `TC-SS-S4-03` | 收敛语义零改动 | 收敛前后全库规范化 JSON diff | 差集为空；漂移裁决项单独 diff 佐证属主真值 |
| `TC-SS-S4-04` | 门禁链全绿含新检 | `audit-all`（含 audit-config-ssot） | 0 error；判重引擎批后复跑 0 违规 |
| `TC-SS-S4-05` | 注入式反例敏感度 | 快照漂移/占位泄漏/退役复活/豁免扩张四注入 | 各自使对应门禁变红，还原后全绿，日志留档 |
| `TC-SS-S4-06` | 文档棘轮与闭环登记 | `audit_docs --baseline` + 路线图总索引 | 0 新增；README 计数=实扫；Phase 70 登记完成（07 期 6/10） |
