---
档号: KALAR-DEV-2026-ST64-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST64 (Phase_68_存档系统统一数据基础设施重构)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_68_存档系统统一数据基础设施重构 —— 阶段4：旧档兼容迁移一致性热更新隔离与异常恢复全量验收测试矩阵
形成日期: 2026-09-06
归档日期: 2026-09-09（晚上）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 数据域划分与统一数据标准; 旧档兼容与数据迁移; 编辑器热更新; domains.json
---

# 施工细则：Phase 68 存档系统统一数据基础设施重构 —— 阶段4：旧档兼容/迁移/一致性/热更新/隔离/异常恢复全量验收测试矩阵

> 施工开始日期：2026-09-06
> 责任人：卡拉尔世界引擎架构组
> 状态：✅ 已完成（89/89 套件通过，608/608 断言通过，18 门禁全绿闭环）

> [!NOTE]
> **【施工目标】**：对阶段1~3 交付物（数据域划分 / 统一域契约 / 三级版本模型 / `SaveDataAccessLayer` / `SaveMigrationEngine` / `EditorHotReloadManager` / `RuntimeModeGate` / domains.json save 段 / persistence.json 布局迁移校验热更新段 / 生命周期直写收敛 / 空壳档阻断）建立全量无头测试验收矩阵，按 P68 第十三节重点验证链覆盖：
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST64-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST64-ATT_附件_案卷共享契约与上下文.md)。
> **旧存档兼容 → 数据迁移 → 物品/背包/技能数据一致性 → 编辑器热更新 → 单机/联机隔离 → 异常恢复 → 全量回归测试**。
> **对应需求源**：P68 总体架构要求第十二节验收标准 + 第十三节重点验证链；承接生命周期审查 L2（空壳档阻断）。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST64-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST64-001_阶段1_存档数据域划分与统一数据模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST64-002_阶段2_统一数据访问迁移热更新与隔离核心算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST64-003_阶段3_配置驱动扩展与工程化接线.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  - `docs/README.md` 三 §3.3 改后验收：重跑 `audit-docs.sh --baseline` → 新增违规 = 0；
  - `AGENTS.md` WebGames 门禁链：`test-run.sh → check-gdscript.sh → audit-all.sh`；
  - `tests/unit/test_persistence_protocol.gd`（现状持久化测试）与 `tests/unit/test_game_lifecycle_pipeline.gd`（停机管线测试，含储档断言）：既有套件形态参考；
  - `benchmarks/bench_persistence.gd`：存档性能基线套件（高频域增量/全量的前后对比基准）。

* **核心不变量约束断言**（阶段1~3 全量收敛）：
  - `Inv-SV-1`：领域划分业务边界 + 单一所有权；
  - `Inv-SV-2`：统一数据标准（信封契约不变）；
  - `Inv-SV-3`：配置/运行时分离（定义不入库）；
  - `Inv-SV-4`：三级版本 + 迁移链不破坏旧档；
  - `Inv-SV-5`：存档仅值类型；
  - `Inv-SV-6`：统一访问入口（业务禁直调 SaveManager）；
  - `Inv-SV-7`：迁移复制-校验-回滚；
  - `Inv-SV-8`：热更新仅单机 + 四层隔离；
  - `Inv-SV-9`：领域级热更新 + 白名单 + 失败回滚；
  - `Inv-SV-10`：脏标记增量（高频域）；
  - `Inv-SV-11~14`：配置驱动 / 布局 / 迁移配置 / 热更新白名单。

* **防漂移最高指示**：
  - 禁止为通过门禁删除/放宽断言（如删 SaveManager 直调扫描规则或迁移回滚断言）；
  - 既有存档相关测试必须零回归（`test-run.sh` 全绿为硬门槛）；
  - 新套件命名遵循 `test_*.gd` 并登记 `test_registry.gd`，禁止绕过注册表直跑。

---

## 一、 测试套件与用例矩阵 (Test Suite & Case Matrix)

**新套件**：
1. `tests/unit/test_save_domain_contracts.gd`（域契约/布局/版本模型）
2. `tests/unit/test_save_migration_engine.gd`（迁移链/回滚/域级演进）
3. `tests/unit/test_editor_hot_reload.gd`（热更新/单机隔离/失败恢复）
4. `tests/unit/test_save_consistency_recovery.gd`（物品/背包/技能一致性 + 异常恢复）

均注册入 `tests/test_registry.gd` 横切套件组。

### 1.1 数据域划分与统一数据标准（阶段1 交付物）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-SV-01 | 数据域划分单一所有权 | 全 11 域契约卡片扫描 | 任一核心数据字段仅属一个域；无多域共写（Inv-SV-1） |
| TC-SV-02 | 统一域契约签名 | 各域 serialize/deserialize 调用 | 签名统一；返回值纯值类型（无 Object/Node/RefCounted 引用，Inv-SV-5） |
| TC-SV-03 | 信封契约保持 | `SaveManager.save_game/load_game` | `data_json` 单字段 + SHA-256 校验；校验与消费同一字节序列（Inv-SV-2） |
| TC-SV-04 | 配置/运行时分离 | 构造含物品定义/技能定义的存档 | 定义属性不入档；存档仅持有实例引用键（canonical_id + instance 判别式，Inv-SV-3） |
| TC-SV-05 | 三级版本模型 | 域版本/架构版本/存档版本设置 | 三版本齐备可识别；互不干扰演进（Inv-SV-4） |

### 1.2 旧档兼容与数据迁移（P68 验证链第 1/2 环）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-SV-06 | legacy 旧档兼容 | 仅含 `data` 无签名历史档 | 降级读取成功（verified=false + 告警），既有语义保留 |
| TC-SV-07 | 整档迁移链 | 旧 format_version 存档 | `migrate_save_data` 逐级迁移至当前版本；steps 留痕（Inv-SV-4） |
| TC-SV-08 | 迁移断链回滚 | 注册表缺中间版本 | 返回 `MIGRATION_PATH_BROKEN` + partial；原档未被修改（Inv-SV-7） |
| TC-SV-09 | 迁移后校验失败 | 迁移产物缺必填字段 | 返回 `MIGRATION_VALIDATION_FAIL` + issues；不写盘（Inv-SV-7） |
| TC-SV-10 | 域级独立迁移 | `migrate_domain("inventory", data, 1)` | 域版本 1→2 提升；整档 Save Version 不变（Inv-SV-4） |
| TC-SV-11 | 迁移可追踪 | 迁移步骤列表 | steps 数组完整；审计日志留痕 |

### 1.3 物品/背包/技能数据一致性（P68 验证链第 3 环）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-SV-12 | 背包引用完整性 | 存档含装备槽/存储物品引用 | 引用的 canonical_id 均在 items 定义表存在；悬空引用被检测告警（Inv-SV-3） |
| TC-SV-13 | 物品定义重建 | 缺定义的持有实例加载 | 按 `item_defaults` 兜底重建（不阻断加载）；无定义属性入库 |
| TC-SV-14 | 技能状态持久化 | SkillSubgraphAST 序列化往返 | nodes/edges/signature_hash 一致；模板属性（默认值）不入档（Inv-SV-3） |
| TC-SV-15 | 循环依赖检测 | 域依赖图扫描 | inventory→item、character→inventory/lattice 单向；无环（Inv-SV-1） |
| TC-SV-16 | 高频域增量保存 | mark_dirty(inventory/economy) + flush_dirty | 仅脏域序列化；payload 不含未脏域（Inv-SV-10） |

### 1.4 编辑器热更新（P68 验证链第 4 环，单机）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-SV-17 | 变更检测 | 白名单配置表内容 hash 变化 | `detect_changes` 精确定位变更表；无变更零结果（Inv-SV-9） |
| TC-SV-18 | 领域级重载 | quest/settings 域热更 | 仅受影响域重载；`persistence.domain_hot_reloaded` 事件广播通知业务刷新 |
| TC-SV-19 | 白名单拒绝 | 未登记域（account/inventory）热更请求 | 返回 `HOT_RELOAD_DENIED`；数据不变（Inv-SV-14） |
| TC-SV-20 | requires_rebuild 拒绝 | combat/economy 域热更请求 | 返回 `REQUIRES_RUNTIME_REBUILD`；运行时拒绝（红线域不热更） |
| TC-SV-21 | 热更新失败回滚 | 重载抛错场景 | 返回失败 + 保持旧 hash（可重试）；不静默半加载（Inv-SV-9） |
| TC-SV-22 | 全量重读禁令 | 热更新触发监控 | 仅领域级重载；无全量配置重读（Inv-SV-9 性能） |

### 1.5 单机/联机隔离（P68 验证链第 5 环）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-SV-23 | 联机阻断热更新 | RuntimeModeGate=ONLINE 时 apply_hot_reload | 返回 `HOT_RELOAD_BLOCKED_IN_ONLINE`（Inv-SV-8） |
| TC-SV-24 | 数据源隔离 | 联机模式 get_active_data_source | 返回服务端权威源（broadcaster/reconciliation）；非本地 SaveManager |
| TC-SV-25 | 无热更新钩子注册 | 联机生命周期扫描 | 联机生命周期零热更新钩子；编辑器修改不改变联机运行时数据 |
| TC-SV-26 | 状态对象隔离 | 单机热更新后检查联机会话 | 共用状态对象零污染；单机热更新不传播到联机（Inv-SV-8） |
| TC-SV-27 | 服务入口隔离 | 联机下热更新入口调用 | 四层隔离全部拦截（模式/数据源/生命周期/服务入口） |

### 1.6 异常恢复与一致性安全（P68 验证链第 6 环）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-SV-28 | 缺失字段默认值恢复 | 存档缺某域字段 | `use_default` 策略从默认值来源恢复，不抛错（一致性安全） |
| TC-SV-29 | 类型不符处理 | 字段类型错误（str 当 int） | `coerce_or_drop`：可转则转，不可转丢弃 + 告警 |
| TC-SV-30 | 未知数据域跳过 | 存档含未知域键 | `warn_and_skip`：跳过 + 日志留痕，不阻断整档（域级隔离恢复） |
| TC-SV-31 | 部分损坏域恢复 | 单域数据损坏 | 损坏域单独失败回滚；其余域正常恢复（Inv-SV-7 域级） |
| TC-SV-32 | 原子写中断 | 写 tmp 失败 | 正档不被破坏；返回错误码 + recovery 提示（SaveManager 契约保持） |
| TC-SV-33 | 空壳档阻断 | providers 全空写档 | 显式告警 + 返回 `EMPTY_SAVE_BLOCKED`（承接审查 L2） |
| TC-SV-34 | 热更新失败恢复 | 失败后重试 | 旧 hash 保持可重试；第二次成功推进基线 |

### 1.7 全量回归与性能（P68 验证链第 7 环）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-SV-35 | 全库 SaveManager 直调扫描 | 全库 grep `SaveManager.save_game\|SaveManager.load_game` | 业务系统零直调（仅统一访问层 + 测试夹具；Inv-SV-6） |
| TC-SV-36 | 既有套件零回归 | 全量 `test-run.sh` | 既有存档/生命周期/全套件全绿（含 test_persistence） |
| TC-SV-37 | 性能基线对比 | `bench_persistence` 重跑 | 高频域增量路径不劣化（对照 P68 前基线）；全量路径持平或更优 |
| TC-SV-38 | 新域接入成本 | 注册新域（模拟 quest 已接入） | 仅配置登记 + 实现 serialize/deserialize；存档核心零改动（P68 第十节） |

## 二、 门禁与覆盖率验收 (Gates & Coverage Acceptance)

| 门禁 | 命令 | 通过标准 |
| :--- | :--- | :--- |
| 单元测试 | `bash scripts/sh/test-run.sh` | exit 0，既有套件 + 4 新套件全绿 |
| GDScript 语法 | `bash scripts/sh/check-gdscript.sh --scope backend` | 0 解析错误 |
| 配置值域 | `bash scripts/py/audit_config.py` | 0 违规（domains.json save 段/persistence.json 新段值域校验通过） |
| 文档门禁 | `bash scripts/sh/audit-docs.sh --baseline benchmarks/reports/docs_baseline.json` | exit 0 且新增违规 = 0 |
| 一键门禁 | `bash scripts/sh/audit-all.sh` | 全绿（含基准/热点/覆盖/密钥/收口） |
| 性能对比 | `bash scripts/sh/bench-sweep.sh --runs 3` + `report_aggregate.py --compare` | 高频域增量路径无显著劣化（对照 P68 前基线） |

## 三、 验收结论判定 (Acceptance Criteria)

| 判定 | 条件 | 处置 |
| :--- | :--- | :--- |
| ✅ 通过 | TC-SV-01~38 全 PASS + 门禁全绿 + 既有套件零回归 + 性能不劣化 | 案卷标记完成，闭环路线图总索引 |
| ⚠️ 部分通过 | 门禁绿但个别用例 FAIL | 修复后重跑；禁止删用例消音 |
| ❌ 不通过 | 门禁红（直调残留/迁移回滚失效/联机污染/套件回归） | 回退至对应阶段修复，复审 |

## 四、 阶段4 交付物与验收断言

| 交付物 | 路径 | 验收断言 |
| :--- | :--- | :--- |
| `test_save_domain_contracts.gd` | `tests/unit/` | TC-SV-01~05 全 PASS；登记 test_registry |
| `test_save_migration_engine.gd` | `tests/unit/` | TC-SV-06~11 全 PASS；登记 test_registry |
| `test_editor_hot_reload.gd` | `tests/unit/` | TC-SV-17~27 全 PASS（含联机隔离）；登记 test_registry |
| `test_save_consistency_recovery.gd` | `tests/unit/` | TC-SV-12~16、28~34 全 PASS；登记 test_registry |
| 门禁全绿 | `audit-all.sh` | 配置/文档/脚本/热点/覆盖零新增违规 |
