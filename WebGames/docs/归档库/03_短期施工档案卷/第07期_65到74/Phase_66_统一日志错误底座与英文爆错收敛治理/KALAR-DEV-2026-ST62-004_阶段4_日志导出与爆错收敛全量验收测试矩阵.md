---
档号: KALAR-DEV-2026-ST62-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST62 (Phase_66_统一日志错误底座与英文爆错收敛治理)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_66_统一日志错误底座与英文爆错收敛治理 —— 阶段4：日志导出与爆错收敛全量验收测试矩阵
形成日期: 2026-09-06
归档日期: 2026-09-09（晚上）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 导出器算法用例; 统一爆错收敛用例; 存量收敛与门禁用例; errors_catalog.json
---

# 施工细则：Phase 66 统一日志错误底座与英文爆错收敛治理 —— 阶段4：日志导出与爆错收敛全量验收测试矩阵

> 施工开始日期：2026-09-06
> 责任人：卡拉尔世界引擎架构组
> 状态：✅ 已完成（第 2 轮实施落地与全量测试验收通过，DoD 100% 达成）
> 验收完成日期：2026-09-06

> [!NOTE]
> **【施工目标】**：对阶段1~3 交付物（`LogRecordDTO` / `LogExportConfigDTO` / `ErrorCodeRegistry` / `LogFileExporter` / `ErrorReporter` / `EventBus.emit_log_record` / `errors_catalog.json` / 存量收敛迁移 / 前端日志终点）建立全量无头测试验收矩阵，覆盖：
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST62-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST62-ATT_附件_案卷共享契约与上下文.md)。
> 1. 数据模型契约（DTO 序列化往返、配置映射、错误码注册表装载）；
> 2. 导出器算法（原子写、轮转截断、级别/通道过滤、热重载重建、sequence 对账）；
> 3. 统一爆错收敛（错误码登记断言、i18n message、存量迁移回归、失败关闭语义保留）；
> 4. 门禁断言（全库 push_error/push_warning 零散落、配置表登记、既有 83 套测试零回归）。
> **对应需求源**：后端无头模块四维度审查报告 → 维度四 L1~L4 验收闭环；文档门禁（audit-docs）与配置门禁（audit_config）同步纳入。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST62-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST62-001_阶段1_日志导出与错误通道统一数据模型设计.md) ｜ [阶段2](KALAR-DEV-2026-ST62-002_阶段2_日志导出器与错误码统一爆错算法实现.md) ｜ [阶段3](KALAR-DEV-2026-ST62-003_阶段3_配置驱动扩展与全域接线工程化.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：
  - `docs/README.md` 三 §3.3 改后验收：重跑 `audit-docs.sh --baseline` → 新增违规 = 0；
  - `AGENTS.md` WebGames 门禁链：`test-run.sh → check-gdscript.sh → bench-sweep.sh → audit-perf.sh`，`audit-all.sh` 一键门禁（含配置值域/文档棘轮）；
  - `tests/unit/test_game_lifecycle_pipeline.gd`：既有停机管线测试形态参考（`GameLifecycleManager.reset_for_test()` 单例复位模式）；
  - `tests/test_registry.gd`：新测试套件须注册入 `get_all_test_classes()`（当前 83 套 → 84 套）。

* **核心不变量约束断言**：
  - `Inv-LG-1`（日志单通道不变量）：全库 `push_error`/`push_warning` grep 断言零散落（存量收敛 M1~M10 全部落地）；
  - `Inv-LG-3`（日志导出完整性不变量）：导出文件行序与广播 sequence 严格一致（对账断言）；
  - `Inv-LG-4`（导出配置收敛不变量）：配置热重载后导出器自动重建（旧配置残留零）；
  - `Inv-LG-2`（错误码英文底座不变量）：未登记错误码调用回退 `ERR_UNKNOWN` 且遥测计数 +1。

* **防漂移最高指示**：
  - 禁止为通过门禁而删除/放宽断言（如删除 push_error 扫描规则）；存量违规零新增是棘轮底线；
  - 既有 83 套测试必须零回归（`test-run.sh` 全绿为硬门槛）；
  - 新测试套件命名遵循 `test_*.gd` 模式并登记 `test_registry.gd`，禁止绕过注册表直跑。

---

## 一、 测试套件与用例矩阵 (Test Suite & Case Matrix)

**新套件**：`tests/unit/test_log_error_base_pipeline.gd`（class_name `TestLogErrorBasePipeline`），注册入 `tests/test_registry.gd` 横切套件组。

### 1.1 数据模型契约用例（阶段1 交付物）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-LG-01 | `LogRecordDTO` 字段齐备与序列化往返 | 构造全字段 record + context 嵌套字典 | `to_dto()` 后 `from_dto()`（若实现）字段一致；context 深拷贝（改原字典不影响 DTO） |
| TC-LG-02 | `LogExportConfigDTO.from_config()` 全字段映射 | `log.json export` 全量配置 | 7 字段全部正确映射；enabled=true 时导出器激活 |
| TC-LG-03 | export 段缺省安全回退 | 临时清空 export 段（测试隔离装载） | 回退默认（enabled=false/directory=""），不抛错 |
| TC-LG-04 | `ErrorCodeRegistry` 装载与查询 | `errors_catalog.json` 装载 | `lookup("ERR_ACCOUNT_NOT_FOUND")` 命中且 severity/domain 正确；`lookup("NO_SUCH_CODE")` 返回 null |
| TC-LG-05 | `missing_codes` 登记断言 | 传入含 2 个未登记 code 的数组 | 精确返回未登记清单；已登记 code 不误报 |

### 1.2 导出器算法用例（阶段2 交付物）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-LG-06 | 原子追加与导出落盘 | enabled=true 且 directory=user://logs_test | 导出文件存在；行数与广播 record 数一致；文件可 JSON 解析 |
| TC-LG-07 | 原子性（tmp 失败不破坏主文件） | 预置非法 tmp 路径（只读目录模拟） | 返回 false；既有主文件内容不变（Inv-LG-3） |
| TC-LG-08 | 级别过滤 | min_level=warn，广播 debug/info/warn/error 各 1 条 | 仅 warn/error 落盘（2 行），debug/info 被过滤 |
| TC-LG-09 | 通道白名单过滤 | include_channels=["contract_registry_index"] | 仅该通道记录落盘，其余通道跳过 |
| TC-LG-10 | 轮转截断 | max_bytes 设极小值触发轮转 + max_files=2 | 主文件重建；轮转文件 ≤2 份；最旧被清理 |
| TC-LG-11 | 热重载重建 | 改 log.json export 配置 + `reload_config()` | 导出器下一跳消费即用新配置（版本比对命中） |
| TC-LG-12 | sequence 对账 | 广播 N 条记录后读导出文件 | 文件行 sequence 从 1 连续递增至 N（Inv-LG-3 对账断言） |
| TC-LG-13 | 导出关闭零开销 | enabled=false | consume 返回 false；无文件创建；不影响广播路径 |

### 1.3 统一爆错收敛用例（阶段2 交付物）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-LG-14 | `emit_error` 已登记错误码 | `emit_error("test", "ERR_ACCOUNT_NOT_FOUND", "msg")` | record.level=error；error_code 保留；i18n message 命中（narratives.errors） |
| TC-LG-15 | 未登记错误码回退 | `emit_error("test", "NO_SUCH_CODE", "msg")` | error_code 回退 `ERR_UNKNOWN`；遥测计数 +1（Inv-LG-2） |
| TC-LG-16 | i18n message 解析 | message_key 命中 narratives.errors | 返回 i18n 文本；缺表回退调用方文本；再缺回退 code 本体 |
| TC-LG-17 | `emit_log` 旧签名兼容 | 既有签名 `emit_log("warn", "msg")` | 不抛错；record 广播 + 前端信号转发 + 导出三路齐发 |
| TC-LG-18 | 前端信号兼容 | 订阅 `log_message_posted` 计数 | 广播 1 条 → 信号收到 1 条（前端战报渲染零回归） |

### 1.4 存量收敛与门禁用例（阶段3 交付物）

| 用例 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| TC-LG-19 | 全库 push_error/push_warning 零散落 | 全库 grep `push_error\|push_warning` | 断言零命中（M1~M10 收敛完成 + 无新增） |
| TC-LG-20 | 失败关闭语义保留 | 构造缺失主配置的 contract_registry_index 装载 | 返回失败 + `_loaded=true`（阻断装载），经统一通道出错误日志而非 push_error |
| TC-LG-21 | 审计文案 i18n 化 | GM 审计记录 1 条 | 日志文本来自 narratives.admin.audit_log 表（配置优先），非代码直写英文 |
| TC-LG-22 | errors_catalog 配置表登记 | `_required_tables` 含 `infrastructure.errors_catalog` | TC-CFG-04（必需配置表零缺失）绿；value_domain 校验绿 |
| TC-LG-23 | 既有套件零回归 | 全量 `test-run.sh` | 83 套既有 + 新套件全绿（84/84） |

## 二、 门禁与覆盖率验收 (Gates & Coverage Acceptance)

| 门禁 | 命令 | 通过标准 |
| :--- | :--- | :--- |
| 单元测试 | `bash scripts/sh/test-run.sh` | exit 0，84/84 套件全绿（含新增 TC-LG-01~23） |
| GDScript 语法 | `bash scripts/sh/check-gdscript.sh --scope backend` | 0 解析错误 |
| 配置值域 | `bash scripts/py/audit_config.py` | 0 违规（errors_catalog/log.json export 值域校验通过） |
| 文档门禁 | `bash scripts/sh/audit-docs.sh --baseline benchmarks/reports/docs_baseline.json` | exit 0 且新增违规 = 0 |
| 一键门禁 | `bash scripts/sh/audit-all.sh` | 全绿（含基准/热点/覆盖/密钥/收口） |
| 覆盖补强 | `report_aggregate.py --promote` 前核对 | 新代码路径覆盖率不降（导出器/错误注册表核心分支 ≥ 目标） |

## 三、 验收结论判定 (Acceptance Criteria)

| 判定 | 条件 | 处置 |
| :--- | :--- | :--- |
| ✅ 通过 | TC-LG-01~23 全 PASS + 门禁全绿 + 83 套零回归 | 案卷标记完成，闭环路线图总索引 |
| ⚠️ 部分通过 | 门禁绿但个别用例 FAIL | 修复后重跑；禁止删用例消音 |
| ❌ 不通过 | 门禁红（push_error 残留/配置未登记/套件回归） | 回退至对应阶段修复，复审 |

## 四、 阶段4 交付物与验收断言

| 交付物 | 路径 | 验收断言 |
| :--- | :--- | :--- |
| `test_log_error_base_pipeline.gd` | `tests/unit/test_log_error_base_pipeline.gd` | 23 用例全 PASS；注册入 test_registry 横切组 |
| `test_registry.gd` 注册 | `tests/test_registry.gd` | 新增 `TestLogErrorBasePipeline` 且套件计数 83 → 84 |
| 门禁全绿 | `audit-all.sh` | 配置/文档/脚本/热点/覆盖零新增违规 |
