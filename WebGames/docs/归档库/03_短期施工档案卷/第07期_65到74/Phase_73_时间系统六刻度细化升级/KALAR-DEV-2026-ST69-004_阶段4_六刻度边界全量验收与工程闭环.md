---
档号: KALAR-DEV-2026-ST69-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST69 (Phase_73_时间系统六刻度细化升级)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_73_时间系统六刻度细化升级 —— 阶段4：六刻度边界全量验收与工程闭环
形成日期: 2026-09-08
归档日期: 2026-09-09（晚上）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: bash 全枚举验证; 同构一致性; 历史保留词兼容回归
---

# 施工细则：时间系统六刻度细化升级 —— 阶段4：六刻度边界全量验收与工程闭环

> [!NOTE]
> **【施工目标】**：对阶段1~3 全部交付物执行全量验收——0~23 小时全枚举边界矩阵、sh/ps1 同构一致性、跨日「半夜」连续性、历史「上午」保留词兼容回归、`--full` 时刻模式，并跑通脚本库与文档门禁闭环。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST69-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST69-ATT_附件_案卷共享契约与上下文.md)。
> **【施工开始日期】：施工开始日期: 2026-09-08（半夜）** —— 真实读取系统时间。
> **对应需求源**：阶段1 常量表 → 阶段2 算法 → 阶段3 文档同步，三者全量回归。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST69-ATT_附件_案卷共享契约与上下文.md) ｜ [阶段1](KALAR-DEV-2026-ST69-001_阶段1_六刻度时间区间契约与边界数据设计.md) ｜ [阶段2](KALAR-DEV-2026-ST69-002_阶段2_六刻度判定核心算法与向后兼容实现.md) ｜ [阶段3](KALAR-DEV-2026-ST69-003_阶段3_配置驱动与全库文档同步工程化.md) ｜ **阶段4 (当前)**

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：阶段1「六刻度区间契约表」、阶段2「判定核心算法」、阶段3「文档同步清单」。
* **核心不变量约束断言**：24 小时**全覆盖、两两互斥、半开区间**三性质在实现层逐小时成立；sh/ps1 逐小时输出一致；「上午」历史戳解析兼容。
* **验收闭环最高指示**：测试必须覆盖**每整点边界 hour ∈ 0..23 全枚举**（不只抽查边界），并针对跨日区间做连续性断言；证据以实际脚本输出为准，严禁口头声明。

---

## 一、 自动化验收套件 (Verification Suite)

### 1.1 bash 全枚举验证（0~23 逐小时）

```bash
# 期望映射（阶段1常量表唯一真源）
# hour 0,1,22,23 -> 半夜；2..5 -> 凌晨；6..9 -> 早上；10..13 -> 中午；14..17 -> 下午；18..21 -> 晚上
for h in $(seq 0 23); do
  # 以伪造 TZ 偏移或 date -d 不可靠，采用直接调用 resolve 逻辑（bash 函数化后按 hour 入参）断言
  echo "hour=$h -> $(PERIOD_LOOKUP "$h")"
done
```

> 实现期将 `timestamp.sh` 判定段提取为可入参函数（如 `resolve_period_by_hour()`），便于逐小时断言；`--full` 仅追加 `date +%H:%M` 时刻后缀，不改变刻度判定。

### 1.2 同构一致性（sh vs ps1）

```bash
# 在可用 PowerShell 的 Windows 环境执行：对同一 hour 集合分别运行 resolve 逻辑，
# 断言输出 100% 相同（pair_parity 语义）；无 PowerShell 的 CI 环境以 bash 全枚举 + 源码比对为准。
```

### 1.3 历史保留词兼容回归

```bash
# 构造含「施工开始日期: 2026-08-12（上午）」文本的临时文件，
# 运行 archive_volume.py 的 resolve_file_formation_period 提取 → 必须返回「上午」且不抛错。
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1: 24 小时全枚举边界矩阵** - 0~23 逐小时断言命中唯一刻度，覆盖死区/重叠两类缺陷
- [ ] **Step 4.2: 跨日「半夜」连续性** - hour 22/23/0/1 四连击断言，验证或分支守卫
- [ ] **Step 4.3: 同构一致与 --full 模式** - sh/ps1 一致；`--full` 输出含时刻且刻度判定不变
- [ ] **Step 4.4: 门禁闭环** - `audit-all.sh`（含 pair_parity / audit-docs 基线棘轮）0 新增违规

---

## 三、 全量工程验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P73-S4-01` | 24 小时全覆盖无死区 | hour 0~23 全枚举 | 每 hour 恰好命中一个刻度，无 None/空输出 |
| `TC-P73-S4-02` | 两两互斥无重叠 | 相邻边界（5/6、9/10、13/14、17/18、21/22、1/2） | 边界两侧归属不同刻度 |
| `TC-P73-S4-03` | 跨日「半夜」连续 | hour ∈ {22,23,0,1} | 四值全部「半夜」，半开区间跨日成立 |
| `TC-P73-S4-04` | 历史词兼容 | 「上午」戳临时文件 | `resolve_file_formation_period` 返回「上午」，零异常 |
| `TC-P73-S4-05` | --full 模式 | `timestamp.sh --full` / `timestamp.ps1 -Full` | 输出 `YYYY-MM-DD HH:MM 刻度`，刻度与小时判定一致 |
| `TC-P73-S4-06` | 门禁全绿 | `audit-all.sh` | 配置/风格/硬编码/文档四域 0 error，基线棘轮 0 新增 |

---

## 四、 验收通过定义 (DoD)

1. 阶段1 常量表六刻度定义在六处文档声明中逐字一致；
2. 阶段2 三实现（sh/ps1/py）对 0~23 全部 hour 输出与常量表 100% 一致；
3. 阶段3 文档同步后 `audit-docs --baseline` 新增违规 = 0；
4. 阶段4 验收矩阵 TC-P73-S4-01~06 全部通过；
5. 存量归档文档「上午」戳零改写、解析兼容回归通过。
