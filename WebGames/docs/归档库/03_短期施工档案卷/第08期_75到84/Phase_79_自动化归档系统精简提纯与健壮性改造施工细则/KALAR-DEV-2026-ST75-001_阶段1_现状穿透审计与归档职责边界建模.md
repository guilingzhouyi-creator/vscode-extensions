---
档号: KALAR-DEV-2026-ST75-001
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-ST (技术研发·短期施工周期归档)
年度: 2026年
案卷号: ST75 (Phase_79_自动化归档系统精简提纯与健壮性改造施工细则)
件号: 001
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: Phase_79_自动化归档系统精简提纯与健壮性改造施工细则 —— 阶段1：现状穿透审计与归档职责边界建模
形成日期: 2026-09-10
归档日期: 2026-09-12（半夜）
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 九类职责物理分布实测表; 功能性缺陷清单; 目标模块拓扑蓝图; docs_governance_rules.json
---

# 施工细则：自动化归档系统精简提纯与健壮性改造 —— 阶段1：现状穿透审计与归档职责边界建模

> [!NOTE]
> **【施工目标】**：完成对 `scripts/py/archive_volume.py`（1557 行单文件 · 33 符号）的穿透审计，建立「巡检/布局审查/周期归档/元数据同步/关键词提纯/链接重算/附件生成/导航注入/CLI 装配」九类职责的边界矩阵，量化其与 `audit_docs.py` 的双重实现重叠面，并产出目标模块拓扑、统一归档配置 Schema 契约与全部功能性缺陷修复清单，为阶段2 模块化拆分提供权威蓝图。
> **案卷全局共享上下文**：👉 [KALAR-DEV-2026-ST75-ATT_附件_案卷共享契约与上下文.md](KALAR-DEV-2026-ST75-ATT_附件_案卷共享契约与上下文.md)。
> **【施工开始日期】：施工开始日期: 2026-09-10（下午）** —— 真实读取系统时间。
> 状态：✅ 已完成 (Round 2 施工与全域验收闭环：九模块包拆分 + D1~D3 缺陷修复 + 锚点三态健壮化，self-test 16/16 断言，audit-all 20/20 全绿，2026-09-10)
> **【用户指令溯源】**：用户明确要求「接下来对WebGames项目的自动化归档系统还得完善改造和优化（由于短期施工区的文件格式已改动以及归档系统过于臃肿，需要精简通用性提纯？）」（2026-09-10），并在立项确认中选定全部四个方向：职责瘦身去重、模块化拆分、配置化提纯+硬编码清理、注入锚点健壮化。
> **对应需求源**：[路线图总索引](../../../../路线图/路线图总索引.md)。
> 📍 **卷内快速直达**：[ATT 共享附件](KALAR-DEV-2026-ST75-ATT_附件_案卷共享契约与上下文.md) ｜ **阶段1 (当前)** ｜ [阶段2](KALAR-DEV-2026-ST75-002_阶段2_单文件模块化拆分与配置化提纯实施.md) ｜ [阶段3](KALAR-DEV-2026-ST75-003_阶段3_注入锚点结构化与脆弱耦合健壮化.md) ｜ [阶段4](KALAR-DEV-2026-ST75-004_阶段4_全量回归验收与工程闭环.md)

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：Phase 75《脚本库注释规范严格化与门禁对齐》（三语言头注释七字段契约，本卷改造的 py 脚本必须同步满足）；Phase 76《脚本库注释规范深化与存量命名违规消化》（SECTION-SEPARATOR / PY-DOCSTRING 两 SHOULD 维度）；`scripts/config/docs_governance_rules.json`（PHASE-VOLUME-STRUCTURE / ROADMAP-CYCLE-OVERDUE / LAYOUT-ARCHIVE-HEAD 等规则真源）；`scripts/py/audit_common.py`（ensure_utf8_stdout / resolve_repo_root 共享底座，拆分后各模块必须继续复用而非复制）。
* **核心不变量约束断言**：
  - `Inv-P79-1-1 (单一权威检查源)`：同一治理规则（README 污染、案卷结构、周期阈值）在全域仅允许一处权威实现，归档器与审计器之间严禁各写一份正则副本；
  - `Inv-P79-1-2 (CLI 表面零变动)`：`archive_volume.py` 对外入口（--detect / --cycle / --apply / --self-test / --reindex-keywords / --cycle-target）与 sh/ps1 双透传层的行为、退出码契约（0=成功 1=阻断 2=用法错误）必须逐字保持；
  - `Inv-P79-1-3 (归档产物幂等稳定)`：改造前后对同一输入卷执行归档，产出的 14 项档案头块、ATT 附件、navbar 注入与重命名映射必须字节级等价（年度等配置化字段除外，其取值来源必须显式可溯）。
* **防漂移最高指示**：细则正文中的全部契约代码必须使用 Python 3.10+ 强类型注解语法；严禁出现模板占位符或未决标记；示例代码中严禁散落 push 类非确定性调用与随机调用。

---

## 一、 存量职责穿透审计 (Responsibility Audit & Boundary Modeling)

### 1.1 九类职责物理分布实测表

| 职责域 | 承载符号（行号区间） | 行数占比 | 与 audit_docs 重叠 |
| :--- | :--- | :---: | :--- |
| 周期归档主流程 | `archive_short_term_cycle` (927~1105) | ~11% | 无 |
| 归档后元数据四处同步 | `sync_metadata_after_cycle` (1120~1314) | ~12% | 无 |
| 巡检报告 | `detect_roadmap_status` (793~869) | ~5% | 高（周期阈值/案卷结构/README 污染三处双写） |
| 总索引布局审查 | `audit_roadmap_index_layout` (872~924) | ~3% | 高（与 ROADMAP-INDEX-MISSING / 精简检测语义重叠） |
| 阶段特化校验 | `verify_stage_distinctness` (732~790) | ~4% | 中（占位符拦截与归档库校验双写） |
| 链接拓扑重算 | `rebase_markdown_links` (216~309) | ~6% | 无 |
| 关键词提纯 | `extract_stage_keywords` / `extract_att_keywords` / `load_master_keywords` / `reindex_archive_keywords` (354~585, 1429~1517) | ~20% | 无（停用词表内嵌于代码） |
| 附件生成与导航注入 | `generate_volume_attachment` (588~677) / `inject_volume_navbars` (680~729) | ~9% | 无 |
| 元数据求解与 DTO | `resolve_*` 系列 + `ArchiveRecordDTO` + `int_to_cn` + `minify/strip` (69~213, 120~139) | ~9% | 无 |

### 1.2 功能性缺陷清单（阶段2 必须全部修复）

```python
# 缺陷 D1（main.py 1537 行）：--apply 形同虚设，恒真表达式
success = reindex_archive_keywords(args.cycle_target, apply=args.apply or True)
# 目标语义：仅当显式传入 --apply 时执行落盘，否则干跑预览

# 缺陷 D2（resolve_file_archive_date，199 行）：file_path 参数从未使用
def resolve_file_archive_date(file_path: Path, default_today: str) -> str:
    return default_today or date.today().isoformat()
# 目标语义：移除死参或在 docstring 声明保留原因，消除签名误导

# 缺陷 D3（archive_short_term_cycle，1054 行）：14 项头块「年度: 2026年」硬编码
# 目标语义：由归档日期动态推导（formation/archive 同源），配置仅保留展示格式
```

### 1.3 目标模块拓扑蓝图

```text
scripts/py/
  archive_volume.py            # 仅剩 CLI 装配与参数分发（目标 ≤ 80 行）
  archive/
    __init__.py                # 包入口与公共 re-export
    constants.py               # 路径常量 + ARCHIVE_CATEGORIES 迁移
    metadata.py                # resolve_* 系列 + ArchiveRecordDTO + int_to_cn + minify/strip
    links.py                   # rebase_markdown_links 及其内部缓存
    keywords.py                # 关键词提取/停用词加载/reindex
    attachment.py              # ATT 生成 + navbar 注入
    cycle.py                   # archive_short_term_cycle + sync_metadata_after_cycle
    inspect.py                 # detect_roadmap_status + audit_roadmap_index_layout（收敛桥接）
    verify.py                  # verify_stage_distinctness
```

### 1.4 与 audit_docs 的职责收敛决议（Inv-P79-1-1 落地）

- 周期阈值超限提示（ROADMAP-CYCLE-OVERDUE 语义）：**权威归 audit_docs**，`inspect.py` 仅消费其结果做展示性汇总，不再复制正则；
- README 污染与案卷结构违规：**权威归 audit_docs**，归档器巡检在归档前置校验场景改为调用 audit_docs 的可编程入口或复用其导出的校验函数，严禁第二份正则；
- 总索引布局审查：保留在归档器（它是归档后自清理的闭环环节），但检测逻辑与 audit_docs 共享同一函数，两端仅存调用关系。

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 1.1: 1557 行全量符号穿透复扫** - 逐符号登记职责、依赖方向与 audit_docs 重叠判定，固化 1.1 表
- [x] **Step 1.2: 双重实现重叠面量化** - 对三处双写正则逐一 diff 语义差异，形成收敛决议（1.4）
- [x] **Step 1.3: 目标模块拓扑与依赖方向建模** - 固化 1.3 蓝图，声明各模块允许的 import 白名单
- [x] **Step 1.4: 归档配置 Schema 契约设计** - 扩展 ARCHIVE_CATEGORIES 为完整归档治理配置（头块模板槽位/停用词表/锚点约定声明位），落成字段级定义供阶段2 消费

---

## 三、 数据结构与契约验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-P79-S1-01` | 职责分布表完备性断言 | 对照 33 个符号逐一核对 1.1 表 | 33/33 符号全部归属唯一职责域，无遗漏无重复登记 |
| `TC-P79-S1-02` | 缺陷清单完备性断言 | 静态审查 main/resolve/头块生成路径 | D1~D3 全部登记且每项附目标语义，不得出现登记外新增缺陷遗漏 |
| `TC-P79-S1-03` | 模块拓扑 import 白名单断言 | 审查 1.3 蓝图依赖声明 | 模块间依赖构成无环有向图，constants 无人反向依赖，CLI 门面不承载业务逻辑 |
| `TC-P79-S1-04` | 收敛决议单一权威断言 | 审查 1.4 决议与 audit_docs 规则真源对照 | 三处双写检查全部给出唯一权威归属，归档器侧无独立正则副本留存 |
