---
档号: KALAR-DEV-2026-RM01-002
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-RM (技术研发·施工路线图)
年度: 2026年
案卷号: RM01 (第01卷: 核心领域模型与多职业)
件号: 002
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组 / DeepMind Antigravity
题名: 第01卷：核心领域模型与多职业 —— 阶段2：核心算法与数学方程
形成日期: 2026-08-30
归档日期: 2026-08-30
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 物品聚合根; 随身穿戴仓储; 裸身容量约束; 经历对数阶位; 多职业兼备; 阶段2_核心算法与数学方程
---

# 施工细则：第一卷：核心领域模型、物品系统与多职业兼备 —— 阶段2：核心算法求解器与数学方程

> [!NOTE]
> **【施工目标】**：实现万物物品领域实体聚合根、背包随身仓储与穿戴槽位模型、经历与称号数据实体、多职业一身动态聚类之【核心算法求解器与数学方程】模块。
> **对应后端需求**：[后端架构需求表1.md (第01卷)](../../../后端架构/后端架构需求表1.md)。

---

## 一、 核心算法与数学方程实现 (Algorithms & Equations)

```gdscript
# 模块路径: res://core/vol01/stage2_solvers.gd
class_name ExperienceCalculator extends RefCounted:
    # R = floor(alpha * ln(1 + Rep) + beta * (Age/MaxLife)^gamma * Expl + delta * sum(Mastery))
    static func evaluate_experience_rank(reputation: float, normalized_age: float, exploration_index: float, skill_mastery_sum: float) -> int:
        var rep_term = 2.5 * log(1.0 + max(0.0, reputation))
        var growth_term = 1.8 * pow(clamp(normalized_age / 100.0, 0.0, 1.0), 1.2) * exploration_index
        var mastery_term = 0.05 * skill_mastery_sum
        return int(floor(rep_term + growth_term + mastery_term))

    static func calculate_memory_decay(initial_mastery: float, elapsed_days: float, int_stat: float) -> float:
        var lambda_decay = 0.05 / max(1.0, int_stat * 0.1)
        return max(0.0, initial_mastery * exp(-lambda_decay * elapsed_days))
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 2.1: 实现经历阶位求解器** - 编写对数经历收敛函数 `evaluate_experience_rank`
- [ ] **Step 2.2: 实现神经记忆衰减模型** - 编写基于智魔力 INT 的半衰期遗忘函数 `calculate_memory_decay`
- [ ] **Step 2.3: 数值守恒审计** - 确保经历增长不发生指数溢出

---

## 三、 单元测试与验收矩阵 (DoD Matrix)

| 检验项 ID | 测试目标 | 输入断言 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-VOL01-S2-01` | 离散算法数值精度 | 合法参数输入求解器 | 离散输出值满足物理/热力学方程精确解 |

