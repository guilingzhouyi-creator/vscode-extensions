---
档号: KALAR-DEV-2026-FE03-003
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-RM (技术研发·施工路线图)
年度: 2026年
案卷号: FE03 (前端第3卷: 角色与养成系统)
件号: 003
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎前端组
题名: 前端第3卷：角色与养成系统 —— 阶段3：通用性、泛化性、可维护性及配置驱动等工程化改造
形成日期: 2026-08-31
归档日期: 2026-08-31
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 六维属性DTO; 潜能加点双轨; 技能点阵树; 装备槽位映射; 单元测试; 阶段3
---

# 施工细则：前端第3卷：角色与养成系统（面板/潜能加点/技能树/装备/背包） —— 阶段3：通用性、泛化性、可维护性及配置驱动等工程化改造

> [!NOTE]
> **【施工目标】**：去除前端代码所有硬编码、接入 `GameConfig` 类型化配置驱动、提取通用组件并治理模块依赖。
> **对应需求源**：[前端架构需求表3.md (前端第3卷)](../../../前端架构/前端架构需求表3.md)。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：[三、 界面交互状态机与导航路由](../../../前端架构/前端架构需求表3.md)
* **核心不变量约束断言**：`全域配置必须经 GameConfig.get_* 类型化读取，严禁使用 Variant get_value 触发项目警告`
* **工程化重构最高指示**：将所有默认值、列表模板、阈值与常数抽离至 `config/frontend/views.json`，实现零硬编码！

---

## 一、 配置驱动与通用组件重构 (Config-Driven & Reusability)

```gdscript
# 模块路径: res://frontend/views/fe03_角色与养成系统/characterprogressionview_config.gd
class_name CharacterProgressionViewStage3 extends Control:
    var config_table: String = "frontend.views"

    # 配置驱动初始化：零硬编码
    func load_view_configuration() -> Dictionary:
        return {
            "table": config_table,
            "keys_loaded": ["fe03_character_progression/base_attributes", "fe03_character_progression/initial_free_potential_points"],
            "status": "CONFIG_DRIVEN_READY"
        }

    # 通用组件方法：支持策略扩展
    func apply_custom_theme_strategy(theme_box: StyleBoxFlat, color_token: Color) -> void:
        if theme_box:
            theme_box.bg_color = color_token
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [x] **Step 03.3.1: 视图硬编码扫描与全面抽离** - 将默认数值、文案、尺寸抽离至 `frontend.views`
- [x] **Step 03.3.2: 接入 GameConfig 类型化取值器** - 使用 `get_int`/`get_float`/`get_string`/`get_array`
- [x] **Step 03.3.3: 提取通用样式盒与公共交互组件** - 复用全局 Theme Token 视觉规范
- [x] **Step 03.3.4: 模块依赖治理与可观测性打标** - 保持纯净单向依赖，统一日志前缀

---

## 三、 工程化验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-FE03-S3-01` | 配置驱动热读取生效 | 动态修改 `views.json` 参数 | 视图表现层即时响应变更，零代码修改 |
| `TC-FE03-S3-02` | 配置缺省安全兜底 | 传入不存在的配置路径 | 严格返回 fallback 默认值，不抛 Fatal |
| `TC-FE03-S3-03` | 静态无硬编码审查通过 | `audit_hardcode.py` 扫描 | 视图控制代码中 0 违规业务数值残留 |
