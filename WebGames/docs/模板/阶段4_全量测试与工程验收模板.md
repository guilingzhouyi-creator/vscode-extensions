---
档号: KALAR-DEV-2026-<PREFIX><NN>-004
全宗号: KALAR (卡拉尔世界引擎全宗)
类别号: DEV-RM (技术研发·施工路线图)
年度: 2026年
案卷号: <PREFIX><NN> (<卷主题>)
件号: 004
保管期限: 永久 (Permanent)
密级: 内部公开 (Internal Open)
责任者: 卡拉尔世界引擎架构组
题名: <卷主题> —— 阶段4：1～3全部测试集、验证集及工程验收条件通过
形成日期: <YYYY-MM-DD>
归档日期: <YYYY-MM-DD>
验收状态: 已归档·100%自动化测试验收通过 (PASS)
主题词/关键词: 单元测试; 回归验证; 边界覆盖; 质量门禁; 阶段4
---

# 施工细则：<卷主题> —— 阶段4：1～3全部测试集、验证集及工程验收条件通过

> [!NOTE]
> **【施工目标】**：完成数据结构、核心业务与工程化改造的 1~3 层全量单元测试、集成回归与质量门禁验收闭环。
> **对应需求源**：[需求表相对路径](<上游需求文档相对链接>)。

---

## 📌 第一性原理溯源指针

* **精准上游规范指针**：[四、 全量测试验证与工程验收矩阵](<上游需求文档相对链接#章节锚点>)
* **核心不变量约束断言**：`<定义本模块必须通过的 100% 测试通过率与质量门禁标准>`
* **验收闭环最高指示**：测试用例必须覆盖正常路径、异常分支、极端边界与配置缺省，做到断言精准、证据确凿！

---

## 一、 自动化测试套件与验收代码 (Test Suite & Verification)

```gdscript
# 模块路径: res://tests/unit/domains/test_<domain_id>.gd
# 注：分层放置（领域 tests/unit/domains/、表现层 tests/unit/frontend/、基建 tests/unit/infrastructure/、
# 跨域管道 tests/integration/pipelines/、架构守护 tests/guards/；严禁在 tests/unit/ 根散落脚本）
class_name Test<Domain>Domain extends TestCase

static func run_all_tests() -> Dictionary:
	var results: Array[Dictionary] = []
	results.append(_test_stage1_data_structures())
	results.append(_test_stage2_core_business())
	results.append(_test_stage3_config_driven())
	results.append(_test_stage4_boundary_cases())
	return TestCase.pack_results("<domain_id>", results)

static func _test_stage1_data_structures() -> Dictionary:
	# 验证数据结构与契约完整性
	var ok := TestCase.assert_true(true, "契约结构验证")
	return TestCase.make_result("stage1_data_structures", ok)

static func _test_stage2_core_business() -> Dictionary:
	# 验证核心业务逻辑、状态转移与不变式
	var ok := TestCase.assert_true(true, "核心业务状态转移验证")
	return TestCase.make_result("stage2_core_business", ok)

static func _test_stage3_config_driven() -> Dictionary:
	# 验证 GameConfig 配置驱动与三层默认降级兜底
	var ok := TestCase.assert_true(true, "配置驱动与兜底降级验证")
	return TestCase.make_result("stage3_config_driven", ok)

static func _test_stage4_boundary_cases() -> Dictionary:
	# 验证极端输入、空值安全与异常容错防御
	var ok := TestCase.assert_true(true, "边界容错与确定性验证")
	return TestCase.make_result("stage4_boundary_cases", ok)
```

---

## 二、 命令式施工执行清单 (Agent Execution Checklist)

- [ ] **Step 4.1: 继承 TestCase 并编写规范化测试套件** - 按分层拓扑归入 `tests/unit/domains/` 等子目录，严禁在 `tests/unit/` 根目录散落任何文件
- [ ] **Step 4.2: 实施极端边界、确定性与异常容错断言** - 验证空数据/超限数值/降级路径，严禁裸随机与循环内瞬态堆分配
- [ ] **Step 4.3: 领域契约双向同步与配置绑定** - 领域测试 1:1 对齐并在 `config/infrastructure/domains.json` 登记 `"test"` 路径与启用状态
- [ ] **Step 4.4: 静态质量门禁与全域验收闭环** - 确保 `test-run.ps1` 100% PASS、`audit_test_coverage.py` 零缺失且 `audit-all.ps1` 20/20 门禁全绿

---

## 三、 全量工程验收矩阵 (DoD Matrix)

| 检验项 ID | 校验目标 | 验证输入 | 预期输出断言 |
| :--- | :--- | :--- | :--- |
| `TC-<PREFIX><NN>-S4-01` | 全量单测套件通过 | `test-run.ps1` 执行 | 该领域全部断言 100% 通过 (PASS)，`pack_results` 全绿 |
| `TC-<PREFIX><NN>-S4-02` | 极端异常注入容错 | Null/超限数值/非法字符 | 优雅处理或返回受控 Result DTO，杜绝未捕获崩溃 |
| `TC-<PREFIX><NN>-S4-03` | 领域契约双向同步 | `audit_test_coverage.py` | `domains.json` 声明与物理测试文件 1:1 双向映射 0 缺失 |
| `TC-<PREFIX><NN>-S4-04` | 静态审查与门禁校验 | `audit-all.ps1` 运行 | 20 项静态门禁（配置/代码风格/硬编码/文档审查）0 error |
