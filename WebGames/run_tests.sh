#!/usr/bin/env bash
# =============================================================================
# run_tests.sh — WebGames 全域单元测试执行代理脚本
# -----------------------------------------------------------------------------
# 单一真源契约：本文件是 scripts/sh/test-run.sh 的稳定用户侧别名，仅为历史
# 文档（README/docs/归档库）引用的 DoD 验收入口。必须保持纯透传（exec + "$@"），
# 严禁在本文件内复制或改写任何测试逻辑——逻辑唯一真源为 scripts/sh/test-run.sh。
# =============================================================================
set -uo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/scripts/sh/test-run.sh" "$@"
