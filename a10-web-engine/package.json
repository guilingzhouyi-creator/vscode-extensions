{
  "name": "a10-web-engine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A-10 Thunderbolt II — engineering-grade Three.js model: modular, config-driven, headless spec/geometry separation, LOD, exploded view, cutaway, rig animation, damage interface. Not a VS Code extension (no engines.vscode) — excluded from extension CI by design.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5174",
    "test": "node tests/run.mjs"
  },
  "dependencies": {
    "three": "0.182.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
