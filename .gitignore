node_modules/
*.vsix
*.tgz
.vscode-test/

# 统一打包输出目录（本地构建产物，不入库；正式产物发布到 GitHub Releases）
dist/

# 各扩展编译产物与覆盖率报告（根级零配置覆盖：任何顶层扩展的 out/、coverage/ 均不入库）
out/
coverage/

# 门禁 SARIF 报告产物（本地与 CI 运行 audit-all --sarif-out 生成，不入库）
sarif/

# 本地运行时数据与工具目录（不入库）
.workspace-timing-data/
.workbuddy/
.vscode/workspace-timing.json
.vscode/workspace-timing.journal

# auto-refactor 运行时缓存、测试语料与基准临时文件（不入库）
**/.auto-refactor-cache/
**/.auto-refactor-cache-*/
**/.bench-corpus/
**/.*-corpus/
**/.corpus/
**/.corpus-*.actual.json
**/*.actual.json
**/.*.actual.json
**/.t04-keypoints-*/
**/.t04-keypoints-*
*.log
*.tsbuildinfo
err.txt
out.txt

# 版本归档（历史快照 / 备份 / 演示材料，统一集中管理）
/archive/

# ============ 密钥与凭证（防误提交泄露；示例模板与公钥除外） ============
# 环境变量 / 本地覆盖配置（.env.example 等示例模板仍可提交）
.env
.env.*
*.env
!.env.example
!*.env.example
*.local
*.local.json
*.local.yaml
*.local.yml
config.local.*
settings.local.json

# 私钥 / 密钥库（*.pub 公钥与 *.crt 证书可正常提交）
*.key
*.pem
*.pfx
*.p12
*.ppk
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*
!*.pub
*.jks
*.jceks
*.keystore
debug.keystore

# 云平台 / OAuth 服务账号凭证
.aws/
.azure/
credentials.json
service_account*.json
service-account*.json
client_secret*.json
client_secrets*.json

# 通用密钥环 / 凭证库
*.secret
*.token
.git-credentials
.netrc
_netrc
.pgpass
.my.cnf
.htpasswd
*.keychain

# 包管理器认证
.npmrc
.pypirc
.gem/credentials

# 操作系统杂物
.DS_Store
Thumbs.db
desktop.ini
