# CNB NPC 协作系统 — 运行手册（RUNBOOK）

> 参考官方文档：<https://docs.cnb.cool/zh/build/npc.html>
> 本仓库配置 **五个 NPC**：
> - 三个 **三职能于一身** 的协作执行 NPC：**协作员·构建** / **协作员·测试** / **协作员·审查**
> - 一个 **路线图规划者** NPC（纯规划/统计/调度/文档维护，不参与构建）
> - 一个 **合入员** NPC（专职 PR 自动化合入决策者：合入门禁汇总 / 冲突分级准入 / 放行·阻断 / 合入后处理；
>   实际合入动作由平台内置任务 `git:auto-merge` 执行，合入员负责决策层）
> 约定体系（指针索引式）：`.cnb/NPC-约定索引.md`（铁律+指针地图+活性机制）+
> `.cnb/NPC-约定-*.md` 分册（按需加载）；执行状态：`.cnb/NPC-路线图.md`；需求收集：`.cnb/NPC-需求池.md`。

---

## 1. 前置条件

- [ ] 仓库已在 CNB 平台**启用云原生构建**
- [ ] 拥有该仓库 **Developer / Master / Owner** 权限（`@` 触发 + 「替我上班」必需）
- [ ] 以下文件已提交到仓库**默认分支**（NPC 读取的是仓库中的配置）：

| 文件 | 作用 |
|------|------|
| `.cnb/settings.yml` | 定义四个 NPC 角色（必需） |
| `.cnb.yml` | 各角色事件流水线（触发行为） |
| `.cnb/LABELS.md` | 标签体系文档（type/priority/status/module/scope 维度，自动打标签配套） |
| `.cnb/scripts/auto-label.sh` | 自动分类打标签程序（issue.open / issue.update / pull_request 触发） |
| `.cnb/scripts/pr-gate.sh` | PR 统一前置门禁程序（冲突检测 + Diff 初筛 + 幂等去重，防卡死） |
| `.cnb/scripts/release.sh` | 打标后发布自动化骨架（触发判断 + 幂等去重 + 失败重试 + 超时兑底，R2） |
| `.cnb/scripts/test-release.sh` | release.sh 异常兜底用例脚本（幂等去重/失败重试/超时升级链，16 断言，R2 测试域产物） |
| `.cnb/NPC-约定索引.md` | 必读：铁律 + 文档指针地图 + 完成即唤醒/讨论修订机制 |
| `.cnb/NPC-约定-*.md` | 分册（角色领域/协作流程/门禁治理/并行拓扑/规划调度/命名项目规范/容错兜底），按需加载 |
| `.cnb/NPC-路线图.md` | 执行状态（分层：路线图→大点→TODO表→任务）+ 大点规划 |
| `.cnb/NPC-需求池.md` | 需求收集记录（规划者维护） |
| `.cnb/NPC-花名册.md` | NPC 身份登记（精确角色名/唤醒通道/协作关系/DRI） |

### 文档所有权（DRI，主维护责任人）

> 每个文档有明确的 **DRI**，对完整性/及时性/一致性负最终责任，**自觉更新**（详见门禁治理分册第 5 章）：

| 文档 | DRI | 更新方式 |
|------|-----|---------|
| 约定索引（铁律）与 .cnb.yml / settings.yml | 仓库管理员 | NPC 只能提修订建议 |
| 约定分册（细则/参数） | 仓库管理员定稿；分册 DRI 落盘 | **约定讨论**平权表决后落盘（活文档） |
| 路线图 / 需求池 / RUNBOOK | **路线图规划者** | 收官必巡检，主动修正 |

> 配置修改后**必须重新提交推送到默认分支**才生效；编辑已发评论不会重新触发。

---

## 2. 运行流程

### 2.1 两条主线

**A. 直接下发意图**（管理员已有明确任务）：
```
1. 新建 Issue，写清意图
2. 在 Issue 描述或评论中 @ 提及协作 NPC 下发意图
3. 勾选「替我上班」（工作模式，需 Developer+ 权限）
4. 协作 NPC 自动执行（分工 → 构建/测试/审查 → 交叉审查 → 两圈收敛）
5. 在 Issue 评论区查看报告与结论
```

**B. 需求汇聚 → 规划 → 自动启动**（管理员/提问者先提需求，攒够后统一开工）：
```
1. 管理员或提问者在 Issue 中 @ 路线图规划者 提出功能需求
2. 规划者收集需求 → 写入需求池 → 统计 → 规划大路线图演化方向
3. 需求达阈值（≥3 条已确认 / 管理员明确指示）→ 更新路线图大点规划
4. 规划者在 Issue 评论中 @ 三个协作 NPC 并下发分工意图（级联启动）
5. 协作 NPC 按常规协作流程干活（构建/测试/审查 + 交叉审查 + 两圈收敛），规划者跟踪进度
6. 两圈收敛后，规划者通过确定性通道唤醒「合入员」接管合入：汇总门禁矩阵 → 分级准入 →
   放行（打 status/merge-ready）→ 平台 git:auto-merge 执行 → 合入后处理（经管理员最终批准，R21）
```

---

## 3. 触发方式（提及 NPC）

```markdown
# 直接下发意图（用纯角色名 @，勿带 @cnb/<仓库路径>(...) 括号格式——会导致事件跳过）
@协作员·构建 请实现工作区时长周报聚合功能
@协作员·测试 请为计时引擎补齐测试并跑 Lint
@协作员·审查 请全面审查本轮改动并两圈收敛

# 收集需求（先攒需求后统一开工）
@路线图规划者 新增需求：支持导出周报为 CSV；修复状态栏显示异常

# 合入相关（专职合入员；实际合入由平台 git:auto-merge 执行）
@合入员 汇总 #12 的门禁矩阵并判定是否放行合入
```

> **触发格式正确性（防事件跳过）**：CNB 要求 @ 提及的**角色名与 `.cnb/settings.yml` 的 `npc.roles[].name` 完全一致**，
> 且**不带 `@` 前缀、不带括号**。错误格式 `@cnb/<仓库路径>(角色名)` 会导致角色名不匹配 → 事件被跳过、走系统默认行为。
> 本仓库角色名（必须精确匹配）：`协作员·构建` / `协作员·测试` / `协作员·审查` / `路线图规划者` / `合入员`。

可同时 @ 多个 NPC，一次最多触发 **10 个** NPC 事件。

### 触发机制审查（NPC 间唤醒 · 已验证）

> **关键事实（已验证）**：CNB 文档将 `issue.comment@npc` 的触发者定义为"当前操作者"，
> **NPC 自己评论中的 `@` 无法触发其他 NPC**（规划者在问题区评论 @ 执行体无响应）。
> **不要设计依赖"NPC 评论 @ 触发他者"的流程。**

**确定性触发架构**（本体系采用 · 2026-08-21 配置修复）：

> ⚠️ 修复说明：此前 `pull_request` / `web_trigger` 误挂在 NPC 角色名顶层 key 下
> （角色名只匹配 NPC 事件），导致 PR 从未触发构建。现已统一挂载到 `.cnb.yml` 的 `$` 下。

| 通道 | 方式 | 确定性 | 用途 |
|------|------|--------|------|
| **`$ pull_request`（主）** | 建分支/PR → 自动串联四段门禁：构建→测试→审查→合入 | ✅ 确定 | 三执行体自动门禁 + 合入员合入门禁（决策） |
| **`$ pull_request.mergeable`（自动合入）** | PR 满足「无冲突 + 评审通过 + 保护分支」时触发 → auto-merge-gate.sh 安全门禁 → 合入员复核 → merge-blocked 复查 → 平台原生 git:auto-merge | ✅ 确定 | 安全的全自动合入（仅 C0 放行；C1/C2/C3 退回人工；否决标签一律阻断） |
| **`api_trigger_npc`（调度）** | `cnb build start-build --env role=<NPC> --env userPrompt=...` 精确调度单个 NPC | ✅ 确定 | 规划者定点调度执行体 / 合入员 |
| **`pull_request.merged`** | PR 合入后自动触发 | ✅ 确定 | 打标发布自动化（release.sh，合入门禁全绿后触发）→ 合入员合入后处理（文档巡检/需求池更新/清标签） |
| **`push`（合入后防线）** | 推送到 main（含 PR 合入合并提交）自动触发 | ✅ 确定 | 主分支构建验证（npm install + compile），失败告警不阻断 |
| **`tag_push`（发布闭环）** | 推送 Git Tag 自动触发 | ✅ 确定 | 打标发布自动化（release.sh，幂等去重） |
| **`crontab`（定时巡视）** | 每周一 09:00（main 分支配置）自动触发 | ✅ 确定 | 规划者定时问题区巡视（R19）+ Issue 生命周期（R20） |
| **web_trigger（手动）** | 页面按钮 / API 触发，传 `userPrompt` 交办事项 | ✅ 确定 | 管理员手动调度 |
| **评论 @（辅助）** | 仅通知留痕、人工触发 | ❌ 不可靠（NPC 互 @） | 不作为触发手段 |

> **全局调度入口（v2）**：`$` 下 `web_trigger` 由**路线图规划者**接管（规划者＝调度者＋问题区巡视员；合入决策归「合入员」，铁律 R21）。
> 页面/API 触发时，userPrompt 含【唤醒】@<NPC>：<交办事项>，规划者通过确定性通道（web_trigger 再触发
> 对应执行体入口 / 推分支建 PR 触发 pull_request）级联启动，评论 @ 仅通知不触发。

**自动分类打标签（v2）**：`issue.open` / `issue.update` / `pull_request` 事件自动执行
`.cnb/scripts/auto-label.sh`，对 Issue/PR 自动定位类型（type/*）并打多维标签；
`issue.update` 内容变更时**去旧加新**重分类。脚本输出【打标签结论】+【唤醒提示】，
供规划者确定性通道接手巡视（识别挂起项：无响应/未排入/过期/重复/阻塞）。

### 自动化审查（无需 @）

PR 打开或推送新提交时，`pull_request` 事件**自动**触发协作员·审查（`R11` 铁律）：
- 自动检查：命名规范 → 门禁（compile/lint/diff）→ 改动质量
- 输出【审查结论】到 PR 评论，发现问题定点修复或上报
- 不需要在 PR 中 `@` 任何人

### PR 统一前置门禁（防卡死）

`$` 级 `pull_request` 事件在打标签后、拉起任何 NPC 前，**先执行 `.cnb/scripts/pr-gate.sh`**：
- **幂等去重（防卡死核心）**：同一 head commit 已审查（`status/gate-ok` 标签/缓存）则直接跳过，避免 4 个 NPC 重复拉起、重复修复、级联无限触发。
- **冲突检测**：git merge 预演目标分支 → 判定 C0/C1/C2/C3 冲突等级。
- **Diff 质量初筛**：统计变更规模，扫描硬编码/TODO/console.log/大重构信号。
- **分级派单**：按 C0/C1/C2/C3 只唤醒本轮主导者（C0 审查主导 / C1 构建测试 / C2 审查+规划者 / C3 高危强制人工），**避免 4 个 NPC 并行互相等待**。
- 各 NPC 的 `pull_request` userPrompt 已加入「先读 pr-gate【门禁结论】→ 幂等跳过 → 按分级分工」的防卡死引导。

### PR 自动化合入（专职合入员 NPC 决策 + 平台原生执行 · 安全的全自动合入）

`$` 级 `pull_request.mergeable` 事件（PR 满足「无冲突 + 评审通过 + 保护分支」时触发）实现**平台原生自动合入**，链路：
`auto-merge-gate.sh 确定性门禁 → 合入员 NPC 复核（决策层）→ merge-blocked 否决复查 → git:auto-merge 执行`。
- **安全门禁**：先执行 `.cnb/scripts/auto-merge-gate.sh`（gate-ok 就绪判定 + `status/merge-blocked` 否决标签检查 + git merge 预演冲突分级 C0-C3 准入）。
- **退出码语义**：`exit 0`（C0 无冲突 + 已过前置门禁 + 无否决标签）→ 放行后续流程；`exit 非 0`（C1/C2/C3 或 **UNKNOWN**（无法确定性判定：目标分支缺失 / 无 git 仓库 / 预演回退异常）或未过门禁或有否决标签）→ 保守阻断自动合入，PR 保持未合入等待人工/审查（fail-safe 原则：无法确证无冲突时绝不降级放行）。
- **合入员复核（决策层）**：读取门禁结论 → 确认放行（输出【自动合入确认】）或发现阻断项打 `status/merge-blocked` 退回人工/审查（铁律 R21：合入决策唯一责任 NPC）。
- **否决复查**：`merge-blocked` 标签（NPC 或人工打标）存在 → 一律阻断 `git:auto-merge`。
- **平台原生合入**：全链通过后由内置任务 `git:auto-merge` 真正执行（squash 合并 + 删除源分支），与上方 `pull_request` 中由「合入员」NPC 决策的合入门禁互补，实现"全自动合入"闭环。已启用 `allowAssigneeApprovedMerge`：PR 有 assignee（处理人）且已由合入员复核批准时仍自动合入（避免分配处理人后自动合入被平台默认跳过）。
- **安全边界**：C2（需审查判断）/C3（高危）强制退回人工（门禁治理 §4.8），杜绝裸用 auto-merge 绕过冲突分级。
- **合入后防线**：合入 main 后 `push` 事件自动执行主分支构建验证（npm install + compile），失败告警不阻断，供规划者/合入员巡检识别。
- **PR 状态标签流转**：门禁任一失败 → `failStages` 自动打 `status/pending-fix`（待修改）并移除 `status/merge-ready`；合入员放行打 `status/merge-ready`；合入后自动打 `status/done` 并清除 `pending-fix`/`merge-ready`/`merge-blocked`。
- **超时策略**：NPC 门禁 30m、脚本门禁 5-10m、合入后处理 30m、发布 10m（超时后按容错分册升级链处理，不无限等待）。
- **发布闭环**：合入后 `pull_request.merged` 触发 release.sh；打 tag 触发 `tag_push` 事件再次执行 release.sh（幂等：同版本已发布则跳过）。
- **覆盖率门禁**：质量门禁后自动收集单测覆盖率（`c8` 生成 lcov.info，当前行覆盖率 64.26%）；`testing:coverage` 上报但暂不设红线（`breakIfNoCoverage: false`），观察数轮后收紧（可设 `lines`/`diffLines` 阈值）。
- **发布工具链闭环（版本更新 → 打包 → 打 Tag → 发布）**：
  1. **触发**：`web_trigger` 发【发布】patch/minor/major（规划者执行 `release-tag.sh`），或管理员手动 `git tag vX.Y.Z && git push origin vX.Y.Z`
  2. **版本更新**：`version-bump.sh` 递增 package.json 版本 + 更新 CHANGELOG.md
  3. **打包**：`release-tag.sh` 用 `@vscode/vsce` 打包 `.vsix` → `.cnb/dist/`（不入库）
  4. **打 Tag**：commit 版本变更 + push main + `git tag vX.Y.Z` + push tag
  5. **发布**：`tag_push` 事件触发 `release.sh`（幂等去重 + 失败重试 + 超时兜底）→ 建 Release + 上传 .vsix 附件（两段式，失败仅告警）
  6. **兜底路径**：`pull_request.merged` 事件同样触发 release.sh（合入即发布，幂等）
- **定时巡视**：`main` 分支 `crontab: 0 9 * * 1` 每周一 09:00 自动唤醒规划者执行问题区巡视 + Issue 生命周期管理。

### 触发限制（重要）

| 限制 | 说明 |
|------|------|
| 提及位置 | 必须写在**普通段落**；引用块/代码块/折叠块/列表/表格内的 `@` **不触发** |
| 重新触发 | 编辑 Issue/评论、重开 Issue **不重新触发**，需新发评论 |
| 评论上限 | 评论超 100 条后不再触发 `@npc` 事件 |

---

## 4. 权限与工作模式

| 触发者角色 | CNB_TOKEN 上限 | 「替我上班」 |
|-----------|---------------|------------|
| Developer / Master / Owner | Developer | 可启用 |
| Reporter / Guest | Reporter | 强制关闭（无法写代码/推分支/建 PR） |

**「替我上班」必须勾选**，否则 NPC 无法自主：写代码、推代码、创建分支、创建 PR。

---

## 5. 协作流程速览（详见约定文档）

- **分工**：以主责切入（构建/测试/审查），可动用全部三职能
- **交叉审查**：按序互审他人改动（构建↔测试，审查收尾），定点 Diff 修复缺陷
- **两圈收敛**：交叉审查循环两圈后输出最终结论
- **冲突处理**：重大冲突由冲突方在 Issue 评论区反馈【冲突方/冲突点/建议方案】，停止改动等待提出者回答
- **大意图/连续构建**：按约定第 9 章平权并行（任务分解→依赖图→共享池领取→流水线接力→三同步点）
- **分层任务拓扑**：按约定第 10 章（路线图→大点→TODO表→任务，树状分叉+依赖 DAG）
- **需求收集与规划调度**：规划者收集→统计→规划→阈值→级联启动（规划调度分册）
- **PR 合入责任（分职门禁）**：**合入员**为专职合入决策者（R21：汇总门禁、分级准入、放行打 `status/merge-ready` / 阻断打 `status/merge-blocked`、合入后处理）；分职门禁矩阵——构建主审编译、测试主审质量、审查主审规范 + 四眼复核；平台 `git:auto-merge` 执行实际合入；提出者（管理员）为最终批准者
- **文档自觉更新**：规划者为路线图/需求池/RUNBOOK 的 DRI，每轮收官必巡检、主动修正；铁律与配置归管理员所有，NPC 只能提修订建议
- **完成即唤醒（确定性通道）**：执行体/规划者通过代码事件（建 PR 触发 `$ pull_request` 串联四段门禁）或 `cnb build start-build` 触发 `api_trigger_npc` 精确调度；评论 @ 仅作通知留痕；规划者调度执行体优先建 PR / CLI；两圈收敛后唤醒合入员接管合入
- **讨论修订（活文档）**：约定分册细则可经【约定讨论】平权表决演化；铁律仅管理员可改
- **引导式命令**：所有流程按打勾检查单执行（`[ ]`/`[x]`），必填占位符 `<...>`，每步含 `→ 通过标准`；输出按【执行清单】回显，未过标准即停
- **命名约定与项目规范**：NPC/文档/代码/提交/评论命名遵循命名项目规范分册（`NPC-约定-命名项目规范.md`）
- **NPC 花名册**：`.cnb/NPC-花名册.md` 为单一权威身份登记——精确角色名、唤醒通道、协作关系、DRI、常读文档；开工必读防"互不知情"
- **问题区巡视**：规划者兼任 Issue 区巡视员（R19）——被提及/收官/触发时扫描 Issue 区与需求池，识别挂起项（无响应/未排入/过期/重复/阻塞）→ 唤醒对应 NPC 或上报管理员 → 输出【巡视报告】；只识别推动不擅自实现
- **Issue 生命周期管理**：规划者兼任生命周期管理员（R20）——关闭（已实现/已解决/无效/超期 7 天）、重启（误关/复发/新信息）、清理挂起；关闭前须确认，不确定保留标注，重启后必唤醒处理
- **自动化门禁（已修复）**：PR 打开/更新自动触发 `$ pull_request` 事件，串联四段门禁——构建门禁→质量门禁→审查门禁→合入门禁（无需 `@`，规划者/合入员建 PR 即全链路自动）
- **全角色自动入口**：构建/测试/审查三个执行体在 `$ pull_request` 流水线中自动按序门禁；合入员在 Stage 4 做合入门禁决策、在 `pull_request.mergeable` 做自动合入复核、在 `pull_request.merged` 做合入后处理（打标发布自动化 release.sh → 文档巡检/需求池更新/清标签）；规划者可用 `cnb build start-build --env role=<NPC>` 触发 `api_trigger_npc` 精确调度任意 NPC
- **门禁自动回写**：PR 验证结果自动回写路线图（构建/质量/规范门禁 ✅/✗），合入员据此汇总合入总门禁
- **PR 门禁自动化系统**：审查 → 分职门禁验证（主审+复核）→ 合入员汇总 → 批准/缺席裁决 → 平台 `git:auto-merge` 合入 的完整闭环
- **合入冲突自动审查**：PR 存在 Git 冲突/逻辑互斥 → 自动唤醒审查员（C1 自动化解 / C2 审查判断 + 合入员门禁 / C3 高危强制人工）→ 检查单审查（合理性/质量/方案/追溯）→ 判定【准入：允许合入】或【禁止：合入阻塞】
- **管理员缺席裁决**：冲突上报后等待期（默认 24h）超时无答复 → 风险分级——L1/L2 低风险自动裁决（可回滚），L2 高风险（合入/越权/破坏性）强制等待管理员
- **容错与兜底**：竞态仲裁（领取按构建→测试→审查顺序、写前占锁）、回滚协议（git revert + 重跑门禁）、超时兜底（升级链：执行体→审查→规划→管理员）、幂等保护（路线图为唯一事实源）、资源纪律（指针加载不冗余读）、审计追踪（每轮收官生成审计摘要）

---

## 6. 运行环境

- 默认镜像：`cnbcool/default-npc:latest`（开箱即用）
- 如需自定义环境（Node 22 + git + ripgrep 等完整工具链）：编写 Dockerfile → 构建推送 → 在 `.cnb.yml` 的 `docker.image` 引用

---

## 7. 故障排查速查

| 现象 | 可能原因 | 处理 |
|------|---------|------|
| `@` 后无响应 | 配置未提交 / 提及在代码块或列表内 / 权限不足 | 提交配置；改普通段落提及；确认 Developer+ 权限 |
| NPC 无法改代码 | 未勾选「替我上班」/ 触发者为 Reporter | 勾选工作模式；用更高权限账号 |
| 不重新触发 | 编辑评论/重开 Issue 不触发 | 新发一条评论 |
| 卡在冲突 | 等待提出者答复中 | 查看 Issue 评论【冲突方/冲突点/建议方案】并回复 |
| 路线图不同步 | 更新锁未释放 / 心跳停滞 | 按约定 7.3 硬约束接管锁并记录变更日志 |
| 规划者未启动协作 NPC | 需求未达阈值 / 未在评论中 @ | 检查需求池计数；确认规划者评论中含三个 `@` 提及 |
| 需求重复 | 未去重 | 规划者按约定 11.3 去重合并，标注关联编号 |

---

## 8. 修改与演进

- NPC 对协作约定有修订建议 → 在 Issue 评论提出（原因+拟改内容）→ 管理员批准后落盘并更新版本号
- 路线图维护 → 每个 NPC 按约定 7.3 硬约束实时更新（版本号/更新锁/心跳/变更日志）
- 需求池维护 → 路线图规划者按约定第 11 章实时更新
